{
  "archFreezeRules": [
    {
      "id": "compatibility-retirement-deletion-contract",
      "description": "Server retirement is enforced by the focused deletion guard.",
      "files": {
        "scripts/__tests__/compatibility-retirement-guard.test.ts": {
          "mustExist": true,
          "mustContain": ["deletes the server package", "wave-10"]
        }
      }
    }
  ]
}
