name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  fallow-push-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - name: Audit new static analysis issues (push)
        if: github.event_name == 'push'
        run: pnpm exec fallow audit --base ${{ github.event.before }} --gate new-only --ci --fail-on-issues --health-baseline reports/baselines/fallow-health-baseline.json --dead-code-baseline reports/baselines/fallow-dead-code-baseline.json

      - name: PR Fallow Audit (on PR)
        if: github.event_name == 'pull_request'
        run: pnpm exec fallow audit --base origin/main --gate new-only --ci --fail-on-issues

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm typecheck

  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm check

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      - run: pnpm test
        env:
          NODE_ENV: test

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      - run: pnpm test:coverage
        env:
          NODE_ENV: test

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/

  postgres-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: trapmap
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm build

      - name: Run PG integration tests
        run: pnpm exec vitest run packages/server/src/lib/queue/task-queue.test.ts packages/server/src/lib/lifecycle/subscribers/subscribers-integration.test.ts
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/trapmap
          NODE_ENV: test

  doc-guardrails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: pnpm/action-setup@v3
        with:
          version: 10.33.0

      - run: pnpm install --frozen-lockfile

      - run: pnpm check:docs-drift
      - run: pnpm check:arch-freeze
      - run: pnpm check:deps
      - run: pnpm check:mermaid
      - run: pnpm check:structure
      - run: pnpm check:complexity
      - run: pnpm check:md-lint
      - run: pnpm check:links || true
      - run: pnpm exec fallow dead-code --boundary-violations --ci --fail-on-issues
      - run: pnpm exec fallow dead-code --unused-deps --ci --fail-on-issues
