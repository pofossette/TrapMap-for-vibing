# @trapmap/app-mcp

TrapMap 的 MCP（Model Context Protocol）server 包：作为外层协议封装层，经 gateway HTTP API 暴露 TrapMap 的知识与 Skill 能力，不在本包内实现后端业务逻辑。

> 由主线 debt-mcp-platformization-mainline Task B1 创建；工具面见后续任务（Task B2 起）。
