# Compatibility Shell Retirement and Owner-Local Infrastructure 收口

> **状态：** archived / completed-with-follow-up
> **归档日期：** 2026-08-01
> **根入口：** [`../../plan.md`](../../plan.md)（当前 active mainline 已切换为 Documentation Validation and Observability Platform）
> **实施路线图：** [`../superpowers/plans/2026-07-13-compatibility-shell-retirement-runtime-infra-ownership.md`](../superpowers/plans/2026-07-13-compatibility-shell-retirement-runtime-infra-ownership.md)

## 完成证据摘要

- `packages/server` 已删除（Wave-10，提交 `a66d94e6`）。
- `packages/runtime-infra` 已删除（Wave-10 前置，2026-07-25）。
- `compatibility-retirement-guard.test.ts` 的 `completedOwnerWaves` 包含 wave-1 至 wave-10 全部完成。
- `store_snapshot`、`JsonStore`、`PostgresStore` 已删除（Wave-9，2026-07-31）。
- 最终验证：retirement guard（38/38）、typecheck、doc-drift、structure、deployment-smoke、eval:smoke、distributed-closeout 均通过。

## 遗留 follow-up

### 已完成（2026-08-01）

- CI `postgres-integration` job 已更新为当前测试路径。
- Eval workflow 的 path filter 已修正为 `packages/service-*/src/**`，Node 版本已统一为 24。
- SYSTEM_TRUTH_SOURCES.md 已系统更新所有引用（30+ 行）。
- 40+ 个 active 文档已更新，添加历史说明或替换为当前路径。
- source-aware 文档残留守卫已建立：`scripts/check-stale-package-refs.ts` 扫描 active docs 中对已删除 package 的过期引用，已集成到 CI `doc-guardrails` job 作为阻断门（阈值 0）。
- `closeout-surface.test.ts` 新增 2 项守卫测试（防止旧计划重新激活、验证 SYSTEM_TRUTH_SOURCES 不把已删除 package 当前权威）。
- `complexity-budgets.json` 新增旧计划路径禁止规则（plan.md 和 docs/todos/README.md）。

### 已完成（2026-08-01 续）

- active 文档中所有 `packages/server` 过期引用已清理完毕（从 345 降至 0）。
- `check:stale-package-refs` 阈值已设为 0，作为 CI 阻断门。
- 详见当前 active mainline [Documentation Validation and Observability Platform](../../docs/todos/documentation-validation-and-observability-platform.md) 中的相关条目。

## 目标

将 `@trapmap/server`、`@trapmap/runtime-infra`、Fastify compatibility routes 与 `store_snapshot` 的具体实现按领域迁移至其真实 service owner；`packages/contracts` 与 `packages/backend-core` 保持唯一共享 domain/port 层。

## 已满足的启动条件

- [x] 可观测性、shared PG 治理与 distributed maturity 主线已归档；Compose restart closeout 已通过。
- [x] 根 `plan.md` 将本文件指定为唯一 active mainline detail。
- [x] 既有路线图冻结了 owner wave、PG-first cutover、empty database 与 legacy snapshot backfill 的验收边界。

## 执行约束

- 不保留 compatibility re-export、dual read 或 runtime fallback；每个 wave 完成后立即移除其运行时 import。
- service 之间不得导入其他 service 的具体实现；跨 owner 行为仅经 `backend-core` ports、内部 HTTP adapter 或 outbox delivery。
- 不新增 `store_snapshot`、shared DB direct-read 或 `runtime-infra -> server` 依赖作为默认业务路径。
- 每个 wave 遵循路线图中的 TDD、最小验证、文档回写和 evidence checklist；本文件记录实际命令结果与外部前置条件。

## 当前执行入口

- [x] 建立 Task 1 deletion contract：[`compatibility-retirement-guard.test.ts`](../../scripts/__tests__/compatibility-retirement-guard.test.ts) 扫描生产 TypeScript、Dockerfile、根脚本与 workspace manifest；测试、spec 和 fixture 不构成新增阻断面。
- [x] Task 2 migration baseline：六个 owner-local baseline 仅支持空数据库；旧 `0000–0020` 数据库须重建，不提供原地升级。`identity-access` 暂管 `store_snapshot`，仅作为 Task 9 一次性 backfill 输入；Task 9 完成导出、回填与核对后必须删除该表及其迁移资产。已通过六个 service 的 `src/migrations.test.ts`：每个 runner 均拒绝 owner-external SQL 和缺失 journal tag；host coordinator 的顺序、失败停止与 pool close 覆盖保留在 `packages/host-distributed/src/migrate.test.ts`。
- [x] Wave-4 governance-review closeout（2026-07-21）：feedback、conflict、remediation、operator projection 的 production compatibility surface 已清空；server route/repository/subscriber 与 runtime-infra aggregate 成员已删除，`completedOwnerWaves` 已包含 `wave-4`，且未改变 Wave-5+ 或 Wave-9 条目。

## 已核验进度（2026-07-21）

完成判定以 [`compatibility-retirement-guard.test.ts`](../../scripts/__tests__/compatibility-retirement-guard.test.ts) 的 `completedOwnerWaves`、生产 surface 扫描和本文件对应 closeout evidence 为准；仅删除部分旧代码不等于完成 wave。

- [x] Wave-1 identity-access：owner ports、迁移与 compatibility repository 删除已完成；guard 将 `wave-1` 标记完成。
- [x] Wave-2 knowledge-write：PG-first write/outbox、artifact read projection 与 server compatibility surface 删除已完成；guard 将 `wave-2` 标记完成。
- [x] Wave-3 candidate-ingestion：candidate、duplicate、lineage 与 processing ownership 已迁移；production scan 无 compatibility import，guard 将 `wave-3` 标记完成。
- [x] Wave-4 governance-review：feedback、conflict、remediation 与 operator projection 已迁移；production/runtime aggregate/badcase boundary scan 均为空，guard 将 `wave-4` 标记完成。
- [x] Wave-6 job-runtime：旧 async/runtime-infra surface 已删除，runtime foundations 与 distributed closeout 已通过；guard 将 `wave-6` 标记完成。
- [x] Wave-7 knowledge-read：已删除 `service-knowledge-read` 对 `runtime-infra`/`server` 的 retrieval/read-model compatibility dependencies 及 runtime-infra retrieval/support assembly；guard 将 `wave-7` 标记完成。
- [x] Wave-8 host surfaces：host composition、capability config、gateway request context 与 host pool ownership 已无 compatibility import；guard 将 `wave-8` 标记完成，且没有新增宽泛 allowlist。
- [x] Wave-9 backfill/delete state：`store_snapshot`、`JsonStore`、`PostgresStore`、legacy migration/export fixture 已删除；一次性 backfill/operator entrypoint 与 PostgreSQL integration evidence 已完成。
- [x] Wave-10 package retirement：`packages/runtime-infra` 已删除（2026-07-25）；`packages/server`、其根依赖和剩余 compatibility Docker/package references 已删除（2026-07-31，提交 `a66d94e6`）。
- [x] 最终 closeout：所有 wave 已关闭，empty-database migration、legacy snapshot backfill、distributed acceptance 已通过。本文件已归档至 `docs/archived/archived-plans/`，当前 active mainline 切换为 Documentation Validation and Observability Platform。

## Task 1 — Deletion contract evidence

`scripts/__tests__/compatibility-retirement-guard.test.ts` 是临时 allowlist 的权威记录。每个对象均为精确的 `{ file, symbol, ownerWave, rationale }` 删除契约；guard 拒绝未登记的生产命中、过期文件、未知标识符、缺失 owner/rationale，以及已完成 wave 遗留的条目。不得使用目录、包或 glob 级豁免。

| Owner wave                   | 当前例外与删除条件                                                                                                                                                                                                                       |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| wave-1 identity-access       | `auth/users/teams/audit` 的旧 snapshot 注记与 identity migration fixture 已不再是 guard 例外；唯一允许的 snapshot 输入是 service-local `IdentityAccessSnapshotPort`，仅供 Task 9 backfill 使用。完整 repository aggregate 迁移仍未完成。 |
| wave-2 knowledge-write       | artifact/knowledge/lifecycle 的 `JsonStore` 与 snapshot fallback；PG-first write path 落地即删除。                                                                                                                                       |
| wave-3 candidate-ingestion   | 已完成：candidate/duplicate/lineage/processing 已完全归属 owner；无 guard allowlist、server/runtime-infra concrete implementation 或 snapshot fallback。                                                                                 |
| wave-4 governance-review     | 已完成：feedback、conflict、remediation、operator projection 归属 `service-governance-review`；server compatibility routes/repositories/subscribers 与 runtime-infra aggregate 成员已删除，guard 无 Wave-4 allowlist 条目。              |
| wave-6 job-runtime           | runtime-infra outbox bridge；job runtime 接管后删除。                                                                                                                                                                                    |
| wave-7 knowledge-read        | retrieval schema 和 service knowledge-read 对 runtime-infra/server 的依赖；read owner 完成即删除。                                                                                                                                       |
| wave-8 host surfaces         | host composition、migration entrypoint、capability config；host-owned runtime surface 完成即删除。                                                                                                                                       |
| wave-9 backfill/delete state | `store_snapshot`、`JsonStore`、`PostgresStore` 与明确的 migration/export/benchmark fixture；完成一次性 backfill 后删除。                                                                                                                 |
| wave-10 package retirement   | root/runtime-infra dependency、repository aggregate、Docker compatibility self-reference；删除 packages 后移除。                                                                                                                         |

已执行：guard 先以空 allowlist 运行，基线报出 68 个未登记生产依赖（预期 RED）；加入精确条目后 `rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts` 通过（4 tests，GREEN）。同轮通过 `rtk pnpm test:file -- scripts/__tests__/closeout-surface.test.ts`（8 tests）、`rtk pnpm check:arch-freeze`（9 rules）、`rtk pnpm exec fallow list --boundaries`、`rtk pnpm exec fallow audit --base main`（18 changed files, no issues）、`rtk pnpm check:docs-drift`（46 rules）及 `rtk pnpm check:structure`。Fallow 保留一个既有 `../../tsconfig.base.json` entry-point 警告，但审计成功。

## Task 2 / Task 3 evidence

- Task 2：`rtk pnpm --filter @trapmap/service-identity-access test --run src/migrations.test.ts`，以及 candidate-ingestion、governance-review、job-runtime、knowledge-read、knowledge-write 的同名 focused test 全部通过（15 tests）。迁移集合校验已共享到 `@trapmap/backend-core`，每个 owner 仅声明自己的唯一 tag。
- Task 3：`service-identity-access` 新增 owner-local PostgreSQL identity port factory 和本地 Drizzle schema；它不再导入 `@trapmap/server`。distributed identity host 直接使用该 factory；host-local 仅接收由 identity service package 构造的 `IdentityAccessPort`。`rtk pnpm --filter @trapmap/service-identity-access test --run src/pg-ports.test.ts src/routes.test.ts src/migrations.test.ts`（7 tests）、`rtk pnpm --filter @trapmap/host-local test --run src/nest/app.test.ts`（7 tests）和 `rtk pnpm typecheck` 均通过。
- `rtk pnpm exec fallow audit --base main` 成功，无 boundary violation；报告 server 与新的 owner-local identity schema 之间的迁移期重复，待 compatibility server 在后续 owner waves 删除时一并消除。
- Wave-1 follow-up：identity service 的 PG factory 现在提供 audit query、actor lookup 和唯一的结构化 `IdentityAccessSnapshotPort` 兼容输入；该 port 不引用 server store 类型，提供 read、transaction 与 identity ID allocation，并明确只服务 Task 9 前的 backfill。`buildUserLookupContextFromRepos` 与 `createAuditEvent` 也已作为 identity-owned API 准备给迁移后的 knowledge owner 使用。兼容 server 当前不能直接导入 service implementation（Fallow `server → service-standard` boundary）；因此既有 server call sites 保持原边界，随 knowledge owner wave 经 port 完成切换。
- Follow-up evidence：`rtk pnpm --filter @trapmap/service-identity-access test --run src/pg-ports.test.ts src/routes.test.ts src/migrations.test.ts`（11 tests）、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（4 tests）、`rtk pnpm check:docs-drift` 与 `rtk pnpm check:structure` 均通过。Wave-1 guard 目前没有 allowlist 例外；runtime-infra repository aggregate 与 server compatibility repository implementation 仍未删除，不能将完整 wave 标记为完成。
- Wave-1 owner-bundle follow-up：`service-identity-access` 现提供结构性 `createIdentityAccessOwnerBundle`；host-local 的 Nest identity module 与 audit capability 均由 PG owner bundle 注入，缺少 pool 时 fail-fast。distributed shared `createServicePorts()` 不再创建或返回 identity/audit repositories；每个业务 host 从 identity owner factory 注入 append-only audit capability。server compatibility repositories 与 runtime-infra aggregate 仍待删除，因此 Wave-1 继续保持未完成。
- 本轮证据：`rtk pnpm --filter @trapmap/service-identity-access test --run src/pg-ports.test.ts src/routes.test.ts src/migrations.test.ts`（12 tests）、`rtk pnpm test:file -- packages/host-local/src/nest/app.test.ts`（7 tests）、`rtk pnpm test:file -- packages/host-distributed/src/shared/database-ownership.test.ts`（7 tests）、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（4 tests）、`rtk pnpm typecheck`、`rtk pnpm check:docs-drift` 与 `rtk pnpm check:structure` 均通过。`rtk pnpm exec fallow audit --base main` 完成但仍报告既有 shared ports/audit complexity 与一个 service-local snapshot adapter complexity；未新增 boundary violation。
- 本轮收口：server 的无 host `index.ts`/`worker.ts` 入口改为显式 fail-fast；`/v1/operations/audit`、knowledge review 与 audit projection 统一读取注入的 identity owner ports，actor handle 使用单次批量 lookup；identity PG audit query 补齐 projection 所需字段。Wave-1 仍未完成：runtime-infra repository aggregate 与 server compatibility repository implementation 尚未删除。
- 本轮继续收口：`runtime-infra` 不再构造或暴露 session、access-key、team、membership、user、audit repositories；host-local 在一次 PostgreSQL pool 组合中创建 identity owner bundle，auth context 改读该 bundle，缺少 pool 时启动即 fail-fast。identity PG audit query 使用独立 count query 并保留 tracing/outcome 字段，actor membership lookup 改为单次集合查询。Wave-1 仍未完成：`packages/server/src/lib/repos` 及其 auth/team/user/audit concrete implementations 尚待迁移删除。
- 本轮验证：`rtk pnpm --filter @trapmap/service-identity-access test --run src/pg-ports.test.ts`（6 tests）、`rtk pnpm test:file -- packages/host-local/src/nest/app.test.ts`（7 tests）、`rtk pnpm test:file -- packages/host-distributed/src/shared/database-ownership.test.ts`（7 tests）、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（4 tests）与 `rtk pnpm typecheck` 通过。`rtk pnpm eval:smoke` 当前在 `evals/retrieval/lib/adapters.ts:createExecutionContext` 调用未注入 identity bundle 的 `buildServer()` 时 fail-fast；同一缺口导致 `rtk pnpm test:deployment-smoke` 的 33 个 `packages/server` app/startup tests 失败。该 eval/test entrypoint 必须迁至 host composition 后才能完成 required 验证。
- Wave-1 composition follow-up：`buildHostLocalServer()` 现在明确保留 host pool 所有权（`app.close()` 后结束 pool）；server app/startup smoke 通过临时 pgvector 空库执行。retrieval eval context 要求 PostgreSQL URL，以 identity bundle ports 写入 user/team/membership/session，并在关闭时清空该 context 的非 migration 表。`rtk pnpm typecheck`、`rtk pnpm test:deployment-smoke`、`rtk pnpm eval:smoke`、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`、`rtk pnpm check:docs-drift`、`rtk pnpm check:structure` 与 `rtk git diff --check` 通过。`rtk pnpm exec fallow audit --base main` 仍报告 inherited clone/complexity hotspots，未将 Wave-1 标记完成。
- Wave-1 entrypoint follow-up：`export-retrieval-db-snapshot`、retrieval-live snapshot restore、graph benchmark 与 skill import/export 均改经 `buildPostgresComposedServer()`；缺少 `TRAPMAP_DATABASE_URL` 时明确报 PostgreSQL host composition 错误，且 host 负责在 app 关闭后结束 pool。`rtk pnpm test:file -- scripts/__tests__/postgres-composition-entrypoints.test.ts`（4 tests）、`rtk pnpm typecheck`、`rtk pnpm test:deployment-smoke`（先通过 135 个非 PG tests，再由新建临时 `pgvector/pgvector:pg16` 容器与空数据库串行通过 app/startup tests）、`rtk pnpm check:docs-drift`、`rtk pnpm check:structure` 与 `rtk git diff --check` 通过。Fallow 直接二进制以 `--base main` 运行时无法为当前已 checkout 的 `main` 创建临时 worktree；以 `--base HEAD^` 审计仍有 23 个 clone groups 与 14 个 complexity findings（含既有 hotspots），因此 Wave-1 保持未完成，也未归档。
- Wave-1 full closeout follow-up：本地审计基准 `wave1-fallow-base` 指向 `d7b9ad749d7c692367939e208dc644858bc3e6e7`，仅用于避免已 checkout `main` 的 Fallow worktree 限制，不提交或推送。retrieval snapshot 的 frozen/rebuild materialization 与 service profile 已收敛为 eval-local helper；snapshot export、benchmark、skill import/export 和 badcase export 入口已拆分为小型协调函数，保留 PG-only、host-owned pool 和无 fallback 约束。retirement guard 明确禁止四个迁移入口重新直调 `buildServer()`，仅允许 Wave-8 host composition helper 提供该 factory path；readiness 失败也在 host close 的 `try/finally` 内清理 pool。`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（7 tests）、`rtk pnpm test:file -- scripts/__tests__/postgres-composition-entrypoints.test.ts`（6 tests）、`rtk pnpm test:file -- scripts/__tests__/entrypoint.test.ts`（1 test）、`rtk pnpm test:file -- evals/retrieval-live/lib/live-eval.test.ts`（19 tests）、`rtk pnpm eval:smoke`、`rtk pnpm typecheck`、`rtk pnpm test:deployment-smoke`（135 个非 PG tests，随后新的临时 `pgvector/pgvector:pg16` 容器和空数据库通过 `app.test.ts`/`startup.test.ts`）、`rtk pnpm check:docs-drift`（46 rules）、`rtk pnpm check:structure` 与 `rtk git diff --check` 均通过。`rtk pnpm exec fallow audit --base wave1-fallow-base --format json --quiet --explain` 通过，报告 `duplication_clone_groups: 0`、`complexity_findings: 0`、无 dead-code 或 boundary issue。Wave-1 的 compatibility repositories/runtime-infra aggregate 尚待后续 owner wave 删除，故本细则保持 active 且不归档。

## Task 4 — Wave-2 in-progress evidence

`service-knowledge-write` 现拥有 `createKnowledgeWriteOwnerBundle`：生命周期状态、`lifecycle_events` 与 `domain_event_outbox` 使用同一 PostgreSQL transaction，失败时回滚；outbox 仅暴露只读诊断，调度、重试、租约与 dead-letter 仍由 `job-runtime` 持有。distributed 与 host-local 均直接从其 host pool 组合该 bundle，已删除 distributed `knowledge-write/ports.ts` 适配层及 shared transaction 测试。`createServicePorts()` 中的 knowledge repository 已收缩为临时只读投影适配器，任何 mutation 均明确拒绝，避免形成第二个写入口；其 direct-read 退出仍属于 Wave-7。

`service-knowledge-write` 的 Drizzle schema 已改为 owner-local 定义，并由 focused migration test 阻止重新导入 `@trapmap/server` 或 server 相对路径；冻结 SQL baseline 继续是 Wave-2 migration truth。

本轮进一步将冻结 baseline 的 artifact/knowledge/label schema 拆入 owner-local schema modules，并新增 `ArtifactWritePort`、PG transaction lifecycle 写入与 service internal artifact read/lifecycle routes；host-local 与 distributed 的 knowledge-write owner bundle 均暴露同一 artifact writer。当前公开 operations artifact 路由及 server compatibility repository 尚未删除，故 Wave-2 仍保持未完成状态。

`/v1/knowledge/maintenance` 已由 gateway 直接转发至 `knowledge-write` 的 canonical command；artifact lifecycle 写入同步追加 owner-local outbox event，并由 focused owner/gateway/compatibility tests 覆盖。其余 artifact operations 仍在 server compatibility shell，不能据此宣告 Wave‑2 完成。

本轮继续收口：gateway 已为 `/v1/operations/artifacts/{import,export,activate,review-queue,:artifactId/edit,:artifactId/history,:artifactId/review,:artifactId/deactivate}` 建立 knowledge-write 转发，内部 owner 提供对应 command handlers；认证 hook 注入 session actor，artifact mutation 忽略请求体伪造的 actor。server operations registration 与 bootstrap 不再注册 artifact routes 或创建 artifact repository。`rtk pnpm typecheck`、gateway routes、knowledge-write owner routes/ports 与 compatibility guard 均通过。

本轮通过 `rtk pnpm --filter @trapmap/service-knowledge-write test --run src/pg-ports.test.ts src/routes.test.ts`（11 tests）、`rtk pnpm --filter @trapmap/host-local test --run src/nest/runtime/host-services.test.ts`（1 test）、`rtk pnpm exec vitest run --project host-distributed packages/host-distributed/src/shared/database-ownership.test.ts packages/host-distributed/src/knowledge-write/routes.test.ts`（10 tests）、`rtk pnpm eval:smoke`、`rtk pnpm typecheck`、compatibility guard、doc-drift 与 structure guard。`rtk pnpm exec fallow audit --base wave1-fallow-base --format json --quiet --explain` 无 dead-code 或 boundary issue，保留迁移期 clone warning。Wave-2 尚未完成：`packages/server/src/lib/artifacts/`、`labels/`、`lifecycle/` 与其 snapshot/JSON fallback 仍待迁移和删除，故不勾选 owner wave 完成项。

本轮收尾：artifact owner 的 `insert` 现将 artifact、完整 revision history 与 lifecycle history 放入单一 PostgreSQL transaction，revision 写入失败会回滚；focused port test 覆盖 commit/rollback。gateway 对八个既有 `/v1/operations/artifacts/*` URL 保持转发，并在 import/export/activate/review 入口执行 body validation、传递 correlation header、用认证 actor 覆盖伪造 body actor。server compatibility shell 已删除未注册的 artifact import/export/activate/edit/history/review route modules 与对应 smoke tests；operations route tests 改为直接注册 plugin，避免绕过 host-owned identity composition。owner routes 使用 `@trapmap/backend-core` 的公开 invocation export，避免 deployment runtime 解析未公开深层入口。

本轮验证：`rtk pnpm --filter @trapmap/service-knowledge-write test --run src/artifact-ports.test.ts src/artifact-routes.test.ts src/migrations.test.ts`（6 tests）、`rtk pnpm test:file -- packages/host-distributed/src/gateway/routes.test.ts`（18 tests）、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（7 tests）、`rtk pnpm test:deployment-smoke`（135 non-PG tests + 23 app tests）、`rtk pnpm eval:smoke`、`rtk pnpm typecheck`、`rtk pnpm check:docs-drift`、`rtk pnpm check:structure` 均通过。`rtk pnpm exec fallow audit --base wave1-fallow-base --format json --quiet --explain` 无 dead-code/boundary violation，但仍列出 inherited complexity 4 与迁移期 duplication；因此 Wave-2 继续保持未完成。

后续收口证据：`ArtifactWritePort` 已收窄为唯一 artifact mutation port；`ArtifactReadProjection` 明确为 Wave-7 前的 PostgreSQL-only 临时读面，仅提供 `getById`、筛选/检索列表、history、export 和 review queue。owner migration test 冻结 artifact、revision、derived、lifecycle、review、maintenance、labels 所需列和查询索引；owner transaction test 覆盖 revision/lifecycle 写入与任一 revision 写失败回滚。`runtime-infra` 的 `SkillShareerRepos` 已删除 artifact 成员和 `createArtifactRepository` 调用；host-local 改为显式注入 `artifactWriter` 与 `artifactReadProjection`，不再暴露 deprecated `artifactRepo`。本轮 focused 验证：`rtk pnpm --filter @trapmap/service-knowledge-write test --run src/artifact-ports.test.ts src/artifact-routes.test.ts src/migrations.test.ts src/pg-ports.test.ts`（15 tests）、`rtk pnpm exec vitest run --project host-local packages/host-local/src/nest/runtime/host-services.test.ts`（1 test）、`rtk pnpm exec vitest run --project runtime-infra packages/runtime-infra/src/shared-infra.test.ts`（3 tests）和 `rtk pnpm typecheck` 通过。Wave-2 仍未完成：server compatibility artifact repository、artifact/label/lifecycle JSON/snapshot fallback、worker injection 和 Wave-9 migration helper 拆分尚未删除；本轮未运行 deployment smoke、eval、docs/structure 或 Fallow full gates，故不勾选完成项。

本轮未完成收尾：server 已取消注册 `/v1/operations/migrate`，并以 host 注入的 `ArtifactReadProjection` 替换 compatibility aggregate 中的 artifact 写 repository；静态 guard 禁止 server/runtime-infra/host/worker 导入、实例化 legacy artifact writer 或保留 `artifactRepo` 注入（仅 `lib/persistence/migrate-artifacts.ts` 仍为 Wave-9 helper allowlist）。`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（9 tests）、`rtk pnpm test:file -- packages/server/src/routes/operations/index.test.ts`（8 tests）与 `rtk pnpm typecheck` 通过。`rtk pnpm --filter @trapmap/server test --run` 失败（328 failures）：该包命令从 `packages/server` 执行，多个 guard/fixture 使用仓库根相对路径，且大量既有测试直接调用未注入 identity bundle；未运行 deployment smoke、eval、docs/structure 或 Fallow gate。因此 Wave-2 保持未完成。

本轮最终收口：`ArtifactReadProjection` 已提升到 `@trapmap/backend-core` 共享 port，server candidate/remediation 只消费该 PG-only 读面；server 的 legacy artifact repository、PG writer/reader、JSON fallback 与 migrate helper 均已删除。不可注册的 Wave-9 backfill helper 位于 `service-knowledge-write/src/wave9-artifact-backfill.ts`，只接受 owner 的 read/write ports，覆盖幂等跳过和逐项失败记录。gateway 的八条 artifact URL 传递 correlation/trace headers，并将认证 actor 置于 `x-trapmap-actor-id`；owner internal routes 从该受信头派生 actor，拒绝 body actor 伪造。focused guards、owner routes、gateway routes、deployment smoke、eval smoke、`rtk pnpm typecheck`、doc-drift 与 structure guard 通过。`rtk pnpm exec fallow audit --base wave1-fallow-base --format json --quiet --explain` 仍失败：3 个本轮 server→backend-core boundary violation 与 `internalHeaders` complexity finding 尚需收敛；因此 Wave-2 不勾选完成。

本轮最终 Fallow 收敛：`ArtifactReadProjection` 已从 `@trapmap/backend-core` 下沉至 `@trapmap/contracts`，并经 contracts 根入口导出；server、knowledge-write 与 host-local 的读投影类型均改为直接消费 contracts，backend-core 不再定义或导出该 port。gateway 已拆分 correlation/trace header 收集、普通 internal request options 与 artifact 专用 trusted-actor header 组装；八条 artifact URL 继续传递 `x-trapmap-actor-id`，其他内部调用不附加该身份头。新增 guard 覆盖 contracts port、backend-core 删除和 server boundary，owner route 覆盖缺失 trusted actor 的 401 与 body spoof 的 canonical 403。通过：`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（11 tests）、`rtk pnpm --filter @trapmap/service-knowledge-write test --run src/artifact-ports.test.ts src/artifact-routes.test.ts src/wave9-artifact-backfill.test.ts src/pg-ports.test.ts`（14 tests）、`rtk pnpm test:file -- packages/host-local/src/nest/runtime/host-services.test.ts`、`rtk pnpm exec vitest run --project runtime-infra packages/runtime-infra/src/shared-infra.test.ts`、`rtk pnpm test:file -- packages/host-distributed/src/gateway/routes.test.ts`（20 tests）、`rtk pnpm exec vitest run --project host-distributed packages/host-distributed/src/shared/database-ownership.test.ts packages/host-distributed/src/knowledge-write/routes.test.ts`（10 tests）、`rtk pnpm test:deployment-smoke`、`rtk pnpm eval:smoke`、`rtk pnpm typecheck`、`rtk pnpm check:docs-drift`、`rtk pnpm check:structure` 与 `rtk git diff --check`。最终 `rtk pnpm exec fallow audit --base wave1-fallow-base --format json --quiet --explain` 报告本轮新增 dead-code 0、complexity 0，但仍有 23 个新增 duplication groups（retrieval adapter、gateway governance branches、owner-local schema/ports 与既有 Wave-2 tests）；故 Wave-2 保持未勾选，待这些精确重复项收敛后再完成。

Wave-2 Fallow closeout：retrieval actor-session assertions、knowledge-write transaction/read-projection fixtures、artifact read hydration 与 compatibility repository assembly 已收敛；`persistence-schema` 以无状态列工厂复用 revision、lifecycle、boundary、derived record 和审计列，且保持冻结 SQL 的表名、列名、默认值与索引语义不变。`rtk pnpm exec fallow audit --base wave1-fallow-base --gate new-only --format json --quiet --explain` 现报告新增 dead-code `0`、complexity `0`、duplication `0`，无新增 boundary。通过：协调 PostgreSQL retrieval adapter focused test（7 tests）、knowledge-write focused tests（14 tests）、`persistence-schema` build、`rtk pnpm typecheck`、`rtk pnpm test:deployment-smoke`、`rtk pnpm eval:smoke`、`rtk pnpm check:docs-drift`、`rtk pnpm check:structure` 与 `rtk git diff --check`。Wave-2 owner migration 的其他 completion 条件仍未满足，因此不勾选该 wave。

Wave-2 PG-first follow-up：`createKnowledgeRepository()` 现缺少 PostgreSQL pool 即 fail-fast，删除 `InMemoryKnowledgeRepository` 的 snapshot mutation/read fallback；`emitLifecycleTransition()` 也拒绝 JSON event-bus dispatch，只能经 PostgreSQL outbox。snapshot usage guard 已去除 knowledge 与已删除 artifact migration 的陈旧豁免；retirement guard 将 schema 的 `store_snapshot` 登记迁至 `persistence-schema` 并删除失效 outbox 条目。协调 PostgreSQL 下 `PgKnowledgeRepository` 契约修正为唯一 lifecycle fixture ID、384 维向量和单次 pool close，29 tests 通过；deployment smoke、eval smoke、typecheck、doc-drift、structure、diff check 与 Wave-1 baseline 的 new-only Fallow audit 均通过，新增 dead-code/complexity/duplication 仍为 `0`。Wave-2 仍未完成：server/runtime-infra 继续构造 legacy knowledge repository，且 server 路由、labels、maintenance/decay、lifecycle subscribers 与 write-side service 尚未完成 owner-local port/internal HTTP 切换。

本轮收尾验证：knowledge-write PG/routes focused tests 为 18/18；`rtk pnpm test:deployment-smoke` 通过 175 tests，`rtk pnpm eval:smoke` 通过 81/81，`rtk pnpm typecheck`、retirement guard（13/13）、doc-drift（46 rules）、structure guard 与 `rtk git diff --check` 均通过。新增 task-worker shutdown 回归覆盖停止竞态，runtime-infra knowledge projection 改为本地读投影类型以清除新增 boundary violation；app smoke 不再出现 pool teardown unhandled rejection。当前 `rtk pnpm exec fallow audit --base wave1-fallow-base --gate new-only --format json --quiet --explain` 仍未通过：基线遗留 28 个 dead-code，且本轮报告 7 个 complexity 与 28 个 duplication groups；因此 `completedOwnerWaves` 仍只含 `wave-1`，尚未回写 `BOUNDARIES.md`/`REPO_STRUCTURE.md` 的 Wave-2 完成记录，待 Fallow 收敛后再执行最终 allowlist/doc closeout。

Wave-2 owner closeout（2026-07-16）：`service-knowledge-write` 的事务、生命周期、outbox 与 artifact owner surface 保持 PG-first；retrieval projection、确定性 fixture builders 与 repository read-model wrapper 下沉到 `packages/contracts`，server 与 `service-knowledge-read` 只保留各自的 normalization/remediation 行为。candidate、labels、graph、queue、compatibility guard 测试复用无副作用 helper，未新增跨 service implementation import。Wave-2 代码收口提交为 `3b2e16b5`、`7a19fc71`、`e8c3c5e7`、`b3374307`；`completedOwnerWaves` 已加入 `wave-2`，遗留 `persistence-schema` knowledge `store_snapshot` 注记归入 wave-9。

本轮证据：focused retrieval/contracts tests 为 3、server read-model 为 9、knowledge-read read-model 为 1、labels backfill 为 1、compatibility retirement guard 为 13，另有此前 candidate detector（24）、PG detector（10）、reconcile（39）与 labels runner（1）通过；`rtk pnpm typecheck`、`rtk pnpm check:docs-drift`（46 rules）、`rtk pnpm check:structure` 与 `rtk git diff --check` 均通过。`rtk pnpm exec fallow audit --base wave1-fallow-base --gate new-only --format json --quiet --explain` 在 `b3374307` 报告 verdict `pass`：新增 dead-code、boundary、complexity、duplication 均为 `0`；总量中的 complexity `23` 与 duplication `66` 明确标记为 inherited。

最终部署证据受环境前置条件限制：`rtk pnpm test:deployment-smoke` 的 135 个非 PG tests 通过，但 app/startup PostgreSQL 阶段因 `/var/run/docker.sock` 不存在而未执行；`rtk pnpm eval:smoke` 因同一 Docker daemon 缺失退出。恢复 Docker daemon 后需重跑这两项，不能将本轮记录解释为 PG/eval 通过。

## Task 5 — Wave-3 closeout evidence

`service-candidate-ingestion` 现拥有 owner-local candidate/duplicate/lineage PostgreSQL bundle 和 processing runtime。candidate 状态、analysis、duplicate case/matches 与 manual result 仅写 owner-local 关系表；`candidates` 行不再保存 analysis、duplicate 或 manual-result JSON 镜像。distributed candidate host 使用 owner-local processing queue adapter，host-local review controller 仅通过公开 `CandidateIngestionPort` 读取和写入；shared distributed ports 不再创建 candidate repository。server compatibility candidate routes、repositories、recovery/worker 和 runtime-infra repository aggregate 均已删除。

收口验证：`rtk pnpm --filter @trapmap/service-candidate-ingestion test --run src/pg-ports.test.ts src/processing.test.ts` 通过（21 tests）；`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts` 通过（17 tests）；`rtk pnpm typecheck` 通过。Docker daemon 可用（`rtk docker info` 成功），但此 worktree 无法 materialize package-local links：`src/migrations.test.ts`、host-local controller focused test、deployment smoke 与 eval smoke 分别在解析 `drizzle-orm/node-postgres`、`@nestjs/common`、`graphology`/CLI `zod.iso` 前置时中止；未将其误记为产品失败。恢复完整 workspace install 后仍须补跑 `rtk pnpm test:deployment-smoke` 与 `rtk pnpm eval:smoke` 的 PostgreSQL acceptance；该环境前置不改变 Wave-3 已删除的 runtime ownership surface。

本轮 owner-domain 增量：candidate duplicate detector 现通过 service-local `CandidateCorpusReadPort` 读取获批 corpus，不导入 knowledge/artifact service 实现或直读其表；exact duplicate 的 fingerprint、analysis snapshot 与 case 都由 candidate owner 生成。candidate resolution、manual-result 与 publish internal routes 仅接受 gateway 的 `x-trapmap-actor-id`，缺失 trusted actor 返回 401，body actor 与该身份不一致返回 403；distributed gateway 以认证 actor 覆盖 body 值并转发该 header。TDD RED 为未传身份时 route 返回 200；修复后 candidate owner focused suite（26 tests）、gateway routes（23 tests）和 package typecheck 通过。Fallow 的增量缓存偶发 `unable to open database file`；使用 `rtk pnpm exec fallow audit --base wave1-fallow-base --gate new-only --format json --quiet --explain --no-cache --output-file /tmp/wave3-foundation-fallow.json` 可稳定完成，报告新增 dead-code、boundary、complexity 均为 `0`，仍有 12 个新增 duplication group。这些重复来自 owner-local schema/PG subtable mapping 与尚未删除的 Wave-3 compatibility 实现并存，因此不通过且不可作为 Wave-3 closeout evidence。该增量不改变 Wave-3 未完成判断：legacy worker、公开 gateway compatibility、server/runtime-infra 删除与 distributed/host-local acceptance 仍待完成。

## Task 5 — Wave-4 implementation evidence (intermediate)

`service-governance-review` 现提供 owner-local `createGovernanceReviewPgOwnerBundle()`；feedback queue 的 PostgreSQL port 由 service package 创建，distributed governance host 与 host-local Nest composition 均只注入该 owner bundle。`host-distributed/shared/ports.ts` 不再构造或暴露 feedback repository，host-local 也已删除将 compatibility `HostLocalRepos['feedback']` 适配为 backend-core port 的实现。最终知识 mutation 仍仅由既有 `KnowledgeWritePort` delegation 执行。

Conflict owner increment：governance baseline 现拥有 `conflict_relations` 及其 canonical-pair unique index；`createGovernanceReviewPgOwnerBundle()` 暴露 owner-local upsert 和 contracts-only `ConflictReadProjection` read interface。该 port 不允许 knowledge-read 导入治理实现。server lifecycle subscriber、job-runtime workflow 和 compatibility conflict repository 仍未切换，故 Wave-4 继续保持 in-progress。

Review correction：最初 owner bundle 的简化 SQL 错误指向不存在的 `feedback_queue`，且 distributed governance composition 遗留已删除的 shared `ports.auditLog` 引用。修正后 bundle 从 persistence schema 的 `feedback_records` 与 `feedback_custom_answers` 表名派生 SQL，覆盖完整 feedback record、custom-answer hydration、filter/update 与 remediation 字段；governance host 直接组合 identity owner 的 `auditLog`。

本轮以 owner-local PG bundle test 作为 RED，但当前 worktree 缺少 `node_modules`，`rtk pnpm --filter @trapmap/service-governance-review test --run src/pg-ports.test.ts` 与 `rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts` 都在 Vitest 启动前报 `Command "vitest" not found`，不可计为产品失败。`rtk pnpm typecheck`、`rtk pnpm check:docs-drift`（46 rules）、`rtk pnpm check:structure` 与 `rtk git diff --check` 通过；Fallow 命令退出成功但明确警告无 `node_modules`，因此不能作为 boundary closeout evidence。以上记录为切换前中间状态；最终 Wave-4 closeout 见下文。

## Wave-4 closeout evidence（2026-07-21）

- `service-governance-review` 现在是 feedback record/admin、conflict detection and relations、remediation、badcase async commands、operator projections 的唯一 owner。最终知识 aggregate mutation 继续通过 `KnowledgeWritePort` 委托；`service-job-runtime` 只消费 typed governance handlers，并继续拥有 task queue、retry/backoff、lease/reclaim、workflow 与 dead-letter。
- `packages/server` 已删除 Wave-4 feedback/conflict/admin/remediation routes、repositories、lifecycle subscriber 与 remediation/badcase handlers；`packages/runtime-infra/src/repos.ts` 不再暴露 feedback/conflict aggregate 成员。badcase export 通过 governance owner boundary，未保留 `@trapmap/server` concrete import。
- `packages/host-distributed` 保留既有 `/v1/feedback`、`/v1/operations/feedback*` public URLs，认证 actor、trace/correlation headers、upstream/error semantics 由 gateway 转发；governance owner 内部 API 仅作为 private service surface。host-local 通过同一 owner ports 组合，不新增跨 service concrete implementation import。
- TDD closeout：新增 guard completion assertion 先以 `completedOwnerWaves = ['wave-1', 'wave-2', 'wave-3']` RED（25 passed, 1 failed），加入 `wave-4` 后 guard GREEN（26/26）；production scan、runtime aggregate scan 与 badcase boundary scan 均为空。
- Full closeout evidence：`rtk pnpm eval:smoke` 81/81；`rtk pnpm test:deployment-smoke` 静态 135/135、PostgreSQL app/startup 37/37；`rtk pnpm check:docs-drift` 46/46；`rtk pnpm check:structure`、`rtk pnpm typecheck`、`rtk git diff --check` 均通过。
- Fallow new-only boundary audit verdict 为 `pass`；本轮新增 dead-code、boundary、duplication、complexity 均为 0，仅保留 inherited complexity。Wave-5+ 与 `store_snapshot` 的 Wave-9 删除范围未在本轮修改。

后续 host-local queue composition：`buildOwnerReviewQueueProjection()` 直接消费 knowledge-write owner 的规范化 `KnowledgeOwnerPort`，不再通过 `HostLocalServices.repos` 读取 legacy raw knowledge records；其 contract 已包含 owner、submission、review note 与 decision，保留原有 team/security/status 过滤语义。TDD 先以缺失 owner projection RED，再完成 governance package 与 Nest controller 切换；governance queue focused test（2 tests）、host-local candidate review controller/runtime focused tests（7 tests）、retirement guard（28/28）与 typecheck 均通过。new-only Fallow 未新增 finding；`HostLocalServices.repos` 仍供 knowledge-read compatibility adapter 使用，待 retrieval owner composition 后删除，故不把该 follow-up 解释为 shared-infra 关闭。

## Task 6 — Wave-6 job-runtime bridge evidence

`service-job-runtime` now provides the owner-local PostgreSQL task queue/outbox transport factory, retaining dedupe, lease reclaim, retry/backoff, dead-letter, and outbox claim/complete/fail semantics. The distributed `job-runtime` host composes that factory directly; governance conflict handling remains a typed workflow call to governance-review and does not move conflict persistence or decisions into the runtime.

`runtime-infra` no longer exports its async factory, queue/outbox transports, task worker, RabbitMQ transport, or lifecycle event-bus compatibility surface. The retirement guard records those exports and server lifecycle queue/outbox/store-snapshot construction as Wave-6 deletion contracts. `completedOwnerWaves` intentionally does not yet include `wave-6`.

本轮继续收口：host-local 通过 `service-job-runtime` owner factory 注入 transport，compatibility server 只接受 host 注入，不再构造 async transport；Job Runtime server 同时管理 task 与 outbox consumer 的启动/停止。RabbitMQ task publishing、outbox 成功/失败/即时停止和 server lifecycle 均有 owner-local focused tests；distributed `knowledge.approved` outbox 事件只投递 typed `governance.conflict-detection`，业务判断仍在 governance-review。`runtime-infra` 与 server 的遗留 async 实现文件已删除，guard 同时拒绝其重新出现。

本轮通过：job-runtime focused suite（9 tests）、retirement guard（28 tests）、host-local async composition（3 tests）、host-distributed database ownership（8 tests）、`rtk pnpm typecheck`、`rtk git diff --check`、`rtk pnpm check:docs-drift` 与 `rtk pnpm check:structure`。`test:deployment-smoke` 的静态段为 135/135；重新构建 job-runtime 后 PostgreSQL app suite 显示 23/23 passed，但该命令的完整 coordinator 收尾未在本轮记录为 closeout evidence。`test:runtime-foundations` 仍受未提供 PostgreSQL 的 host composition 前置影响；`test:distributed-closeout` 保留既有两处 500 closeout failure。Fallow new-only audit 仍报告迁移期复杂度/重复与 runtime-infra 已移除依赖，未将 wave-6 标记完成。

2026-07-21 follow-up：distributed closeout 的两个 500 已定位为 smoke harness 在 candidate/governance 到 knowledge-write 的二跳中遗漏 `x-trapmap-actor-id`；knowledge-write 的 `trustedActor` 正确拒绝该缺失身份。harness 现转发受信 actor，且 closeout test 断言 request、trace 和 actor header 均保留。`rtk pnpm test:distributed-closeout` 通过（35/35），job-runtime handler/ownership focused tests（15/15）、`rtk pnpm typecheck`、compatibility guard（28/28）和 `rtk git diff --check` 通过。`test:runtime-foundations` 现使用与 deployment smoke 相同的 PostgreSQL coordinator，并支持显式 `TRAPMAP_POSTGRES_COORDINATOR_URL`：Docker daemon 不可用时，该 URL 指向专用 PostgreSQL 管理库，runner 仍创建唯一数据库、按六个 owner migration 顺序执行、验证 `vector` 扩展并清理数据库。以临时本地 PostgreSQL 18 和从 `pgvector` 0.8.1 源码编译的隔离扩展目录运行 `TRAPMAP_POSTGRES_COORDINATOR_URL=postgres://trapmap@127.0.0.1:55432/postgres rtk pnpm test:runtime-foundations` 通过（150/150），且数据库清理查询为空。Fallow trace 后移除了四个无消费者的 compatibility exports；当前 `rtk pnpm exec fallow audit --base main --format json --quiet --explain` verdict 为 `pass`，新增 dead-code、boundary 和 duplication 均为 0，剩余 3 个 complexity finding 均为 inherited。guard 的 Wave-6 production scan 为空，故 `completedOwnerWaves` 现包含 `wave-6`。

## Task 7 — Wave-7 knowledge-read evidence

`service-knowledge-read` 现本地拥有 retrieval/support default assembly、graph query structural seam 与 retrieval read-model repository seam；生产 source 不再导入 `@trapmap/server` 或 `@trapmap/runtime-infra`，其 package manifest 也不再依赖两者。`runtime-infra` 的 knowledge-read retrieval/support assembly 文件、导出与过期测试均已删除；retirement allowlist 同步移除已经不存在的 Wave-7 例外。

TDD：先将 read-service import-boundary test 扩展为禁止两个 compatibility packages，观察到 4 个预期失败；owner-local assembly 后变绿。`service-knowledge-read` 全量 focused suite（56 tests）、runtime-infra assembly-retirement tests（7 tests）、retirement guard（28 tests）、`rtk pnpm typecheck`、doc-drift、structure 与 diff check 都通过。后续将 SQL assertion 与四个 import-boundary tests 的重复机制分别收敛为 local/shared test helpers，new-only Fallow duplication 从 42 降至 37，且未新增 test dependency 或 boundary finding；knowledge-read 自身只保留与尚未删除的 server compatibility source 的三个重复组。Docker coordinator 下 `rtk pnpm test:deployment-smoke`（静态 135/135、PostgreSQL app/startup 成功）与 `rtk pnpm eval:smoke` 均已通过。该 eval RED 暴露 `buildPostgresComposedServer()` 未注入 job-runtime outbox worker factory；以 `createJobRuntimeOutboxConsumer` 按 host-local 模式注入后，composition 回归测试（9/9）与 eval smoke 通过。当前 import-boundary suite（12/12）和 `fallow audit --base main`（new-only dead-code、boundary、duplication 均为 0）确认 read owner 的 compatibility edge 已清空；`store_snapshot` schema 的删除责任改归 Wave-9，guard 现将 `wave-7` 标记完成。

## Task 8 — Wave-8 host surfaces progress

distributed gateway 现在本地拥有仅包含 request/correlation fields 的 `GatewayRequestContext`，不再导入 `@trapmap/server` 的 compatibility request-context type；retirement allowlist 已删除该 gateway 例外。TDD 静态 import-boundary test 先因原 server import 失败，替换为 owner-local structural type 后通过。`OTEL_DISABLED=true rtk pnpm exec vitest run --project host-distributed packages/host-distributed/src/gateway/server.test.ts`（6 tests）、`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts`（28 tests）和 `rtk pnpm typecheck` 均通过。未启用该测试变量时，`server.close()` 会等待连接不可用的本地 OTLP exporter（`localhost:4318`）并超时；这与 request-context ownership change 无关。Wave-8 其余 host composition、capability config 与 compatibility dependencies 仍待迁移，不能标记完成。

host-local follow-up：`host-services.ts` 与 `server-composition.ts` 不再从 `runtime-infra` 导入 `getStorePool`；host-local 的结构性 `getHostLocalStorePool()` 只依赖可选 `getPool()` seam，并保留缺少 PostgreSQL pool 时的既有 fail-fast。先新增 import-boundary RED，再切换 helper 并删除两条过期 Wave-8 allowlist entry。`rtk pnpm exec vitest run --project host-local packages/host-local/src/nest/runtime/import-boundary.test.ts packages/host-local/src/nest/runtime/host-services.test.ts`（5 tests）、retirement guard（28/28）、`rtk pnpm typecheck` 与 `rtk git diff --check` 通过；new-only Fallow 没有新增 host-local boundary finding。`rtk pnpm test:distributed-closeout` 通过（35/35），`OTEL_DISABLED=true rtk pnpm test:deployment-smoke` 通过静态 135/135 与 coordinated PostgreSQL app/startup suite；未禁用时本地 OTLP exporter 不可达会使 metrics test 的 exporter shutdown 超过 5 秒，coordinator 下单测以相同环境变量通过，属环境 exporter 前置而非 pool-seam 回归。`shared-infra.ts`、host manifest 和 compatibility server composition 仍保留，故 Wave-8 继续保持未完成。

distributed host follow-up：治理 read adapter 的两个重复 remote-error mapper 改为 host-owned `toInvocationError()`；`conflict-read` 与 governance ports 继续保留相同的 canonical `InvocationError` taxonomy。`rtk pnpm exec vitest run --project host-distributed packages/host-distributed/src/governance-review/conflict-read.test.ts packages/host-distributed/src/governance-review/ports.test.ts packages/host-distributed/src/governance-review/delegation-acceptance.test.ts`（9 tests）和 typecheck 通过；new-only Fallow 的 introduced complexity 从 17 降至 15、duplication 从 37 降至 36。该 cleanup 不改变 Wave-8 未完成状态。

host-local retrieval follow-up：`service-knowledge-read` 现提供 `createKnowledgeReadOwnerRetrievalServices()`，仅接收 knowledge-write 的 `KnowledgeOwnerPort`、artifact read projection、governance retrieval projection 与 host-owned AI/store/graph seams。`host-runtime.ts` 不再把整个 `HostLocalServices` 强制转换成 retrieval services；`HostLocalServices` 也不再暴露 compatibility `repos` 或 `usageAnalyticsRepo`。embedding cache 写入扩展为 knowledge owner 的显式 `updateEmbeddingCache()` operation，并由 knowledge-write PostgreSQL bundle 持久化，避免 retrieval assembly 回退到 legacy repository。TDD RED：host-local composition assertion 在仍有 `repos` 时失败，knowledge-write port test 在缺少该 operation 时失败；GREEN：knowledge-write focused tests（18/18）、knowledge-read seam/search tests（2/2）、host-local composition/import-boundary tests（5/5）、retirement guard（28/28）与 `rtk pnpm typecheck` 均通过。`OTEL_DISABLED=true rtk pnpm test:deployment-smoke` 通过静态 135/135 与 coordinated PostgreSQL app/startup 23/23；doc-drift（46 rules）、structure 和 diff check 也通过。new-only Fallow 仍为既有迁移期 6 dead-code boundary、15 complexity 与 36 duplication groups，未增加 host-local boundary finding，故其 verdict 不能作为 closeout。该组合移除了 host retrieval 对 runtime-infra aggregate 的依赖，但 `shared-infra.ts` 及其 server-owned AI/indexing/graph compatibility composition 仍存在，故 Wave-8 继续保持未完成。

host-local adapter follow-up：`adapterRegistry` 在 host-local 只由 `RuntimeInfraShared` 创建并透传到 `HostLocalServices`，没有生产消费者；移除该字段、类型 alias 与 `buildDefaultAdapterRegistry()` compatibility import，避免 runtime-infra 继续为 host-local 组装无消费的 server indexing implementation。TDD RED：host-local composition test 在 registry 仍被暴露时失败；GREEN：host-local/runtime-infra focused tests（12/12）、retirement guard（28/28）、typecheck 与 `OTEL_DISABLED=true rtk pnpm test:deployment-smoke`（静态 135/135、PostgreSQL app/startup 23/23）均通过。runtime-infra 仍拥有 AI、graph 与 legacy store composition，因此本项只是缩小 Wave-8/10 迁移面，不能作为 package retirement 或 Wave-8 closeout。

runtime-infra aggregate follow-up：host-local 已不消费 `RuntimeInfraShared.repos`，该 aggregate 只为创建 graph index 而构造已废弃的 knowledge projection 和 usage analytics repository。shared composition 现仅内部创建所需 graph index；`repos.ts`、公开 `createRuntimeInfraRepos`/`RuntimeInfraRepos` export、无消费的 backend-core/contracts/drizzle dependencies 均已删除。TDD RED：runtime-infra entry test 在仍公开 aggregate 时失败；guard RED：删除文件后检测到过期 `repos.ts` allowlist，移除该条后 GREEN。runtime-infra focused tests（6/6）、host-local composition/import-boundary（5/5）、retirement guard（28/28）、typecheck 与 diff check 通过；clean Fallow audit 也消除该 aggregate 的 boundary/unresolved-import finding，new-only 现为 5 dead-code boundary、15 complexity、36 duplication。`runtime-infra` 仍通过 AI、graph 和 legacy store 依赖 `@trapmap/server`，故 Wave-10 仍未完成。

capability config follow-up：共享 `DeploymentCapabilities`、host-local/server config schema、server compatibility deployment profile 与 retrieval fixture 不再声明或接受 `supportsJsonStore`；`storagePosture` 继续表达 profile 的存储要求。TDD RED：local-agent capability assertion 在旧字段仍存在时失败；GREEN：backend-core capability model（32/32）、server config/profile（48/48）和 host-local config boundary（1/1）通过。guard 随之删除 backend-core、host-local config、server config 与 server deployment-profile 的四项过期 `JsonStore` allowlist，retirement guard（28/28）与 diff check 通过。Wave-8/9 其余 host composition 与 snapshot backfill/delete surface 仍在，不能标记完成。

## Wave-9 preflight — legacy snapshot bucket ownership（2026-07-21）

在删除 `store_snapshot` 前，逐项核对 `packages/server/src/lib/store/store-data.ts`。表中的 “covered” 仅表示已有 owner-local、可保留记录 ID 的导入实现；“rebuild” 表示目标是派生投影而非业务真相，必须以 authoritative owner 的数据重建并计数核验；任何 “missing” 均禁止进入 destructive migration。

| Legacy bucket | Real owner / target | Existing owner-local path | Required disposition |
| --- | --- | --- | --- |
| `counters` | none; IDs are now owner-generated or supplied | none | discard only after a production scan proves no `nextId`/counter consumer remains |
| `users`, `teams`, `memberships`, `accessKeys`, `sessions`, `auditEvents` | `service-identity-access` tables | `migrateIdentityAudit()` preserves IDs, timestamps and payloads, and verifies table counts | covered |
| `knowledgeEntries` | `service-knowledge-write` authoritative knowledge tables | `KnowledgeOwnerPort` has no snapshot-preserving import operation | missing: add an owner-local importer that retains history, submissions, lifecycle, index/maintenance metadata and IDs |
| `skillArtifacts` | `service-knowledge-write` artifact tables | `migrateSkillArtifacts()` inserts a complete `SkillArtifact` idempotently | partially covered; verify every legacy record converts losslessly |
| `artifactFilePayloads` | artifact content storage owned by knowledge-write | revision `files` store metadata only; no owner-local durable payload importer exists | missing: retain content keyed by artifact/revision/path before deleting snapshot |
| `candidateSubmissions`, `duplicateCases`, `entityLineage` | `service-candidate-ingestion` tables | PG owner bundle has idempotent candidate insert, duplicate upsert and lineage insert | missing: expose a dedicated snapshot importer plus count/field verification; candidate analysis/manual-result fields must be retained |
| `graphIndexDocuments` | `service-knowledge-read` derived graph projection | PG graph table exists, but no read-owner importer is established | rebuild from authoritative knowledge/artifact owners; verify source and destination/rebuild counts, not snapshot reuse |
| `conflicts`, `feedbackQueue` | `service-governance-review` tables | PG bundle has conflict upsert and feedback insert | missing: add a dedicated snapshot importer with idempotency and custom-answer/remediation field verification |
| `promptVersion`, `rebuildState` | `service-knowledge-read` rebuild coordinator | only legacy snapshot state exists | discard and start a fresh owner-local rebuild; verify no interrupted legacy state is silently resumed |

已有 identity backfill tests cover dry-run, idempotent insert and table-count verification. Artifact tests cover skip-on-existing behavior but do not include `artifactFilePayloads`, so they are insufficient for the full artifact bucket. Candidate and governance PG-port tests cover their normal owner operations, not a snapshot migration. This preflight is therefore not Wave-9 completion evidence.

Candidate-ingestion first slice：新增 `migrateCandidateIngestionSnapshot()`，仅接受 candidate owner 的结构性 ports 与已经抽取的 snapshot records，不导入 server store 类型或实现。它逐项保留 candidate、analysis/manual-result、duplicate case 与 lineage 的 ID 和完整记录；逐项回读核验 source/destination count，完全相同的 rerun 标记 skipped，而同 ID 的不同记录拒绝覆盖并报告错误。TDD RED 先因缺少模块失败，后以冲突 destination 断言得到预期 RED；focused candidate snapshot/PG-port tests（19/19）、`rtk pnpm typecheck` 与 `rtk pnpm eval:smoke` 均通过。该 slice 尚未提供 legacy snapshot export command、representative database evidence 或其他 bucket importer，故 Wave-9 仍未完成。

Artifact payload follow-up：`skill_artifact_files` 现在含有 owner-local `content` 列，knowledge-write baseline migration 同步创建该列。`createArtifactFilePayloadOwner()` 以 artifact/revision/path 为稳定键写入并回读 payload；`migrateArtifactFilePayloads()` 使用该结构性 owner port，保留完整 content，验证写后回读，允许完全相同的 rerun，并拒绝 destination 内容冲突。TDD RED 为缺失迁移模块，GREEN 为 payload/artifact port focused tests（5/5）及 typecheck。它尚未替代 legacy export command 或覆盖 artifact/knowledge 其余 backfill，因此不构成 destructive delete evidence。

Governance snapshot follow-up：`migrateGovernanceSnapshot()` 的 owner input 现使用完整 `FeedbackQueueRecord`/`ConflictRelation` contract，而非仅 ID。对既有 destination 与写后回读均逐字段比较：完全相同的 rerun 才能 skip/verify，任何差异均记录为 error 而不会覆盖。TDD RED 分别覆盖同 ID destination 冲突和写后不一致；GREEN：governance snapshot/PG-port focused tests（9/9）通过，`rtk git diff --check` 通过。`rtk pnpm eval:smoke` 已执行，但本环境 Docker daemon 不可用，PostgreSQL coordinator 在连接 `/var/run/docker.sock` 前失败；须在有 Docker 的环境补跑。knowledge bucket 仍缺少完整 owner-local 字段映射，Wave-9 继续保持未完成。

Knowledge snapshot storage follow-up：legacy record 的完整 metadata、agent review、index/decay/evidence/remediation state 现在具有 `knowledge_entries` 的 owner-local JSONB 落点；submission aggregate 与 reviewer decision history 分别落入 `knowledge_submissions`、`knowledge_review_decisions`，而 revision/review note/lifecycle history 继续使用既有 owner tables。TDD RED：migration contract 缺失 `index_state` 等字段；GREEN：knowledge-write migration test（7/7）、persistence-schema typecheck 与 diff check 通过。此项仅建立无损导入的 storage prerequisite；实际 snapshot importer、回读核验和 representative database evidence 尚未完成。

Knowledge snapshot coordinator follow-up：`migrateKnowledgeSnapshot()` 定义 contracts-only 的完整 legacy aggregate input，不导入 server store 类型；它保留 ID、revision/submission/review/lifecycle histories 及全部 metadata，并要求 owner port 首次写入后完整回读验证。TDD RED：缺失 module；GREEN：snapshot coordinator tests（2/2）覆盖首次导入、匹配 rerun 和不同 history 的 destination 拒绝。knowledge-write package typecheck 仍由既有 `routes.ts` 缺失 `toInvocationErrorResponse` 引入而失败，未归因于该 coordinator；PostgreSQL owner adapter 和代表性数据库证据仍待完成。

Knowledge snapshot PostgreSQL owner follow-up：`createKnowledgeSnapshotOwner()` 在单事务中写入 `knowledge_entries`、labels、revisions、submissions、review decisions 与 lifecycle events；完整 aggregate 作为同一 entry 的 `metadata.legacySnapshotRecord` 仅供 Task 9 回读核验，未创建共享 `store_snapshot` 或导入 server 类型。TDD RED：缺失 owner adapter；GREEN：owner/coordinator focused tests（4/4）覆盖 canonical writes、exact metadata readback、首次迁移、rerun 与冲突拒绝。代表性数据库 backfill 与 package typecheck（仍被既有 `routes.ts` 缺失 helper 阻断）待后续验证。

Task-9 orchestrator core follow-up：新增 `scripts/legacy-snapshot-backfill.ts` 的纯协调层，结构性读取一次 legacy JSON snapshot，将 identity、knowledge、artifact、artifact payload、candidate 和 governance bucket 分别交给 owner-local importer；它不导入 `@trapmap/server` 或 `@trapmap/runtime-infra` 类型。所有 authoritative owner 返回无错误后，才允许 knowledge-read 从 knowledge/artifact source 重建 derived graph projection；legacy `graphIndexDocuments` 只作为重建计数证据，绝不复制。`counters`、`promptVersion`、`rebuildState` 是唯一明确列出的 discard bucket，结果中固定记录；任何 owner conflict 会阻止 graph rebuild 和 compatibility-state deletion authorization。TDD RED 为缺少编排模块，GREEN：`rtk pnpm --config.store-dir=/tmp/pnpm-store test:file -- scripts/__tests__/legacy-snapshot-backfill.test.ts`（2/2）通过，覆盖完整 bucket fixture、single read、derived rebuild 和 conflict rejection。该核心尚未接入真实 PostgreSQL pool/CLI，也没有 representative DB evidence，因此 Wave-9 仍未完成。

Artifact verification hardening：`migrateSkillArtifacts()` 不再把任意 existing ID 视为可跳过；它现在先逐字段比对 existing artifact，首次写入也必须经 owner read projection 完整回读，只有完全相同才递增 `verified`。同 ID 不同 payload 或写后回读不一致均为 migration error，阻断上层 Task-9 deletion authorization。TDD RED：两项新断言分别证明缺失 verified/readback 和 destination conflict 被误标 skipped；GREEN：`rtk pnpm --config.store-dir=/tmp/pnpm-store --filter @trapmap/service-knowledge-write test --run src/wave9-artifact-backfill.test.ts`（2/2）通过。CLI 和 representative database evidence 仍待完成。

Graph rebuild count hardening：Task-9 coordinator 现在以 `knowledgeEntries.length + skillArtifacts.length` 作为 expected authoritative source count；只有 graph owner 回报的 source count 与 destination count 都精确等于该值，才可返回 `readyForCompatibilityStateDeletion`。TDD RED：graph rebuild 对两个 source 回报 `1/1` 时旧实现错误授权删除；GREEN：legacy orchestrator focused tests（3/3）通过。实际 graph owner rebuild command、PostgreSQL evidence 与 destructive migration 仍待完成。

Legacy source adapter follow-up：`scripts/legacy-snapshot-source.ts` 是 Task-9-only PostgreSQL reader，唯一执行 `SELECT data FROM store_snapshot WHERE key = 'main'` 的 source access；它不导入 compatibility store 或 server types，并拒绝缺失任一 owner bucket、无效 counters/promptVersion/rebuildState 的 JSONB row。TDD RED：缺少 source module；GREEN：`rtk pnpm --config.store-dir=/tmp/pnpm-store test:file -- scripts/__tests__/legacy-snapshot-source.test.ts`（2/2）验证 singleton single-read 与缺桶拒绝。真实 target owner wiring、graph rebuild 与 representative DB evidence 仍待完成。

Owner wiring follow-up：`scripts/legacy-snapshot-owner-wiring.ts` 现在使用各 service 的直接 owner-local module（identity audit importer、knowledge/artifact owners、candidate bundle、governance bundle）构造 Task-9 importer dependencies；不使用 workspace runtime alias、server store 或 runtime-infra。graph rebuild 仍为显式 knowledge-read owner callback，不回退到 legacy graph repository。focused wiring test（1/1）通过。直接单文件 `tsc` 仅被既有 `service-governance-review/src/admin.ts` 类型错误及未加载项目 pg declaration 阻断，未报告 wiring 映射错误；完整 package/root typecheck 仍待现有 worktree 错误修复后执行。

Task-9 command follow-up：根 `backfill:legacy-snapshot` 现调用 `scripts/backfill-legacy-snapshot.ts`，并显式要求 legacy source 与已迁移 target 的 PostgreSQL URL；命令只返回删除授权判定，绝不执行 destructive compatibility-state 操作。命令入口/owner wiring focused tests（2/2）、knowledge-write artifact payload tests（7/7）、root typecheck、doc-drift、structure、diff check 与 Fallow duplicate-export check 均通过。当前执行环境没有可用 Docker daemon、PostgreSQL 实例或 `pgvector` 扩展，因此尚未取得 representative database backfill evidence；不得删除 `store_snapshot`、相关 migration assets 或 allowlist。

Task-9 PostgreSQL acceptance follow-up（2026-07-22）：以临时本机 PostgreSQL 18 + pgvector coordinator 创建六个 owner baseline 后，`scripts/__tests__/legacy-snapshot-backfill.postgres.test.ts` 成功从真实 `store_snapshot.main` 回填 identity、knowledge、artifact、artifact file payload、candidate、governance，并从 authoritative knowledge/artifact tables 重建 2 条 graph documents；命令结果为空错误、graph source/destination 均为 2，且断言 `store_snapshot` 表仍存在。测试先暴露并修复 artifact payload 与 artifact revision 的并发次序、JSON key-order 误判、artifact PostgreSQL ISO timestamp/read projection 映射，以及 identity count verification 在单 client 上并发 query 的 pg@9 弃用路径。相关 owner/编排 focused tests（24/24）、coordinated PostgreSQL acceptance（1/1）、root typecheck、doc-drift、structure 均通过。该演练刻意复用同一临时 migrated database 作为 source/target；它是真实 owner SQL evidence，但不是受控 representative legacy source-to-target cutover，因此仍不得删除 `store_snapshot`、其 migration assets 或 allowlist。

Separated source-to-target acceptance follow-up（2026-07-23）：`rtk pnpm exec tsx --tsconfig tsconfig.base.json scripts/run-postgres-coordinated.ts -- pnpm exec vitest run scripts/__tests__/legacy-snapshot-backfill.postgres.test.ts` 曾以独立 source 与已迁移 six-owner target 验证首次导入、重复运行与 destination conflict。该演练证明 owner-local import/readback 的 SQL 路径，但没有证明仍在运行的 compatibility server 可以在删除 `store_snapshot` 后继续工作。随后新增的 coordinated app regression 直接调用 compatibility `PostgresStore.snapshot()`，在提前 drop table 的数据库中以 `42P01` 失败；因此撤回 identity-access 的 destructive migration。`store_snapshot` 的删除必须与 Wave-10 移除所有 server/runtime-infra store consumer 在同一 cutover 中重新引入并验收。根脚本、一次性 command/source/wiring 文件及其 acceptance tests 仍保持删除；此处仅保留历史导入证据，不能视为 state deletion authorization。

Compatibility store consumer follow-up：`teams`、`members` 与 `access-keys` routes 已移除 JSON/snapshot fallback，只使用 identity-access owner repositories；owner 不可用时显式返回 `503 identity_unavailable`。root typecheck、retirement guard（30/30）与 diff check 通过。当前 route-level consumers 仅余 maintenance、admin boundary search、admin benchmark 与 evidence：前三者须先由 knowledge-read 提供 owner-local read projection，evidence 须先由 knowledge-write 提供原子 evidence-review command。它们与 indexing/graph/job consumer 一起构成 Wave-9 destructive migration 的剩余前置，不得提前删除 `store_snapshot`。

Route-level owner projection follow-up（2026-07-23）：host-local 现从 knowledge-write、artifact-read 与 governance retrieval owner ports 构造 `createOwnerReadModelProjection()` 并注入 compatibility gateway。maintenance、admin boundary search 与 admin benchmark 都只读取该 projection；缺失时明确返回 `503 knowledge_read_unavailable`，不回退到 compatibility state。evidence route 先通过 knowledge owner projection 进行现有 team/level authorization，再调用新的 `KnowledgeOwnerPort.reviewEvidence()`；knowledge-write 在单一事务中锁定 entry、更新 `evidence_meta` 并写入 outbox event，route 不再使用 store transaction。focused evidence route/knowledge-write tests（1/1、19/19）、knowledge-read/host-local composition tests（4/4、5/5）、root typecheck、retirement guard（30/30）、doc-drift 与 diff check 均通过。协调 route integration 当前被 workspace package `dist` 的既有独立 build failures 阻断；该限制不得被误记为 snapshot deletion authorization。生产 inventory 现在只余 indexing、graph、job、operations read-model 与 migration fixture consumers。

Operations payload cleanup follow-up：`listArtifactRevisionFilePayloads()` 没有生产调用方，且只会从 compatibility snapshot 读取 artifact payload；已删除而非扩展新的 owner port。snapshot usage guard 同步删除这个 projection exception、此前已迁移 routes 以及已不存在 files 的过期 allowlist 条目；focused guard（3/3）、root typecheck、retirement guard（30/30）和 diff check 均通过。剩余 inventory 不再包含 operations read-model。

Legacy index-backfill cleanup follow-up：server-only `backfill-indexes` CLI 与其 snapshot-backed implementation 没有 owner-local consumer，已作为 Wave-9 migration fixture 删除；`@trapmap/server` package script、snapshot guard entry、retirement allowlist 和异步基础设施文档的运行指引一并移除。focused snapshot guard（3/3）、retirement guard（30/30）、root typecheck 与 diff check 均通过。剩余 production inventory 不再包含 index-backfill fixture。

Graph projection ownership follow-up（2026-07-23）：`GraphIndexDocumentRecord` 与 `GraphIndexRepositoryPort` 现由 contracts 统一定义；`service-knowledge-read` 提供 PostgreSQL-only `createKnowledgeReadGraphIndexRepository()`，不会接收 compatibility store。`runtime-infra` 的 shared composition 改由该 owner port 创建 memory graph-query backend，并在缺少 PostgreSQL pool 时 fail-fast，移除了其对 server graph-index factory 及其 JSON/snapshot fallback 的生产依赖。TDD RED：runtime-infra import-boundary test 在仍导入 `@trapmap/server/lib/graph-index` 时失败；GREEN：knowledge-read graph repository（1/1）、contracts export（196/196）、runtime-infra boundary（4/4）、root typecheck 与 diff check 通过。server 的 indexing pipeline、label repair/backfill 与 compatibility repository aggregate 仍直接创建或消费旧 graph repository，故这不是 graph/index cluster closeout，也不授权删除 `store_snapshot`。

Graph repository aggregate follow-up（2026-07-23）：host-local 现在从 knowledge-read owner 创建 `GraphIndexRepositoryPort` 并传入 compatibility gateway；`createAllRepos()` 不再接受 store 或创建 `createGraphIndexRepository()`，bootstrap 只把 host 注入的 graph port 放入 aggregate。TDD RED：host composition test 先证明 factory 未被调用；GREEN：host-local composition（6/6）、server repository aggregate（6/6）与 root typecheck 通过。旧 server graph repository/factory 仍被 label backfill/merge-repair CLI 使用，indexing event/worker 仍直接 transaction legacy store；这些剩余消费者阻止删除 `store_snapshot` 和 graph compatibility files。

Trap graph indexing follow-up（2026-07-23）：`graphIndexAdapter` 现优先使用注入的 `GraphIndexRepositoryPort` 读取、cycle-check 和 upsert/remove graph documents；knowledge pipeline、lifecycle event 和 shared job handler 将 host graph owner port 一路透传。没有 owner port 的旧 unit-test path 才保留 store-backed helper。TDD GREEN：graph adapter owner-port assertion与 indexing adapter/event/pipeline focused tests（25/25）和 root typecheck 通过。knowledge entry 的 index state、vector/keyword adapter 与 skill graph pipeline 仍 transaction compatibility store，因此本项只迁移 `graphIndexDocuments` 的 trap 写路径，不能视为 indexing cluster 或 Wave-9 closeout。

Skill graph indexing follow-up（2026-07-23）：`artifactGraphIndexAdapter` 现与 trap adapter 一致，优先经注入的 `GraphIndexRepositoryPort` 执行 list/cycle-check/upsert 与 source removal，不再写入 `StoreData.graphIndexDocuments`；artifact fan-out、skill lifecycle event、shared job handler、worker bootstrap 和非 PG inline follow-up 均透传该 owner port。旧 adapter unit path 未注入 port 时保留 store-backed helper，以维持未完成 compatibility shell 的测试与迁移期行为。TDD RED：新增 owner-port sync/remove assertions（2 failures：port 未调用）；GREEN：`rtk pnpm --filter @trapmap/server test --run src/lib/indexing/adapters/artifact-graph.test.ts src/lib/indexing/artifact-pipeline.test.ts src/lib/indexing/skill-events.test.ts`（66/66）及 `rtk pnpm typecheck` 通过。skill event、artifact source read 与其余 indexing state 仍在 compatibility transaction 中，故不授权删除 `store_snapshot`，Wave-9 保持未完成。

Inline lifecycle graph-owner follow-up（2026-07-23）：knowledge indexing subscriber 现显式接收 `GraphIndexRepositoryPort`，并在无 PostgreSQL shared-job fallback 时透传给 `runKnowledgeIndexEvent`；lifecycle bootstrap 从 host-injected aggregate 传入该 port。skill lifecycle scheduling 的 services pick 也纳入 `graphIndex`，使 inline 与 shared-job path 均不会因类型边界丢失 owner projection。TDD RED：subscriber owner-port assertion 在 port 被忽略时失败；GREEN：subscriber、skill lifecycle、knowledge event/pipeline focused tests（27/27）通过。包含 PostgreSQL subscriber 的 integration file 在本 shell 因 `TRAPMAP_DATABASE_URL` 为空、host composition fail-fast 于 pool 初始化之前失败，尚未作为 acceptance evidence；此项同样不授权删除 `store_snapshot`。

Index follow-up snapshot cleanup（2026-07-23）：knowledge 与 skill index event 的外部 `data` 参数均未参与读取，真实 source 始终由 event 内部 transaction 提供；已删除该误导性服务契约以及 shared-job handler、skill inline follow-up、knowledge inline subscriber 为构造该参数而执行的 `store.snapshot()`。`syncKnowledgeIndex()` 仍只接收 transaction-scoped `StoreData`，没有改变 index-state/vector/keyword compatibility writes。focused indexing/lifecycle tests（87/87）和 snapshot usage guard（3/3）通过。其余 transaction consumer、reconcile 与 legacy store implementation 仍是 Wave-9 前置，不授权删除 `store_snapshot`。

Unconsumed maintenance/decay compatibility cleanup（2026-07-23）：production scan 确认 `maintenance/batch.ts`、`decay/batch.ts` 和 `decay/supersede.ts` 仅由彼此与专属 unit tests 引用，route 实际写入已使用 `KnowledgeOwnerPort` 的 knowledge-write owner command。因此删除三份 compatibility `StoreData` mutation helper 及专属 tests，而非迁移第二份 dead implementation；retirement guard 新增文件缺失契约防止回归。同步将 supersede/decay 文档改为 owner-local transaction + outbox 事实。TDD RED：新增 guard 在旧文件存在时失败；GREEN：retirement guard（31/31）、knowledge-write PG-port/routes tests（26/26）、snapshot usage guard（3/3）、doc drift 与 diff check 通过。graph reconciliation、legacy store 和其他 transaction consumer 仍存在，Wave-9 未完成。

Unconsumed artifact serializer cleanup（2026-07-24）：production scan 确认 `packages/server/src/lib/artifacts/index.ts`、`model.ts` 与 `model/{helpers,index,serialize}.ts` 仅互相引用，`toSkillArtifact()` 没有 host、service 或 route consumer；保留 `derive/`，它仍被 eval/test 入口使用。retirement guard 新增逐文件缺失契约，覆盖 serializer aggregate 和专属 `model.test.ts`；TDD RED 在旧 serializer files 存在时失败，随后删除这些 retired paths。清理同时发现 derive test 仍从已退役的 model seam 导入 apply helper，以及一个已在 `3de7d491` 删除实现的 `deriveAndApplyOutputs()` test block；测试改直接覆盖 `derive/legacy.ts` 的实际 helper，并删除过时测试，架构文档改为 contracts + knowledge-write owner ports 和纯 derivation helpers 的事实。独立 review 还发现四个 lifecycle 图仍引用已删除 writer/apply APIs，现已改为 owner-local command/persistence 描述。GREEN：retirement guard（32/32）、snapshot usage guard（3/3）、derive source test（20/20）、root typecheck、doc drift、structure、production scan 与 diff check 通过。包级 artifact suite 的 score integration 仍从 stale `@trapmap/contracts` dist 得到缺失 export；尝试重建 contracts 被既有 `retrieval-fixtures.ts`、`retrieval-projection.ts` 与 `task-queue.ts` 的 4 个 source type errors 阻断，不能作为本次 serializer deletion 的回归或 Wave-9 deletion authorization。`store_snapshot`、graph reconciliation 和其他 transaction consumer 仍保留，Wave-9 未完成。

Label runner graph-owner composition follow-up（2026-07-24）：支持的 label backfill/merge-repair 根命令已从 server executable runners 迁至 root scripts；host-owned runner pool 通过 `service-knowledge-read` 的 PostgreSQL `GraphIndexRepositoryPort` 读取和写回 graph documents，旧 `PostgresStore` runner allowlist 已删除。根 Vitest scripts project 现在按 source alias 解析该组合，label runner test 覆盖 owner graph/knowledge/artifact source 汇集、缺少 `DATABASE_URL` 时不创建 pool 的失败，以及入口 Promise 等待并标记退出失败；两个 entrypoint 均顶层等待该 Promise。GREEN：label runner（3/3）、merge repair（7/7）、retirement guard（33/33）、root typecheck、doc-drift、structure 与 diff check 通过。Fallow audit 无 dead-code、dependency 或 boundary violation，也未归因新增 complexity；仍报告 artifact test 的 inherited complexity 和 working-tree duplication。server label catalog core 仍是 Wave-10 的临时 `@trapmap/server` dependency，旧 graph repository 的 type/in-memory compatibility 仍由 retrieval/graph-query consumers 使用；本项不关闭 Wave-9，亦不授权删除 `store_snapshot`。

Graph reconciliation owner-port follow-up（2026-07-24）：`reconcileGraphIndexesFromSnapshot()` 与 `fullRebuildGraphIndexes()` 现将 graph document 的 list/remove/upsert 全部委派给注入的 `GraphIndexRepositoryPort`；`reconcileGraphIndexes()` 要求该 port，startup 已由 host-owned knowledge-read graph owner 提供，不再有 reconciliation 的 store-backed graph fallback。此前 compatibility store transaction 包裹 owner I/O，会使 in-memory owner 重入同一 store 锁；现在先读取一次 source snapshot，执行 graph owner I/O，再单独 checkpoint `promptVersion`/`rebuildState`，解除该锁耦合。独立 review 发现 one-shot PostgreSQL composition 未传递必需 graph owner，可能被 startup error handler 吞掉而跳过 reconciliation；`buildPostgresComposedServer()` 现同样构造 `createKnowledgeReadGraphIndexRepository(pool)` 并注入，覆盖 snapshot export、benchmark 与 skill import/export 入口。TDD RED：owner-port reconciliation 和 PostgreSQL composition assertions 分别证明旧实现缺少 injected owner；GREEN：reconcile focused tests（18/18）、PostgreSQL composition entrypoints（10/10）、snapshot usage guard（3/3）与 root typecheck 通过。兼容 snapshot 仍是 approved knowledge/artifact source 与 prompt/rebuild checkpoint 的输入，且 server graph repository 的 type/in-memory compatibility 仍有 retrieval/graph-query test consumers；因此 `store_snapshot`、`JsonStore`、`PostgresStore` 均不得删除，Wave-9 继续未完成。Fallow audit 无 dead-code/dependency/boundary violation 或新增 complexity，仍有 artifact test 的 inherited complexity 和 working-tree duplication。

Index metadata owner foundation follow-up（2026-07-24）：`KnowledgeOwnerPort` 新增原子 `updateIndexMetadata(entryId, { indexState, embeddingCache })`；knowledge-write PostgreSQL owner 在单条 `UPDATE knowledge_entries` 中同时写入两个 owner-local 列，并且其 read projection 现在映射 `index_state`。TDD RED 分别证明原子 checkpoint 方法与 readback projection 缺失；GREEN：`rtk pnpm --filter @trapmap/service-knowledge-write test --run src/pg-ports.test.ts`（20/20）和 `rtk pnpm typecheck` 通过。现有 server indexing pipeline、lifecycle event 与 reconciliation 仍以 compatibility store 作为 source/checkpoint，尚未改用该 owner port；此基础工作不构成 `store_snapshot`、`JsonStore` 或 `PostgresStore` 的删除授权，Wave-9 继续未完成。

Server graph repository aggregate retirement follow-up（2026-07-24）：`packages/server/src/lib/graph-index/{index,repository}.ts` 及其专属 tests 已删除；所有 production graph-query/recall consumers 改为直接依赖 contracts 的 `GraphIndexRepositoryPort`，唯一 PostgreSQL 实现仍由 `service-knowledge-read` 提供。原 repository 的 in-memory 实现未迁移到运行时；只有需要 mutable fixture 的 reconciliation/Neo4j tests 在测试文件内声明本地 fake。TDD RED：retirement guard 和 snapshot usage guard 分别先因 aggregate 存在、过期 allowlist 而失败；GREEN：graph query/reconciliation/recall focused tests（26 passed, 1 skipped）、retirement guard（34/34）、snapshot usage guard（3/3）和 root typecheck 通过。Capsule graph channel suite 仍在本次变更前已有 `isRemediationSuppressed is not a function` 的 contracts runtime-resolution failure；它不引用已删除 aggregate，不能作为本项回归或 compatibility-state deletion authorization。Wave-9 与 Wave-10 仍未完成。

Indexing source owner projection follow-up（2026-07-24）：`KnowledgeOwnerPort` 新增 `getIndexingEntry()`，明确返回 indexing 所需的最小 owner-local source（当前 entry 字段、最大 owner revision、`indexState`、`embeddingCache`）；knowledge-write PostgreSQL owner 只通过 `knowledge_entries` 与 `knowledge_revisions` 查询构造它，不再以伪造的通用 `getById()` record 承担此职责。TDD RED：调用缺失的 owner method 失败；GREEN：knowledge-write PG owner tests（21/21）与 root typecheck 通过。server lifecycle/worker 尚未注入该 projection，adapter I/O 也尚未完全移出 compatibility transaction，故本项只建立下一步 cutover 所需 source contract，不能删除 `store_snapshot`、`JsonStore` 或 `PostgresStore`。

Owner-local knowledge indexing follow-up（2026-07-25）：`syncKnowledgeIndexFromOwner()` 现在从 `KnowledgeOwnerPort.getIndexingEntry()` 读取最小 owner projection，并在 adapter I/O 后通过单次 `updateIndexMetadata()` checkpoint `indexState` 与 `embeddingCache`；该路径不调用 compatibility store transaction。approved lifecycle event、inline subscriber 与 shared-job worker 都透传 host 注入的 knowledge owner，PG runtime 不再由该链路读取或写入 compatibility knowledge state。deactivated source 会移除已登记 adapter projection 并清空两个 owner-local metadata 字段；graph adapter 在 owner-local 路径缺少 `GraphIndexRepositoryPort` 时 fail-fast，避免回退写入 compatibility graph documents。TDD RED：owner event 在旧 transaction-only flow 中报 entry missing；GREEN：indexing pipeline/event/subscriber focused tests（28/28）与 root typecheck 通过。`reconcileKnowledgeIndexes()` 和维护端全量操作仍使用 compatibility store；现有 owner `listByFilter()` 有 `LIMIT 100`，在提供分页 indexing enumeration 之前不得把它误用为全量 reconciliation。因此仍不授权删除 `store_snapshot`、`JsonStore`、`PostgresStore`，Wave-9 保持未完成。

Host-local composition and indexing reconciliation follow-up（2026-07-25）：`RuntimeInfraShared` 的 store/AI/graph temporary composition 已直接移入 host-local；host-local manifest 和 runtime source 不再依赖 `@trapmap/runtime-infra`，import-boundary test 先扩大扫描范围而 RED，随后通过。compatibility server primitives 仍只在 host-local shared/server composition 作为 Wave-9/10 的过渡输入，未建立新的 service-to-service concrete import。为避免 maintenance reconciliation 误用固定 `LIMIT 100` 的通用 owner projection，`KnowledgeOwnerPort` 新增 `listIndexingEntries({ offset, limit })`，knowledge-write 使用 owner-local entries/revisions 的分页 SQL 返回 authoritative revision；`reconcileKnowledgeIndexesFromOwner()` 和 maintenance route 在已注入 owner 时读取该 source、执行 adapter I/O 并经 owner checkpoint，不再事务扫描 compatibility store。TDD RED：host runtime-infra boundary 与 missing owner pagination/reconciliation entrypoint 均失败；GREEN：host-local focused tests（8/8）、knowledge-write PG ports（22/22）、indexing/event focused tests（22/22）、retirement guard（34/34）和 root typecheck 通过。skill indexing、legacy store implementation/fixtures 与 compatibility server 仍是删除前置，Wave-8/9/10 均未完成。

Runtime-infra package retirement follow-up（2026-07-25）：`packages/runtime-infra/` 已删除。删除前 production/test import scan 确认没有外部 consumer；host-local shared composition 已在本轮前本地化。同步移除 root/host/read TypeScript project references、path aliases、所有 Vitest alias 与 `runtime-infra` project、Fallow zone/rules、distributed Docker build/production copies、lockfile importer 和 runtime-infra guard allowlist；knowledge-read README、repository architecture tree 与 Dockerfile assertion 改为当前事实。TDD RED：删除 package 后 retirement guard 精确报出 10 条 stale allowlist；移除条目后 GREEN：retirement guard（34/34）、distributed Dockerfile test（2/2）、root typecheck、doc-drift 与 diff check 通过。`packages/server`、`store_snapshot`、`JsonStore`、`PostgresStore` 仍存在，故 Wave-10 不能标记完成，也不授权最终 closeout。

Artifact indexing owner-projection follow-up（2026-07-25）：`ArtifactReadProjection` 新增最小 `getIndexingEntry()` contract，knowledge-write 的 owner-local artifact/revision 查询只返回 graph/capsule 索引需要的治理字段、当前 revision 与 derived output。skill indexing 的 owner branch 在 compatibility transaction 之外读取该 projection，并将 artifact adapter fan-out/removal 交给注入的 graph owner；缺少 graph owner 时 fail-fast，绝不回退写入 `StoreData.graphIndexDocuments`。graph document builder、graph adapter 与 capsule adapter 同时接受该结构投影，legacy `SkillArtifactRecord` 仅保留给未迁移的 JSON compatibility path。shared-job worker、worker bootstrap 与 inline follow-up 均透传 artifact read projection。TDD RED：owner event 在旧 transaction-only flow 中调用 `store.transact()`；GREEN：owner projection port（6/6）、server artifact indexing focused suite（73/73）和 `rtk pnpm typecheck` 通过。`rtk pnpm eval:smoke` 在启动前因 Docker daemon 不可用（`/var/run/docker.sock` 不存在）退出，未作为评测失败或通过记录。skill source compatibility branch、legacy reconcile/store implementation 与 server package 仍存在，因此 Wave-8/9/10 都不勾选完成。

Owner graph reconciliation follow-up（2026-07-26）：`ArtifactReadProjection` 现提供 owner-local `listIndexingEntries({ offset, limit })`，以 artifact 的当前 revision 分页返回最小 graph/capsule source。`reconcileGraphIndexesFromOwners()` 同时枚举 knowledge-write 与 artifact owner projections，再复用原有 stale document removal、cycle validation 和 graph-owner upsert 语义；它不接收或访问 compatibility store，也不迁移 `promptVersion`/`rebuildState`。API startup 在两个 owner projection 都已注入时选择该路径，保留 legacy reconciliation 仅供暂存 JSON compatibility runtime；graph projection sync 仍通过 knowledge-read graph owner 完成。TDD RED：artifact page method、owner reconciliation 和 startup bridge 分别因缺失 API/仍调用 snapshot 而失败；GREEN：knowledge-write artifact ports（7/7）、server reconciliation/bootstrap focused tests（20/20）、root typecheck 与 retirement guard（34/34）通过。`store_snapshot`、`JsonStore`、`PostgresStore` 与 `packages/server` 仍有 production compatibility consumers，故 Wave-9 和 Wave-10 均继续保持未完成。

Graph adapter transaction retirement follow-up（2026-07-26）：trap graph adapter 已删除直接 `store.transact()` 的图文档读写；写入、删除和 cycle validation 现在要求 knowledge-read 的 `GraphIndexRepositoryPort`，其 source guard 不再允许 adapter 重新进入 compatibility transaction。测试改用持久化 graph-owner fake，覆盖写入、空图文档、幂等和删除；TDD RED 为 snapshot usage guard 报出原 transaction，GREEN：graph adapter（6/6）、snapshot usage guard（3/3）和 root typecheck 通过。为保持现有 label alignment 行为，adapter 暂时仍只读 compatibility store 的 pool seam；该 seam 和 `StoreData` compatibility input 必须随 label owner/剩余 JSON runtime 一起删除，不能作为 Wave-9 完成依据。

Lifecycle indexing event retirement follow-up（2026-07-26）：`runKnowledgeIndexEvent()` 已删除 JSON entry lookup、`store.transact()`、StoreData graph document removal 和旧 `syncKnowledgeIndex()` fallback。所有非 noop lifecycle transition 现在要求 knowledge-write indexing owner，并经 `syncKnowledgeIndexFromOwner()` 读取 source、调用 projection adapters 和 owner-local metadata checkpoint；缺少 owner 立即 fail-fast，不能静默回退。TDD RED：snapshot usage guard 报出 events transaction；GREEN：owner-only lifecycle event tests（7/7，包含 approve checkpoint、deactivate graph-owner removal、missing-owner fail-fast）、snapshot usage guard（3/3）和 root typecheck 通过。其余 pipeline/reconcile compatibility transactions、store-pool label seam 与 server package 仍是 Wave-9/10 前置。

Graph startup fallback retirement follow-up（2026-07-26）：`bootstrapGraphReconciliation()` 不再导入或调用 `reconcileGraphIndexes()`；startup 仅接受 knowledge-write 与 artifact owner projections，缺任一 projection 时记录明确的 configuration error，绝不执行 `store.snapshot()`。TDD RED：owner-missing bootstrap 调用 legacy snapshot；GREEN：bootstrap/reconcile focused tests（21/21）和 root typecheck 通过，production scan 仅剩 legacy function definition 而无调用。`reconcileGraphIndexes()`、`reconcileGraphIndexesFromSnapshot()`、prompt rebuild compatibility state 及其 JSON tests 仍待删除，不能将此项视为 Wave-9 completion。

Owner-only indexing reconciliation follow-up（2026-07-26）：`reconcile.ts` 已删除 `reconcileGraphIndexes()`、`reconcileGraphIndexesFromSnapshot()`、full-rebuild/prompt-rebuild compatibility state 和所有 `StoreData` 输入；图投影只从 knowledge-write 与 artifact owner projections 重建。knowledge pipeline 同时删除 `syncKnowledgeIndex()` 与 `reconcileKnowledgeIndexes()` 的 compatibility transaction 实现；maintenance endpoint 现在要求 knowledge 与 graph owner，否则返回 `503 indexing_owner_unavailable`，不会回退读取 snapshot。skill lifecycle event 也要求 artifact 与 graph owner，非 noop 缺 owner 时 fail-fast。TDD RED：snapshot guard 分别报告 `skill-events.ts` 和 `pipeline.ts` 的直接 transaction；GREEN：reconcile/bootstrap（6/6）、skill events（29/29）、owner pipeline（3/3）、snapshot usage guard（3/3）、retirement guard（34/34）及 root typecheck 均通过。maintenance route 的 existing integration test 在本 shell 因无 PostgreSQL host composition 而不能启动，未作为通过证据。artifact/graph adapter 的 `StoreData` compatibility seam、`store_snapshot`、`JsonStore`、`PostgresStore` 与 server package 仍存在，Wave-9/10 继续保持未完成。

Wave-9/10 unconsumed tooling cleanup（2026-07-26）：根 workspace 已删除 `@trapmap/server` development dependency；`scripts/bench-store.ts` 与 `bench:store` root command 一并删除，因为它只比较 `JsonStore`/`PostgresStore` compatibility persistence，未提供 owner-local runtime value。TDD RED：retirement guard 在先移除 allowlist entries 后分别报告 root dependency 和 benchmark 的两项 store symbol；GREEN：retirement guard（34/34）、root typecheck、doc-drift、structure 与 diff check 均通过。其余 server package、store implementation/schema、host bridge 与 snapshot export fixture 仍存在，Wave-9/10 不勾选完成。

Retrieval snapshot exporter owner cutover（2026-07-26）：`scripts/export-retrieval-db-snapshot.ts` 已删除 server config、store record types 与 `buildPostgresComposedServer()` repository aggregate。它现在仅用短生命周期 PostgreSQL pool 构造 knowledge-write owner bundle、artifact read projection 与 knowledge-read graph repository，按原 team filter 导出同一 eval snapshot payload 并结束 pool。TDD RED：entrypoint contract test 在 exporter 缺少 owner factories 时失败；GREEN：owner-projection entrypoint contract、retirement guard（34/34）、root typecheck、doc-drift、structure 与 diff check 通过。真实 PostgreSQL export acceptance 仍需在具备数据库的环境补跑；`store_snapshot`、`JsonStore`、`PostgresStore`、host bridge 和 server package 仍存在，Wave-9/10 继续未完成。

Compatibility graph benchmark retirement（2026-07-26）：已删除 `packages/server/scripts/benchmark-graph-backend.ts` 与 server package `graph-db:benchmark-startup` command；该脚本只量测 compatibility server startup，无法作为 knowledge-read owner 的验收证据。性能参考页改为直接比较 memory、healthy Neo4j 与 forced-fallback retrieval smoke。TDD RED：retirement guard 在 benchmark 文件存在时失败；GREEN：retirement guard（35/35）、root typecheck 与 diff check 通过。其余 compatibility package/store surface 仍存在，Wave-9/10 未完成。

Skill bundle owner-tool cutover（2026-07-26）：`scripts/test-skill-import-export.ts` 不再构造 compatibility Fastify server、session 或 test team；它要求显式现有 `--actor-id`，以短生命周期 PostgreSQL pool 直接调用 knowledge-write 的 `createArtifactBundleImportPort()` 并从 `createArtifactReadProjection()` 验证 revision file count。knowledge-write 同时新增 canonical bundle importer：路径和 single-file constraints 在事务开始前校验，artifact、revision、lifecycle event 与每个 file payload 在同一 owner transaction 内写入。内部 artifact import route 返回既有 batch response，并在 bundle required level 超过 trusted actor level 时拒绝；distributed gateway 只在该 import hop 转发已验证的 handle、security level 与 active team metadata。TDD RED：owner importer、internal route、tool composition 与 gateway header assertions 分别在缺少新 owner path 时失败；GREEN：knowledge-write artifact port/route（13/13）、gateway routes（27/27）、PostgreSQL composition entrypoints（8/8）、retirement guard（36/36）和 root typecheck 通过。真实 PostgreSQL round-trip 仍需具有已存在 identity actor 的 database 环境补跑；该工具迁移不删除 `store_snapshot` 或 server package，Wave-8/9/10 保持未完成。

Wave-8 test-bridge retirement follow-up（2026-07-26）：删除未被 host-local runtime 消费的 `nest/runtime/server-composition.ts` compatibility bridge。server package tests 的 PostgreSQL helper 直接复用 scripts 中专用 `buildPostgresComposedServer()`，不再经 host-local 构造 Fastify compatibility shell；host-local import-boundary test 以文件不存在作为 deletion contract，retirement allowlist 删除该桥的 Wave-8 例外。TDD RED：新增 deletion assertion 在文件仍存在时失败；GREEN：host-local import boundary（3/3）、PostgreSQL composition entrypoints（8/8）、retirement guard（36/36）、root typecheck 与 diff check 通过。直接运行 compatibility server app/startup package tests 时未提供 `TRAPMAP_DATABASE_URL`，32 个启动前失败均为 coordinator precondition，不能作为 runtime acceptance evidence。`shared-infra.ts` 仍直接导入 `@trapmap/server`，`store_snapshot` 与 compatibility package 仍存在，因此 Wave-8、Wave-9、Wave-10 均不得标记完成。

Wave-8 embeddings bridge follow-up（2026-07-26）：host-local 的 Nest retrieval runtime 一直以显式 AI service input 调用 knowledge-read，未消费 server 的 `generateEmbedding()` global compatibility state；移除 `shared-infra.ts` 中对 `setGlobalEmbeddingsProvider()` 的 import 与初始化，避免 host-local 在启动时写入 compatibility process-global。TDD RED：新的 host-local boundary assertion 在该 bridge 尚存时失败；GREEN：host-local runtime/import composition（10/10）、retirement guard（36/36）、root typecheck 与 diff check 通过。AI provider、memory graph backend 与 PostgreSQL store assembly 仍由 `shared-infra.ts` 从 server 导入，故这只是缩小 Wave-8 seam，绝不构成 Wave-8/9/10 closeout。

Wave-8 host pool ownership follow-up（2026-07-26）：`shared-infra.ts` 不再构造 compatibility `SkillShareerStore` 或调用 `getStorePool()`；host-local 直接创建并关闭 PostgreSQL pool，并只向 owner bundles 与 knowledge-read retrieval 提供结构性 `getPool()` seam。host-local manifest 现在直接声明 `pg`。TDD RED：pool ownership boundary 在 `createSkillShareerStore`/`getStorePool` 尚存时失败；GREEN：host-local import/composition focused tests（11/11）与 host-local typecheck 通过。AI provider 与 graph-query implementation 仍来自 server，且 `store_snapshot`、JSON compatibility source 与 server package 未删除，Wave-8/9/10 均继续未完成。

Docker acceptance follow-up（2026-07-26）：Docker coordinator 已可用。`rtk pnpm test:deployment-smoke` 通过（静态 135/135 与 PostgreSQL app/startup suite 通过）；`rtk pnpm eval:smoke` 在 coordinator 内完成 retrieval smoke；`rtk pnpm test:distributed-closeout` 通过（35/35）；`rtk pnpm test:observability-closeout` 通过（52/52）；`rtk pnpm test:discovery-closeout` 通过（30/30）。`rtk pnpm exec fallow audit --base main --format json --quiet` verdict 为 pass，new-only dead-code、complexity 与 duplication 均为 0。`test:runtime-foundations` 的 PostgreSQL subscriber fixture 原先遗漏 job-runtime task queue，故 queue scheduler 合理 no-op；将 fixture 改为与 bootstrap 相同地注入 `asyncTransport.task` 后 focused subscriber suite（18/18）通过。完整 `rtk pnpm test:runtime-foundations` 随后在同一 coordinator 中通过（11 files、202 tests）；此前 trace-header 单次顺序敏感失败未复现，未据此改动生产代码。Wave-8/9/10 的 production deletion contracts 仍未满足，均保持未完成。

Shared AI provider retirement follow-up（2026-07-27）：server AI compatibility facade 的 `types.ts`、`provider-config.ts`、`providers.ts` 与其 moved tests 已删除；`lib/ai/index.ts` 不再 re-export shared provider/config/types，只保留 server-owned prompt/template、cache、parse 与 dynamic surfaces。retirement guard 现在同时要求三个 retired modules 不存在，且 host-local `shared-infra.ts` 不得再导入 `@trapmap/server/lib/ai`。TDD RED：`rtk pnpm test:file -- scripts/__tests__/compatibility-retirement-guard.test.ts` 因 retired module 仍存在而失败（1 failed, 37 passed）；GREEN：同一 focused guard 通过（38/38）。host-local 的 `shared-infra.ts` 仍直接导入 server graph-query implementation，因此 Wave-8 保持未完成；该 graph-query edge 仍是本 task 范围外的唯一 shared-infra server allowlist 依据，未被移除。

Wave-8 graph-query owner injection follow-up（2026-07-29）：host-local `shared-infra.ts` 现仅从 `service-knowledge-read` 创建 memory graph-query backend，并从 contracts 使用 `GraphQueryBackend`/`GraphQueryRuntimeState`；保留 literal `{ backendKind: 'memory', mode: 'disabled', failOpen: true }`。兼容 PostgreSQL server composition 同时通过 `BuildServerOptions` 注入同一 owner graph index 构造的 `graphQueryBackend` 与该 runtime state，仍允许测试调用方显式覆盖两者。TDD RED：host-local import boundary 因旧 `@trapmap/server/lib/graph-query/index.js` import 失败（1 failed, 6 passed），PostgreSQL composition source assertion 因未注入 graph query 失败（1 failed, 8 passed）；knowledge-read boundary 通过（13/13）。GREEN：contracts graph-query（2/2）、host-local boundary（7/7）、knowledge-read boundary/backend（14/14）、server graph-query command（9 passed, 1 skipped；指定 graphology 文件未匹配测试）、PostgreSQL composition entrypoints（9/9）及 retirement guard（38/38）通过，`rtk pnpm typecheck` 无错误，server source scan 未发现 `@trapmap/service-knowledge-read` import。retirement guard 仅移除已不存在的 host-local graph-query `@trapmap/server` allowlist；当前没有其余 Wave-8 allowlist entry。按要求运行的 Fallow audit 未产生 JSON（quiet stdout 为 0 bytes），诊断运行受本地 `pnpm exec` 的 `unable to open database file` 阻断，故未将其作为 clean audit 证据。Wave-8 仍未完成，且不授权删除 `store_snapshot`、`JsonStore`、`PostgresStore` 或 compatibility server package。

Wave-8 closeout（2026-07-31）：production compatibility inventory 已确认没有 Wave-8 条目；host composition、capability config、gateway request context 与 pool ownership 均由 host 或明确 service owner 本地拥有。retirement guard 现在要求 `completedOwnerWaves` 包含 `wave-8`，且没有加入目录、包或 glob 级例外。TDD RED：Wave-8 completion assertion 在状态数组缺少该 wave 时失败；GREEN：guard、host-local import/config boundaries 与禁用本地 OTLP exporter 后的 distributed gateway surface 通过。全局 compatibility inventory 的 empty assertion 随 Wave-10 package deletion 一并启用，因为此时仍存在明确标记为 Wave-10 的 label runner catalog dependency。

Wave-9 legacy snapshot backfill/delete closeout（2026-07-31）：已删除 `store_snapshot` schema、`JsonStore`、`PostgresStore`、create-store 与 legacy-snapshot 模块；server composition 直接持有并注入 PostgreSQL `Pool`，`SkillShareerServices` 不再有 `store` 字段。`auth-store-helpers.ts` 已经由 identity/knowledge/artifact owner repositories 和 owner-local pool 播种，`postgres-server-composition.ts` 接受并传递直接 pool。遗留 auth/member/access-key、decay、capsule-index、status 和 lifecycle subscriber 测试均移除 `app.skillShareer.store`；它们分别使用 host PostgreSQL composition、identity repositories、owner read projection 或直接 pool。生命周期 PostgreSQL subscriber 测试已验证 shared-job enqueue；过去要求未注册 compatibility capability route 的 lifecycle flow 测试已删除，而非重新添加 server route。追加审计退休了 `demo-acceptance`、`retrieval-workflow` 与旧 `routes/retrieval` 三个 store-backed history fixture：它们分别依赖已移除的 `server.store`、无 host identity bundle 的 `buildServer()`，或未注册的 artifact lifecycle compatibility routes；实际 artifact lifecycle 仅由 knowledge-write owner 的 internal surface 提供。retirement guard 明确要求这些历史 fixture 不得重现。进一步退休 `PostgresStore` CRUD 与旧 retrieval contract 测试；repo、artifact pipeline、skill event fixture 改为 host-independent 或 `pool: null` owner seam，derive fixture 改为纯 `StoreData` DTO 和局部 ID allocation。除 guard 文本外，测试不再构造 `JsonStore`/`PostgresStore`。`rtk rg -n "app\\.skillShareer\\.store" packages/server/src --glob '*.test.ts'` 无命中；retirement/snapshot guards、typecheck 与 focused PostgreSQL route/subscriber suites通过。Wave-9 的 destructive deletion 与 backfill/delete state 已闭合；Wave-8/10 仍独立未完成。

## Deferred 边界

平台化、物理 database isolation/PgBouncer、工程维护热点和未证实安全候选仍由 [`open-debt-and-compromises.md`](open-debt-and-compromises.md) 管理，不得与本主线并行启动。
