# 基线快照 — 2026-09-01 架构收敛主线冻结

> 生成于 `pre@81752c31`（`main@31cd53a2` 前 8 commit）。本文件为 P0.1 冻结锚点，供 P0.4 回滚参照与量化证据对齐。

## 42 表（PG-first）

- `packages/db/src/schema/` 实测 42 张 `pgTable`（`check:table-schema 42/42` 绿）
- 分布：`auth 6 / knowledge 7 (含 embeddings/search_documents) / artifacts 11 / candidates 4 / experience-genes 3 / queue 2 (task_queue, workflow_runs) / retrieval 1 / labels 4 / cron 1` + 其他 3
- `conflict_relations` 例外：仅 `service-governance-review/src/pg-ports.ts` 原生 SQL `INSERT INTO conflict_relations`，无 Drizzle 建模（待 P4 收敛）
- `DATABASE_SCHEMA.md` `## 表总览 (42 张表)` 与节计数一致，`check:table-schema` 绿

## 四热点文件（单文件≤300 目标）

| 文件 | 行数 | 状态 |
|---|---|---|
| `packages/service-governance-review/src/routes.ts` | 914 | 超阈，需 P1.1 按 `queue/feedback/maintenance/admin` 拆 |
| `packages/service-knowledge-write/src/routes.ts` | 826 | 超阈，需 P1.2 按 `knowledge/submission` 拆 |
| `packages/service-knowledge-read/src/retrieval-recall-coordinator.ts` | 586 | 超阈，需 P1.3 抽 `hybrid/semantic/graph` 通道 |
| `packages/service-knowledge-read/src/search-knowledge.ts` | 391 | 超阈，需 P1.4 拆 `search-v2 / search-v3-plan` |

其他关注：
- `host-local/src/nest/app.module.ts` 287（预 P2 阈值 120）
- `runtime/backend-core-adapters.ts` 360（待拆 6 adapter 各 80-120）
- `host-local gateway.route-defs.ts` 290（待瘦至 ≤150）
- `host-distributed gateway/internal-client.ts` 1307（待拆 client/breaker/health）
- `host-distributed gateway/route-defs.ts` 1460（待按资源拆）

## Go 双栈

- `services/knowledge-read-go` Go 1.23，`services/go-accelerator` Go 1.22（双栈）
- 合计约 3695 行（含两侧），`knowledge-read-go` 内 6 模块：`api/query/recall/ranking/assembly/cache`，当前 `ranking` 已拆 `merge/rerank/boundary` 各 ≤57，`recall` 三通道 `graph/keyword/semantic`，`cache lru.go 61`，总体已满足 `file≤300 module≤600 ratio≤30%` 但双 Go 栈未收敛
- `go vet/test/golangci-lint` 待 P3 锁死白名单

## 契约

- `packages/contracts/src/domain/operations.ts` 658 行（`operations.d.ts` 5187 行生成物），`test/index.test.ts` 3424 行 — 待 P6 按域拆 ≤400
- `pnpm check:go-contract` 绿（21 schemas）

## 质量门

- `fallow audit --base main` 55 changed files，`complexity 2 warn / duplication 4 clone groups`，5 inherited findings excluded，0 fallow-ignore 残留除外（当前含 7 处 `fallow-ignore-file` 豁免，待 P1 根除）
- `pnpm check:docs` 38/38 绿，`check:structure` 3/3 绿，`check:mermaid` 115→116 块绿（含本文件 mermaid），`check:complexity` 9/9 绿（旧预算宽松 future→新阈值收紧）
- `pnpm check:table-schema 42/42` 绿，`pgtable-single-source` 绿（当前为 DB-centralized 语义）

## Mermaid

- 全仓 `docs/**/*.md` 中 ` ```mermaid ` 块计 116 块（本文件纳入后），`pnpm exec tsx scripts/check-mermaid.ts` 绿

## 语义冻结声明（P0.3）

- 本主线零语义变更：检索分数/排序/hash 字节一致由 `infra/fallback.ts` 的 `canonicalHashWithFallback / batchCosineWithFallback` 单测保障
- `TRAPMAP_READ_IMPL=off|shadow|dual|go` 绞杀器不变，`host-local` 恒走 JS，`distributed` 按阈值切 Go

## 回滚锚点（P0.4）

- 每 Phase 独立 commit，失败 `git revert <phase-commit>` 回至本基线
- 本文件提交即 P0 基线冻结锚点：`docs/reference/BASELINE_2026-09-01.md` 为 `git log --oneline main..HEAD` 首个 P0 commit

