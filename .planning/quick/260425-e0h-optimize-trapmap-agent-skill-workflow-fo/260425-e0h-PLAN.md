# Quick Task Plan: Optimize TrapMap Agent Skill Workflow

## Goal

Make the TrapMap agent-facing skill more discoverable, harder to misuse, and more aligned with compact experience reuse.

## Tasks

1. Update the canonical `packages/skills/trapmap-knowledge-workflow` skill with hard retrieval gates, trap-first selection, and compact capture rules.
2. Add a project-discoverable `.claude/skills/trapmap-knowledge-workflow` copy with local references.
3. Update architecture docs so the skill discovery path matches the implementation.
4. Validate skill frontmatter and check that referenced CLI commands match the current Commander source.

## Verification

- Run `quick_validate.py` on both skill directories.
- Inspect `git diff` to confirm `.agents` deletions are not reverted and no unrelated files were changed.
