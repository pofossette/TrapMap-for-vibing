---
phase: 15-client-activation-for-references-assets-and-scripts
plan: "03"
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - packages/contracts/src/domain/operations.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/routes/operations.ts
  - packages/server/src/routes/operations.test.ts
  - packages/cli/src/commands/operations.ts
  - packages/cli/src/commands/operations.test.ts
  - packages/cli/src/lib/skill-artifact-export.ts
autonomous: true
requirements:
  - ACTV-01
  - COMP-01
must_haves:
  truths:
    - "The client can selectively fetch only the references/assets/scripts it needs for activation."
    - "Activation reuses the existing artifact payload store and audited operations boundary."
    - "CLI download/staging flow enforces effective policy before any script becomes executable or runnable."
  artifacts:
    - path: "packages/contracts/src/domain/operations.ts"
      provides: "Shared activation/download request and response contracts"
    - path: "packages/server/src/routes/operations.ts"
      provides: "Selective activation payload delivery with existing auth, team, level, and audit checks"
    - path: "packages/cli/src/commands/operations.ts"
      provides: "User-facing activation/download commands"
    - path: "packages/cli/src/lib/skill-artifact-export.ts"
      provides: "Safe selective file materialization helpers reused by activation commands"
  key_links:
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/store.ts"
      via: "artifactFilePayloads lookup for selected paths"
      pattern: "artifactFilePayloads"
    - from: "packages/cli/src/commands/operations.ts"
      to: "packages/cli/src/lib/skill-artifact-export.ts"
      via: "safe local file writes for fetched activation payloads"
      pattern: "materialize|validate"
    - from: "packages/cli/src/commands/operations.ts"
      to: "packages/cli/src/lib/activation-policy.ts"
      via: "effective-policy gate before script staging/execution prompts"
      pattern: "policy"
---

<objective>
Add the explicit CLI activation/download workflow that turns retrieval hints into local references, assets, and script files without breaking the current contracts-first boundary.

Purpose: deliver ACTV-01 on top of existing artifact export infrastructure and the policy model from 15-02.
Output: shared activation contracts, a selective server fetch path, and CLI commands that download/stage files safely.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-client-activation-for-references-assets-and-scripts/15-RESEARCH.md
@packages/contracts/src/domain/operations.ts
@packages/server/src/routes/operations.ts
@packages/cli/src/commands/operations.ts
@packages/cli/src/lib/skill-artifact-export.ts

<interfaces>
From packages/contracts/src/domain/operations.ts:
```ts
export const artifactExportRequestSchema = z.object({
  artifactId: entityIdSchema,
  format: artifactExportFormatSchema.default('bundle-json'),
});
```

From packages/server/src/routes/operations.ts:
```ts
const filePayloads = data.artifactFilePayloads?.filter(
  (p) => p.artifactId === artifactId && p.revision === artifact.latestRevision.revision,
) ?? [];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add selective activation fetch contracts and server route behavior</name>
  <files>packages/contracts/src/domain/operations.ts, packages/contracts/src/index.test.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts</files>
  <behavior>
    - Test 1: the shared activation request can target one artifact revision and a bounded set of selected paths.
    - Test 2: the server returns only requested files after existing auth, team, and level checks pass.
    - Test 3: audit logging still records activation/download activity through the operations boundary.
  </behavior>
  <action>Add an additive activation/download contract to `packages/contracts/src/domain/operations.ts` instead of overloading retrieval responses with bytes. Implement the server side inside `routes/operations.ts` by reusing the existing artifact lookup, access control, and `artifactFilePayloads` store seam. Keep path selection explicit and validated, and scope the response to only the requested references/assets/scripts.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts && pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>The server exposes a contract-validated, audited activation fetch path that delivers only the selected files for one accessible artifact revision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI activation and download commands</name>
  <files>packages/cli/src/commands/operations.ts, packages/cli/src/commands/operations.test.ts, packages/cli/src/lib/skill-artifact-export.ts</files>
  <behavior>
    - Test 1: the CLI can fetch selected references/assets/scripts and materialize them locally using safe path handling.
    - Test 2: script downloads respect the effective policy computed in 15-02 and fail closed when blocked or unresolved.
    - Test 3: command output stays shell-friendly and JSON mode mirrors the shared activation response contract.
  </behavior>
  <action>Extend the existing operations command family with activation/download commands that consume the new activation route, select requested paths, and reuse `skill-artifact-export.ts` for safe writes. Do not implement implicit server execution or hidden subprocess launches. If a script is staged locally, gate it through the effective-policy helper and make actual execution an explicit, policy-aware user action rather than a side effect of download.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts</automated>
  </verify>
  <done>The CLI can materialize activation files on demand, stays JSON/shell friendly, and enforces effective policy before any script-oriented workflow progresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload store -> client filesystem | Persisted artifact bytes cross onto the user's machine here |
| CLI path selection -> local materialization | User-controlled output choices influence local writes here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | T | `packages/server/src/routes/operations.ts` | mitigate | Validate selected paths against the artifact manifest and return only files from the requested artifact revision. |
| T-15-08 | I | `packages/cli/src/lib/skill-artifact-export.ts` | mitigate | Reuse safe path validation and controlled materialization helpers to prevent traversal or arbitrary writes. |
| T-15-09 | E | `packages/cli/src/commands/operations.ts` | mitigate | Enforce effective script policy before staging executable workflows and never run scripts implicitly during download. |
</threat_model>

<verification>
Run activation-focused checks:
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- `pnpm --filter @skill-shareer/cli exec tsc --noEmit`
</verification>

<success_criteria>
- The client can fetch only the activation files it needs.
- Activation uses the existing operations boundary and payload store.
- CLI materialization is safe and policy-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-activation-for-references-assets-and-scripts/15-03-SUMMARY.md`
</output>
