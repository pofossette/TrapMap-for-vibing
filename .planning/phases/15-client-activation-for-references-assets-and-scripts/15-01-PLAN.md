---
phase: 15-client-activation-for-references-assets-and-scripts
plan: "01"
type: execute
wave: 1
depends_on:
  - 14-04
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/lib/retrieval/assembly.ts
  - packages/server/src/lib/retrieval/assembly.test.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements:
  - RETR-05
  - ACTV-01
  - COMP-01
must_haves:
  truths:
    - "Retrieval v2 stays distilled-first and metadata-only even after activation hints are added."
    - "Activation hints are sourced from governed artifact manifests, not ad hoc CLI inference."
    - "The shared retrieval contract remains the only server/CLI truth for activation hint shapes."
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "Capsule-first retrieval response enriched with activation hint metadata"
    - path: "packages/server/src/lib/retrieval/assembly.ts"
      provides: "Pure shaping for read-next hints, asset metadata, and script profiles"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "Manifest-backed activation hint selection for matched artifacts"
    - path: "packages/server/src/lib/retrieval.test.ts"
      provides: "Integration coverage for v2 retrieval activation metadata"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/artifacts/derive.ts"
      via: "latestRevision.derived.clientManifest"
      pattern: "clientManifest"
    - from: "packages/server/src/lib/retrieval/assembly.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "shared response schema parse"
      pattern: "retrievalV2ResponseSchema"
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "thin route validation and delegation"
      pattern: "/v2/retrieval/search"
---

<objective>
Extend the v2 retrieval surface so each capsule-first result can tell the client what to read or fetch next without returning raw bundle payloads.

Purpose: deliver RETR-05 and the retrieval-facing half of ACTV-01 by bridging matched artifacts to `clientManifest` metadata.
Output: additive retrieval contracts plus pure server shaping that exposes read-next references, asset availability, and script profiles.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-client-activation-for-references-assets-and-scripts/15-RESEARCH.md
@.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-04-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/contracts/src/domain/artifacts.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/assembly.ts
@packages/server/src/routes/retrieval.ts

<interfaces>
From packages/contracts/src/domain/retrieval.ts:
```ts
export const retrievalV2ResponseSchema = z.object({
  capsules: z.array(capsuleMatchSchema).default([]),
  profileHints: z.array(profileHintSchema).default([]),
  refinementSummary: z.string().nullable(),
  summary: retrievalSummarySchema.nullable().default(null),
});
```

From packages/contracts/src/domain/artifacts.ts:
```ts
export const clientManifestSchema = z.object({
  artifactId: entityIdSchema,
  revision: z.number().int().min(1),
  references: z.array(clientManifestReferenceSchema).default([]),
  assets: z.array(clientManifestAssetSchema).default([]),
  scripts: z.array(clientManifestScriptSchema).default([]),
  sourceHash: z.string().length(64),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add activation hint schemas to retrieval v2 contracts</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: v2 response schema accepts read-next reference hints, related asset metadata, and script profile metadata.
    - Test 2: response payloads remain metadata-only and do not require file content or script bodies.
    - Test 3: legacy retrieval contracts remain intact and additive coexistence is preserved.
  </behavior>
  <action>Extend `retrievalV2ResponseSchema` with additive activation-hint sections that reference artifact/manifest metadata rather than raw payloads. Reuse existing `clientManifest` field shapes where practical instead of duplicating file metadata concepts. Keep the new output optional and compact so empty responses still parse cleanly when an artifact has no activation content.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>The shared retrieval v2 contract can express activation guidance for references, assets, and scripts without expanding into bundle payload transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shape manifest-backed activation hints inside the v2 retrieval pipeline</name>
  <files>packages/server/src/lib/retrieval/assembly.ts, packages/server/src/lib/retrieval/assembly.test.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: matched artifacts with `clientManifest` data surface read-next reference hints and related activation metadata.
    - Test 2: retrieval responses still exclude raw file bodies and activation-only payload content.
    - Test 3: route and integration tests prove the `/v2/retrieval/search` path validates and returns the enriched contract.
  </behavior>
  <action>Keep `assembly.ts` pure and have `orchestrator.ts` select activation hints from governed `latestRevision.derived.clientManifest` on the artifacts that already matched the query. Prefer lightweight "read next" hints tied to capsule/profile relevance instead of dumping full manifests into every result. Keep the route thin and update retrieval tests to pin the new contract shape end to end.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/assembly.test.ts src/lib/retrieval.test.ts src/routes/retrieval.test.ts</automated>
  </verify>
  <done>Capsule-native retrieval responses include useful activation guidance, stay metadata-only, and remain contract-validated through the route boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived manifest -> retrieval response | Stored activation metadata becomes user-visible guidance here |
| ranked capsule hits -> read-next hints | Relevance logic decides which references/assets/scripts are surfaced first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I | `packages/server/src/lib/retrieval/assembly.ts` | mitigate | Keep activation hints metadata-only; never include file bodies or script text in retrieval output. |
| T-15-02 | T | `packages/server/src/lib/retrieval/orchestrator.ts` | mitigate | Source activation metadata only from governed `clientManifest`, not from direct payload reads or CLI guesses. |
| T-15-03 | R | `packages/contracts/src/domain/retrieval.ts` | mitigate | Validate the enriched v2 response through shared schemas so CLI/server stay aligned and auditable. |
</threat_model>

<verification>
Run focused retrieval checks:
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/assembly.test.ts src/lib/retrieval.test.ts src/routes/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- Retrieval v2 responses tell the client which references to read next and what assets/scripts are available.
- The response remains distilled and metadata-only.
- Contracts remain the only shared truth for the new activation hint shape.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-activation-for-references-assets-and-scripts/15-01-SUMMARY.md`
</output>
