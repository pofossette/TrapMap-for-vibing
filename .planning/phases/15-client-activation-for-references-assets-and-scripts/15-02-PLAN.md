---
phase: 15-client-activation-for-references-assets-and-scripts
plan: "02"
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - packages/contracts/src/domain/artifacts.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/lib/activation-policy.ts
  - packages/server/src/lib/activation-policy.test.ts
  - packages/cli/src/lib/config.ts
  - packages/cli/src/lib/activation-policy.ts
  - packages/cli/src/lib/activation-policy.test.ts
autonomous: true
requirements:
  - ACTV-02
  - ACTV-03
  - ACTV-04
  - COMP-01
must_haves:
  truths:
    - "The server only publishes policy metadata and never executes scripts."
    - "Client overrides can only tighten the effective policy, never relax it."
    - "Policy vocabulary is shared and explicit across contracts, server, and CLI."
  artifacts:
    - path: "packages/contracts/src/domain/artifacts.ts"
      provides: "Shared activation-policy schemas for scripts and client overrides"
    - path: "packages/server/src/lib/activation-policy.ts"
      provides: "Pure server-side default policy shaping with no execution side effects"
    - path: "packages/cli/src/lib/activation-policy.ts"
      provides: "Client-side effective-policy resolution that enforces stricter-only overrides"
    - path: "packages/cli/src/lib/config.ts"
      provides: "CLI persistence for local activation-policy preferences"
  key_links:
    - from: "packages/contracts/src/domain/artifacts.ts"
      to: "packages/server/src/lib/activation-policy.ts"
      via: "shared policy enums and payload schema"
      pattern: "activation.*policy|script.*policy"
    - from: "packages/contracts/src/domain/artifacts.ts"
      to: "packages/cli/src/lib/activation-policy.ts"
      via: "shared policy ordering and override schema"
      pattern: "activation.*policy|script.*policy"
    - from: "packages/cli/src/lib/config.ts"
      to: "packages/cli/src/lib/activation-policy.ts"
      via: "local override lookup"
      pattern: "activation"
---

<objective>
Define the script activation policy model and the stricter-only client override rule before any CLI download or execution workflow lands.

Purpose: satisfy ACTV-02/03/04 and lock the governance boundary that Phase 15-03 will consume.
Output: shared policy contracts, pure server/CLI helpers, and persisted CLI override settings with targeted tests.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-client-activation-for-references-assets-and-scripts/15-RESEARCH.md
@packages/contracts/src/domain/artifacts.ts
@packages/cli/src/lib/config.ts
@packages/server/src/lib/artifacts/derive.ts

<interfaces>
From packages/contracts/src/domain/artifacts.ts:
```ts
export const clientManifestScriptSchema = z.object({
  path: z.string().min(1).max(512),
  sha256: z.string().length(64),
  capability: z.string().min(1).max(280),
  argsSchemaSummary: z.string().max(280).default(''),
  sideEffectSummary: z.string().max(280).default(''),
  defaultPolicy: z.enum(['manual', 'auto', 'blocked']),
});
```

From .planning/REQUIREMENTS.md:
```md
ACTV-02: reference-only | needs-approval | client-executable | blocked
ACTV-03: server never executes scripts
ACTV-04: client may be stricter, never looser
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce shared activation-policy contracts for scripts</name>
  <files>packages/contracts/src/domain/artifacts.ts, packages/contracts/src/index.ts, packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: shared schemas accept the four required execution policies.
    - Test 2: script-facing metadata can carry a server default policy and optional client override intent without embedding script bodies.
    - Test 3: existing artifact and manifest schemas remain backward-compatible where possible.
  </behavior>
  <action>Extend the artifact-domain contracts with explicit activation-policy vocabulary for scripts and any companion override/config schemas needed by the CLI. Replace the current ambiguous `manual|auto|blocked` policy surface with a shape that matches ACTV-02 and remains metadata-only. Keep `packages/contracts` as the canonical truth and update exports/tests accordingly.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>The repo has one shared, explicit script-policy model that both server and CLI can consume without inference or duplicated enums.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stricter-only effective-policy resolution on server and CLI</name>
  <files>packages/server/src/lib/activation-policy.ts, packages/server/src/lib/activation-policy.test.ts, packages/cli/src/lib/config.ts, packages/cli/src/lib/activation-policy.ts, packages/cli/src/lib/activation-policy.test.ts</files>
  <behavior>
    - Test 1: server helpers only compute and publish metadata; they never execute or spawn scripts.
    - Test 2: client override resolution always chooses the stricter policy when server default and local override differ.
    - Test 3: CLI config persists override settings in a form that later commands can consume without widening permissions accidentally.
  </behavior>
  <action>Create pure policy helpers on both sides of the boundary. The server helper should translate stored script metadata into the shared default policy shape used in retrieval/activation responses. The CLI helper should resolve the effective policy from server default plus local override with a monotonic ordering that forbids relaxation. Persist overrides in CLI config now, but do not add command-side download or execution behavior in this plan.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/activation-policy.test.ts && pnpm --filter @skill-shareer/cli test -- src/lib/activation-policy.test.ts</automated>
  </verify>
  <done>Pure policy resolution exists, local overrides are persisted, and tests prove the client can tighten policy but never loosen the server default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server manifest metadata -> policy defaults | Server-owned governance data becomes client-visible execution guidance here |
| local CLI config -> effective policy | User-controlled local preferences influence script handling here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | E | `packages/cli/src/lib/activation-policy.ts` | mitigate | Encode a strict policy ordering and resolve the stricter of server default and local override only. |
| T-15-05 | T | `packages/server/src/lib/activation-policy.ts` | mitigate | Keep server helpers metadata-only and side-effect free; no script execution or subprocess launch. |
| T-15-06 | R | `packages/cli/src/lib/config.ts` | mitigate | Persist explicit override values so later activation commands can explain why a script was blocked or downgraded. |
</threat_model>

<verification>
Run policy-focused tests:
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/activation-policy.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/lib/activation-policy.test.ts`
- `pnpm --filter @skill-shareer/cli exec tsc --noEmit`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- The required four-state script policy vocabulary exists in shared contracts.
- The server still only returns policy metadata and never executes scripts.
- CLI-side overrides can only tighten policy.
</success_criteria>

<output>
After completion, create `.planning/phases/15-client-activation-for-references-assets-and-scripts/15-02-SUMMARY.md`
</output>

