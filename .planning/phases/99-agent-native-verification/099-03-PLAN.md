---
phase: 99-agent-native-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/skills/trapmap-knowledge-workflow/references/retrieval.md
autonomous: true
requirements: [V99-04]

must_haves:
  truths:
    - "references/retrieval.md in .claude/ and packages/ are consistent in content (both have Agent Context Load section with trapmap load CLI usage)"
  artifacts:
    - path: "packages/skills/trapmap-knowledge-workflow/references/retrieval.md"
      provides: "Synchronized retrieval.md matching .claude/ copy"
      contains: "Agent Context Load"
  key_links:
    - from: ".claude/skills/trapmap-knowledge-workflow/references/retrieval.md"
      to: "packages/skills/trapmap-knowledge-workflow/references/retrieval.md"
      via: "content consistency check"
      pattern: "trapmap load"
---

<objective>
Close the SKILL.md consistency gap (V99-04) identified by verification. The `.claude/` copy of `references/retrieval.md` has an "Agent Context Load" section (lines 59-69) documenting `trapmap load` CLI usage that the `packages/` copy lacks. SKILL.md files are already identical after prior sync.

Purpose: Ensure both copies of references/retrieval.md are consistent so the skill behaves identically regardless of which copy is loaded.

Output: packages/skills/trapmap-knowledge-workflow/references/retrieval.md synchronized with .claude/ copy.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-agent-native-verification/99-CONTEXT.md
@.planning/phases/99-agent-native-verification/099-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize references/retrieval.md from .claude/ to packages/</name>
  <files>packages/skills/trapmap-knowledge-workflow/references/retrieval.md</files>
  <read_first>
    - .claude/skills/trapmap-knowledge-workflow/references/retrieval.md (source of truth — has "Agent Context Load" section at lines 59-69)
    - packages/skills/trapmap-knowledge-workflow/references/retrieval.md (target — missing "Agent Context Load" section, jumps from line 57 to "Trap-First Selection")
  </read_first>
  <action>
Insert the "Agent Context Load" section into `packages/skills/trapmap-knowledge-workflow/references/retrieval.md` between line 57 (end of the `trapmap search` code block) and the current line 59 ("## Trap-First Selection").

The exact content to insert (from `.claude/` copy lines 59-69):

```markdown
## Agent Context Load

Use `load` for pre-formatted agent-consumable context (markdown with routing, plan, and skill sections):

```bash
trapmap load "<seed>" --scope project --json
```

`load` flags: `--scope`, `--label` (repeatable), `--skill-budget`, `--max-depth`, `--fallback`, `--stdin`, `--json`.

Default output is markdown wrapped in `<!-- trapmap-load-context -->` markers. Use `--json` for raw structured data.

```

After insertion, verify with diff:
```bash
diff .claude/skills/trapmap-knowledge-workflow/references/retrieval.md packages/skills/trapmap-knowledge-workflow/references/retrieval.md
```

Expected: Files are identical (exit 0).
  </action>
  <verify>
    <automated>diff .claude/skills/trapmap-knowledge-workflow/references/retrieval.md packages/skills/trapmap-knowledge-workflow/references/retrieval.md</automated>
  </verify>
  <done>
    - `diff .claude/skills/trapmap-knowledge-workflow/references/retrieval.md packages/skills/trapmap-knowledge-workflow/references/retrieval.md` exits 0 (files identical)
    - `grep -c "Agent Context Load" packages/skills/trapmap-knowledge-workflow/references/retrieval.md` returns 1
    - `grep -c "trapmap load" packages/skills/trapmap-knowledge-workflow/references/retrieval.md` returns 1
  </done>
  <acceptance_criteria>
    - `diff .claude/skills/trapmap-knowledge-workflow/references/retrieval.md packages/skills/trapmap-knowledge-workflow/references/retrieval.md` exits 0
    - packages/skills/trapmap-knowledge-workflow/references/retrieval.md contains string `## Agent Context Load`
    - packages/skills/trapmap-knowledge-workflow/references/retrieval.md contains string `trapmap load`
    - packages/skills/trapmap-knowledge-workflow/references/retrieval.md contains string `--scope`
    - packages/skills/trapmap-knowledge-workflow/references/retrieval.md contains string `<!-- trapmap-load-context -->`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `diff .claude/skills/trapmap-knowledge-workflow/references/retrieval.md packages/skills/trapmap-knowledge-workflow/references/retrieval.md` -- exits 0
2. `diff .claude/skills/trapmap-knowledge-workflow/SKILL.md packages/skills/trapmap-knowledge-workflow/SKILL.md` -- exits 0 (already synced)
3. `grep -c "trapmap load" packages/skills/trapmap-knowledge-workflow/references/retrieval.md` -- returns 1
</verification>

<success_criteria>
- references/retrieval.md identical across both copy locations
- SKILL.md identical across both copy locations (already satisfied)
- "Agent Context Load" section present in packages/ copy
- trapmap load CLI usage documented in packages/ copy
</success_criteria>

<output>
After completion, create `.planning/phases/99-agent-native-verification/099-03-SUMMARY.md`
</output>
