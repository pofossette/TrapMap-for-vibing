---
phase: 99-agent-native-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [V99-01, V99-03, V99-04, V99-05, V99-06]

must_haves:
  truths:
    - "Full monorepo typecheck exits with 0 errors"
    - "Full test suite exits with 0 failures"
    - "CLI test suite exits with 0 failures"
    - "SKILL.md in .claude/ and packages/ are consistent in content (both mention trapmap load, both have same Reference Map entries)"
    - "Phase 97 verification is gated on init.ts existence (conditional)"
    - "Phase 98 verification is gated on SKILL.md rewrite (conditional)"
  artifacts: []
  key_links:
    - from: ".claude/skills/trapmap-knowledge-workflow/SKILL.md"
      to: "packages/skills/trapmap-knowledge-workflow/SKILL.md"
      via: "content consistency check"
      pattern: "trapmap load"
---

<objective>
Run full verification gates (typecheck, CLI tests, full test suite) and verify SKILL.md consistency across both copy locations. Gate Phase 97/98 verification behind file-existence checks since those phases have not been executed.

Purpose: Ensure no regressions from Phase 96 implementation and catch SKILL.md drift between the two copies. Phase 97/98 verification is pre-staged for when those phases complete.

Output: Verification pass/fail report and SKILL.md drift diagnosis.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/99-agent-native-verification/99-CONTEXT.md
@.planning/phases/99-agent-native-verification/99-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full monorepo typecheck and CLI test suite regression gate</name>
  <files></files>
  <read_first>
    - packages/cli/src/lib/markdown-formatter.test.ts (verify Plan 01's new tests are present)
    - packages/cli/vitest.config.ts (test runner configuration)
  </read_first>
  <action>
Run three verification gates in sequence. Each must exit 0 for this task to pass.

**Gate 1: TypeScript compilation**
```bash
pnpm typecheck
```
Expected: 0 errors across all packages. If errors found, report them in SUMMARY.md (do NOT fix -- this is verification-only).

**Gate 2: CLI test suite**
```bash
pnpm --filter @trapmap/cli test
```
Expected: All tests pass (321+ tests across 16+ test files). This includes the new formatter tests from Plan 01.

**Gate 3: Full monorepo test suite**
```bash
pnpm test
```
Expected: All tests pass (2734+ tests across 154+ test files). Run from monorepo root.

**Gate 4: Build verification**
```bash
pnpm build
```
Expected: Build succeeds across all packages.

Report results in SUMMARY.md:
- Typecheck: PASS/FAIL + error count
- CLI tests: PASS/FAIL + test count + failure count
- Full tests: PASS/FAIL + test count + failure count
- Build: PASS/FAIL
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm typecheck 2>&1 | tail -5 && pnpm --filter @trapmap/cli test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `pnpm typecheck` exits 0 with 0 errors
    - `pnpm --filter @trapmap/cli test` exits 0 with 0 failures
    - `pnpm test` exits 0 with 0 failures
    - `pnpm build` exits 0
    - SUMMARY.md contains PASS/FAIL status for each gate with counts
  </done>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm --filter @trapmap/cli test` exits 0
    - `pnpm test` exits 0
    - SUMMARY.md contains string `Typecheck: PASS` or `Typecheck: FAIL`
    - SUMMARY.md contains string `CLI Tests: PASS` or `CLI Tests: FAIL`
    - SUMMARY.md contains string `Full Tests: PASS` or `Full Tests: FAIL`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SKILL.md consistency check and Phase 97/98 gate verification</name>
  <files></files>
  <read_first>
    - .claude/skills/trapmap-knowledge-workflow/SKILL.md (current state -- has trapmap load reference in Control Path step 2)
    - packages/skills/trapmap-knowledge-workflow/SKILL.md (current state -- MISSING trapmap load reference in Control Path step 2)
  </read_first>
  <action>
**Check 1: SKILL.md consistency between both copies**

Run diff between the two SKILL.md files:
```bash
diff .claude/skills/trapmap-knowledge-workflow/SKILL.md packages/skills/trapmap-knowledge-workflow/SKILL.md
```

Known difference (verified in research): `.claude/` copy has `trapmap load` mention in Control Path step 2, `packages/` copy does not.

In SUMMARY.md, report:
- Whether both files exist: YES/NO
- Whether files are identical: YES/NO
- If not identical, list the exact differences (full diff output)

Also check Reference Map entries are consistent:
```bash
diff .claude/skills/trapmap-knowledge-workflow/references/ packages/skills/trapmap-knowledge-workflow/references/
```
Both should have identical files in references/.

**Check 2: Phase 97 conditional verification (trapmap init)**

Check if Phase 97 has been executed by testing for file existence:
```bash
test -f packages/cli/src/commands/init.ts && echo "Phase 97: IMPLEMENTED" || echo "Phase 97: NOT YET IMPLEMENTED -- skipping verification"
```

If init.ts exists:
- Run `pnpm --filter @trapmap/cli test -- packages/cli/src/commands/init.test.ts`
- Check `trapmap init --help` output contains expected options

If init.ts does NOT exist:
- Report in SUMMARY.md: "Phase 97 (trapmap init): NOT YET EXECUTED -- verification skipped"
- Do NOT report this as a failure

**Check 3: Phase 98 conditional verification (SKILL.md rewrite)**

Check if Phase 98 has been executed by testing for SKILL.md simplification:
```bash
grep -c "artifacts.md" .claude/skills/trapmap-knowledge-workflow/SKILL.md
```

If artifacts.md is NOT referenced (Phase 98 rewrote SKILL.md):
- Verify both copies are consistent
- Verify retrieval.md is removed from Reference Map

If artifacts.md IS still referenced (Phase 98 NOT executed):
- Report in SUMMARY.md: "Phase 98 (SKILL.md rewrite): NOT YET EXECUTED -- verification skipped"
- Do NOT report this as a failure

Report all checks in SUMMARY.md with clear PASS/SKIP/FAIL status.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && diff .claude/skills/trapmap-knowledge-workflow/SKILL.md packages/skills/trapmap-knowledge-workflow/SKILL.md; echo "---"; test -f packages/cli/src/commands/init.ts && echo "Phase 97: IMPLEMENTED" || echo "Phase 97: NOT YET IMPLEMENTED"; echo "---"; grep -c "artifacts.md" .claude/skills/trapmap-knowledge-workflow/SKILL.md || true</automated>
  </verify>
  <done>
    - diff output between two SKILL.md files is captured and reported
    - Phase 97 status (IMPLEMENTED / NOT YET IMPLEMENTED) is reported
    - Phase 98 status (EXECUTED / NOT YET EXECUTED) is reported
    - SUMMARY.md contains SKILL.md drift diagnosis
    - No false failures for Phases 97/98 that haven't executed
  </done>
  <acceptance_criteria>
    - SUMMARY.md contains string `SKILL.md Consistency:` followed by IDENTICAL or DRIFT DETECTED
    - SUMMARY.md contains string `Phase 97:` followed by IMPLEMENTED or NOT YET IMPLEMENTED
    - SUMMARY.md contains string `Phase 98:` followed by EXECUTED or NOT YET EXECUTED
    - `diff .claude/skills/trapmap-knowledge-workflow/SKILL.md packages/skills/trapmap-knowledge-workflow/SKILL.md` output is captured (exit code may be 1 for drift)
    - `grep -c "trapmap load" .claude/skills/trapmap-knowledge-workflow/SKILL.md` returns 1 (trapmap load is mentioned)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase adds no production code. Verification commands and test-only changes have no trust boundary impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | N/A | All | accept | Verification-only phase: tests and typecheck do not modify production behavior. No security-relevant changes to analyze. |
</threat_model>

<verification>
1. `pnpm typecheck` -- exits 0 (monorepo)
2. `pnpm test` -- exits 0 (monorepo)
3. `pnpm --filter @trapmap/cli test` -- exits 0 (CLI package)
4. `diff .claude/skills/trapmap-knowledge-workflow/SKILL.md packages/skills/trapmap-knowledge-workflow/SKILL.md` -- drift diagnosed
5. Phase 97/98 conditional gates reported (expected: NOT YET IMPLEMENTED)
</verification>

<success_criteria>
- TypeScript compilation: 0 errors
- CLI test suite: 0 failures (includes Plan 01's new formatter tests)
- Full test suite: 0 failures
- SKILL.md drift between .claude/ and packages/ copies is documented
- Phase 97/98 status is correctly diagnosed as NOT YET EXECUTED (conditional verification)
- No false failures reported for unimplemented phases
</success_criteria>

<output>
After completion, create `.planning/phases/99-agent-native-verification/099-02-SUMMARY.md`
</output>
