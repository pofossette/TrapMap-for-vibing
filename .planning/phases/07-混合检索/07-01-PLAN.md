---
phase: 07-混合检索
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/recall/keyword.ts
  - packages/server/src/lib/retrieval/recall/keyword.test.ts
autonomous: true
user_setup: []
requirements:
  - HYBR-01
  - BOUND-03
  - BOUND-05
must_haves:
  truths:
    - "The server has a keyword recall adapter that scores already-eligible knowledge entries at query time"
    - "Keyword recall uses only approved and authorized entries passed in from the filter stage"
    - "Hybrid groundwork exists as internal candidate metadata, without changing the public retrieval response shape"
  artifacts:
    - path: "packages/server/src/lib/retrieval/recall/keyword.ts"
      provides: "query-time keyword recall adapter over eligible KnowledgeRecord entries"
      contains: "export async function keywordRecall("
    - path: "packages/server/src/lib/retrieval/types.ts"
      provides: "channel-aware retrieval candidate metadata for later merge and rerank stages"
      contains: "keyword or recall-channel candidate metadata"
    - path: "packages/server/src/lib/retrieval/recall/keyword.test.ts"
      provides: "deterministic coverage for tokenization, normalization, and score bounds"
      contains: "keyword recall unit tests"
  key_links:
    - from: "packages/server/src/lib/retrieval/recall/keyword.ts"
      to: "packages/server/src/lib/retrieval/types.ts"
      via: "shared candidate metadata"
      pattern: "channel|keyword"
    - from: "packages/server/src/lib/retrieval/recall/keyword.ts"
      to: "packages/server/src/lib/store.ts"
      via: "KnowledgeRecord fields shortcut/detail/labels"
      pattern: "shortcut|detail|labels"
---

<objective>
Create the Phase 7 keyword recall foundation without wiring hybrid mode yet.

Purpose: add the lexical recall adapter Phase 7 needs while preserving the Phase 6 invariants: filter-first ordering, unchanged public response shape, mode/scope separation, and contract-first CLI/API boundaries.

Planning basis: no `CONTEXT.md` exists for Phase 7. This plan is based on roadmap, requirements, current code, and Phase 6 research/verification/summary artifacts only.

Output: a query-time keyword recall module plus internal candidate metadata that later plans can merge and rerank.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-混合检索/07-RESEARCH.md
@.planning/phases/06-检索架构重构/06-VERIFICATION.md
@.planning/phases/06-检索架构重构/06-01-SUMMARY.md
@.planning/phases/06-检索架构重构/06-02-SUMMARY.md
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/retrieval/recall/semantic.ts

<interfaces>
From `packages/server/src/lib/retrieval/types.ts`:
```ts
export interface ScoredEntry {
  entry: KnowledgeRecord;
  score: number;
}
```

From `packages/server/src/lib/retrieval/recall/semantic.ts`:
```ts
export function buildEmbeddingText(entry: KnowledgeRecord): string;
export function computeScore(
  similarity: number,
  entry: KnowledgeRecord,
  filters: RetrievalQuery['filters'],
): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define channel-aware retrieval candidate contracts</name>
  <files>packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/recall/keyword.test.ts</files>
  <behavior>
    - Test 1: internal candidate types can represent keyword-only evidence without changing the public API contract
    - Test 2: candidate metadata can distinguish semantic, keyword, and future merged evidence for later plans
  </behavior>
  <action>
    Extend `packages/server/src/lib/retrieval/types.ts` with internal-only candidate metadata that later hybrid merge and rerank stages can consume. Keep the current `ScoredEntry` compatibility path available if needed, but add a richer representation for query-time recall evidence such as recall channel, normalized per-channel score, and token-match details. Do not change anything under `packages/contracts/`; per BOUND-03 and BOUND-05 this groundwork must remain entirely inside the server retrieval pipeline and must not alter the public response shape.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/keyword.test.ts</automated>
  </verify>
  <done>
    - internal retrieval types can carry keyword recall evidence needed for merge/rerank
    - the public retrieval request/response contract remains unchanged
    - no mode wiring or lifecycle/index persistence work is introduced
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement query-time keyword recall adapter over eligible entries</name>
  <files>packages/server/src/lib/retrieval/recall/keyword.ts, packages/server/src/lib/retrieval/recall/keyword.test.ts</files>
  <behavior>
    - Test 1: keyword recall tokenizes short queries and matches against `shortcut`, `detail`, and `labels`
    - Test 2: keyword scores are normalized to `[0,1]` and deterministic for the same fixture input
    - Test 3: entries with no token overlap are excluded from the candidate list
    - Test 4: the adapter operates only on entries passed in by the caller and does not perform approval/team filtering itself
  </behavior>
  <action>
    Create `packages/server/src/lib/retrieval/recall/keyword.ts` as a pure query-time adapter for HYBR-01. It must accept the already-filtered eligible `KnowledgeRecord[]`, normalize the query into lexical tokens, score overlap across `shortcut`, `detail`, and `labels`, and return stable channel-aware candidates sorted by descending keyword relevance. Keep scoring bounded to `[0,1]`, prefer exact label/shortcut hits over weaker body-text hits, and document that this module consumes filter output rather than raw store snapshots. Do not add persistence, background indexing, cache tables, or lifecycle hooks; those belong to Phase 8 and are explicitly out of scope here.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/keyword.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>
    - `keywordRecall(...)` exists and returns deterministic, normalized candidates
    - lexical matching uses `shortcut`, `detail`, and `labels`
    - the adapter is query-time only and does not bypass or duplicate server-side eligibility filtering
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/keyword.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- Optional non-blocking workspace baseline check: `pnpm typecheck` and record only unrelated known failures in `packages/server/src/routes/operations.ts` and `packages/cli/src/commands/audit.ts`
</verification>

<success_criteria>
- A dedicated keyword recall adapter exists at `packages/server/src/lib/retrieval/recall/keyword.ts`
- Internal types can carry recall-channel metadata needed for later merge/rerank work
- Phase 6 security ordering is preserved because the adapter consumes already-eligible entries only
- No Phase 8 indexing or persistence responsibilities are pulled into this plan
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter stage -> keyword recall | only already-approved and authorized entries may cross into lexical scoring |
| query text -> lexical tokenizer | untrusted user input is converted into tokens used for scoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `recall/keyword.ts` | mitigate | require the adapter API to accept only eligible entries from `filterEligibleEntries`, never raw store snapshots |
| T-07-02 | Tampering | keyword score calculation | mitigate | normalize scores to `[0,1]`, keep weighting deterministic, and cover scoring rules with unit tests |
| T-07-03 | Denial of Service | query tokenization | mitigate | bound processing to query-time token parsing over existing fields only; avoid unbounded expansion or remote calls |
</threat_model>

<output>
After completion, create `.planning/phases/07-混合检索/07-01-SUMMARY.md`
</output>
