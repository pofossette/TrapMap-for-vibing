---
phase: 07-混合检索
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/merge.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/cli/src/commands/retrieval.test.ts
autonomous: true
user_setup: []
requirements:
  - HYBR-02
  - HYBR-04
  - BOUND-01
  - BOUND-02
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Hybrid mode returns a normal retrieval response instead of a 501 placeholder"
    - "Semantic and keyword candidates are merged and deduplicated before bucket assembly"
    - "Response shape stays `globalConstraints + projectKnowledge + refinementSummary` and scope still means business scope, not retrieval mode"
  artifacts:
    - path: "packages/server/src/lib/retrieval/merge.ts"
      provides: "semantic + keyword merge and dedupe logic"
      contains: "mergeCandidates"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "hybrid mode dispatch using filter -> recall -> merge -> assembly ordering"
      contains: "case 'hybrid'"
    - path: "packages/server/src/routes/retrieval.test.ts"
      provides: "route coverage proving hybrid mode is accepted"
      contains: "hybrid"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/recall/keyword.ts"
      via: "hybrid dispatch"
      pattern: "keywordRecall"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/merge.ts"
      via: "candidate merge"
      pattern: "mergeCandidates"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "unchanged response assembly"
      pattern: "assembleResponseBuckets"
---

<objective>
Wire hybrid mode into the orchestrator by merging semantic and keyword candidates while keeping the public API and CLI contract unchanged.

Purpose: satisfy HYBR-02 and HYBR-04 with a server-side hybrid retrieval path that preserves Phase 6 invariants and keeps `global`/`project` as scope semantics, not retrieval-mode semantics.

Planning basis: no `CONTEXT.md` exists for Phase 7. This plan is based on roadmap, requirements, current code, and prior Phase 6 artifacts only.

Output: a reusable merge module, hybrid-mode orchestration, and regression coverage across server route and CLI boundaries.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-混合检索/07-RESEARCH.md
@.planning/phases/06-检索架构重构/06-03-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/assembly.ts
@packages/server/src/routes/retrieval.test.ts
@packages/cli/src/commands/retrieval.test.ts

<interfaces>
From `packages/contracts/src/domain/retrieval.ts`:
```ts
export const retrievalQueryModeSchema = z.enum(['semantic', 'hybrid', 'graph-assisted']);
export const retrievalResponseSchema = z.object({
  globalConstraints: z.array(retrievalMatchSchema),
  projectKnowledge: z.array(retrievalMatchSchema),
  refinementSummary: z.string().nullable(),
});
```

From `packages/server/src/lib/retrieval/assembly.ts`:
```ts
export function assembleResponseBuckets(
  scoredEntries: ScoredEntry[],
  filters: RetrievalQuery['filters'],
): { globalConstraints: RetrievalMatch[]; projectKnowledge: RetrievalMatch[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement merge and dedupe for semantic plus keyword candidates</name>
  <files>packages/server/src/lib/retrieval/merge.ts, packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval.test.ts</files>
  <behavior>
    - Test 1: when the same entry appears in both channels, the merged output contains one candidate keyed by `entry.id`
    - Test 2: merge preserves normalized channel evidence so later rerank can see combined support
    - Test 3: final merged candidates are sorted deterministically and remain bounded before assembly
  </behavior>
  <action>
    Create `packages/server/src/lib/retrieval/merge.ts` for HYBR-02. The module should merge semantic and keyword candidate lists, deduplicate by `entry.id`, preserve enough per-channel evidence for later rerank, and return a stable merged list without changing the public response schema. Keep this logic server-internal per BOUND-01 and BOUND-02. Use an intermediate candidate cap larger than `maxResults` so each channel can contribute candidates before final truncation, but do not introduce persistence or cache tables.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts</automated>
  </verify>
  <done>
    - semantic and keyword candidates can be merged into one deduplicated internal list
    - merge output remains deterministic for identical fixtures
    - internal metadata needed for final ranking is preserved
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the hybrid 501 path with orchestrated hybrid retrieval wiring</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <behavior>
    - Test 1: `mode: 'hybrid'` executes semantic recall + keyword recall + merge instead of returning 501
    - Test 2: omitting mode still follows the existing semantic path and keeps default behavior unchanged
    - Test 3: hybrid responses preserve the existing response shape and bucket split semantics
    - Test 4: CLI passthrough for `--mode hybrid` still depends only on contracts and does not change output formatting responsibilities
  </behavior>
  <action>
    Update `packages/server/src/lib/retrieval/orchestrator.ts` so `case 'hybrid'` runs the existing filter-first order, dispatches semantic recall and keyword recall over the same eligible entries, merges the resulting candidates, and then feeds the unchanged assembly layer. Keep refinement best-effort and downstream of hybrid ordering, keep `graph-assisted` untouched, and do not reinterpret `scope` as retrieval mode. Extend route and CLI regression tests to prove the boundary stays contract-first: no response shape change, no CLI awareness of channel internals, and semantic default behavior stays intact.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>
    - hybrid mode is a working server retrieval path
    - API responses remain contract-compatible
    - CLI behavior remains a thin mode passthrough with unchanged output shape expectations
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- Optional non-blocking workspace baseline check: `pnpm typecheck` and ignore only the known unrelated failures already documented in Phase 7 research
</verification>

<success_criteria>
- `mode: 'hybrid'` no longer returns the Phase 6 placeholder error
- Hybrid retrieval merges semantic and keyword candidates before assembly
- Response shape and scope semantics remain unchanged
- CLI and API boundaries remain contract-first, with no hybrid-channel details exposed publicly
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eligible entries -> hybrid dispatch | only filtered entries can reach semantic/keyword recall |
| merged candidates -> response assembly | internal ranking evidence must not alter public contract structure |
| CLI -> API mode boundary | CLI may choose `hybrid`, but contracts remain the sole request/response truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | `orchestrator.ts` hybrid branch | mitigate | preserve `filterEligibleEntries` as the only source of hybrid input candidates before any recall work |
| T-07-05 | Tampering | `merge.ts` dedupe logic | mitigate | deduplicate strictly by `entry.id`, preserve deterministic ordering, and cover overlap cases with tests |
| T-07-06 | Elevation of Privilege | scope/mode handling | mitigate | keep bucket split in `assembly.ts` and test that mode does not replace `global` or `project` scope semantics |
| T-07-07 | Repudiation | CLI/API boundary drift | mitigate | leave response contract unchanged and regression-test CLI passthrough using shared contracts only |
</threat_model>

<output>
After completion, create `.planning/phases/07-混合检索/07-02-SUMMARY.md`
</output>
