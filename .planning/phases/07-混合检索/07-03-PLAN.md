---
phase: 07-混合检索
plan: "03"
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - packages/server/src/lib/retrieval/rerank.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
user_setup: []
requirements:
  - HYBR-03
  - HYBR-05
  - BOUND-03
  - BOUND-05
must_haves:
  truths:
    - "Hybrid retrieval applies a deterministic rerank stage after merge and before response assembly"
    - "Short-query fixtures show a measurable hybrid ordering/recall improvement over semantic-only behavior"
    - "Approved-only and team-safe retrieval boundaries still hold after rerank is introduced"
  artifacts:
    - path: "packages/server/src/lib/retrieval/rerank.ts"
      provides: "deterministic rerank stage for merged candidates"
      contains: "rerankCandidates"
    - path: "packages/server/src/lib/retrieval.test.ts"
      provides: "comparative validation for semantic vs hybrid short-query behavior"
      contains: "hybrid improvement"
    - path: "packages/server/src/lib/retrieval-workflow.test.ts"
      provides: "approval-boundary regression coverage after rerank wiring"
      contains: "approved"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/rerank.ts"
      via: "post-merge ranking"
      pattern: "rerankCandidates"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "final assembled results after rerank"
      pattern: "assembleResponseBuckets"
    - from: "packages/server/src/lib/retrieval-workflow.test.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "authenticated search flow"
      pattern: "/v1/retrieval/search"
---

<objective>
Add a deterministic rerank stage and validation proving hybrid retrieval improves short-query outcomes without breaking server-side safety guarantees.

Purpose: complete Phase 7 with a simple rerank implementation and verification evidence for HYBR-03 and HYBR-05, still limited to query-time logic and still outside Phase 8 lifecycle/indexing scope.

Planning basis: no `CONTEXT.md` exists for Phase 7. This plan is based on roadmap, requirements, current code, and prior phase artifacts only.

Output: a rerank module, orchestrator integration, and deterministic tests demonstrating short-query improvement.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-混合检索/07-RESEARCH.md
@.planning/phases/06-检索架构重构/06-VERIFICATION.md
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval.test.ts
@packages/server/src/lib/retrieval-workflow.test.ts
@packages/server/src/routes/retrieval.test.ts

<interfaces>
From `packages/server/src/lib/retrieval/orchestrator.ts`:
```ts
const eligibleEntries = filterEligibleEntries(...);
const { globalConstraints, projectKnowledge } = assembleResponseBuckets(topMatches, parsed.filters);
```

From `packages/contracts/src/domain/retrieval.ts`:
```ts
export const retrievalResponseSchema = z.object({
  globalConstraints: z.array(retrievalMatchSchema),
  projectKnowledge: z.array(retrievalMatchSchema),
  refinementSummary: z.string().nullable(),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic rerank after hybrid merge</name>
  <files>packages/server/src/lib/retrieval/rerank.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts</files>
  <behavior>
    - Test 1: rerank reorders merged candidates deterministically using combined channel evidence for the same input
    - Test 2: final results are truncated after rerank, not before merge
    - Test 3: rerank does not introduce new entries or bypass the filtered candidate set
  </behavior>
  <action>
    Create `packages/server/src/lib/retrieval/rerank.ts` for HYBR-03 and wire it into the hybrid branch in `orchestrator.ts` after merge and before `assembleResponseBuckets`. Keep the rerank logic deterministic and local: combine lexical precision signals, semantic score, and multi-channel support without calling external models or services. Do not expose rerank details in the public response schema yet; Phase 10 owns public citation/channel metadata. Preserve semantic-only behavior unless hybrid mode is selected.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts</automated>
  </verify>
  <done>
    - `rerankCandidates(...)` exists and is used only in the hybrid path
    - final result ordering is determined after merge, before assembly
    - no contract changes or external-model dependencies are introduced
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove short-query improvement and preserve approval-first safety</name>
  <files>packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/retrieval-workflow.test.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: a short-query fixture shows hybrid mode returning a better top hit or a better candidate set than semantic mode alone
    - Test 2: approved-only workflow protection still holds when searching with hybrid mode
    - Test 3: route-level tests continue to accept hybrid requests and return the unchanged contract shape
  </behavior>
  <action>
    Add deterministic validation for HYBR-05 using repository-local fixtures in the existing retrieval test surface. The comparison must be explicit: same fixture corpus, same short query, semantic-only result versus hybrid+rerkank result, with an asserted improvement in ordering or recall. Extend workflow and route coverage so the approval -> filtering -> retrieval -> output order remains intact after rerank. Keep the evidence phase-scoped and query-time only; do not add benchmarking infrastructure, index persistence, or lifecycle-triggered refresh logic.
  </action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>
    - tests demonstrate a concrete short-query improvement for hybrid mode
    - workflow tests confirm unapproved content still cannot surface
    - route coverage confirms unchanged response shape and successful hybrid-mode execution
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- Optional non-blocking workspace baseline check: `pnpm typecheck` while treating only the already-documented unrelated failures as baseline noise
</verification>

<success_criteria>
- Hybrid mode includes a deterministic rerank stage between merge and assembly
- Automated tests show a concrete short-query improvement versus semantic-only retrieval
- Filter-first, approved-only, and team-safe behavior remains preserved end to end
- Phase 7 completes without adding Phase 8 indexing or lifecycle responsibilities
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged candidates -> rerank | rerank must only reorder existing safe candidates, never add new ones |
| rerank -> assembly | final ordered list must still map into the unchanged public response contract |
| authenticated route -> workflow verification | approval and team constraints must still apply before hybrid search results are returned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | `rerank.ts` | mitigate | keep rerank deterministic, bounded, and covered by comparative fixture tests |
| T-07-09 | Information Disclosure | hybrid workflow after rerank | mitigate | verify rerank operates only on merged candidates derived from `eligibleEntries` and add workflow tests for unapproved content |
| T-07-10 | Denial of Service | rerank stage | mitigate | use local heuristic scoring only, with no network calls or persistent-index work |
| T-07-11 | Repudiation | improvement claim validation | mitigate | encode HYBR-05 proof in automated tests comparing semantic and hybrid outputs on fixed fixtures |
</threat_model>

<output>
After completion, create `.planning/phases/07-混合检索/07-03-SUMMARY.md`
</output>
