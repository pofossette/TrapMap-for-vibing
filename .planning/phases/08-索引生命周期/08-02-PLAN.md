---
phase: 08-索引生命周期
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - packages/server/src/lib/indexing/events.ts
  - packages/server/src/lib/indexing/events.test.ts
  - packages/server/src/routes/review.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
autonomous: true
requirements:
  - IDX-03
  - IDX-04
  - BOUND-01
  - BOUND-02
  - BOUND-03
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Lifecycle transitions map to explicit index actions instead of implicit query-time warmup."
    - "Reviewer approval automatically triggers index sync for the approved entry."
    - "Approval remains a server-side lifecycle gate before anything becomes searchable."
  artifacts:
    - path: "packages/server/src/lib/indexing/events.ts"
      provides: "lifecycle trigger mapping and route-facing sync helper"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/review.ts"
      provides: "approval path that triggers lifecycle-driven indexing after review commit"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/lib/retrieval-workflow.test.ts"
      provides: "approval-triggered indexing regression coverage"
      contains: "indexState"
  key_links:
    - from: "packages/server/src/routes/review.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "reviewer-approved trigger"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/lib/indexing/events.ts"
      to: "packages/server/src/lib/indexing/pipeline.ts"
      via: "dispatch upsert/remove/noop"
      pattern: "syncKnowledgeIndex|removeKnowledgeIndex"
    - from: "packages/server/src/lib/retrieval-workflow.test.ts"
      to: "packages/server/src/routes/review.ts"
      via: "approval workflow verification"
      pattern: "POST /v1/knowledge/review|decision: 'approve'"
---

<objective>
Translate lifecycle transitions into explicit indexing actions and wire the approval path so approved entries are indexed immediately after review.

Purpose: satisfy IDX-03 and IDX-04 without moving approval, RBAC, team filtering, or audit behavior out of the server, preserving BOUND-03 and BOUND-05.

Output: a route-safe lifecycle event helper, approval-triggered sync wiring, and workflow tests proving approved content is indexed because of lifecycle sync rather than query-time recomputation.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-索引生命周期/08-RESEARCH.md
@.planning/phases/08-索引生命周期/08-VALIDATION.md
@packages/server/src/lib/indexing/pipeline.ts
@packages/server/src/lib/knowledge.ts
@packages/server/src/routes/review.ts
@packages/server/src/lib/retrieval-workflow.test.ts

<interfaces>
From `packages/server/src/lib/knowledge.ts`:
```ts
export function applyReviewDecision(args: {
  store: JsonStore;
  data: StoreData;
  entry: KnowledgeRecord;
  reviewerUserId: string;
  decidedAt: string;
  decision: 'approve' | 'reject';
  notes: string;
}): void;
```

From `packages/server/src/lib/indexing/pipeline.ts`:
```ts
export async function syncKnowledgeIndex(
  services: SkillShareerServices,
  entryId: string,
  adapters?: IndexAdapter[],
): Promise<IndexSyncResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 lifecycle-event tests and approval indexing workflow assertions</name>
  <read_first>packages/server/src/routes/review.ts, packages/server/src/lib/knowledge.ts, packages/server/src/lib/retrieval-workflow.test.ts, .planning/phases/08-索引生命周期/08-VALIDATION.md, packages/server/src/lib/indexing/events.test.ts</read_first>
  <files>packages/server/src/lib/indexing/events.test.ts, packages/server/src/lib/retrieval-workflow.test.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/indexing/events.test.ts`
    - `rg -n "reviewer-approved|updated|deactivated|noop|remove|upsert" packages/server/src/lib/indexing/events.test.ts`
    - `rg -n "indexState|embeddingCache|knowledge/review" packages/server/src/lib/retrieval-workflow.test.ts`
  </acceptance_criteria>
  <behavior>
    - Test 1: lifecycle event mapping returns `upsert` for approval, `remove` for deactivation, and `noop` for rejected/non-approved transitions
    - Test 2: approving an entry builds index state before the next retrieval assertion reads the store snapshot
    - Test 3: approval-triggered sync does not bypass the existing approved-only search boundary
  </behavior>
  <action>Create `packages/server/src/lib/indexing/events.test.ts` and extend `packages/server/src/lib/retrieval-workflow.test.ts` per the Phase 8 validation gaps. Pin a concrete helper name such as `runKnowledgeIndexEvent`, assert event-to-action mapping for `reviewer-approved`, `updated`, and `deactivated`, and add workflow assertions that approved entries gain persisted `indexState` immediately after `POST /v1/knowledge/review` succeeds.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/indexing/events.test.ts src/lib/retrieval-workflow.test.ts</automated>
  </verify>
  <done>The missing lifecycle-event test file exists and approval workflow coverage now proves index state is created from a review event, not lazily during search.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lifecycle event dispatch and approval-triggered index sync</name>
  <read_first>packages/server/src/lib/indexing/pipeline.ts, packages/server/src/lib/knowledge.ts, packages/server/src/routes/review.ts, packages/server/src/lib/indexing/events.test.ts, packages/server/src/lib/retrieval-workflow.test.ts, .planning/phases/08-索引生命周期/08-RESEARCH.md</read_first>
  <files>packages/server/src/lib/indexing/events.ts, packages/server/src/routes/review.ts</files>
  <acceptance_criteria>
    - `rg -n "export function determineKnowledgeIndexAction|export async function runKnowledgeIndexEvent" packages/server/src/lib/indexing/events.ts`
    - `rg -n "runKnowledgeIndexEvent" packages/server/src/routes/review.ts`
    - `rg -n "reviewer-approved|decision === 'approve'|decision: payload.decision" packages/server/src/routes/review.ts`
  </acceptance_criteria>
  <action>Create `packages/server/src/lib/indexing/events.ts` with a concrete event helper that accepts `{ services, entryId, previousState, nextState, reason }`, maps lifecycle transitions to `upsert`, `remove`, or `noop`, and delegates to the pipeline from 08-01. Wire `packages/server/src/routes/review.ts` so a successful approval transaction calls `runKnowledgeIndexEvent` after commit using the real `previousState` captured before `applyReviewDecision`. Reject decisions must not index content; they should resolve to `noop` or `remove` according to current lifecycle state. Keep audit and permission behavior unchanged, and do not alter retrieval request/response shapes.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/indexing/events.test.ts src/lib/retrieval-workflow.test.ts</automated>
  </verify>
  <done>Review approval now triggers lifecycle-driven sync through a dedicated events module, while non-approved transitions remain non-searchable and business-boundary logic stays in existing server flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review transaction -> post-commit index event | lifecycle authority stays in review flow, index sync is a derived side effect |
| lifecycle event helper -> pipeline | event mapping must not misclassify rejected/deactivated content as searchable |
| workflow tests -> persisted store snapshot | approval assertions must inspect actual stored index state, not inferred behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | `review.ts` approval hook | mitigate | call index sync only after existing permission and higher-level checks succeed, leaving review authority unchanged |
| T-08-06 | Information Disclosure | `events.ts` transition mapping | mitigate | map only `approved` content to `upsert`, test rejected/non-approved states as `noop` or `remove` |
| T-08-07 | Tampering | `events.ts` dispatcher | mitigate | require explicit previous/next lifecycle state inputs and cover all supported transition types in unit tests |
| T-08-08 | Repudiation | approval-triggered sync | mitigate | keep approval audit logging untouched and make workflow tests assert persisted index state after the same review call |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/indexing/events.test.ts src/lib/retrieval-workflow.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- `indexing/events.ts` maps lifecycle transitions to explicit sync actions
- `POST /v1/knowledge/review` approval path triggers index sync after commit
- Approval remains the gate for searchable content; no new CLI or contract surface is introduced
</success_criteria>

<must_haves>
<truths>
- Review approval remains the only point where searchable lifecycle state is granted, and indexing is a derived server-side side effect after that transition.
- Lifecycle event mapping stays internal to the server and does not repurpose `global` or `project` as retrieval modes, preserving BOUND-04.
- No CLI flags, contract fields, or external job system are introduced to support approval-triggered indexing, preserving BOUND-01 and BOUND-02.
</truths>

<artifacts>
- `packages/server/src/lib/indexing/events.ts` defines explicit lifecycle-to-index action mapping.
- `packages/server/src/routes/review.ts` triggers index sync only after the review transaction commits successfully.
- `packages/server/src/lib/indexing/events.test.ts` exists as Wave 0 coverage for transition mapping.
- `packages/server/src/lib/retrieval-workflow.test.ts` asserts approval creates persisted index state before retrieval assertions run.
</artifacts>

<key_links>
- `review.ts` must pass real `previousState` and `nextState` into `events.ts` so rejected or non-approved entries cannot be mistakenly indexed.
- `events.ts` must call the pipeline entrypoint from 08-01 rather than reimplement adapter or normalization logic in the route layer.
- The workflow test must verify both persisted index state and approved-only retrieval visibility so the approval boundary remains intact.
</key_links>
</must_haves>

<output>
After completion, create `.planning/phases/08-索引生命周期/08-02-SUMMARY.md`
</output>
