---
phase: 08-索引生命周期
plan: "04"
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - packages/server/src/routes/knowledge.ts
  - packages/server/src/routes/operations.ts
  - packages/server/src/routes/operations.test.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
autonomous: true
requirements:
  - IDX-05
  - IDX-06
  - BOUND-01
  - BOUND-02
  - BOUND-03
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Approved content updates refresh persisted index state without waiting for a later query."
    - "Deactivated knowledge is removed from vector and keyword index representations."
    - "Search and persisted index state stay aligned after update and deactivate workflows."
  artifacts:
    - path: "packages/server/src/routes/knowledge.ts"
      provides: "update flow that triggers lifecycle-driven index refresh when needed"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/operations.ts"
      provides: "deactivate flow that removes index state after lifecycle mutation"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/operations.test.ts"
      provides: "route coverage for update/deactivate index side effects"
      contains: "deactivated"
  key_links:
    - from: "packages/server/src/routes/knowledge.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "updated lifecycle trigger"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "deactivated lifecycle trigger"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/lib/retrieval-workflow.test.ts"
      to: "packages/server/src/routes/knowledge.ts"
      via: "approved-update refresh verification"
      pattern: "PATCH /v1/knowledge/:entryId"
---

<objective>
Finish Phase 8 by wiring update and deactivate flows into lifecycle-driven indexing and proving search/index alignment survives those mutations.

Purpose: satisfy IDX-05 and IDX-06 while keeping all lifecycle authority, permissions, and audit semantics inside the server and preserving the approved -> permission-filtered -> retrieval sequence from BOUND-05.

Output: update/deactivate lifecycle sync wiring plus regression coverage that approved updates refresh indexes and deactivated entries disappear from persisted index state and retrieval results.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-索引生命周期/08-RESEARCH.md
@.planning/phases/08-索引生命周期/08-VALIDATION.md
@packages/server/src/lib/indexing/events.ts
@packages/server/src/lib/indexing/pipeline.ts
@packages/server/src/routes/knowledge.ts
@packages/server/src/routes/operations.ts
@packages/server/src/routes/operations.test.ts
@packages/server/src/lib/retrieval-workflow.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend workflow and operations tests for update refresh and deactivate removal</name>
  <read_first>packages/server/src/routes/knowledge.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts, packages/server/src/lib/retrieval-workflow.test.ts, .planning/phases/08-索引生命周期/08-VALIDATION.md</read_first>
  <files>packages/server/src/routes/operations.test.ts, packages/server/src/lib/retrieval-workflow.test.ts</files>
  <acceptance_criteria>
    - `rg -n "indexState|knowledge-deactivated|deactivated" packages/server/src/routes/operations.test.ts`
    - `rg -n "PATCH /v1/knowledge|updated|indexState" packages/server/src/lib/retrieval-workflow.test.ts`
  </acceptance_criteria>
  <behavior>
    - Test 1: updating an approved entry refreshes persisted vector/keyword state and subsequent search reflects the new content
    - Test 2: deactivating an entry clears persisted adapter state and removes the entry from retrieval results
    - Test 3: non-approved update paths remain non-searchable and do not accidentally upsert searchable index state
  </behavior>
  <action>Extend `packages/server/src/lib/retrieval-workflow.test.ts` with an approved-entry update scenario that checks persisted `indexState` changes after `PATCH /v1/knowledge/:entryId`. Extend `packages/server/src/routes/operations.test.ts` from schema-only assertions to concrete route-behavior coverage for deactivation side effects, including persisted state removal and search absence after the route succeeds.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval-workflow.test.ts src/routes/operations.test.ts</automated>
  </verify>
  <done>Workflow coverage now proves Phase 8 handles approved updates and deactivations end to end instead of only through unit tests or placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire update and deactivate routes into lifecycle-driven sync/remove behavior</name>
  <read_first>packages/server/src/lib/indexing/events.ts, packages/server/src/lib/indexing/pipeline.ts, packages/server/src/routes/knowledge.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts, packages/server/src/lib/retrieval-workflow.test.ts</read_first>
  <files>packages/server/src/routes/knowledge.ts, packages/server/src/routes/operations.ts</files>
  <acceptance_criteria>
    - `rg -n "runKnowledgeIndexEvent" packages/server/src/routes/knowledge.ts`
    - `rg -n "runKnowledgeIndexEvent" packages/server/src/routes/operations.ts`
    - `rg -n "updated|deactivated|previousState" packages/server/src/routes/knowledge.ts packages/server/src/routes/operations.ts`
  </acceptance_criteria>
  <action>Update `packages/server/src/routes/knowledge.ts` so the privileged `PATCH /v1/knowledge/:entryId` path captures `previousState`, commits the revision update, and then invokes `runKnowledgeIndexEvent` with an `updated` reason. Approved entries must refresh persisted vector and keyword state; non-approved entries must resolve to `noop` through the events module. Update `packages/server/src/routes/operations.ts` so deactivation calls the same lifecycle helper after commit, forcing adapter removal and clearing persisted index state for the entry. Keep existing permission checks, team guards, and audit events unchanged, and do not add new persistence layers, external jobs, CLI flags, or contract fields.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval-workflow.test.ts src/routes/operations.test.ts</automated>
  </verify>
  <done>Approved updates trigger index refresh, deactivation removes all persisted index material, and search/index state stays consistent after both lifecycle mutations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| update/deactivate route -> lifecycle helper | privileged mutations must remain the only source of index refresh/removal triggers |
| lifecycle helper -> persisted adapter state | stale approved data or deactivated remnants must not survive after route success |
| route tests -> retrieval results | regression checks must prove both stored index state and search visibility remain aligned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Information Disclosure | `operations.ts` deactivation path | mitigate | route must call lifecycle removal after commit and tests must assert persisted adapter state is cleared plus search returns no match |
| T-08-14 | Tampering | `knowledge.ts` approved update path | mitigate | refresh by current revision/content hash after update, and test that changed content updates persisted state rather than leaving stale vectors/tokens |
| T-08-15 | Elevation of Privilege | update/deactivate route hooks | mitigate | preserve existing `requirePermission`, `requireTeamAccess`, and `requireHigherLevel` enforcement before any index event is emitted |
| T-08-16 | Repudiation | lifecycle-driven refresh/remove | mitigate | keep audit events untouched and extend workflow tests to inspect post-route persisted state directly |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval-workflow.test.ts src/routes/operations.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- `pnpm test`
</verification>

<success_criteria>
- Approved updates refresh persisted index state automatically
- Deactivated entries are removed from persisted vector and keyword index payloads
- Retrieval behavior remains contract-compatible and aligned with lifecycle state after update/deactivate flows
</success_criteria>

<must_haves>
<truths>
- Approved updates refresh persisted index artifacts automatically after the mutation commits; stale searchable content is not left behind.
- Deactivation removes persisted vector and keyword state so retrieval cannot accidentally surface inactive content.
- The phase stays server-internal with no CLI or contract changes, and all routing continues to respect approval, RBAC, team access, and audit sequencing.
</truths>

<artifacts>
- `packages/server/src/routes/knowledge.ts` emits an indexing event after privileged updates using the existing lifecycle and permission checks.
- `packages/server/src/routes/operations.ts` emits an indexing removal event after deactivation using the existing audit and permission flow.
- `packages/server/src/routes/operations.test.ts` and `packages/server/src/lib/retrieval-workflow.test.ts` verify update/deactivate side effects against persisted index state and search results.
</artifacts>

<key_links>
- `knowledge.ts` must send post-commit update events through `events.ts` so approved updates refresh both adapters and non-approved updates remain `noop`.
- `operations.ts` must route deactivation through the same lifecycle helper so removal semantics stay centralized and consistent.
- Regression tests must inspect both store state and retrieval output to prove lifecycle state and index state remain synchronized after update and deactivate flows.
</key_links>
</must_haves>

<output>
After completion, create `.planning/phases/08-索引生命周期/08-04-SUMMARY.md`
</output>
