---
phase: 10-回答与引用
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - packages/server/src/lib/retrieval/summary.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/summary.test.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements:
  - SUMM-01
  - SUMM-02
  - SUMM-03
  - SUMM-04
  - SUMM-05
  - BOUND-03
  - BOUND-05
must_haves:
  truths:
    - "summary 是可选输出，默认关闭时检索结果与无摘要请求一致"
    - "summary 只基于已命中的批准知识与其 citations 生成，不会重新检索或绕过权限过滤"
    - "summary 自身能返回支撑它的 citations"
  artifacts:
    - path: "packages/server/src/lib/retrieval/summary.ts"
      provides: "optional summary builder over safe hits"
    - path: "packages/server/src/lib/retrieval/summary.test.ts"
      provides: "unit coverage for opt-in summary behavior and citation carry-through"
    - path: "packages/server/src/lib/retrieval-workflow.test.ts"
      provides: "filter-first / approval-first integration proof after summary wiring"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/summary.ts"
      via: "includeSummary gate after citation-bearing assembly"
      pattern: "includeSummary"
    - from: "packages/server/src/lib/retrieval/summary.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "contract-shaped summary output with citations"
      pattern: "summary"
---

<objective>
把 optional summary 作为 server output stage 的最后一步接入，并确保它严格消费已过滤命中与 citations。

Purpose: 满足 `SUMM-01`..`SUMM-05`，同时把摘要能力限制在安全边界内，不改变 recall/filter 的职责边界。
Output: `summary.ts`、summary unit/integration tests、接入 summary gate 的 orchestrator。
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-回答与引用/10-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/citations.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval.test.ts
@packages/server/src/lib/retrieval-workflow.test.ts

<interfaces>
Expected contract direction from Plan 01:
```typescript
type RetrievalCitation = {
  source: { entryId: string; scope: 'global' | 'project'; shortcut: string };
  snippet: string;
  tags: string[];
  recallChannels: Array<'semantic' | 'keyword' | 'graph'>;
  scores: { preRerank: number; final: number };
};

type RetrievalSummary = {
  text: string;
  citations: RetrievalCitation[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现只消费 safe hits 的 Summary Builder</name>
  <files>packages/server/src/lib/retrieval/summary.ts, packages/server/src/lib/retrieval/summary.test.ts</files>
  <behavior>
    - Test 1: 传入空 hits 或关闭 summary 时，builder 返回 `null`/不生成 summary。
    - Test 2: summary 文本只由传入命中内容提炼，返回值同时包含至少一个 citation。
    - Test 3: `summary.ts` 不依赖 store、recall adapters 或 graph index。
  </behavior>
  <action>新增 `summary.ts`，实现 deterministic extractive summary baseline：输入仅限 query、已组装或可公开的 hits/citations，输出 contract-shaped summary。禁止在该文件中 import `services.store`、`filterEligibleEntries`、`keywordRecall`、`graph-assisted` 或任何再次召回的 helper。summary 可简洁，但必须可追溯到引用，不允许“未来再补动态实现”的降级语句。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/summary.test.ts</automated>
  </verify>
  <done>`summary.ts` 作为纯函数存在，默认不开启，开启后可从现有 safe hits 生成带 citations 的 summary。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 summary gate 接入 orchestrator 与集成回归</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/retrieval-workflow.test.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: includeSummary 关闭时，不生成 summary，且原结果集保持不变。
    - Test 2: includeSummary 开启时，summary 仅总结当前响应里的命中，不会引入未授权或未批准内容。
    - Test 3: route 与 workflow tests 证明顺序仍是 approval -> permission/team filtering -> retrieval -> output。
  </behavior>
  <action>在 orchestrator 中把 summary 放到 citations/assembly 之后的最后一步，并通过 Plan 01 的 request flag 显式 gate。若需要兼容旧 `includeRefinement` / `refinementSummary`，必须让兼容逻辑停留在 output stage，不能把旧 provider stub 重新变成核心路径。扩展 retrieval、workflow、route 测试，覆盖 summary 开关、approval gating 和 team/level safety。不要把 summary 设计成新的召回模式，也不要动 `globalConstraints` / `projectKnowledge` 分桶边界。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/summary.test.ts src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts</automated>
  </verify>
  <done>summary 已安全接入真实查询链路；开启时有摘要与引用，关闭时行为保持兼容；审批和权限边界继续先于输出阶段执行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| safe hits→summary builder | 摘要只允许看到已过滤命中，不能主动扩大读取范围 |
| optional flag→runtime behavior | 新开关不能默默改变默认搜索成本与输出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | I | `packages/server/src/lib/retrieval/summary.ts` | mitigate | Summary Builder 设计为纯函数，只接受 orchestrator 传入的 safe hits/citations |
| T-10-08 | T | `packages/server/src/lib/retrieval/orchestrator.ts` | mitigate | 通过显式 `includeSummary` gate 控制运行，默认关闭，避免默认行为漂移 |
| T-10-09 | R | `packages/server/src/lib/retrieval-workflow.test.ts` | mitigate | 增加 approval/team/level 边界回归，证明 summary 没有绕过 filter-first 顺序 |
</threat_model>

<verification>
先以 summary unit tests 锁定 builder 行为，再用 retrieval/workflow/route 集成测试证明 summary 只出现在 output stage。
</verification>

<success_criteria>
- `packages/server/src/lib/retrieval/summary.ts` 存在且是纯函数
- includeSummary 默认关闭
- 开启 summary 时返回结构化文本与 citations
- workflow tests 证明 summary 不会引入未批准、跨团队或越权内容
</success_criteria>

<output>
After completion, create `.planning/phases/10-回答与引用/10-03-SUMMARY.md`
</output>
