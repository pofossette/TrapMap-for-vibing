---
phase: 10-回答与引用
plan: "04"
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-02"
  - "10-03"
files_modified:
  - packages/cli/src/commands/retrieval.ts
  - packages/cli/src/commands/retrieval.test.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements:
  - SUMM-06
  - BOUND-01
  - BOUND-02
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "CLI 仅通过共享 contracts 消费 citation/summary 字段"
    - "JSON 输出能完整返回 contract-defined citation/summary，文本输出则保持可扫读"
    - "route 继续只是 auth + permission + contract parse + orchestrator 调用的薄层"
  artifacts:
    - path: "packages/cli/src/commands/retrieval.ts"
      provides: "contract-only citation/summary rendering"
    - path: "packages/cli/src/commands/retrieval.test.ts"
      provides: "JSON/text mode regression coverage"
    - path: "packages/server/src/routes/retrieval.ts"
      provides: "shared contract parse on final response shape"
  key_links:
    - from: "packages/cli/src/commands/retrieval.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "retrievalResponseSchema.parse"
      pattern: "retrievalResponseSchema.parse"
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "searchKnowledge"
      pattern: "searchKnowledge"
---

<objective>
完成 CLI 与 route 的最终接线和 Phase 10 phase gate，确保新增 citation/summary 在终端与 JSON 模式都可用且不突破业务边界。

Purpose: 收口 `BOUND-02` 与 `SUMM-06`，把新增输出真正交付到 CLI，而不让 CLI 依赖 server 内部细节。
Output: 更新后的 CLI formatter、CLI/route regressions、Phase 10 完整验证命令。
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-回答与引用/10-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/routes/retrieval.ts
@packages/server/src/routes/retrieval.test.ts
@packages/cli/src/commands/retrieval.ts
@packages/cli/src/commands/retrieval.test.ts

<interfaces>
From packages/cli/src/commands/retrieval.ts:
```typescript
const parsed = retrievalResponseSchema.parse(response.data);
printResult(parsed, flags, formatRetrievalResponse);
```

From packages/server/src/routes/retrieval.ts:
```typescript
const query = retrievalQuerySchema.parse(request.body);
const result = await searchKnowledge(app.skillShareer, auth, query);
return retrievalResponseSchema.parse(result);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 更新 CLI 以渲染 contract-defined citations 与 optional summary</name>
  <files>packages/cli/src/commands/retrieval.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <behavior>
    - Test 1: `--json` 输出保留完整 contract shape，包括 citation/summary 字段。
    - Test 2: 普通文本模式显示高价值 citation/summary 信息，但不打印内部 debug blob。
    - Test 3: CLI 请求侧只发送 contract 定义的 summary 开关，不依赖 server-internal types。
  </behavior>
  <action>基于 Plan 01 的共享 contracts 更新 CLI 请求与渲染：增加 summary 开关参数，文本模式按可扫读方式展示 citation 来源、片段、通道与 summary，JSON 模式直接输出完整 contract。不要在 CLI 里重算 snippet、scores 或 recall channels；也不要把 `globalConstraints` / `projectKnowledge` 重解释成检索模式分组。测试需同时覆盖 JSON fidelity 和 text formatter 的关键信息呈现。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>CLI 只通过 shared contracts 消费 Phase 10 新字段；JSON 完整、文本精简，且没有 server-internal 依赖泄漏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 完成 route 薄层验证并执行 Phase 10 phase gate</name>
  <files>packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: route 继续通过 shared query/response schema parse 请求与响应。
    - Test 2: summary 请求开关与新 response fields 在 route 层可被接收并验证。
    - Test 3: 全 phase gate 覆盖 contracts、server、CLI tests 与 server typecheck。
  </behavior>
  <action>根据前面 plans 完成后的 contract shape 调整 route schema parse 与回归测试，确保 route 仍然是薄层，不引入任何 citation/summary 业务逻辑。随后执行本阶段的最终验证命令，至少包含 contracts tests、server focused tests、CLI tests 和 server `tsc --noEmit`。如果兼容保留了旧字段，也必须在 route tests 中清楚验证兼容语义，而不是留给人工猜测。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test && pnpm --filter @skill-shareer/server test -- src/lib/retrieval/citations.test.ts src/lib/retrieval/summary.test.ts src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>Phase 10 端到端新增能力已经过 contracts/server/CLI/tsc 联合验证，且 route 仍保持 auth + parse + delegate 的薄层职责。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server response→CLI formatter | CLI 必须消费共享 contract，而不是读取 server 内部结构 |
| route boundary | route 负责输入输出校验，不能吸入 citation/summary 业务实现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | I | `packages/cli/src/commands/retrieval.ts` | mitigate | 文本模式只显示 curated citation/summary 信息，完整 payload 仅在 JSON 模式输出 |
| T-10-11 | T | `packages/cli/src/commands/retrieval.ts` | mitigate | CLI 只发送/解析 contract-defined fields，禁止重算 snippet、channel 或 score |
| T-10-12 | T | `packages/server/src/routes/retrieval.ts` | mitigate | route 继续通过 shared schema parse request/response 并委托 orchestrator，避免业务逻辑漂移到 route 层 |
</threat_model>

<verification>
用 CLI tests 验证 contract fidelity 和人类可读输出，再执行本阶段完整 gate，确保 Phase 10 已处于可交付状态。
</verification>

<success_criteria>
- CLI 支持 summary 开关并渲染 citation/summary
- JSON 输出可被 `retrievalResponseSchema` 直接 parse
- route 仍是薄层 shared-schema boundary
- Phase 10 完整验证命令全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-回答与引用/10-04-SUMMARY.md`
</output>
