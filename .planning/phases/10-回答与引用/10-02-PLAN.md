---
phase: 10-回答与引用
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/merge.ts
  - packages/server/src/lib/retrieval/rerank.ts
  - packages/server/src/lib/retrieval/citations.ts
  - packages/server/src/lib/retrieval/assembly.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/citations.test.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements:
  - CITE-01
  - CITE-02
  - CITE-03
  - CITE-04
  - CITE-05
  - CITE-06
  - BOUND-03
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "每个返回命中都带有可审计 citation，而不是 CLI 或 reason 文本临时拼出来的解释"
    - "citation 只来自已过滤、已召回、已 rerank 的 safe hits"
    - "globalConstraints / projectKnowledge 的业务分桶语义保持不变"
  artifacts:
    - path: "packages/server/src/lib/retrieval/citations.ts"
      provides: "citation builder over reranked candidates"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "output stage wiring for citations before response assembly"
    - path: "packages/server/src/lib/retrieval/citations.test.ts"
      provides: "direct verification of citation source/snippet/tags/channels/scores"
  key_links:
    - from: "packages/server/src/lib/retrieval/rerank.ts"
      to: "packages/server/src/lib/retrieval/citations.ts"
      via: "preserved preRerank/final score fields"
      pattern: "(preRerank|final)Score"
    - from: "packages/server/src/lib/retrieval/citations.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "citation-bearing match objects"
      pattern: "citation"
---

<objective>
在 server retrieval output stage 中实现 Citation Builder，把 Phase 7-9 的内部证据转成公共 contract 可见的审计数据。

Purpose: 满足 `CITE-01`..`CITE-06`，同时把 citation 固定在 server 内部输出链路，而不是泄露到 CLI 或 recall 层。
Output: citation-ready internal candidate types、Citation Builder、组装后的 response matches 与回归测试。
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-回答与引用/10-RESEARCH.md
@.planning/phases/07-混合检索/07-03-SUMMARY.md
@.planning/phases/09-图辅助检索/09-04-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/retrieval/merge.ts
@packages/server/src/lib/retrieval/rerank.ts
@packages/server/src/lib/retrieval/assembly.ts
@packages/server/src/lib/retrieval/orchestrator.ts

<interfaces>
From packages/server/src/lib/retrieval/types.ts:
```typescript
export interface MergedCandidate {
  entry: KnowledgeRecord;
  semanticScore: number;
  keywordScore: number;
  graphScore?: number;
  combinedScore: number;
  tokenMatches: TokenMatchDetail[];
  channels: RecallChannel[];
}
```

From packages/server/src/lib/retrieval/orchestrator.ts:
```typescript
const topMatches = await dispatchByMode(parsed.mode, parsed.seed, eligibleEntries, parsed);
const { globalConstraints, projectKnowledge } = assembleResponseBuckets(topMatches, parsed.filters);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 保留 citation 所需的内部审计证据</name>
  <files>packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/merge.ts, packages/server/src/lib/retrieval/rerank.ts, packages/server/src/lib/retrieval.test.ts</files>
  <behavior>
    - Test 1: rerank 之后仍能区分 pre-rerank score 与 final score。
    - Test 2: merged/reranked candidates 保留 recall channels 与 snippet/tags 所需 entry evidence，不必依赖 `reason` 字符串回推。
    - Test 3: semantic / hybrid / graph-assisted 三种 mode 的排序与过滤边界保持正确。
  </behavior>
  <action>扩展 internal candidate types，明确保留 `preRerankScore` 与 `finalScore`，并让 merge/rerank 输出的结构足以支撑 citation builder。不要把 citation 塞到 recall adapters，也不要靠 `ScoredEntry.reason` 反向解析通道或分数。同步更新 `retrieval.test.ts` 的 mode-specific 断言，证明新增审计字段没有破坏已有 filter-first 与 graph-assisted 行为。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts</automated>
  </verify>
  <done>server internal retrieval types 能无损承载 citation 所需证据，且三种 mode 的现有搜索行为仍然受测试保护。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新增 Citation Builder 并接入 output stage</name>
  <files>packages/server/src/lib/retrieval/citations.ts, packages/server/src/lib/retrieval/assembly.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/citations.test.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: 每个 response match 都包含 source、snippet、tags、recallChannels、审计分数字段。
    - Test 2: citation 只从 orchestrator 已过滤的 hits 构建，不新增 store/retrieval side reads。
    - Test 3: `globalConstraints` / `projectKnowledge` 仍按业务 scope 分桶，而不是被 recall channel 改写。
  </behavior>
  <action>创建 `packages/server/src/lib/retrieval/citations.ts`，让它消费 Task 1 的 reranked candidates，并在 assembly 前生成 contract-shaped citation 数据。更新 orchestrator 为 `dispatch -> citations -> assembly` 链路，保持 `approval -> permission/team filtering -> retrieval -> output` 顺序不变。必要时扩展 assembly 的 match shaping，但不要改变顶层分桶名称与含义。路由测试必须通过共享 contract parse 返回新字段，防止 route/contract 漂移。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/citations.test.ts src/lib/retrieval.test.ts src/routes/retrieval.test.ts</automated>
  </verify>
  <done>server response 已稳定携带完整 citation，且其来源可追溯到 reranked safe hits；业务 scope 分桶未被检索模式或 citation 通道污染。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filtered hits→output stage | citation 必须只从已通过审批、团队与级别过滤的命中构建 |
| server internal evidence→public response | 需要公开足够审计信息，同时不能泄露不必要的内部 debug 内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | I | `packages/server/src/lib/retrieval/citations.ts` | mitigate | Citation Builder 只接收 orchestrator 已过滤的 reranked candidates，不读取 store snapshot 或 recall helpers |
| T-10-05 | R | `packages/server/src/lib/retrieval/rerank.ts` | mitigate | 显式保留 `preRerankScore` 和 `finalScore`，避免分数来源不可追溯 |
| T-10-06 | T | `packages/server/src/lib/retrieval/assembly.ts` | mitigate | 保持 `globalConstraints` / `projectKnowledge` 分桶逻辑独立于 citation channel，防止输出语义漂移 |
</threat_model>

<verification>
以 `citations.test.ts` 验证 builder 本身，再用 retrieval/route 回归证明 citation 已进入真实响应且不破坏 scope buckets。
</verification>

<success_criteria>
- `packages/server/src/lib/retrieval/citations.ts` 存在并被 orchestrator 调用
- response matches 带完整 citation 字段
- rerank 前后分数都可审计
- `globalConstraints` / `projectKnowledge` 继续表示业务范围
</success_criteria>

<output>
After completion, create `.planning/phases/10-回答与引用/10-02-SUMMARY.md`
</output>
