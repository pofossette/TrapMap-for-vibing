---
phase: 10-回答与引用
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/indexing/adapters/index.ts
  - packages/server/src/lib/indexing/adapters/vector.ts
  - packages/server/src/lib/indexing/adapters/keyword.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/retrieval/recall/graph-assisted.test.ts
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.test.ts
autonomous: true
requirements:
  - CITE-06
  - SUMM-06
  - BOUND-01
  - BOUND-05
must_haves:
  truths:
    - "server typecheck baseline is trustworthy before Phase 10 additions land"
    - "retrieval contracts define structured citation and canonical optional summary fields as the only public schema source"
    - "summary remains opt-in at request level instead of changing default retrieval behavior"
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "citation/summary request and response schemas"
    - path: "packages/contracts/src/index.test.ts"
      provides: "contract regression coverage for retrieval schema changes"
    - path: "packages/server/src/lib/indexing/adapters/index.ts"
      provides: "green typecheck baseline for server package"
  key_links:
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "retrievalQuerySchema / retrievalResponseSchema"
      pattern: "retrieval(Response|Query)Schema"
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/cli/src/commands/retrieval.ts"
      via: "shared RetrievalResponse / RetrievalQuery types"
      pattern: "Retrieval(Response|Query)"
---

<objective>
先吸收当前 server `tsc --noEmit` 红基线，并把 Phase 10 需要的 citation/summary 契约固定到 shared contracts。

Purpose: 让后续 server output-stage 改动建立在可信基线和唯一契约真源之上，避免边做边背既有 typecheck 噪音。
Output: 绿色的 server typecheck baseline、更新后的 retrieval 契约、contract regression tests。
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-回答与引用/10-RESEARCH.md
@.planning/phases/06-检索架构重构/06-03-SUMMARY.md
@.planning/phases/09-图辅助检索/09-04-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/indexing/adapters/index.ts
@packages/server/src/lib/indexing/adapters/vector.ts
@packages/server/src/lib/indexing/adapters/keyword.ts
@packages/server/src/lib/retrieval.test.ts
@packages/server/src/lib/retrieval/recall/graph-assisted.test.ts

<interfaces>
From packages/contracts/src/domain/retrieval.ts:
```typescript
export const retrievalQuerySchema = z.object({
  seed: z.string().min(1).max(2000),
  filters: retrievalFiltersSchema.default({ labels: [], scopes: [] }),
  maxResults: z.number().int().min(1).max(50).default(10),
  includeRefinement: z.boolean().default(true),
  mode: retrievalQueryModeSchema.default('semantic'),
});

export const retrievalResponseSchema = z.object({
  globalConstraints: z.array(retrievalMatchSchema),
  projectKnowledge: z.array(retrievalMatchSchema),
  refinementSummary: z.string().nullable(),
});
```

From packages/server/src/lib/indexing/adapters/index.ts:
```typescript
export {
  vectorIndexAdapter,
  upsertVectorIndex,
  removeVectorIndex,
  getVectorPayload,
  type VectorIndexPayload,
} from './vector.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复 Phase 10 之前的 server typecheck 红基线</name>
  <files>packages/server/src/lib/indexing/adapters/index.ts, packages/server/src/lib/indexing/adapters/vector.ts, packages/server/src/lib/indexing/adapters/keyword.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/retrieval/recall/graph-assisted.test.ts</files>
  <behavior>
    - Test 1: `pnpm --filter @skill-shareer/server exec tsc --noEmit` 不再因为 adapter export、`indexState.graph` 缺失、retrieval query typing 或 graph-assisted test typing 失败。
    - Test 2: 修复不改变既有 retrieval mode / graph-assisted 行为，原有 focused tests 仍通过。
  </behavior>
  <action>先处理研究文档标出的既有红基线，再开始 Phase 10 契约扩展。补齐 `vector.ts` / `keyword.ts` 与 `index.ts` 的导出一致性；构造 `indexState` 时显式保留 graph bucket，避免 Phase 9 建立的 graph 索引契约被回退；修复 retrieval tests 中缺失的 `mode` 字段和 graph-assisted fixture typing/nullability。不要在这一步引入 citation/summary 逻辑，只把 baseline 清到可信状态，为后续 plan 提供可靠 phase gate。该任务落实研究中的 baseline absorption 要求。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server exec tsc --noEmit && pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval/recall/graph-assisted.test.ts</automated>
  </verify>
  <done>server typecheck 绿灯，且已知错误全部被吸收或修复；Phase 7/9 retrieval seams 仍可通过现有回归证明未被破坏。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 shared contracts 中定义 citation 与 optional summary 契约</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: retrieval query schema 暴露显式 summary 开关，默认不生成 summary。
    - Test 2: retrieval response schema 暴露结构化 citation 和 canonical optional summary 字段，且继续保留 `globalConstraints` / `projectKnowledge` 业务分桶。
    - Test 3: contracts package 导出的 retrieval schema/type 可被 server 和 CLI 直接消费，无需读取 server-internal types。
  </behavior>
  <action>按 `BOUND-01` 与 `SUMM-06` 先改 contracts，再让后续 plan 依赖这些 schema。为 response 增加结构化 citation schema（source、snippet、tags、recallChannels、审计分数），并新增 canonical optional `summary` object；请求侧新增 `includeSummary` 开关，默认关闭。`includeRefinement` / `refinementSummary` 仅作为一阶段兼容别名保留，兼容语义必须在 shared contracts 中被清晰编码，但 canonical source of truth 只能是 `includeSummary` / `summary`。不要把 filtering/recall 语义塞进 contracts；只表达输出与请求开关。同步扩展 `packages/contracts/src/index.test.ts` 验证新 schema 默认值、兼容 parse 行为与 canonical 字段优先级。</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test && pnpm --filter @skill-shareer/contracts exec tsc --noEmit</automated>
  </verify>
  <done>contracts package 成为 Phase 10 新字段的唯一真源；后续 server/CLI 计划可以只依赖共享 schema 和 type，不需要再发明本地协议。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts→server/cli | 新增输出字段会被 server 和 CLI 共同消费，契约漂移会直接造成行为分叉 |
| pre-phase baseline→Phase 10 work | 既有红基线会掩盖新增问题，导致验证失真 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `packages/contracts/src/domain/retrieval.ts` | mitigate | 所有新请求/响应字段先在 shared Zod schema 定义，再由 server/CLI 消费，避免本地私有协议 |
| T-10-02 | R | server typecheck baseline | mitigate | 先修复既有 `tsc` 红基线，再把 `tsc --noEmit` 作为后续 phase gate |
| T-10-03 | I | retrieval response contract | mitigate | summary 开关默认关闭，避免默认输出泄露更多内容或改变现有搜索行为 |
</threat_model>

<verification>
先跑 server typecheck 清基线，再跑 contracts package tests/typecheck，确认 Phase 10 的公共 schema 已稳定且可消费。
</verification>

<success_criteria>
- `pnpm --filter @skill-shareer/server exec tsc --noEmit` 成功
- `packages/contracts/src/domain/retrieval.ts` 已定义 citation/summary schema 与请求开关
- `globalConstraints` / `projectKnowledge` 仍是 response 顶层业务分桶
- contracts tests 覆盖默认值与 parse 行为
</success_criteria>

<output>
After completion, create `.planning/phases/10-回答与引用/10-01-SUMMARY.md`
</output>
