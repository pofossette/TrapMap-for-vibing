---
phase: 04-retrieval-and-cli-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - packages/cli/src/commands/retrieval.ts
  - packages/cli/src/commands/retrieval.test.ts
  - packages/cli/src/index.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - RAG-01
must_haves:
  truths:
    - "A logged-in user can run a `search` command from the CLI."
    - "CLI output is human-readable by default and JSON when `--json` is used."
    - "Search command visibility follows the cached permission-aware command surface."
  artifacts:
    - "packages/cli/src/commands/retrieval.ts registers the search command."
    - "packages/cli/src/index.ts exposes `search` only when `knowledge:search` is available."
    - "packages/cli/src/commands/retrieval.test.ts proves default and JSON output behavior."
  key_links:
    - "CLI search command -> apiRequest -> POST /v1/retrieval/search"
    - "cached session permissions -> command registration -> `api:list` output"
---

<objective>
Add the retrieval command to the CLI using the repo’s established Commander, JSON-output, and permission-visibility patterns.

Purpose: Deliver the user-facing terminal workflow for Phase 4’s search capability.
Output: A `search` command, formatter coverage, and command-surface registration tied to `knowledge:search`.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-identity-teams-and-rbac/02-03-SUMMARY.md
@.planning/phases/03-knowledge-intake-and-review/03-02-SUMMARY.md
@.planning/phases/04-retrieval-and-cli-workflow/04-02-PLAN.md
@packages/contracts/src/domain/retrieval.ts
@packages/cli/src/index.ts
@packages/cli/src/commands/knowledge.ts
@packages/cli/src/lib/http.ts
@packages/cli/src/lib/input.ts
@packages/cli/src/lib/output.ts

<interfaces>
From packages/cli/src/lib/output.ts:
```typescript
export function printResult<T>(value: T, options: JsonFlag, formatter: (input: T) => string): void;
```

From packages/cli/src/lib/input.ts:
```typescript
export async function resolveTextInput(
  options: { file?: string; stdin?: boolean; text?: string },
  fieldName: string,
): Promise<string>;
```

From packages/contracts/src/domain/retrieval.ts:
```typescript
export type RetrievalQuery = z.infer<typeof retrievalQuerySchema>;
export type RetrievalResponse = z.infer<typeof retrievalResponseSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the CLI `search` command with shell-friendly input options</name>
  <files>packages/cli/src/commands/retrieval.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <read_first>packages/cli/src/commands/knowledge.ts, packages/cli/src/lib/input.ts, packages/cli/src/lib/output.ts, packages/cli/src/lib/http.ts, packages/contracts/src/domain/retrieval.ts</read_first>
  <acceptance_criteria>
    - `packages/cli/src/commands/retrieval.ts` registers `.command('search')`.
    - The command supports a text seed from direct argument or `--stdin`, and supports `--label`, `--scope`, `--max-results`, `--json`, and `--no-refinement`.
    - The command posts to `/v1/retrieval/search` through `apiRequest`.
    - Human-readable output prints separate `Global constraints` and `Project knowledge` sections when those buckets are non-empty.
    - `packages/cli/src/commands/retrieval.test.ts` contains one text-output case and one `--json` case.
  </acceptance_criteria>
  <behavior>
    - Test 1: a direct seed argument produces a formatted search result.
    - Test 2: piped stdin seed content is accepted when no direct seed argument is provided.
    - Test 3: `--json` prints raw contract-shaped retrieval data instead of formatted text.
  </behavior>
  <action>Create `packages/cli/src/commands/retrieval.ts` and mirror the style of `packages/cli/src/commands/knowledge.ts`. Implement `search [seed]` so agents and humans can provide the retrieval seed directly, from `--stdin`, or from a file if you find that helpful to keep parity with the existing input helper. Support repeated `--label` flags with `collectValues`, repeated `--scope` flags if needed for the schema, `--max-results <n>`, `--no-refinement`, and `--json`. Do not add a `--team-id` override in this phase; search must use the caller's already-selected active team so the CLI stays aligned with `retrievalQuerySchema` and the active-team access model. Parse the server response with `retrievalResponseSchema`, format default output into distinct "Global constraints" and "Project knowledge" sections, and include the match `reason` and `score` for each item. Write the tests first in `packages/cli/src/commands/retrieval.test.ts` and keep them isolated from live HTTP by mocking `apiRequest` or the formatter boundary.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>The CLI has a working search command that accepts shell-friendly input and prints either formatted text or JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire search into the permission-aware command surface</name>
  <files>packages/cli/src/index.ts, packages/cli/src/commands/retrieval.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <read_first>packages/cli/src/index.ts, packages/cli/src/commands/review.ts, packages/cli/src/commands/knowledge.ts, packages/server/src/lib/rbac.ts</read_first>
  <acceptance_criteria>
    - `packages/cli/src/index.ts` computes `allowKnowledgeSearch` from `knowledge:search`.
    - `packages/cli/src/index.ts` lists `search` in `api:list` only when `allowKnowledgeSearch` is true.
    - `packages/cli/src/index.ts` imports and calls `registerRetrievalCommands(program, { allowSearch: ... })`.
    - `pnpm --filter @skill-shareer/cli typecheck` succeeds after the new command is registered.
  </acceptance_criteria>
  <behavior>
    - Test 1: a session with `knowledge:search` exposes the `search` command.
    - Test 2: a session without `knowledge:search` does not register `search`.
  </behavior>
  <action>Update `packages/cli/src/index.ts` to add a dedicated `allowKnowledgeSearch` visibility check using the cached session’s effective permissions, then wire in `registerRetrievalCommands`. Keep the existing visibility model intact: do not fetch the server on startup and do not register unauthorized commands. Extend `api:list` so `search` appears next to the existing retrieval-related workflow surfaces when the cached session permits it. Reuse or extend the retrieval command test file to cover visibility if that is the least noisy option.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts && pnpm --filter @skill-shareer/cli typecheck</automated>
  </verify>
  <done>The CLI exposes `search` exactly when the cached session grants `knowledge:search`, and the command remains aligned with the existing JSON/text output conventions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal input -> CLI search command | Arbitrary user or agent input becomes API request payload. |
| cached CLI session -> command registration | Local session state controls what commands appear without a fresh server round trip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | S | `packages/cli/src/index.ts` | mitigate | Base visibility on the cached authenticated session only; do not expose privileged commands without the stored permission. |
| T-04-08 | T | `packages/cli/src/commands/retrieval.ts` | mitigate | Parse responses with shared retrieval schemas and send only supported flags/values to the server. |
| T-04-09 | I | default search formatter | mitigate | Present only the server-returned buckets and fields; do not synthesize hidden metadata in CLI output. |
</threat_model>

<verification>
Run the focused CLI tests and CLI typecheck. Confirm `api:list` includes `search` only when `knowledge:search` is present in the cached session.
</verification>

<success_criteria>
A logged-in user can run `skill-shareer search ...` and receive either human-readable grouped results or raw JSON, and unauthorized sessions do not see the command.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-and-cli-workflow/04-03-SUMMARY.md`
</output>
