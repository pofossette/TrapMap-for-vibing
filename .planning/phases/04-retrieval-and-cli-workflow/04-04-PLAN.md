---
phase: 04-retrieval-and-cli-workflow
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - packages/cli/src/commands/knowledge.ts
  - packages/cli/src/commands/retrieval.ts
  - packages/cli/src/workflows/retrieval-workflow.test.ts
  - packages/server/src/lib/retrieval.test.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-03
  - CLI-04
must_haves:
  truths:
    - "Agents can submit knowledge through shell-friendly flags/stdin and later retrieve it from the CLI."
    - "Users can still inspect their submission and rejection history after the retrieval workflow is added."
    - "The end-to-end retrieval workflow is repeatable in local and CI environments without live model credentials."
  artifacts:
    - "packages/cli/src/workflows/retrieval-workflow.test.ts exercises login, team selection, submit, reject, resubmit, approve, search, and review-status."
    - "packages/cli/src/commands/knowledge.ts and packages/cli/src/commands/retrieval.ts stay consistent on JSON/stdin behavior."
  key_links:
    - "submit/resubmit stdin helpers -> stored approved knowledge -> retrieval route -> CLI search formatter"
    - "review-status -> rejected details and history after retrieval changes"
---

<objective>
Lock the Phase 4 CLI workflow together with end-to-end tests so search, stdin-based submission, and review-history inspection work as one terminal-first experience.

Purpose: Finish the phase with executable workflow proof instead of isolated command and route tests.
Output: End-to-end workflow tests plus any consistency fixes required for JSON mode, stdin submission, and review-status inspection.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-and-cli-workflow/04-VALIDATION.md
@.planning/phases/03-knowledge-intake-and-review/03-02-SUMMARY.md
@.planning/phases/03-knowledge-intake-and-review/03-04-SUMMARY.md
@.planning/phases/04-retrieval-and-cli-workflow/04-03-PLAN.md
@packages/cli/src/commands/knowledge.ts
@packages/cli/src/commands/retrieval.ts
@packages/cli/src/lib/input.ts
@packages/cli/src/lib/output.ts
@packages/server/src/app.ts
@packages/server/src/routes/knowledge.ts
@packages/server/src/routes/retrieval.ts

<interfaces>
Workflow surfaces that must interoperate:
```text
skill-shareer login
skill-shareer team select
skill-shareer submit --scope --label --shortcut [--detail|--file|--stdin] [--json]
skill-shareer resubmit <entryId> [--shortcut|--detail|--file|--stdin|--label] [--json]
skill-shareer review-status [entryId] [--json]
skill-shareer search [seed] [--stdin] [--json]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end retrieval workflow coverage from submission through search</name>
  <files>packages/cli/src/workflows/retrieval-workflow.test.ts, packages/server/src/lib/retrieval.test.ts</files>
  <read_first>packages/cli/src/commands/knowledge.ts, packages/cli/src/commands/retrieval.ts, packages/server/src/app.ts, packages/server/src/routes/knowledge.ts, packages/server/src/routes/retrieval.ts, packages/server/src/lib/store.ts</read_first>
  <acceptance_criteria>
    - `packages/cli/src/workflows/retrieval-workflow.test.ts` creates a temporary data file and does not depend on a pre-existing `.data/skill-shareer.json`.
    - The workflow test covers login, team select, submit via stdin or file-backed text input, reviewer rejection, `resubmit`, reviewer approval, CLI search, and `review-status`.
    - The workflow test asserts the approved knowledge shortcut appears in CLI search output after approval and does not appear before approval.
    - The workflow test asserts `review-status` exposes the rejection details before `resubmit` and the retained lifecycle history after the corrected submission is approved.
  </acceptance_criteria>
  <behavior>
    - Test 1: submitted but unapproved knowledge is absent from retrieval results.
    - Test 2: after reviewer rejection, the submitter can correct the entry with `resubmit` and preserve linkage to the prior attempt.
    - Test 3: after final approval, the corrected entry becomes searchable through the CLI.
    - Test 4: the submitter can inspect lifecycle and reviewer feedback through `review-status` across rejection and approval.
  </behavior>
  <action>Create an end-to-end Vitest workflow test at `packages/cli/src/workflows/retrieval-workflow.test.ts` that boots the Fastify server against a temporary JSON data file, drives the CLI-facing command modules or executable entrypoint through login/team-select/submit/reject/resubmit/approve/search/review-status, and proves both the approval gate and the corrected-entry workflow established in Phase 3 still hold after retrieval is added. Keep the environment credential-free: set `includeRefinement` or provider config so the workflow passes without external APIs. The test must assert that the initial rejected submission is not searchable, that `resubmit` preserves lifecycle linkage and reviewer feedback visibility, and that only the approved corrected entry appears in CLI search output. If the cleanest assertion path is to reuse a focused retrieval service fixture from `packages/server/src/lib/retrieval.test.ts`, do so rather than duplicating store setup logic.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/workflows/retrieval-workflow.test.ts</automated>
  </verify>
  <done>The repo has an automated workflow proving a knowledge item moves from stdin-based submission to approved searchable result while `review-status` continues to expose history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize JSON and stdin behavior across the retrieval workflow commands</name>
  <files>packages/cli/src/commands/knowledge.ts, packages/cli/src/commands/retrieval.ts, packages/cli/src/workflows/retrieval-workflow.test.ts</files>
  <read_first>packages/cli/src/commands/knowledge.ts, packages/cli/src/commands/retrieval.ts, packages/cli/src/lib/input.ts, packages/cli/src/lib/output.ts</read_first>
  <acceptance_criteria>
    - `packages/cli/src/commands/knowledge.ts` still routes submission/resubmission detail input through `resolveTextInput`.
    - `packages/cli/src/commands/retrieval.ts` routes search seed input through the same helper pattern or an equivalent single-source helper.
    - Both command files call `printResult` for `--json` instead of bespoke JSON branches.
    - The workflow test contains assertions for at least one JSON-mode command and one stdin-driven command.
    - `pnpm --filter @skill-shareer/cli typecheck` succeeds after the workflow consistency fixes.
  </acceptance_criteria>
  <behavior>
    - Test 1: a piped submission succeeds without `--detail`.
    - Test 2: a piped `resubmit` succeeds without introducing a second stdin helper path.
    - Test 3: a JSON-mode search or review-status call returns parseable structured output.
  </behavior>
  <action>Use the workflow test as the red-green driver to normalize any inconsistencies between existing submission/status commands and the new search command. Keep the existing `submit`, `resubmit`, and `review-status` command names and semantics intact per the roadmap, and explicitly verify that `resubmit` still participates in the same shell-friendly stdin and JSON conventions after the new retrieval command lands. Do not introduce a second formatting helper or alternate stdin path; the goal is one repeatable shell-friendly pattern across submit, resubmit, search, and review-status.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/workflows/retrieval-workflow.test.ts && pnpm --filter @skill-shareer/cli typecheck</automated>
  </verify>
  <done>Submit, resubmit, search, and history inspection all follow the same JSON/stdin conventions and are covered by a repeatable automated workflow test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin/file input -> CLI workflow | Shell-provided content becomes submission detail and retrieval seed material. |
| approved knowledge lifecycle -> retrieval visibility | Review-state transitions determine whether search exposes an entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | T | CLI workflow commands | mitigate | Reuse one input helper and one output helper so stdin/JSON handling stays consistent and testable across commands. |
| T-04-11 | I | end-to-end workflow | mitigate | Assert in tests that unapproved knowledge is absent from search before approval and present only after reviewer approval. |
| T-04-12 | D | CI workflow execution | accept | Keep the workflow credential-free and file-backed so it remains deterministic and cheap enough for regular test runs. |
</threat_model>

<verification>
Run the end-to-end workflow test and CLI typecheck. Confirm the workflow proves approval gating, stdin submission, JSON output, and review-history inspection.
</verification>

<success_criteria>
The CLI supports a full terminal workflow from solved-problem submission to approved retrieval, with repeatable automated proof that history inspection and shell-friendly interfaces still work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-and-cli-workflow/04-04-SUMMARY.md`
</output>
