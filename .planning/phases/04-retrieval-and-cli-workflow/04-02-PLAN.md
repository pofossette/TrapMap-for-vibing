---
phase: 04-retrieval-and-cli-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/server/src/lib/retrieval.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/routes/retrieval.ts
autonomous: true
requirements:
  - RAG-04
  - RAG-05
must_haves:
  truths:
    - "Relevant global constraints are returned separately from project knowledge."
    - "Search may add refinement when configured, but search still succeeds without model credentials."
    - "Every returned match explains why it was surfaced."
  artifacts:
    - "packages/server/src/lib/retrieval.ts shapes results into the contract buckets."
    - "packages/server/src/lib/retrieval.test.ts proves bucket separation and best-effort refinement behavior."
  key_links:
    - "ranked matches -> globalConstraints/projectKnowledge buckets"
    - "includeRefinement flag -> best-effort summary generation -> `refinementSummary`"
---

<objective>
Finish the server-side retrieval behavior by separating global constraints from project knowledge and adding optional refinement without making credentials mandatory.

Purpose: Deliver the Phase 4 response shape that the CLI can present directly to humans and agents.
Output: A shaped retrieval response with reasons, optional refinement, and tests for bucketed output.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/skill-shareer/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retrieval-and-cli-workflow/04-RESEARCH.md
@.planning/phases/04-retrieval-and-cli-workflow/04-01-PLAN.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval.ts
@packages/server/src/routes/retrieval.ts
@packages/server/src/lib/pre-review.ts

<interfaces>
From packages/contracts/src/domain/retrieval.ts:
```typescript
export const retrievalMatchSchema = z.object({
  entryId: entityIdSchema,
  scope: scopeSchema,
  requiredLevel: securityLevelSchema,
  shortcut: z.string(),
  detail: z.string(),
  labels: z.array(labelSchema),
  score: z.number().min(0).max(1),
  reason: z.string().min(1),
});
```

From packages/server/src/lib/pre-review.ts:
```typescript
// Existing LangChain pattern to mirror: Runnable-based, provider-friendly, deterministic fallback acceptable.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shape ranked matches into global and project buckets</name>
  <files>packages/server/src/lib/retrieval.ts, packages/server/src/lib/retrieval.test.ts</files>
  <read_first>packages/server/src/lib/retrieval.ts, packages/contracts/src/domain/retrieval.ts, packages/contracts/src/domain/knowledge.ts, packages/server/src/lib/pre-review.ts</read_first>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval.ts` contains explicit bucketing into `globalConstraints` and `projectKnowledge`.
    - Match reasons include concrete metadata such as matching labels, scope, or text overlap rather than an empty placeholder string.
    - `packages/server/src/lib/retrieval.test.ts` contains a case where a relevant `global` entry is returned in `globalConstraints` and a relevant `project` entry is returned in `projectKnowledge`.
    - `packages/server/src/lib/retrieval.test.ts` contains a case proving the same entry ID does not appear in both buckets.
  </acceptance_criteria>
  <behavior>
    - Test 1: relevant global constraints are separated from project knowledge in the final response.
    - Test 2: each returned item includes a non-empty reason explaining the match.
    - Test 3: bucket shaping preserves ranking order within each bucket.
  </behavior>
  <action>Modify `packages/server/src/lib/retrieval.ts` from Plan 04-01 so the final ranked hits are shaped into the existing `retrievalResponseSchema` buckets using the persisted `scope` and `metadata.scopeLabel` semantics from approved knowledge entries. Preserve per-item `score` and populate `reason` with deterministic explanations such as matched labels, shared scope, or seed-term overlap. Keep the route contract unchanged; this plan is about shaping the already-authorized results, not changing auth. Add or extend tests in `packages/server/src/lib/retrieval.test.ts` before implementation so the bucketed response is pinned down.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts</automated>
  </verify>
  <done>Search results are separated into the two required buckets with stable ordering and non-empty reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add best-effort refinement without making the route credential-dependent</name>
  <files>packages/server/src/lib/retrieval.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.ts</files>
  <read_first>packages/server/src/lib/retrieval.ts, packages/server/src/routes/retrieval.ts, packages/server/src/config.ts, packages/server/package.json, .planning/phases/04-retrieval-and-cli-workflow/04-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval.ts` checks `includeRefinement` before attempting summary generation.
    - `packages/server/src/lib/retrieval.ts` has a no-provider path that returns `refinementSummary: null` instead of throwing.
    - If a provider-backed refinement helper is added, the implementation is isolated behind one function in `packages/server/src/lib/retrieval.ts`.
    - `packages/server/src/lib/retrieval.test.ts` contains a case proving search still succeeds when refinement is requested but provider config is absent.
    - `pnpm --filter @skill-shareer/server typecheck` succeeds after the refinement branch is added.
  </acceptance_criteria>
  <behavior>
    - Test 1: `includeRefinement: false` returns `refinementSummary: null`.
    - Test 2: `includeRefinement: true` with no configured provider still returns matches and `refinementSummary: null`.
    - Test 3: when a provider-backed refinement stub is configured in tests, the returned summary is passed through the response contract.
  </behavior>
  <action>Add best-effort refinement to the retrieval library using the same LangChain-friendly design used in pre-review, but do not make live model credentials a requirement. Keep the query contract exactly as-is: `includeRefinement` should mean "attempt refinement if configured." If you add env-driven provider handling, keep it local to the retrieval module or `packages/server/src/config.ts`, document the env names in code comments only where the branch is non-obvious, and ensure the route still returns contract-valid data when no provider is available. Extend route wiring only if needed to pass config cleanly; do not introduce a new route or response shape.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts && pnpm --filter @skill-shareer/server typecheck</automated>
  </verify>
  <done>The retrieval response cleanly separates constraints from project knowledge and optional refinement never breaks search in local or CI environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieval results -> refinement layer | Ranked knowledge enters an optional summarization step that must not alter access control or crash the request path. |
| search response -> CLI/agents | Returned reasons and summaries become user-visible guidance and must remain grounded in the filtered result set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | I | result shaping | mitigate | Bucket only from already-authorized ranked hits; never re-fetch or widen the candidate set during shaping or refinement. |
| T-04-05 | T | refinement branch | mitigate | Keep `includeRefinement` best-effort and return `null` on missing config or provider failure instead of partial malformed output. |
| T-04-06 | R | match reasons / summaries | accept | Deterministic reasons and optional summary are explanatory only; the canonical evidence remains the returned entries themselves. |
</threat_model>

<verification>
Run the retrieval service tests again, focusing on bucket separation, non-empty reasons, and refinement fallback behavior.
</verification>

<success_criteria>
The server returns contract-valid bucketed results, surfaces relevant global constraints distinctly, and never requires LLM credentials just to complete a search.
</success_criteria>

<output>
After completion, create `.planning/phases/04-retrieval-and-cli-workflow/04-02-SUMMARY.md`
</output>
