---
phase: 13-skill-import-export-pipeline
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - packages/server/src/lib/import-export.ts
  - packages/server/src/routes/operations.test.ts
  - packages/cli/src/commands/operations.ts
  - packages/cli/src/commands/operations.test.ts
autonomous: true
requirements:
  - IMEX-03
  - COMP-01
  - COMP-02
  - COMP-04
must_haves:
  truths:
    - "A lone `SKILL.md` input is auto-wrapped into the same canonical artifact import pipeline rather than routed through a separate legacy submission path."
    - "The compatibility path is intentionally minimal: it creates one-file artifacts with `sourceKind: 'single-skill-md'` and does not fabricate missing `references/`, `assets/`, or `scripts/`."
    - "Import governance, audit, and machine-readable CLI behavior remain unchanged from the canonical directory flow."
  artifacts:
    - path: "packages/cli/src/commands/operations.ts"
      provides: "single-file detection and minimal artifact wrapper reuse"
      contains: "single-skill-md packaging path"
    - path: "packages/server/src/lib/import-export.ts"
      provides: "canonical import normalization that accepts `single-skill-md` payloads without a separate server route"
      contains: "sourceKind-aware import helper"
    - path: "packages/cli/src/commands/operations.test.ts"
      provides: "compatibility import regression coverage"
      contains: "single SKILL.md tests"
  key_links:
    - from: "packages/cli/src/commands/operations.ts"
      to: "packages/server/src/routes/operations.ts"
      via: "same import command and bundle-json route used for both directory and single-file inputs"
      pattern: "sourceKind: 'single-skill-md'"
    - from: "packages/server/src/lib/import-export.ts"
      to: "packages/server/src/lib/artifacts/model.ts"
      via: "reuse of the canonical artifact creation input without knowledge-entry fallback"
      pattern: "createSkillArtifactRecord"
---

<objective>
Add the bounded compatibility path for a single `SKILL.md` import by auto-wrapping it into the canonical artifact-native flow created in Plan 13-01.

Purpose: satisfy IMEX-03 without reopening the legacy knowledge-entry importer or pretending that a lone file contains full directory structure.
Output: minimal single-file wrapper logic on the CLI and server normalization seam plus focused route/CLI regressions proving the same governed import pipeline is reused.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-skill-import-export-pipeline/13-RESEARCH.md
@.planning/phases/13-skill-import-export-pipeline/13-VALIDATION.md
@.planning/phases/13-skill-import-export-pipeline/13-01-PLAN.md
@.planning/phases/12-skill-artifact-canonical-model/12-03-SUMMARY.md
@packages/contracts/src/domain/artifacts.ts
@packages/contracts/src/domain/operations.ts
@packages/server/src/lib/import-export.ts
@packages/server/src/routes/operations.test.ts
@packages/cli/src/commands/operations.ts

<interfaces>
From `packages/contracts/src/domain/artifacts.ts`:
```typescript
export const skillArtifactMetadataSchema = z.object({
  sourceKind: z.enum(['skill-directory', 'single-skill-md', 'legacy-knowledge']),
  ...
});
```

From Plan `13-01` expected seam:
```typescript
operations import accepts either a directory path or a direct `SKILL.md` path and emits one canonical artifact bundle payload.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse the canonical import normalization seam for `single-skill-md` artifacts</name>
  <files>packages/server/src/lib/import-export.ts, packages/server/src/routes/operations.test.ts</files>
  <behavior>
    - Test 1: a payload marked `sourceKind: 'single-skill-md'` imports successfully through the same route and creates a minimal artifact revision with only `SKILL.md`.
    - Test 2: the resulting derived outputs and client manifest remain bounded to what one-file input can actually supply.
    - Test 3: requested-level checks, pre-review, and audit events remain identical to directory import behavior.
  </behavior>
  <action>Update the canonical import helpers from Plan 13-01 so `single-skill-md` is treated as a first-class `sourceKind`, not as a second route or knowledge fallback. The server-side normalization should require exactly one `SKILL.md` file for this mode, reuse the same create/persist/derive path, and reject payloads that try to masquerade as single-file while carrying extra directories. Extend `packages/server/src/routes/operations.test.ts` with route coverage that proves the import path stays governed and minimal.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts</automated>
  </verify>
  <done>The server compatibility path is only a bounded normalization mode on top of the canonical importer, with no parallel legacy logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detect lone `SKILL.md` inputs in the CLI and auto-wrap them into minimal artifact bundles</name>
  <files>packages/cli/src/commands/operations.ts, packages/cli/src/commands/operations.test.ts</files>
  <behavior>
    - Test 1: `operations import --file path/to/SKILL.md` produces a one-file canonical bundle with `sourceKind: 'single-skill-md'`.
    - Test 2: the CLI does not fabricate `references/`, `assets/`, or `scripts/`, and the relative path remains exactly `SKILL.md`.
    - Test 3: text output and `--json` output stay stable while returning artifact-native success metadata.
  </behavior>
  <action>Extend the Plan 13-01 CLI import logic so the same command detects a direct `SKILL.md` path, packages only that file as a minimal canonical artifact bundle, and posts it to the existing bundle-json route. Keep the command surface additive: no new compatibility command, no hidden filesystem assumptions, and no local review/audit logic. Add focused CLI tests for one-file detection, payload shape, and negative cases where a non-`SKILL.md` file should not enter the compatibility wrapper.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts</automated>
  </verify>
  <done>A lone `SKILL.md` can be imported through the same command and transport as directory artifacts, and the compatibility boundary remains explicit and minimal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| single local file -> CLI wrapper | the CLI must verify this is truly a `SKILL.md` compatibility input |
| compatibility payload -> canonical importer | the server must prevent single-file mode from smuggling extra content or bypassing governance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing | CLI compatibility detection | mitigate | require basename `SKILL.md` and canonical relative path `SKILL.md` before auto-wrap proceeds |
| T-13-06 | Tampering | compatibility payload shape | mitigate | enforce exactly one file for `single-skill-md`, reject extra directories, and reuse the same shared schema validation as directory import |
| T-13-07 | Elevation of Privilege | compatibility import route path | mitigate | preserve Plan 13-01 route-level permission, ownership, level, pre-review, and audit checks unchanged |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts`
</verification>

<success_criteria>
- Single-file compatibility import exists, but it is only an auto-wrap into the canonical artifact flow
- Minimal artifacts created from `SKILL.md` do not invent missing directories or server-side behaviors
- CLI and server tests prove the compatibility path stays governed and bounded
</success_criteria>

<output>
After completion, create `.planning/phases/13-skill-import-export-pipeline/13-02-SUMMARY.md`
</output>
