---
phase: 59-ownership-verification-sla-management
plan: 03
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - packages/cli/src/commands/maintenance.ts
  - packages/cli/src/index.ts
  - packages/server/src/lib/maintenance/model.test.ts
  - packages/server/src/lib/maintenance/batch.test.ts
  - packages/server/src/routes/maintenance.test.ts
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02

must_haves:
  truths:
    - "CLI can list entries missing owner, overdue review, or stale verification"
    - "CLI can assign owner to entries"
    - "CLI can mark entries as re-verified with optional review extension"
    - "Batch operations support dry-run mode for preview"
    - "Server model helpers correctly identify overdue and stale entries"
  artifacts:
    - path: "packages/cli/src/commands/maintenance.ts"
      provides: "maintenance-list, maintenance-assign, maintenance-verify CLI commands"
      exports: ["registerMaintenanceCommands"]
    - path: "packages/cli/src/index.ts"
      provides: "Command registration"
      contains: "registerMaintenanceCommands"
    - path: "packages/server/src/lib/maintenance/model.test.ts"
      provides: "Unit tests for maintenance model helpers"
      contains: "describe.*maintenance.*model"
    - path: "packages/server/src/lib/maintenance/batch.test.ts"
      provides: "Unit tests for batch operations"
      contains: "describe.*maintenance.*batch"
    - path: "packages/server/src/routes/maintenance.test.ts"
      provides: "Route integration tests"
      contains: "describe.*maintenance.*route"
  key_links:
    - from: "packages/cli/src/commands/maintenance.ts"
      to: "/v1/operations/maintenance/entries"
      via: "GET API call"
      pattern: "apiRequest.*maintenance/entries"
    - from: "packages/cli/src/commands/maintenance.ts"
      to: "/v1/operations/maintenance/batch"
      via: "POST API call"
      pattern: "apiRequest.*maintenance/batch"
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/commands/maintenance.ts"
      via: "import and register"
      pattern: "registerMaintenanceCommands"
---

<objective>
Create CLI commands for maintenance operations and comprehensive test coverage for the server maintenance module.

Purpose: Deliver the complete MAINT-02 user-facing interface and verify all server logic works correctly.
Output: Three CLI commands (list, assign, verify), unit tests for model/batch, route integration tests.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-ownership-verification-sla-management/59-RESEARCH.md
@.planning/phases/59-ownership-verification-sla-management/59-PATTERNS.md
@.planning/phases/59-ownership-verification-sla-management/59-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 - server module contracts -->

From packages/contracts/src/domain/maintenance.ts:
```typescript
export const maintenanceEntryListResponseSchema: z.ZodObject<{
  items: z.ZodArray<maintenanceAwareListItemSchema>;
  total: z.ZodNumber;
}>;
export const maintenanceBatchOperationResponseSchema: z.ZodObject<{
  action: maintenanceActionSchema;
  dryRun: z.ZodBoolean;
  items: z.ZodArray<maintenanceBatchOperationItemSchema>;
  totalEligible: z.ZodNumber;
  totalIneligible: z.ZodNumber;
  appliedAt: z.ZodNullable<z.ZodIsoDatetime>;
}>;
```

From packages/server/src/lib/maintenance/model.ts:
```typescript
export function validateMaintenanceMeta(meta: unknown): MaintenanceMeta;
export function computeDefaultReviewBy(days: number): string;
export function isReviewOverdue(reviewBy: string | null, now: Date): boolean;
export function isStaleVerification(lastVerifiedAt: string | null, staleDays: number, now: Date): boolean;
```

From packages/server/src/lib/maintenance/batch.ts:
```typescript
export function planMaintenanceOperation(data, input, now): MaintenanceOperationPlanItem[];
export function executeMaintenanceOperation(store, data, input, now): KnowledgeRecord[];
```

From packages/cli/src/commands/decay.ts (pattern reference):
```typescript
export function registerDecayCommands(program: Command, options: DecayCommandOptions): void;
// Uses: loadCliState(), requireSessionToken(state), apiRequest<T>(state, {path, method, body}), printResult(data, flags, formatter)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI maintenance commands and register</name>
  <files>packages/cli/src/commands/maintenance.ts, packages/cli/src/index.ts</files>
  <read_first>
    packages/cli/src/commands/decay.ts
    packages/cli/src/index.ts
    packages/cli/src/lib/config.ts
    packages/cli/src/lib/http.ts
    packages/cli/src/lib/output.ts
    packages/contracts/src/domain/maintenance.ts
  </read_first>
  <action>
1. Create `packages/cli/src/commands/maintenance.ts` following decay.ts pattern:
   - Import response schemas from `@trapmap/contracts`: `maintenanceEntryListResponseSchema`, `maintenanceBatchOperationResponseSchema`
   - Import `Command` from `commander`
   - Import `loadCliState`, `requireSessionToken`, `apiRequest`, `printResult` from their modules
   - Define `MaintenanceCommandOptions` interface with `allowManage: boolean`

   Define `formatMaintenanceList` function:
   - If no items: return 'No entries found'
   - For each item show: `{id}  [maintainer-or-unassigned]  [reviewBy-or-none]  {shortcut}`
   - Use `item.maintainer?.handle ?? 'unassigned'` for maintainer display
   - Use `item.reviewBy ?? 'none'` for review date

   Define `formatMaintenanceBatch` function (same pattern as formatBatchResult from decay.ts):
   - Show dry-run prefix, action, eligible/ineligible counts
   - Per item: status marker, entryId, proposedChange, ineligibilityReason

   Export `registerMaintenanceCommands(program: Command, options: MaintenanceCommandOptions): void`:
   - If `!options.allowManage` return early

   **maintenance-list command:**
   - Description: 'List entries needing maintenance attention'
   - Options: `--missing-owner` (boolean), `--overdue` (boolean), `--stale` (boolean), `--stale-days <n>` (int, default 180), `--scope <scope>`, `--label <labels>` (comma-separated), `--limit <n>` (default '25'), `--json`
   - Action: loadCliState, requireSessionToken, build query params, GET `/v1/operations/maintenance/entries?{params}`, parse response, printResult

   **maintenance-assign command:**
   - Description: 'Assign maintainer to entries'
   - Required options: `--entries <ids>` (comma-separated), `--owner <userId>`
   - Optional: `--owner-handle <handle>`, `--dry-run`, `--json`
   - Action: loadCliState, requireSessionToken, build body with `action: 'assign-owner'`, POST to `/v1/operations/maintenance/batch`, parse, printResult

   **maintenance-verify command:**
   - Description: 'Mark entries as re-verified'
   - Required options: `--entries <ids>` (comma-separated)
   - Optional: `--extend-days <n>` (int, default 90), `--dry-run`, `--json`
   - Action: loadCliState, requireSessionToken, build body with `action: 'mark-verified'`, POST to `/v1/operations/maintenance/batch`, parse, printResult

2. Modify `packages/cli/src/index.ts`:
   - Add `import { registerMaintenanceCommands } from './commands/maintenance.js';` after the feedback-admin import (line 8)
   - Add `registerMaintenanceCommands(program, { allowManage: visibility.allowKnowledgeUpdate });` after `registerDecayCommands` (line 152)
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm --filter @trapmap/cli typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - maintenance.ts exports registerMaintenanceCommands with list, assign, verify sub-commands
    - index.ts registers maintenance commands
    - CLI typechecks clean
  </done>
  <acceptance_criteria>
    - `grep "maintenance-list" packages/cli/src/commands/maintenance.ts` returns 1 match
    - `grep "maintenance-assign" packages/cli/src/commands/maintenance.ts` returns 1 match
    - `grep "maintenance-verify" packages/cli/src/commands/maintenance.ts` returns 1 match
    - `grep "registerMaintenanceCommands" packages/cli/src/index.ts` returns 2 matches (import + call)
    - `pnpm --filter @trapmap/cli typecheck` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests for maintenance model, batch, and routes</name>
  <files>packages/server/src/lib/maintenance/model.test.ts, packages/server/src/lib/maintenance/batch.test.ts, packages/server/src/routes/maintenance.test.ts</files>
  <read_first>
    packages/server/src/lib/maintenance/model.ts
    packages/server/src/lib/maintenance/batch.ts
    packages/server/src/routes/maintenance.ts
    packages/server/src/lib/decay/batch.test.ts
    packages/server/src/routes/decay.test.ts
    packages/server/src/lib/store.ts
  </read_first>
  <action>
1. Create `packages/server/src/lib/maintenance/model.test.ts`:
   - Import vitest `describe`, `it`, `expect`
   - Import `isReviewOverdue`, `isStaleVerification`, `computeDefaultReviewBy`, `validateMaintenanceMeta` from `./model.js`
   - Test `isReviewOverdue`:
     - Returns false for null reviewBy
     - Returns false for future date
     - Returns true for past date
   - Test `isStaleVerification`:
     - Returns true for null lastVerifiedAt (never verified)
     - Returns false for recent lastVerifiedAt within staleDays
     - Returns true for old lastVerifiedAt exceeding staleDays
   - Test `computeDefaultReviewBy`:
     - Returns valid ISO timestamp
     - Date is approximately N days in the future
   - Test `validateMaintenanceMeta`:
     - Accepts valid meta with maintainer and reviewBy
     - Accepts null fields
     - Throws on invalid data

2. Create `packages/server/src/lib/maintenance/batch.test.ts` following decay/batch.test.ts pattern:
   - Create `makeTestEntry` helper that builds a KnowledgeRecord with default values, optionally accepting overrides for maintenanceMeta, decayMeta, lifecycleState
   - Create `makeTestData` helper that wraps entries in StoreData structure
   - Import `planMaintenanceOperation`, `executeMaintenanceOperation`, `MaintenanceOperationInput`
   - Test `planMaintenanceOperation`:
     - Returns ineligible for non-existent entry
     - Returns ineligible for non-approved entry
     - Returns eligible for assign-owner with newMaintainerId
     - Returns eligible for extend-review
     - Returns eligible for mark-verified
   - Test `executeMaintenanceOperation` for assign-owner:
     - Sets maintenanceMeta.maintainerUserId on target entry
     - Preserves existing reviewBy when assigning owner
     - Does not modify non-target entries
   - Test `executeMaintenanceOperation` for extend-review:
     - Sets maintenanceMeta.reviewBy to future date
     - Preserves existing maintainer when extending review
   - Test `executeMaintenanceOperation` for mark-verified:
     - Sets maintenanceMeta.reviewBy to future date
     - Updates decayMeta.lastVerifiedAt to now
     - Initializes decayMeta if null (using 'active' state, 'evergreen' freshness)

3. Create `packages/server/src/routes/maintenance.test.ts` following decay.test.ts pattern:
   - Import `buildServer` from `../app.js`
   - Import `FastifyInstance`
   - Create helper to set up test server with authenticated user
   - Create helper to create test knowledge entries with maintenance metadata
   - Test GET /v1/operations/maintenance/entries:
     - Returns empty list when no entries match filters
     - Returns entries with missing owner when `missingOwner=true`
     - Returns entries with overdue review when `reviewOverdue=true`
     - Returns entries with stale verification when `staleVerification=true`
     - Filters by scope when provided
     - Filters by labels when provided
     - Respects limit parameter
   - Test POST /v1/operations/maintenance/batch:
     - Assigns owner with `assign-owner` action
     - Extends review date with `extend-review` action
     - Marks verified with `mark-verified` action
     - Dry-run mode returns plan without modifying data
     - Returns 401 without auth
   - Use `afterEach` to close server instance
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm --filter @trapmap/server test -- --run packages/server/src/lib/maintenance/model.test.ts packages/server/src/lib/maintenance/batch.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - model.test.ts covers isReviewOverdue, isStaleVerification, computeDefaultReviewBy, validateMaintenanceMeta
    - batch.test.ts covers plan and execute for all three actions with edge cases
    - routes/maintenance.test.ts covers GET entries filtering and POST batch operations
    - All tests pass
  </done>
  <acceptance_criteria>
    - `grep -c "describe\|it(" packages/server/src/lib/maintenance/model.test.ts` returns >= 5
    - `grep -c "describe\|it(" packages/server/src/lib/maintenance/batch.test.ts` returns >= 8
    - `grep -c "describe\|it(" packages/server/src/routes/maintenance.test.ts` returns >= 6
    - `grep "assign-owner" packages/server/src/lib/maintenance/batch.test.ts` returns at least 2 matches
    - `grep "mark-verified" packages/server/src/lib/maintenance/batch.test.ts` returns at least 2 matches
    - `pnpm --filter @trapmap/server test -- --run packages/server/src/lib/maintenance/model.test.ts packages/server/src/lib/maintenance/batch.test.ts` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> API | CLI sends user-provided IDs and parameters to API |
| Test code -> server | Test code exercises routes directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-07 | T | CLI command options | mitigate | CLI passes values through Zod-validated API schemas |
| T-59-08 | I | Test fixtures | accept | Test data is non-sensitive synthetic data |
</threat_model>

<verification>
- `pnpm --filter @trapmap/server test -- --run packages/server/src/lib/maintenance/` passes
- `pnpm --filter @trapmap/server typecheck` succeeds
- `pnpm --filter @trapmap/cli typecheck` succeeds
</verification>

<success_criteria>
- Three CLI commands registered: maintenance-list, maintenance-assign, maintenance-verify
- Model tests pass: validation, overdue/stale checks, review date computation
- Batch tests pass: plan and execute for all three actions including mark-verified dual-update
- Route tests pass: GET filtering and POST batch with auth checks
- All typechecks clean
</success_criteria>

<output>
After completion, create `.planning/phases/59-ownership-verification-sla-management/59-03-SUMMARY.md`
</output>
