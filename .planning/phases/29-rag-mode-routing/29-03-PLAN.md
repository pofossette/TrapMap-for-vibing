---
phase: 29-rag-mode-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 29-01
  - 29-02
files_modified:
  - evals/retrieval/lib/types.ts
  - evals/retrieval/lib/report.ts
  - evals/retrieval/run.ts
  - evals/retrieval/**/*.test.ts
  - evals/retrieval/README.md
  - reports/.gitkeep
autonomous: true
requirements:
  - EOPS-03
must_haves:
  truths:
    - "Evaluation outputs can compare retrieval behavior against stable internal mode IDs rather than ad-hoc labels."
    - "Baseline artifacts capture routing reason and fallback data needed to interpret regressions."
    - "Failure policy distinguishes hard governance failures from allowed ranking drift by mode slice."
  artifacts:
    - path: "evals/retrieval/lib/types.ts"
      provides: "baseline-aware eval result types"
      contains: "mode IDs, routing traces, and failure policy fields"
    - path: "evals/retrieval/lib/report.ts"
      provides: "report/baseline serialization"
      contains: "stable per-mode slices and regression status fields"
    - path: "evals/retrieval/run.ts"
      provides: "maintainer-facing baseline flow"
      contains: "baseline write/compare behavior and explicit exit semantics"
    - path: "evals/retrieval/README.md"
      provides: "operator guidance"
      contains: "baseline artifact location and failure-policy meaning"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "evals/retrieval/lib/types.ts"
      via: "routing trace ingestion"
      pattern: "selectedMode|routingReason|fallbackApplied"
    - from: "evals/retrieval/lib/types.ts"
      to: "evals/retrieval/lib/report.ts"
      via: "stable baseline and regression fields"
      pattern: "baseline|regression|mode"
---

<objective>
Extend retrieval evaluation outputs with baseline and failure-policy support for Phase 29.

Purpose: satisfy EOPS-03 by turning the new routing metadata into stable report slices, baseline artifacts, and explicit regression rules rather than ad-hoc judgment.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/29-rag-mode-routing/29-CONTEXT.md
@.planning/phases/29-rag-mode-routing/29-RESEARCH.md
@.planning/phases/29-rag-mode-routing/29-02-PLAN.md
@.planning/phases/28-ci-integration-and-evaluation-reporting/28-01-SUMMARY.md
@.planning/phases/28-ci-integration-and-evaluation-reporting/28-02-SUMMARY.md
@evals/retrieval/run.ts
@evals/retrieval/lib/types.ts
@evals/retrieval/**/*.test.ts
</context>

<tasks>

<task type="execute">
  <name>Task 29-03-01: Add stable mode-aware baseline fields to retrieval evaluation results and reports</name>
  <files>evals/retrieval/lib/types.ts, evals/retrieval/lib/report.ts, evals/retrieval/**/*.test.ts</files>
  <read_first>
    @evals/retrieval/lib/types.ts
    @evals/retrieval/lib/report.ts
    @evals/retrieval/run.ts
    @.planning/phases/29-rag-mode-routing/29-RESEARCH.md
    @.planning/phases/28-ci-integration-and-evaluation-reporting/28-01-SUMMARY.md
    @.planning/phases/28-ci-integration-and-evaluation-reporting/28-02-SUMMARY.md
  </read_first>
  <action>
    Expand retrieval eval result and report types so every case and aggregate slice can record the stable internal mode ID chosen by the router, the routing reason, whether fallback occurred, and baseline comparison status. Update report serialization to preserve both machine-readable and human-readable output keyed by canonical mode identifiers rather than unstable display labels.
  </action>
  <acceptance_criteria>
    - `evals/retrieval/lib/types.ts` contains `selectedMode`
    - `evals/retrieval/lib/types.ts` contains `fallbackApplied`
    - `evals/retrieval/lib/report.ts` contains baseline or regression status fields
    - eval report tests assert per-mode slices use canonical stable identifiers
    - `pnpm test -- --run evals/retrieval` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --run evals/retrieval</automated>
  </verify>
  <done>
    Retrieval eval result/report layers serialize stable canonical mode IDs with routing trace fields, and tests confirm slice keys remain regression-safe across baseline comparisons.
  </done>
</task>

<task type="execute">
  <name>Task 29-03-02: Define baseline write/compare flow and explicit failure policy</name>
  <files>evals/retrieval/run.ts, evals/retrieval/README.md, reports/.gitkeep, evals/retrieval/**/*.test.ts</files>
  <read_first>
    @evals/retrieval/run.ts
    @evals/retrieval/README.md
    @package.json
    @.planning/REQUIREMENTS.md
    @.planning/phases/29-rag-mode-routing/29-RESEARCH.md
  </read_first>
  <action>
    Add a maintainer-facing baseline flow to the retrieval runner so reports can be written and compared against a stable artifact path. Define the explicit failure policy in code and docs: governance leaks always fail, empty-result expectation mismatches fail, and ranking regressions compare against named tolerance rules per canonical mode slice. Document the artifact path, command behavior, and exit semantics so CI and maintainers interpret regressions consistently.
  </action>
  <acceptance_criteria>
    - `evals/retrieval/run.ts` contains a baseline write or compare option
    - `evals/retrieval/README.md` contains `baseline`
    - `evals/retrieval/README.md` contains `governance leaks always fail`
    - `evals/retrieval/README.md` contains `empty-result`
    - `pnpm test -- --run evals/retrieval` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --run evals/retrieval</automated>
  </verify>
  <done>
    Maintainers can write and compare baseline artifacts through the retrieval runner, and the documented failure policy makes governance leaks and empty-result mismatches hard failures with explicit exit semantics.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Routed retrieval traces -> eval artifacts | Baseline data must represent actual routed behavior and not omit fallback/governance context |
| Eval comparison -> CI/maintainer decisions | Failure policy must be explicit so red/green status is not ambiguous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | R | baseline artifacts | mitigate | Store canonical mode IDs and routing context in report data so later comparisons remain attributable. |
| T-29-08 | T | failure policy | mitigate | Encode governance and empty-result failures as unconditional failures with test coverage. |
| T-29-09 | I | regression comparisons | mitigate | Compare only against stable artifact fields and documented tolerance values per mode slice. |
</threat_model>

<verification>
Run retrieval eval tests and confirm reports can write/read baseline-aware artifacts with explicit policy outcomes for governance failures and mode-specific ranking regressions.
</verification>

<success_criteria>
Phase 29-03 closes EOPS-03 by making retrieval regressions mode-aware, baseline-driven, and governed by explicit failure policy rather than manual judgment.
</success_criteria>
