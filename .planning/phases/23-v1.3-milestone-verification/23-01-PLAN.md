---
phase: 23
plan: 01
type: verification
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-cli-skill-lookup-commands/VERIFICATION.md
  - .planning/phases/19-skill-edit-flow-with-history/VERIFICATION.md
  - .planning/phases/20-skill-edit-review-workflow/VERIFICATION.md
  - packages/cli/src/commands/skill.test.ts
requirements: [SKED-01, SKED-02, SKED-03, SKED-04]
autonomous: true
must_haves:
  - SKED-01: skill search-by-content CLI command and server endpoint exist
  - SKED-02: skill edit CLI command creates pending revision
  - SKED-03: skill review approve/reject with RBAC enforcement
  - SKED-04: skill history preserves previous versions
---

# Plan 23-01: Verify SKED Requirements (Skill Editing)

**Goal:** Goal-backward verification of SKED-01, SKED-02, SKED-03, SKED-04 requirements across Phases 18-20, with gap fixes.

---

## Task 1: Verify SKED-01 (Skill Search-by-Content) -- Phase 18

<read_first>
- .planning/phases/18-cli-skill-lookup-commands/18-01-SUMMARY.md
- .planning/phases/18-cli-skill-lookup-commands/18-02-SUMMARY.md
- packages/contracts/src/domain/retrieval.ts
- packages/server/src/lib/retrieval/skill-lookup.ts
- packages/cli/src/commands/skill.ts
</read_first>

<action>
Create VERIFICATION.md file at `.planning/phases/18-cli-skill-lookup-commands/VERIFICATION.md` with goal-backward analysis:

1. Document requirement traceability table showing SKED-01 requirement mapped to Phase 18
2. Verify must-haves:
   - skillLookupQuerySchema exists in contracts/retrieval.ts with text and maxResults fields
   - skillLookupResultItemSchema exists with artifactId, title, slug, labels, scope, requiredLevel, sourceKind, score, reason
   - skillLookupResponseSchema exists
   - Server endpoint POST /v1/retrieval/skills/search-by-content exists in routes/retrieval.ts
   - skill-lookup.ts helper implements governance filtering (team, security level, approved state)
   - CLI `skill search-by-content` command exists and is discoverable via api:list
3. Document test evidence: 163 contract tests, 461 server tests, 81 CLI tests passing
4. Mark status as PASSED if all must-haves verified
</action>

<acceptance_criteria>
- File `.planning/phases/18-cli-skill-lookup-commands/VERIFICATION.md` exists
- File contains "SKED-01" requirement traceability
- File contains "skillLookupQuerySchema" evidence
- File contains "POST /v1/retrieval/skills/search-by-content" evidence
- File contains status "PASSED" or "GOAL ACHIEVED"
</acceptance_criteria>

---

## Task 2: Verify SKED-02 and SKED-04 (Skill Edit + History) -- Phase 19

<read_first>
- .planning/phases/19-skill-edit-flow-with-history/19-01-SUMMARY.md
- .planning/phases/19-skill-edit-flow-with-history/19-02-SUMMARY.md
- .planning/phases/19-skill-edit-flow-with-history/19-03-SUMMARY.md
- packages/contracts/src/domain/operations.ts
- packages/server/src/lib/artifacts/edit.ts
- packages/cli/src/commands/skill.ts
</read_first>

<action>
Create VERIFICATION.md file at `.planning/phases/19-skill-edit-flow-with-history/VERIFICATION.md` with goal-backward analysis:

1. Document requirement traceability for SKED-02 and SKED-04 mapped to Phase 19
2. Verify SKED-02 must-haves:
   - skillEditRequestSchema exists with artifactId, title, labels, files fields
   - skillEditResponseSchema exists with artifact, previousRevision, lifecycleTransition
   - Server endpoint POST /v1/operations/artifacts/:artifactId/edit exists
   - Edit creates pending revision that enters review queue (agent-pass lifecycle)
   - CLI `skill edit` command exists with --title, --labels, --file options
3. Verify SKED-04 must-haves:
   - skillHistoryRequestSchema and skillHistoryResponseSchema exist
   - skillRevisionSummarySchema captures revision, submittedAt, submittedBy, summary, lifecycleState
   - Server endpoint GET /v1/operations/artifacts/:artifactId/history exists
   - CLI `skill history` command exists
   - Previous versions are preserved (revision append logic in edit.ts)
4. Document test evidence: 163 contract tests, 435+ server tests passing
5. Mark status as PASSED if all must-haves verified
</action>

<acceptance_criteria>
- File `.planning/phases/19-skill-edit-flow-with-history/VERIFICATION.md` exists
- File contains "SKED-02" and "SKED-04" requirement traceability
- File contains "POST /v1/operations/artifacts/:artifactId/edit" evidence
- File contains "GET /v1/operations/artifacts/:artifactId/history" evidence
- File contains "submitSkillEdit" or "appendSkillArtifactRevision" evidence
- File contains status "PASSED" or "GOAL ACHIEVED"
</acceptance_criteria>

---

## Task 3: Verify SKED-03 (Skill Edit Review) -- Phase 20

<read_first>
- .planning/phases/20-skill-edit-review-workflow/20-01-SUMMARY.md
- .planning/phases/20-skill-edit-review-workflow/20-02-SUMMARY.md
- packages/contracts/src/domain/operations.ts
- packages/server/src/routes/operations.ts
- packages/cli/src/commands/skill.ts
</read_first>

<action>
Create VERIFICATION.md file at `.planning/phases/20-skill-edit-review-workflow/VERIFICATION.md` with goal-backward analysis:

1. Document requirement traceability for SKED-03 mapped to Phase 20
2. Verify must-haves:
   - skillReviewQueueItemSchema and skillReviewQueueResponseSchema exist
   - skillReviewDecisionRequestSchema exists with artifactId, decision, notes
   - skillReviewDecisionResponseSchema exists
   - Server endpoint GET /v1/operations/artifacts/review-queue exists
   - Server endpoint POST /v1/operations/artifacts/:artifactId/review exists
   - RBAC enforcement: knowledge:review permission required
   - Review decisions update lifecycle state (approved/rejected)
   - Audit events created for review decisions
   - CLI `skill review:queue`, `skill review:approve`, `skill review:reject` commands exist
3. Document test evidence: 163 contract tests, 435+ server tests passing
4. Mark status as PASSED if all must-haves verified
</action>

<acceptance_criteria>
- File `.planning/phases/20-skill-edit-review-workflow/VERIFICATION.md` exists
- File contains "SKED-03" requirement traceability
- File contains "GET /v1/operations/artifacts/review-queue" evidence
- File contains "POST /v1/operations/artifacts/:artifactId/review" evidence
- File contains "knowledge:review" permission evidence
- File contains status "PASSED" or "GOAL ACHIEVED"
</acceptance_criteria>

---

## Task 4: Fix CLI Test Gap -- SkillCommandOptions Properties

<read_first>
- packages/cli/src/commands/skill.ts
- packages/cli/src/commands/skill.test.ts
</read_first>

<action>
Fix the test file `packages/cli/src/commands/skill.test.ts` to provide all required `SkillCommandOptions` properties:

The `SkillCommandOptions` interface requires:
```typescript
interface SkillCommandOptions {
  allowSearch: boolean;
  allowSubmit: boolean;
  allowExport: boolean;
  allowReview: boolean;
}
```

Update all test calls to `registerSkillCommands(program, options)` to include all four properties:

1. Line 24: `{ allowSearch: true }` → `{ allowSearch: true, allowSubmit: false, allowExport: false, allowReview: false }`
2. Line 34: `{ allowSearch: false }` → `{ allowSearch: false, allowSubmit: false, allowExport: false, allowReview: false }`
3. Line 43: `{ allowSearch: true }` → `{ allowSearch: true, allowSubmit: false, allowExport: false, allowReview: false }`
4. Line 54: `{ allowSearch: true }` → `{ allowSearch: true, allowSubmit: false, allowExport: false, allowReview: false }`
5. Line 81: `{ allowSearch: true }` → `{ allowSearch: true, allowSubmit: false, allowExport: false, allowReview: false }`
6. Line 96: `{ allowSearch: false }` → `{ allowSearch: false, allowSubmit: false, allowExport: false, allowReview: false }`

After fix, run `pnpm --filter @trapmap/cli test` to verify tests pass.
</action>

<acceptance_criteria>
- Command `pnpm --filter @trapmap/cli test` exits with code 0
- `grep -c "allowSubmit" packages/cli/src/commands/skill.test.ts` returns 6 or more
- `grep -c "allowExport" packages/cli/src/commands/skill.test.ts` returns 6 or more
- `grep -c "allowReview" packages/cli/src/commands/skill.test.ts` returns 6 or more
</acceptance_criteria>

---

## Verification Criteria

### Must-Haves (Goal-Backward)

| Requirement | Phase | Must Have | Status |
|-------------|-------|-----------|--------|
| SKED-01 | 18 | skill search-by-content CLI command and server endpoint | must verify |
| SKED-02 | 19 | skill edit CLI command creates pending revision | must verify |
| SKED-03 | 20 | skill review approve/reject with RBAC | must verify |
| SKED-04 | 19 | skill history preserves previous versions | must verify |

### Test Gates

- [ ] 163 contract tests pass
- [ ] 461+ server tests pass (10 pre-existing failures acceptable)
- [ ] 81 CLI tests pass (after fix)

### Documents Created

- [ ] `.planning/phases/18-cli-skill-lookup-commands/VERIFICATION.md`
- [ ] `.planning/phases/19-skill-edit-flow-with-history/VERIFICATION.md`
- [ ] `.planning/phases/20-skill-edit-review-workflow/VERIFICATION.md`