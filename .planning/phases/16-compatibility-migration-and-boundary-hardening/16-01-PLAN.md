---
phase: 16-compatibility-migration-and-boundary-hardening
plan: "01"
type: execute
wave: 1
depends_on:
files_modified:
  - packages/contracts/src/domain/operations.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/lib/import-export.ts
  - packages/server/src/lib/artifacts/model.ts
  - packages/server/src/routes/operations.ts
  - packages/server/src/routes/operations.test.ts
  - packages/cli/src/commands/operations.ts
  - packages/cli/src/commands/operations.test.ts
autonomous: true
requirements:
  - ARTF-04
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "A legacy knowledge entry can be converted into a minimal Claude-compatible artifact without inventing references/assets/scripts."
    - "Migration runs through governed operations boundaries with audit events and existing permission checks."
    - "Legacy `/v1` compatibility remains intact while migrated artifacts become the new canonical shape."
  artifacts:
    - path: "packages/contracts/src/domain/operations.ts"
      provides: "Shared migration/status request and response contracts"
    - path: "packages/server/src/lib/import-export.ts"
      provides: "Pure legacy-entry to minimal-artifact normalization seam"
    - path: "packages/server/src/routes/operations.ts"
      provides: "Governed migration endpoint with audit logging and access checks"
    - path: "packages/cli/src/commands/operations.ts"
      provides: "Shell-friendly migration invocation and JSON/human output"
  key_links:
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/import-export.ts"
      via: "migration normalization helper"
      pattern: "migrate|legacy|artifact"
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/artifacts/model.ts"
      via: "artifact record creation"
      pattern: "createSkillArtifactRecord"
    - from: "packages/cli/src/commands/operations.ts"
      to: "packages/contracts/src/domain/operations.ts"
      via: "shared contract parse"
      pattern: "migration|status"
---

<objective>
Introduce the governed Phase 16 migration seam that converts legacy knowledge entries into minimal skill artifacts without breaking the v1 compatibility window.

Purpose: deliver ARTF-04 while preserving COMP-03 and keeping `packages/contracts` as the only shared truth.
Output: migration contracts, pure normalization logic, audited server route behavior, and CLI command coverage.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-compatibility-migration-and-boundary-hardening/16-RESEARCH.md
@.planning/phases/16-compatibility-migration-and-boundary-hardening/16-VALIDATION.md
@packages/contracts/src/domain/knowledge.ts
@packages/contracts/src/domain/artifacts.ts
@packages/contracts/src/domain/operations.ts
@packages/server/src/lib/import-export.ts
@packages/server/src/lib/artifacts/model.ts
@packages/server/src/routes/operations.ts
@packages/cli/src/commands/operations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared migration/status contracts for legacy-entry compatibility work</name>
  <files>packages/contracts/src/domain/operations.ts, packages/contracts/src/index.test.ts</files>
  <read_first>
    - packages/contracts/src/domain/operations.ts
    - packages/contracts/src/domain/knowledge.ts
    - packages/contracts/src/domain/artifacts.ts
    - .planning/phases/16-compatibility-migration-and-boundary-hardening/16-RESEARCH.md
  </read_first>
  <action>Add additive schemas in `packages/contracts/src/domain/operations.ts` for a legacy-entry migration request, migration result items, and compatibility status summary. The request must carry explicit legacy entry IDs or a bounded selection mode, and the response must expose migrated artifact IDs, skipped/error reasons, and counts of remaining unmigrated entries. Do not alter existing `/v1` import/export/activate contract shapes. Export the new schemas through the public contracts surface and pin them in `packages/contracts/src/index.test.ts`.</action>
  <acceptance_criteria>
    - `packages/contracts/src/domain/operations.ts` contains migration request and response schemas with explicit `entryId` and `artifactId` fields
    - `packages/contracts/src/index.test.ts` asserts the new migration/status schemas parse valid payloads
    - existing artifact import/export and knowledge import/export schema tests still pass without contract renames
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>Contracts define a stable migration/status surface that both server and CLI can consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic legacy knowledge to minimal artifact normalization on the server</name>
  <files>packages/server/src/lib/import-export.ts, packages/server/src/lib/artifacts/model.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts</files>
  <read_first>
    - packages/server/src/lib/import-export.ts
    - packages/server/src/lib/artifacts/model.ts
    - packages/server/src/routes/operations.ts
    - packages/contracts/src/domain/knowledge.ts
    - packages/contracts/src/domain/artifacts.ts
    - .planning/phases/15-client-activation-for-references-assets-and-scripts/15-03-SUMMARY.md
  </read_first>
  <action>Create a pure helper that maps one legacy knowledge record into a minimal artifact bundle containing a canonical `SKILL.md` payload, preserving `labels`, `scope`, `requiredLevel`, team ownership, and provenance back to the legacy entry ID. Use the existing artifact creation/model seams to persist the migrated artifact, emit audit events through `packages/server/src/routes/operations.ts`, and reject migrations that would lower required level, skip ownership, or mark non-approved legacy entries as approved artifacts without explicit parity rules. Add route tests for success, duplicate/skip handling, permission failure, and audit emission.</action>
  <acceptance_criteria>
    - `packages/server/src/lib/import-export.ts` contains a pure migration helper that produces a single-file `SKILL.md` bundle from a legacy record
    - `packages/server/src/routes/operations.ts` exposes a migration path that requires existing operations permissions
    - `packages/server/src/routes/operations.test.ts` covers successful migration, denied access, skipped duplicates, and audit event creation
    - migrated artifacts are created through existing artifact model helpers rather than direct store mutation only
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts</automated>
  </verify>
  <done>The server can migrate legacy entries into minimal artifacts through a governed, audited path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add shell-friendly CLI migration command and status output</name>
  <files>packages/cli/src/commands/operations.ts, packages/cli/src/commands/operations.test.ts</files>
  <read_first>
    - packages/cli/src/commands/operations.ts
    - packages/contracts/src/domain/operations.ts
    - packages/server/src/routes/operations.ts
    - packages/cli/src/lib/output.ts
  </read_first>
  <action>Extend the operations command family with an explicit migration command that accepts entry IDs or a bounded migration mode, calls the new operations endpoint, and prints stable human and `--json` output including migrated artifact IDs, skipped/error reasons, and remaining legacy count. Keep output imperative and scriptable, and do not overload existing import/export commands with hidden migration behavior.</action>
  <acceptance_criteria>
    - `packages/cli/src/commands/operations.ts` registers a dedicated migration command rather than silently changing existing commands
    - `packages/cli/src/commands/operations.test.ts` verifies human output and `--json` output contain `entryId`, `artifactId`, and remaining-count fields
    - the CLI parses server responses with the shared migration/status schemas
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts</automated>
  </verify>
  <done>Operators can run and automate legacy-entry migration from the terminal with stable output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy knowledge record -> migrated artifact | Old governed content becomes the new canonical artifact representation here |
| CLI migration request -> operations route | Admin/operator input selects which legacy records are migrated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | T | `packages/server/src/lib/import-export.ts` | mitigate | Normalize from explicit legacy fields only and preserve required level, scope, and labels without inventing richer content. |
| T-16-02 | R | `packages/server/src/routes/operations.ts` | mitigate | Emit audit events with source entry IDs, migrated artifact IDs, and actor information for every migration outcome. |
| T-16-03 | S | `packages/contracts/src/domain/operations.ts` | mitigate | Keep the migration/status schema shared so CLI and server cannot drift on identifiers or result semantics. |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/operations.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- `pnpm --filter @skill-shareer/cli exec tsc --noEmit`
</verification>

<success_criteria>
- A legacy knowledge entry can be migrated into a minimal artifact through a governed endpoint and CLI command.
- The migration output is shared-contract validated and scriptable.
- `/v1` compatibility remains available while migrated artifacts become traceable.
</success_criteria>

<output>
After completion, create `.planning/phases/16-compatibility-migration-and-boundary-hardening/16-01-SUMMARY.md`
</output>
