---
phase: 16-compatibility-migration-and-boundary-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - packages/server/src/routes/operations.ts
  - packages/server/src/routes/operations.test.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/cli/src/commands/retrieval.test.ts
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04
must_haves:
  truths:
    - "v1 and v2 coexistence keeps approval, team scope, required level, and audit behavior intact."
    - "Compatibility hardening does not introduce server-side script execution or bundle-body leakage."
    - "Parity is proven with executable tests, not assumed from design intent."
  artifacts:
    - path: "packages/server/src/routes/operations.test.ts"
      provides: "Governance parity coverage for legacy and artifact-native operations"
    - path: "packages/server/src/routes/retrieval.test.ts"
      provides: "Coexistence coverage for `/v1` and `/v2` retrieval boundaries"
    - path: "packages/server/src/lib/retrieval.test.ts"
      provides: "Integration coverage for filtered, governed retrieval behavior"
    - path: "packages/cli/src/commands/retrieval.test.ts"
      provides: "CLI-level coexistence behavior for legacy and v2 retrieval flows"
  key_links:
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval.ts"
      via: "legacy and v2 delegation paths"
      pattern: "/v1/retrieval/search|/v2/retrieval/search"
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/rbac.ts"
      via: "permission and level enforcement"
      pattern: "requirePermission|requireHigherLevel|requireTeamAccess"
---

<objective>
Harden the v1/v2 coexistence window by proving that review, audit, scope, level, and server-side execution boundaries remain intact across legacy and artifact-native paths.

Purpose: satisfy COMP-02 and COMP-04 while keeping COMP-03 measurable instead of implicit.
Output: focused parity tests and any minimal route fixes they require.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-compatibility-migration-and-boundary-hardening/16-RESEARCH.md
@.planning/phases/16-compatibility-migration-and-boundary-hardening/16-VALIDATION.md
@packages/server/src/routes/operations.ts
@packages/server/src/routes/retrieval.ts
@packages/server/src/lib/retrieval.ts
@packages/server/src/lib/rbac.ts
@packages/contracts/src/domain/knowledge.ts
@packages/contracts/src/domain/artifacts.ts
@packages/cli/src/commands/retrieval.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coexistence parity coverage for auth, scope, level, and audit behavior</name>
  <files>packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts, packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts, packages/server/src/lib/retrieval.test.ts</files>
  <read_first>
    - packages/server/src/routes/operations.ts
    - packages/server/src/routes/retrieval.ts
    - packages/server/src/lib/retrieval.test.ts
    - packages/server/src/lib/rbac.ts
    - .planning/phases/14-seed-intent-retrieval-and-capsule-ranking/VERIFICATION.md
  </read_first>
  <action>Add parity-focused tests that compare legacy knowledge operations and retrieval with artifact-native migration/retrieval for the same caller and governance inputs. Cover at least: denied access when team scope mismatches, denied access when required level exceeds caller level, exclusion of non-approved content from retrieval, and audit event presence for migration/operations actions. If existing routes fail these parity assertions, make the smallest route/library fixes needed to restore equivalent behavior without changing public `/v1` schemas.</action>
  <acceptance_criteria>
    - `packages/server/src/routes/retrieval.test.ts` contains tests that exercise both `/v1/retrieval/search` and `/v2/retrieval/search`
    - `packages/server/src/routes/operations.test.ts` verifies team and level enforcement on migration/compatibility operations
    - `packages/server/src/lib/retrieval.test.ts` proves non-approved or out-of-scope data is filtered before result shaping
    - any route fixes preserve existing endpoint paths and request/response versions
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts src/routes/retrieval.test.ts src/lib/retrieval.test.ts</automated>
  </verify>
  <done>Parity between legacy and artifact-native paths is proven by tests and enforced in the server boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin no-script-execution and metadata-only coexistence guarantees</name>
  <files>packages/server/src/routes/operations.test.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <read_first>
    - packages/server/src/routes/operations.test.ts
    - packages/server/src/routes/retrieval.test.ts
    - packages/cli/src/commands/retrieval.test.ts
    - .planning/phases/15-client-activation-for-references-assets-and-scripts/15-03-SUMMARY.md
    - packages/contracts/src/domain/artifacts.ts
  </read_first>
  <action>Add explicit regression coverage that coexistence hardening does not cause the server to execute scripts, emit script bodies, or return raw artifact bundle payloads through retrieval or compatibility-status surfaces. Reuse the Phase 15 policy/activation boundary and keep retrieval outputs metadata-only. If route output currently overexposes any script or payload data, tighten the shaping code without adding browser/UI dependencies.</action>
  <acceptance_criteria>
    - `packages/server/src/routes/operations.test.ts` contains assertions that activation/migration responses do not include script bodies
    - `packages/server/src/routes/retrieval.test.ts` asserts retrieval responses remain capsule/distilled metadata only
    - `packages/cli/src/commands/retrieval.test.ts` verifies text and JSON output do not surface raw bundle payload content
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>Compatibility hardening preserves the existing no-execution, metadata-only boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1 route surface -> v2 route surface | Different public compatibility paths must enforce the same governance rules |
| artifact activation metadata -> retrieval/compatibility output | Metadata from newer artifact flows must not leak payload bodies or execution capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | E | `packages/server/src/routes/operations.ts` | mitigate | Reuse RBAC and higher-level checks for migration/coexistence operations instead of introducing bypasses. |
| T-16-05 | I | `packages/server/src/routes/retrieval.ts` | mitigate | Keep approval/team/level filtering ahead of response shaping on both `/v1` and `/v2`. |
| T-16-06 | E | `packages/server/src/routes/operations.ts` | mitigate | Add regression tests proving the server still never executes scripts and never returns script bodies in coexistence flows. |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/routes/operations.test.ts src/routes/retrieval.test.ts src/lib/retrieval.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- `pnpm --filter @skill-shareer/cli exec tsc --noEmit`
</verification>

<success_criteria>
- v1/v2 coexistence has executable parity coverage for auth, scope, level, and audit behavior.
- The server still does not execute scripts or expose raw payload bodies through compatibility flows.
- `/v1` and `/v2` remain available while parity gaps are surfaced immediately by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/16-compatibility-migration-and-boundary-hardening/16-02-SUMMARY.md`
</output>
