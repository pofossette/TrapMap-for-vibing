---
phase: 36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract
plan: 03
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - packages/contracts/src/domain/operations.ts
  - packages/contracts/src/index.ts
  - packages/server/src/lib/indexing/skill-events.ts
  - packages/server/src/routes/operations.ts
  - packages/server/src/lib/indexing/skill-events.test.ts
  - packages/server/src/routes/operations.test.ts
autonomous: true
requirements:
  - P36-02
must_haves:
  truths:
    - "Approved skill artifacts index only derived capsule/profile text and never raw asset or script bodies."
    - "Skill approval, edit, deactivation, and reapproval transitions refresh or remove graph documents through a post-commit path."
    - "Indexed skill graph documents inherit artifact governance and revision lineage."
  artifacts:
    - path: packages/server/src/lib/indexing/skill-events.ts
      provides: "Skill graph source building and lifecycle event runner."
    - path: packages/server/src/routes/operations.ts
      provides: "Skill approval/edit/deactivation post-commit graph indexing hooks."
    - path: packages/contracts/src/domain/operations.ts
      provides: "Skill deactivation request/response contracts."
  key_links:
    - from: packages/server/src/lib/indexing/skill-events.ts
      to: packages/server/src/lib/indexing/graph-lite/store.ts
      via: "skill graph document upsert/remove"
      pattern: "sourceType: 'skill'"
    - from: packages/server/src/routes/operations.ts
      to: packages/server/src/lib/indexing/skill-events.ts
      via: "post-commit skill lifecycle indexing"
      pattern: "runSkillIndexEvent"
---

<objective>
Add the skill-side graph indexing path and wire it to the artifact lifecycle so approved skills enter and leave the graph safely.

Purpose: satisfy the skill half of Phase 36 using governed derived text only, including the missing deactivation path for skill artifacts.
Output: skill graph event module, operations contracts/route changes, and tests proving activation-only content is excluded.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md
@packages/server/src/lib/artifacts/derive.ts
@packages/server/src/lib/artifacts/edit.ts
@packages/server/src/routes/operations.ts
@packages/server/src/lib/store.ts
@packages/server/src/lib/indexing/events.ts
@packages/server/src/lib/indexing/graph-lite/documents.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build governed skill graph source extraction from derived outputs</name>
  <files>packages/server/src/lib/indexing/skill-events.ts, packages/server/src/lib/indexing/skill-events.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md, packages/server/src/lib/artifacts/derive.ts, packages/server/src/lib/artifacts/edit.ts, packages/server/src/lib/store.ts, packages/server/src/lib/indexing/events.ts</read_first>
  <behavior>
    - Test 1: building a skill graph document reads `latestRevision.derived.profile` and `latestRevision.derived.capsules` only.
    - Test 2: asset paths and script descriptors in `clientManifest` do not appear in persisted graph node text or evidence.
    - Test 3: the resulting graph document preserves artifact `teamId`, `scope`, `requiredLevel`, `artifactId`, and `revision`.
  </behavior>
  <action>Create `packages/server/src/lib/indexing/skill-events.ts` with exact helpers `buildSkillGraphDocument(artifact)`, `determineSkillIndexAction(previousState, nextState)`, and `runSkillIndexEvent({ services, artifactId, previousState, nextState, reason })`. Build the graph source text from `artifact.latestRevision.derived.profile.summary`, `profile.keywords`, and each capsule field `situation`, `problem`, `goal`, `content`, `labels`; do not read raw `artifactFilePayloads`, `latestRevision.files` bodies, `clientManifest.assets`, or `clientManifest.scripts` per D-01/D-02. Persist skill graph documents with `sourceType: 'skill'`, `sourceId: artifact.id`, and `revision: artifact.latestRevision.revision`; remove them when the artifact is not approved.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/skill-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/indexing/skill-events.ts` exports `buildSkillGraphDocument`, `determineSkillIndexAction`, and `runSkillIndexEvent`.
    - `packages/server/src/lib/indexing/skill-events.ts` contains the exact strings `latestRevision.derived?.profile`, `latestRevision.derived?.capsules`, and `sourceType: 'skill'`.
    - `packages/server/src/lib/indexing/skill-events.ts` does not contain a direct read of `clientManifest.scripts` or `clientManifest.assets` bodies as graph content sources.
    - The automated test command passes.
  </acceptance_criteria>
  <done>Skill graph documents are derived from governed artifact summaries/capsules only, and tests prove activation-only content is excluded from graph persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire skill approve, edit, deactivate, and reapprove transitions to graph indexing</name>
  <files>packages/contracts/src/domain/operations.ts, packages/contracts/src/index.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, packages/contracts/src/domain/operations.ts, packages/contracts/src/index.ts, packages/server/src/routes/operations.ts, packages/server/src/lib/indexing/skill-events.ts, packages/server/src/lib/artifacts/edit.ts</read_first>
  <behavior>
    - Test 1: approving an artifact triggers `runSkillIndexEvent(... nextState: 'approved' ...)` after the transaction commits.
    - Test 2: editing an approved artifact refreshes graph state for the new revision after commit.
    - Test 3: deactivating an artifact removes its graph documents, and approving it again rebuilds them.
  </behavior>
  <action>Update `packages/server/src/routes/operations.ts` to mirror the trap post-commit indexing pattern for skills. After artifact review commits, call `runSkillIndexEvent` when `previousState !== newState`. After `submitSkillEdit` completes, if the artifact ends in `approved`, call `runSkillIndexEvent` with `reason: 'updated'`. Add a new authenticated route `POST /v1/operations/artifacts/:artifactId/deactivate` that requires `knowledge:update`, uses a request body schema `{ reason: string }`, writes a lifecycle event of type `deactivated`, sets `artifact.lifecycleState = 'deactivated'`, records an audit event `artifact-deactivated`, and then calls `runSkillIndexEvent` post-commit with `reason: 'deactivated'`. Add matching request/response schemas and exports in `packages/contracts/src/domain/operations.ts` and `packages/contracts/src/index.ts`.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/routes/operations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/routes/operations.ts` contains the exact string `POST /v1/operations/artifacts/:artifactId/deactivate` in the route registration or documented route list.
    - `packages/server/src/routes/operations.ts` contains the exact string `runSkillIndexEvent(`.
    - `packages/contracts/src/domain/operations.ts` exports request and response schemas for artifact deactivation.
    - The automated test command passes.
  </acceptance_criteria>
  <done>Skill artifact approval, edit, deactivate, and reapprove transitions all drive post-commit graph indexing through a dedicated skill event module, and the missing deactivation contract exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived artifact outputs -> skill graph indexing | Governed skill summaries become durable graph documents |
| artifact lifecycle routes -> graph event runner | Committed lifecycle state controls whether graph docs exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | I | `skill-events.ts` | mitigate | Build graph text only from `derived.profile` and `derived.capsules`; exclude `assets/` and `scripts/` bodies from graph content per D-01/D-02. |
| T-36-10 | E | `skill-events.ts` | mitigate | Persist `teamId`, `scope`, and `requiredLevel` directly from the artifact root so graph state inherits governance exactly. |
| T-36-11 | T | `operations.ts` | mitigate | Run skill indexing only after the lifecycle transaction commits, using explicit `previousState` and `nextState` inputs so stale pre-commit state is never indexed. |
| T-36-12 | I | artifact deactivation route | mitigate | Remove graph documents immediately on deactivation so over-scoped or stale skill state cannot remain queryable after lifecycle change. |
</threat_model>

<verification>
Run `pnpm --filter @trapmap/server test -- src/lib/indexing/skill-events.test.ts src/routes/operations.test.ts`.
</verification>

<success_criteria>
Approved skills produce graph documents from derived capsule/profile text only, edits and reviews refresh them after commit, and deactivation removes them through a first-class operations route.
</success_criteria>

<output>
After completion, create `.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-03-SUMMARY.md`
</output>
