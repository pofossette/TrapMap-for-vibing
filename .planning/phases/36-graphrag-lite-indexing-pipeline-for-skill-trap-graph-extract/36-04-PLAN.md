---
phase: 36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract
plan: 04
type: execute
wave: 3
depends_on:
  - 36-02
  - 36-03
files_modified:
  - packages/server/src/lib/indexing/reconcile.ts
  - packages/server/src/app.ts
  - packages/server/src/lib/indexing/events.test.ts
  - packages/server/src/routes/review.test.ts
  - packages/server/src/lib/indexing/reconcile.test.ts
autonomous: true
requirements:
  - P36-03
  - P36-04
must_haves:
  truths:
    - "Stale, rejected, or deactivated graph documents are removed automatically across trap and skill domains."
    - "Startup reconciliation can repair missing graph documents for currently approved traps and skills."
    - "Hard dependency cycles and over-scoped graph state are treated as security failures, not ignored drift."
  artifacts:
    - path: packages/server/src/lib/indexing/reconcile.ts
      provides: "Cross-domain graph reconciliation and stale-state cleanup."
    - path: packages/server/src/app.ts
      provides: "Startup reconciliation hook for graph drift repair."
    - path: packages/server/src/lib/indexing/reconcile.test.ts
      provides: "Regression coverage for rebuild/remove reconciliation."
  key_links:
    - from: packages/server/src/app.ts
      to: packages/server/src/lib/indexing/reconcile.ts
      via: "startup onReady reconcile call"
      pattern: "reconcileGraphIndexes"
    - from: packages/server/src/lib/indexing/reconcile.ts
      to: packages/server/src/lib/indexing/skill-events.ts
      via: "skill rebuild path"
      pattern: "buildSkillGraphDocument"
---

<objective>
Close the lifecycle-drift and stale-state security gap by reconciling graph documents across both domains and running that repair path at startup.

Purpose: ensure approved content is the only graph content that remains durable, and guarantee recovery from failed or partial indexing work.
Output: cross-domain reconciliation module, startup hook integration, and regression tests for remove/rebuild behavior.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md
@packages/server/src/app.ts
@packages/server/src/lib/indexing/events.ts
@packages/server/src/lib/indexing/pipeline.ts
@packages/server/src/lib/indexing/skill-events.ts
@packages/server/src/lib/indexing/graph-lite/store.ts
@packages/server/src/routes/review.ts
@packages/server/src/routes/operations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-domain graph reconciliation and stale-state cleanup</name>
  <files>packages/server/src/lib/indexing/reconcile.ts, packages/server/src/lib/indexing/reconcile.test.ts, packages/server/src/lib/indexing/events.test.ts, packages/server/src/routes/review.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-VALIDATION.md, packages/server/src/lib/indexing/events.ts, packages/server/src/lib/indexing/pipeline.ts, packages/server/src/lib/indexing/skill-events.ts, packages/server/src/lib/indexing/graph-lite/store.ts</read_first>
  <behavior>
    - Test 1: reconciliation removes graph documents whose source trap or skill is missing, deactivated, rejected, or no longer on the latest approved revision.
    - Test 2: reconciliation rebuilds missing graph documents for approved knowledge entries and approved skill artifacts.
    - Test 3: route-level review/update tests show reapprove and approved-update transitions leave no stale graph documents behind.
  </behavior>
  <action>Create `packages/server/src/lib/indexing/reconcile.ts` with exact helpers `reconcileGraphIndexes({ store })` and `reconcileGraphIndexesFromSnapshot({ store, data })`. The reconcile pass must load all persisted graph documents, compute the allowed source set from approved `knowledgeEntries` and approved `skillArtifacts`, remove any document whose `{sourceType, sourceId, revision}` is not the currently approved/latest revision, and rebuild the missing approved documents using the trap graph adapter path and `buildSkillGraphDocument` for skills. Treat stale graph documents for deactivated or rejected entities as security-sensitive removals, not warnings. Extend `events.test.ts` and `review.test.ts` to assert `approved -> deactivated` removes docs and `approved -> approved` update/reapprove flows rebuild the latest revision.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/events.test.ts src/routes/review.test.ts src/lib/indexing/reconcile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/indexing/reconcile.ts` exports `reconcileGraphIndexes` and `reconcileGraphIndexesFromSnapshot`.
    - `packages/server/src/lib/indexing/reconcile.ts` contains logic referencing both `knowledgeEntries` and `skillArtifacts`.
    - `packages/server/src/lib/indexing/reconcile.ts` removes stale documents by comparing persisted `{sourceType, sourceId, revision}` against current approved sources.
    - The automated test command passes.
  </acceptance_criteria>
  <done>Cross-domain reconciliation removes stale or over-scoped graph state and restores missing approved graph documents through an automated repair path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run graph reconciliation automatically at server startup</name>
  <files>packages/server/src/app.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md, packages/server/src/app.ts, packages/server/src/lib/indexing/reconcile.ts</read_first>
  <behavior>
    - Test 1: the server startup hook invokes graph reconciliation after the store is ready.
    - Test 2: graph reconciliation runs independently from candidate recovery and does not block server startup on non-fatal repair errors.
  </behavior>
  <action>Update `packages/server/src/app.ts` to import `reconcileGraphIndexes` and add a second `onReady` recovery block after candidate recovery setup. The hook must call `await reconcileGraphIndexes({ store: app.skillShareer.store })`, log `Graph index reconciliation complete` with counts for rebuilt and removed documents, and catch/log errors as `Graph index reconciliation failed` without crashing startup. Keep candidate recovery intact; do not fold graph reconciliation into candidate recovery logic.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/reconcile.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/app.ts` contains the exact strings `reconcileGraphIndexes`, `Graph index reconciliation complete`, and `Graph index reconciliation failed`.
    - `packages/server/src/app.ts` still contains the interrupted candidate recovery hook.
    - The automated test command passes.
  </acceptance_criteria>
  <done>The server repairs graph drift on startup, logs the outcome, and keeps recovery failures isolated from the main boot path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted graph docs -> reconciliation logic | Previously indexed state may be stale, over-scoped, or partially written |
| server startup -> automatic repair hooks | Repair logic runs without a human reviewer during process boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-13 | I | `reconcile.ts` | mitigate | Remove graph documents whose source is non-approved, deactivated, missing, or on an older revision so stale graph state cannot leak forbidden content. |
| T-36-14 | T | `reconcile.ts` | mitigate | Rebuild approved trap and skill documents from current source-of-truth records instead of trusting existing persisted graph docs. |
| T-36-15 | D | `app.ts` startup hook | mitigate | Catch and log reconciliation errors so startup remains available while still surfacing repair failures for follow-up. |
| T-36-16 | E | cross-domain graph repair | mitigate | Reconciliation derives the allowed source set from current `teamId`, `scope`, and `requiredLevel` metadata, preventing graph docs from outliving governance changes. |
</threat_model>

<verification>
Run `pnpm --filter @trapmap/server test -- src/lib/indexing/events.test.ts src/routes/review.test.ts src/lib/indexing/reconcile.test.ts`.
</verification>

<success_criteria>
The server can detect and repair stale graph state across traps and skills, and startup reconciliation keeps the durable graph index aligned with approved source content without manual cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-04-SUMMARY.md`
</output>
