---
phase: 30-fixture-trace
plan: 01
type: execute
wave: 1
depends_on:
  - phase: 29
    status: complete
files_modified:
  - evals/retrieval/lib/adapters.ts
  - evals/retrieval/run.ts
autonomous: true
requirements:
  - EOPS-01
  - EOPS-02
must_haves:
  truths:
    - "Retrieval evaluation must execute against real scenario data, not empty stores, to be a valid regression surface."
    - "Fixture IDs in seeded data must match scenario definitions exactly for relevance checks to work."
    - "Seeded data must respect governance constraints: lifecycle state, team boundaries, security levels."
  artifacts:
    - path: "evals/retrieval/lib/adapters.ts"
      provides: "scenario fixture materialization"
      contains: "seedScenarioFixtures implementation that loads scenario and pushes to store"
    - path: "evals/retrieval/run.ts"
      provides: "execution workflow with fixture seeding"
      contains: "seedScenarioFixtures call before executeCase"
  key_links:
    - from: "evals/retrieval/lib/load.ts"
      to: "evals/retrieval/lib/adapters.ts"
      via: "loadScenario"
      pattern: "scenarioId"
    - from: "packages/server/src/lib/knowledge.ts"
      to: "evals/retrieval/lib/adapters.ts"
      via: "createKnowledgeEntryRecord"
      pattern: "KnowledgeRecord"
---

<objective>
Implement real fixture materialization so retrieval evaluation executes against actual scenario data instead of empty stores.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-fixture-trace/30-CONTEXT.md
@.planning/phases/30-fixture-trace/30-PATTERNS.md
@evals/retrieval/lib/adapters.ts
@evals/retrieval/lib/load.ts
@evals/retrieval/run.ts
@packages/server/src/lib/retrieval.test.ts
@packages/server/src/lib/knowledge.ts
@packages/server/src/lib/store.ts
</context>

<tasks>

<task type="execute">
  <name>Task 30-01-01: Implement seedScenarioFixtures for knowledge entries and skill artifacts</name>
  <files>evals/retrieval/lib/adapters.ts</files>
  <read_first>
    @evals/retrieval/lib/adapters.ts
    @evals/retrieval/lib/load.ts
    @packages/server/src/lib/retrieval.test.ts
    @packages/server/src/lib/knowledge.ts
    @packages/server/src/lib/store.ts
    @.planning/phases/30-fixture-trace/30-PATTERNS.md
  </read_first>
  <action>
    Implement `seedScenarioFixtures` function in `evals/retrieval/lib/adapters.ts`:

    1. Import `loadScenario` from `./load.js`
    2. Load scenario using `loadScenario(case_.scenarioId)` - return early if scenario not found
    3. For each entry in `scenario.fixtures.knowledgeEntries` (cast as array of fixture objects):
       - Create a mock `AgentReviewResult` with `status: lifecycleState` from fixture and empty `issues`, `suggestions`, `duplicateCandidates` arrays
       - Call `createKnowledgeEntryRecord({ store, data, ownerUserId: ctx.actorId, teamId: entry.teamId, payload: { scope, labels, shortcut, detail }, requiredLevel, createdAt: nowIso(), preReview })`
       - Set `record.lifecycleState = entry.lifecycleState` after creation
       - Set `record.id = entry.id` to use exact fixture ID
       - Push to `data.knowledgeEntries`
    4. For each artifact in `scenario.fixtures.skillArtifacts`:
       - Build `SkillArtifactRecord` with:
         - `id: artifact.id`
         - `teamId: artifact.teamId`
         - `scope: artifact.scope`
         - `labels: artifact.labels`
         - `title: artifact.title`
         - `slug: artifact.slug`
         - `requiredLevel: artifact.requiredLevel`
         - `lifecycleState: artifact.lifecycleState`
         - `ownerUserId: ctx.actorId`
         - `latestRevision: { revision: 1, capsules: artifact.capsules, files: [], createdAt: nowIso(), ... }`
         - `history: []`
         - `metadata: {}`
         - `agentReview: null`
         - `reviewHistory: []`
         - `reviewNotes: []`
         - `lifecycleHistory: []`
         - `createdAt: nowIso()`
         - `updatedAt: nowIso()`
       - Push to `data.skillArtifacts`
    5. Create team record if `scenario.actor.activeTeamId` is specified and team doesn't exist
    6. Call `createActorSession(ctx, scenario.actor)` to set up permissions
  </action>
  <acceptance_criteria>
    - `grep -n "loadScenario" evals/retrieval/lib/adapters.ts` returns at least one match
    - `grep -n "createKnowledgeEntryRecord" evals/retrieval/lib/adapters.ts` returns at least one match
    - `grep -n "data.knowledgeEntries.push" evals/retrieval/lib/adapters.ts` returns at least one match
    - `grep -n "data.skillArtifacts.push" evals/retrieval/lib/adapters.ts` returns at least one match
    - Function `seedScenarioFixtures` has no "placeholder" or "For Phase 26" comments
    - `pnpm -r test` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r test</automated>
  </verify>
  <done>
    seedScenarioFixtures loads scenario by ID and materializes knowledge entries and skill artifacts with exact fixture IDs, enabling retrieval eval to execute against real data.
  </done>
</task>

<task type="execute">
  <name>Task 30-01-02: Integrate fixture seeding into execution workflow</name>
  <files>evals/retrieval/run.ts</files>
  <read_first>
    @evals/retrieval/run.ts
    @evals/retrieval/lib/adapters.ts
    @evals/retrieval/lib/load.ts
  </read_first>
  <action>
    Modify `executeAllCases` in `evals/retrieval/run.ts` to call `seedScenarioFixtures` before each case:

    1. Import `seedScenarioFixtures` from `./lib/adapters.js` (add to existing imports)
    2. Inside the for loop in `executeAllCases`, before calling `executeCase`:
       - Add `await seedScenarioFixtures(ctx, case_)` to seed fixtures for this case's scenario
    3. The execution context already creates a unique store file per run (timestamp + random suffix), so fixtures are isolated between runs

    The execution flow becomes:
    ```
    for (const case_ of cases_) {
      await seedScenarioFixtures(ctx, case_);  // NEW
      const adapterResult = await executeCase(ctx, case_);
      // ... governance, metrics, result push
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -n "seedScenarioFixtures" evals/retrieval/run.ts` returns at least one match
    - The call to `seedScenarioFixtures` occurs inside the case execution loop before `executeCase`
    - `pnpm -r test` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r test</automated>
    <manual>pnpm eval:retrieval --tier smoke --verbose</manual>
  </verify>
  <done>
    Retrieval eval runner seeds fixtures before each case, enabling real data execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval runner -> in-process store | Fixtures must be isolated per execution context |
| Fixture IDs -> relevance checks | IDs must match exactly without transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | T | fixture seeding | mitigate | Use unique store files per context with timestamp+random suffix |
| T-30-01-02 | I | ID mapping | mitigate | Use exact fixture IDs, no transformation or generation |
| T-30-01-03 | D | fixture validation | mitigate | Fail fast with clear error if fixture fields missing |
</threat_model>

<verification>
1. Run retrieval smoke eval: `pnpm eval:retrieval --tier smoke --verbose`
2. Verify cases show non-zero hits when fixtures seeded correctly
3. Verify governance checks work with real data (forbidden cases fail as expected)
4. Run all tests: `pnpm -r test`
</verification>

<success_criteria>
Retrieval evaluation executes against real seeded data, producing actual results that pass/fail based on scenario fixtures rather than empty stores.
</success_criteria>
