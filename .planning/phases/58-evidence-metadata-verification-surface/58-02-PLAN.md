---
phase: 58-evidence-metadata-verification-surface
plan: 02
type: execute
wave: 1
depends_on:
  - 58-01-PLAN
files_modified:
  - packages/contracts/src/domain/knowledge.ts
  - packages/contracts/src/domain/artifacts.ts
  - packages/contracts/src/domain/review.ts
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/domain/operations.ts
requirements:
  - EVIDENCE-01
  - EVIDENCE-02
autonomous: true

must_haves:
  truths:
    - "Knowledge entry schema has nullable evidenceMeta field"
    - "Skill artifact schema has nullable evidenceMeta field"
    - "Review decision request schema has optional evidence field"
    - "Retrieval match schemas include optional evidence hint"
    - "Operations list schema includes evidence filter parameters"
  artifacts:
    - path: "packages/contracts/src/domain/knowledge.ts"
      provides: "knowledgeEntrySchema extended with evidenceMeta"
      contains: "evidenceMeta"
    - path: "packages/contracts/src/domain/artifacts.ts"
      provides: "skillArtifactSchema extended with evidenceMeta"
      contains: "evidenceMeta"
    - path: "packages/contracts/src/domain/review.ts"
      provides: "reviewDecisionRequestSchema extended with evidence field"
      contains: "evidence"
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "capsuleMatchSchema and retrievalMatchSchema extended with evidence hint"
      contains: "evidence"
    - path: "packages/contracts/src/domain/operations.ts"
      provides: "knowledgeListRequestSchema extended with evidence filters"
      contains: "evidenceLevel"
  key_links:
    - from: "packages/contracts/src/domain/knowledge.ts"
      to: "packages/contracts/src/domain/evidence.ts"
      via: "import evidenceMetaSchema"
      pattern: "import.*evidenceMetaSchema.*from.*evidence"
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/contracts/src/domain/evidence.ts"
      via: "import evidenceHintSchema"
      pattern: "import.*evidenceHintSchema.*from.*evidence"
    - from: "packages/contracts/src/domain/operations.ts"
      to: "packages/contracts/src/domain/evidence.ts"
      via: "import evidenceLevelSchema, evidenceSourceTypeSchema"
      pattern: "import.*evidenceLevelSchema.*from.*evidence"
---

# 58-02-PLAN: Domain Schema Extensions for Evidence

<objective>
Extend existing domain schemas (knowledge, artifacts, review, retrieval, operations) to incorporate evidence metadata fields created in 58-01-PLAN.

Purpose: Wire evidence types into the schemas that server and CLI consume, so downstream plans can use them without schema changes.
Output: Five domain schema files updated with evidence fields.
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/evidence-chain/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/evidence-chain/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/58-evidence-metadata-verification-surface/58-RESEARCH.md
@.planning/phases/58-evidence-metadata-verification-surface/58-01-SUMMARY.md

@packages/contracts/src/domain/evidence.ts
@packages/contracts/src/domain/knowledge.ts
@packages/contracts/src/domain/artifacts.ts
@packages/contracts/src/domain/review.ts
@packages/contracts/src/domain/retrieval.ts
@packages/contracts/src/domain/operations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend knowledge, artifacts, and review schemas with evidence</name>
  <files>packages/contracts/src/domain/knowledge.ts, packages/contracts/src/domain/artifacts.ts, packages/contracts/src/domain/review.ts</files>
  <action>
Extend three domain schemas to include evidence fields. Each follows the same pattern: import from evidence.js and add the new field.

**knowledge.ts:**
1. Add import: `import { evidenceMetaSchema } from './evidence.js';`
2. Extend `knowledgeEntrySchema`: add `evidenceMeta: evidenceMetaSchema.nullable().default(null)` after the `lifecycleHistory` field and before `.merge(auditMetadataSchema)`.

**artifacts.ts:**
1. Add import: `import { evidenceMetaSchema } from './evidence.js';`
2. Extend `skillArtifactSchema`: add `evidenceMeta: evidenceMetaSchema.nullable().default(null)` after the `lifecycleHistory` field.

**review.ts:**
1. Add import: `import { evidenceMetaSchema } from './evidence.js';`
2. Extend `reviewDecisionRequestSchema`: add `evidence: evidenceMetaSchema.optional()` field.

After all three changes, verify contracts build:
```bash
pnpm --filter @trapmap/contracts build
```
  </action>
  <verify>
    <automated>pnpm --filter @trapmap/contracts build</automated>
  </verify>
  <done>All three files import from evidence.js and include their respective evidence field. Contracts build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Extend retrieval and operations schemas with evidence</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/domain/operations.ts</files>
  <action>
Extend retrieval and operations schemas for evidence exposure and filtering.

**retrieval.ts:**
1. Add import: `import { evidenceHintSchema } from './evidence.js';`
2. Extend `capsuleMatchSchema`: add `evidence: evidenceHintSchema.optional()` after the `reason` field.
3. Extend `retrievalMatchSchema`: add `evidence: evidenceHintSchema.optional()` after the `citation` field.

**operations.ts:**
1. Add imports: `import { evidenceLevelSchema, evidenceSourceTypeSchema } from './evidence.js';` and `import { isoTimestampSchema } from './common.js';` (if not already imported).
2. Extend `knowledgeListRequestSchema`: add after `ownerUserId`:
```typescript
/** Filter by evidence level */
evidenceLevel: z.array(evidenceLevelSchema).optional(),
/** Filter by source type */
sourceType: z.array(evidenceSourceTypeSchema).optional(),
/** Filter entries verified before this timestamp */
verifiedBefore: isoTimestampSchema.optional(),
/** Filter entries verified after this timestamp */
verifiedAfter: isoTimestampSchema.optional(),
/** Filter entries with missing evidence metadata */
missingEvidence: z.boolean().optional(),
```

After both changes, verify contracts build and tests pass:
```bash
pnpm --filter @trapmap/contracts build
pnpm --filter @trapmap/contracts test -- --run
```
  </action>
  <verify>
    <automated>pnpm --filter @trapmap/contracts build && pnpm --filter @trapmap/contracts test -- --run</automated>
  </verify>
  <done>Retrieval schemas have optional evidence hint fields. Operations schema has evidence filter parameters. Contracts build and tests pass.</done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @trapmap/contracts build
pnpm --filter @trapmap/contracts test -- --run
```
</verification>

<success_criteria>
- knowledgeEntrySchema has `evidenceMeta: evidenceMetaSchema.nullable().default(null)`
- skillArtifactSchema has `evidenceMeta: evidenceMetaSchema.nullable().default(null)`
- reviewDecisionRequestSchema has `evidence: evidenceMetaSchema.optional()`
- capsuleMatchSchema and retrievalMatchSchema have `evidence: evidenceHintSchema.optional()`
- knowledgeListRequestSchema has evidenceLevel, sourceType, verifiedBefore, verifiedAfter, missingEvidence filters
- Contracts build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/58-evidence-metadata-verification-surface/58-02-SUMMARY.md`
</output>
