---
phase: 06-检索架构重构
plan: "03"
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.ts
  - packages/cli/src/commands/retrieval.ts
  - packages/cli/src/commands/retrieval.test.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
user_setup: []
requirements:
  - ARCH-06
  - ARCH-05
  - BOUND-01
  - BOUND-02
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Query mode is defined in shared contracts, not ad hoc in the CLI or server"
    - "Semantic remains the default mode so existing callers keep current behavior"
    - "The server orchestrator dispatch seam is ready for later hybrid and graph-assisted modes"
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "shared retrieval query mode contract"
      contains: "semantic, hybrid, graph-assisted"
    - path: "packages/cli/src/commands/retrieval.ts"
      provides: "CLI mode flag wired to shared contract"
      contains: "mode option with semantic default"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "mode-aware dispatch seam"
      contains: "semantic branch as current implementation"
  key_links:
    - from: "packages/cli/src/commands/retrieval.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "shared request schema"
      pattern: "mode"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "mode interpretation"
      pattern: "semantic"
---

<objective>
Define the shared query mode interface and wire it through contracts, CLI, and server without changing current retrieval output.

Purpose: satisfy the contract-first architecture boundary and create a stable extension seam for hybrid and graph-assisted retrieval in later phases.

Output: a shared `mode` field with `semantic` default plus end-to-end compatibility coverage.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-检索架构重构/06-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts
@packages/cli/src/commands/retrieval.ts
@packages/server/src/lib/retrieval/orchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add query mode to shared retrieval contracts</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/index.ts, packages/server/src/lib/retrieval/types.ts</files>
  <read_first>
    - packages/contracts/src/domain/retrieval.ts
    - packages/contracts/src/index.ts
    - packages/server/src/lib/retrieval/types.ts
  </read_first>
  <action>
    1. Add a shared retrieval query mode schema in `packages/contracts/src/domain/retrieval.ts` with exact values `semantic`, `hybrid`, and `graph-assisted`.
    2. Add an optional `mode` field to `retrievalQuerySchema` and default it to `semantic`.
    3. Export the new query mode schema and type through contracts so the CLI and server consume the same source of truth.
    4. Mirror the contract in server-side internal retrieval types only where necessary; do not create a separate conflicting enum in the CLI.
  </action>
  <acceptance_criteria>
    - `packages/contracts/src/domain/retrieval.ts` contains `semantic`
    - `packages/contracts/src/domain/retrieval.ts` contains `hybrid`
    - `packages/contracts/src/domain/retrieval.ts` contains `graph-assisted`
    - `packages/contracts/src/domain/retrieval.ts` contains a `mode` field defaulting to `semantic`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test && pnpm --filter @skill-shareer/contracts typecheck</automated>
  </verify>
  <done>
    - query mode is contract-defined
    - semantic remains the default for callers that omit mode
    - contracts stay the only schema source of truth
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mode through CLI and server dispatch without enabling new behavior yet</name>
  <files>packages/cli/src/commands/retrieval.ts, packages/cli/src/commands/retrieval.test.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts</files>
  <read_first>
    - packages/cli/src/commands/retrieval.ts
    - packages/cli/src/commands/retrieval.test.ts
    - packages/server/src/lib/retrieval/orchestrator.ts
    - packages/server/src/routes/retrieval.ts
  </read_first>
  <action>
    1. Add a CLI option that passes `mode` in the request body using the contract values; keep the default value `semantic`.
    2. Update the server orchestrator to read `parsed.mode` and dispatch the `semantic` path explicitly.
    3. For `hybrid` and `graph-assisted`, keep behavior behind a controlled seam that returns a clear unsupported-path error or an explicit placeholder decision only if the existing contract and tests can represent it without changing current default behavior.
    4. Keep `global` and `project` scopes untouched; query mode must not replace or overload scope filtering.
    5. Extend tests so semantic default behavior is preserved when mode is omitted.
  </action>
  <acceptance_criteria>
    - `packages/cli/src/commands/retrieval.ts` contains a mode option
    - `packages/cli/src/commands/retrieval.ts` sends `mode` in the request body
    - `packages/server/src/lib/retrieval/orchestrator.ts` contains a branch for `semantic`
    - `packages/server/src/routes/retrieval.test.ts` continues to pass valid retrieval query schema checks
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test && pnpm --filter @skill-shareer/server test -- src/routes/retrieval.test.ts src/lib/retrieval.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - CLI and server both use the same mode contract
    - semantic remains the default and current behavior is unchanged
    - future modes have an explicit dispatch seam
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/contracts test`
- `pnpm --filter @skill-shareer/cli test`
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts`
- `pnpm typecheck`
</verification>

<success_criteria>
- Shared contracts define `semantic`, `hybrid`, and `graph-assisted`
- CLI continues to depend only on shared contracts and API semantics
- Current semantic retrieval output remains compatible
- Phase 7 and Phase 9 can add new recall channels without reopening the public contract shape
</success_criteria>

<threat_model>
## Trust Boundaries

- Shared contracts boundary between CLI and server
- Mode dispatch boundary inside server orchestrator

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-05 | Tampering | contract/schema changes | mitigated | define mode once in contracts and reuse it from CLI and server |
| T-6-06 | Denial of Service | unsupported future modes | mitigated | keep non-semantic branches explicit and controlled instead of silently executing incomplete logic |
| T-6-07 | Information Disclosure | mode/scope confusion | mitigated | keep scope filtering unchanged and test that `global` and `project` semantics remain tied to business scope only |
</threat_model>

<output>
After completion, create `.planning/phases/06-检索架构重构/06-03-SUMMARY.md`
</output>
