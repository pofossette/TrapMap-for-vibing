---
phase: 06-检索架构重构
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/server/src/lib/retrieval/filters.ts
  - packages/server/src/lib/retrieval/recall/semantic.ts
  - packages/server/src/lib/retrieval/assembly.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
autonomous: true
user_setup: []
requirements:
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Filtering, recall, and result assembly are split into dedicated modules"
    - "The semantic retrieval path still returns the same response buckets"
    - "Business scope remains a knowledge scope concept, not a retrieval mode concept"
  artifacts:
    - path: "packages/server/src/lib/retrieval/filters.ts"
      provides: "eligibility and metadata filtering"
      contains: "approval/team/security/scope/label filtering logic"
    - path: "packages/server/src/lib/retrieval/recall/semantic.ts"
      provides: "semantic recall adapter"
      contains: "embedding lookup and scoring inputs"
    - path: "packages/server/src/lib/retrieval/assembly.ts"
      provides: "response shaping module"
      contains: "globalConstraints and projectKnowledge assembly"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/filters.ts"
      via: "pipeline import"
      pattern: "filters"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/recall/semantic.ts"
      via: "semantic recall import"
      pattern: "semantic"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "response assembly import"
      pattern: "assembly"
---

<objective>
Split the semantic retrieval implementation into filter, recall, and assembly modules while preserving the current response structure.

Purpose: satisfy the architectural extraction requirements and make later multi-channel retrieval work additive rather than invasive.

Output: extracted retrieval modules with regression-tested compatibility.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-检索架构重构/06-RESEARCH.md
@.planning/phases/06-检索架构重构/06-01-PLAN.md
@packages/server/src/lib/retrieval/orchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract server-side filtering logic</name>
  <files>packages/server/src/lib/retrieval/filters.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts</files>
  <read_first>
    - packages/server/src/lib/retrieval/orchestrator.ts
    - packages/server/src/lib/retrieval.ts
    - packages/server/src/lib/retrieval.test.ts
  </read_first>
  <action>
    1. Move approved-state, security-level, active-team, scope, and label filtering into `packages/server/src/lib/retrieval/filters.ts`.
    2. Export named helpers from `filters.ts` and make the orchestrator call them instead of keeping inline filter logic.
    3. Keep the meaning of `scope` unchanged: it still determines business grouping (`global` vs `project`) and must not become a query mode selector.
    4. Add or update unit tests to assert filtering still excludes unapproved entries, high-level entries, and other-team entries.
  </action>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval/filters.ts` contains filtering logic for `lifecycleState !== 'approved'`
    - `packages/server/src/lib/retrieval/filters.ts` contains team filtering using `auth.activeTeamId`
    - `packages/server/src/lib/retrieval/orchestrator.ts` imports from `./filters.js`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts</automated>
  </verify>
  <done>
    - filter logic is isolated in one module
    - orchestrator delegates filtering rather than inlining it
    - existing eligibility behavior remains covered by tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract semantic recall and response assembly modules</name>
  <files>packages/server/src/lib/retrieval/recall/semantic.ts, packages/server/src/lib/retrieval/assembly.ts, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/retrieval-workflow.test.ts</files>
  <read_first>
    - packages/server/src/lib/retrieval/orchestrator.ts
    - packages/server/src/lib/retrieval.ts
    - packages/server/src/lib/embeddings.ts
    - packages/server/src/lib/retrieval-workflow.test.ts
  </read_first>
  <action>
    1. Move embedding text generation, cached embedding reuse, cosine similarity, and score calculation into `packages/server/src/lib/retrieval/recall/semantic.ts`.
    2. Move match reason generation, `retrievalMatchSchema` shaping, and `globalConstraints` / `projectKnowledge` bucket assembly into `packages/server/src/lib/retrieval/assembly.ts`.
    3. Update the orchestrator so it explicitly calls filtering, then semantic recall, then assembly, then optional refinement.
    4. Keep the response shape identical to the current contract, including `refinementSummary: null` when no provider is configured.
  </action>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval/recall/semantic.ts` contains `cosineSimilarity`
    - `packages/server/src/lib/retrieval/assembly.ts` contains `globalConstraints`
    - `packages/server/src/lib/retrieval/assembly.ts` contains `projectKnowledge`
    - `packages/server/src/lib/retrieval/orchestrator.ts` imports both `./recall/semantic.js` and `./assembly.js`
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts</automated>
  </verify>
  <done>
    - semantic recall is isolated from filter and assembly concerns
    - response bucket assembly is isolated and reusable
    - current semantic behavior remains intact
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts`
- `pnpm typecheck`
- Inspect `packages/server/src/lib/retrieval/orchestrator.ts` to confirm the pipeline order is filter -> recall -> assembly -> refinement
</verification>

<success_criteria>
- `filters.ts`, `recall/semantic.ts`, and `assembly.ts` exist and are used by the orchestrator
- Existing API response buckets are unchanged
- Scope semantics remain business-oriented, not retrieval-mode-oriented
- Extraction leaves later hybrid and graph work with clear extension points
</success_criteria>

<threat_model>
## Trust Boundaries

- Filter boundary before recall candidate generation
- Recall boundary before result assembly
- Response assembly boundary before HTTP output

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Information Disclosure | filters extraction | mitigated | regression-test approval, team, and level checks after extraction |
| T-6-04 | Tampering | response assembly split | mitigated | validate `globalConstraints` and `projectKnowledge` bucket semantics via workflow tests |
| T-6-05 | Elevation of Privilege | orchestration order | mitigated | keep filter stage ahead of recall and document the order in orchestrator code and tests |
</threat_model>

<output>
After completion, create `.planning/phases/06-检索架构重构/06-02-SUMMARY.md`
</output>
