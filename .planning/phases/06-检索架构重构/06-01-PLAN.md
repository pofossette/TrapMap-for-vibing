---
phase: 06-检索架构重构
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/retrieval.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/retrieval-workflow.test.ts
autonomous: true
user_setup: []
requirements:
  - ARCH-01
  - BOUND-03
  - BOUND-05
must_haves:
  truths:
    - "searchKnowledge has a dedicated orchestrator entrypoint under packages/server/src/lib/retrieval/"
    - "The pipeline order stays approval -> permission/team filtering -> retrieval -> response assembly"
    - "Server route integration keeps the current retrieval API behavior"
  artifacts:
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "retrieval orchestration entrypoint"
      contains: "export async function searchKnowledge("
    - path: "packages/server/src/lib/retrieval.ts"
      provides: "compatibility facade"
      contains: "re-export or thin wrapper for searchKnowledge"
  key_links:
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "searchKnowledge import"
      pattern: "searchKnowledge"
---

<objective>
Create a dedicated retrieval orchestrator entrypoint without changing current semantic retrieval behavior.

Purpose: establish the architectural seam Phase 7 and later phases can extend, while preserving all server-side business boundary enforcement.

Output: a server retrieval orchestrator module plus route-level regression coverage.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-检索架构重构/06-RESEARCH.md
@packages/server/src/lib/retrieval.ts
@packages/server/src/routes/retrieval.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce retrieval orchestrator module</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval.ts</files>
  <read_first>
    - packages/server/src/lib/retrieval.ts
    - packages/server/src/lib/context.ts
    - packages/contracts/src/domain/retrieval.ts
  </read_first>
  <action>
    1. Create `packages/server/src/lib/retrieval/orchestrator.ts` and move the `searchKnowledge` entrypoint there.
    2. Create `packages/server/src/lib/retrieval/types.ts` for internal pipeline types used by the orchestrator.
    3. Keep `packages/server/src/lib/retrieval.ts` as the public import path for current callers by re-exporting `searchKnowledge` and `updateEntryEmbeddingCache` from the new module location.
    4. Preserve the current semantic path and the current response schema exactly.
    5. Preserve the execution order so the orchestrator still applies approved-state, security-level, and active-team constraints before response output is returned.
  </action>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval/orchestrator.ts` contains `export async function searchKnowledge(`
    - `packages/server/src/lib/retrieval.ts` contains `from './retrieval/orchestrator.js'`
    - `packages/server/src/lib/retrieval/types.ts` exists and exports at least one retrieval pipeline type
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts</automated>
  </verify>
  <done>
    - retrieval entrypoint lives in a dedicated orchestrator module
    - existing imports continue to work through the compatibility facade
    - no behavior change is introduced yet
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire route integration to the orchestrator seam</name>
  <files>packages/server/src/routes/retrieval.ts, packages/server/src/lib/retrieval-workflow.test.ts, packages/server/src/routes/retrieval.test.ts</files>
  <read_first>
    - packages/server/src/routes/retrieval.ts
    - packages/server/src/lib/retrieval-workflow.test.ts
    - packages/server/src/routes/retrieval.test.ts
  </read_first>
  <action>
    1. Update the retrieval route import so it points at the new orchestrator-backed retrieval module path.
    2. Keep auth resolution and `requirePermission(auth, 'knowledge:search')` in the route; do not move RBAC into contracts or CLI.
    3. Extend regression coverage to prove the route still exposes `POST /v1/retrieval/search` and the workflow tests still validate approved-only search behavior.
  </action>
  <acceptance_criteria>
    - `packages/server/src/routes/retrieval.ts` contains `requirePermission(auth, 'knowledge:search')`
    - `packages/server/src/routes/retrieval.ts` contains `searchKnowledge(`
    - `packages/server/src/lib/retrieval-workflow.test.ts` still asserts unapproved entries are excluded from search results
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts</automated>
  </verify>
  <done>
    - route integration uses the orchestrator seam
    - RBAC and approval gates remain server-side
    - retrieval API behavior is regression-covered
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts src/routes/retrieval.test.ts`
- `pnpm typecheck`
- Inspect `packages/server/src/lib/retrieval.ts` to confirm it is now a facade, not the full pipeline implementation
</verification>

<success_criteria>
- Orchestrator entrypoint exists under `packages/server/src/lib/retrieval/`
- Existing server callers still resolve `searchKnowledge`
- Approval, RBAC, team filtering, and output ordering remain server-owned
- No retrieval response shape changes are introduced
</success_criteria>

<threat_model>
## Trust Boundaries

- HTTP request boundary at `packages/server/src/routes/retrieval.ts`
- Auth/RBAC boundary before retrieval orchestration
- Store snapshot boundary inside server retrieval pipeline

## STRIDE Register

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Spoofing | retrieval route | mitigated | keep auth resolution and permission checks in route before orchestrator execution |
| T-6-02 | Information Disclosure | orchestrator extraction | mitigated | preserve approved/team/level filtering order and cover with regression tests |
| T-6-03 | Tampering | retrieval facade refactor | mitigated | use compatibility re-export instead of changing public import paths abruptly |
</threat_model>

<output>
After completion, create `.planning/phases/06-检索架构重构/06-01-SUMMARY.md`
</output>
