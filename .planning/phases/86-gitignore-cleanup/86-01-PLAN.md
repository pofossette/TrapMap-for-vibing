---
phase: 86-gitignore-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/CONTRIBUTING.md
autonomous: true
requirements:
  - INFRA-86-01
  - INFRA-86-02
  - INFRA-86-03

must_haves:
  truths:
    - "dist/ is excluded from version control and no dist files are tracked"
    - ".gitignore has no redundant or conflicting patterns"
    - "Git repository loose objects are pruned, repo size reduced"
    - "docs/CONTRIBUTING.md documents .gitignore conventions for contributors"
  artifacts:
    - path: ".gitignore"
      provides: "Clean, deduplicated gitignore rules"
      contains: "dist/"
    - path: "docs/CONTRIBUTING.md"
      provides: "Contribution guidelines including gitignore conventions"
      contains: ".gitignore"
  key_links:
    - from: ".gitignore"
      to: "dist/"
      via: "pattern rule"
      pattern: "^dist/$"
---

<objective>
Clean up git repository configuration and reduce repository size.

Purpose: Ensure build artifacts are properly excluded, remove redundant .gitignore patterns, prune loose git objects, and document gitignore conventions for contributors.
Output: Cleaned .gitignore, pruned git objects, updated CONTRIBUTING.md
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-gitignore-cleanup/86-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up .gitignore and prune git objects</name>
  <files>.gitignore</files>
  <read_first>
    .gitignore
  </read_first>
  <acceptance_criteria>
    - `.gitignore` no longer contains the conflicting `.claude/` then `!.claude/` then `.claude/*` pattern sequence
    - `.gitignore` retains `!.claude/skills/` and `!.claude/skills/trapmap-knowledge-workflow/` and `!.claude/skills/trapmap-knowledge-workflow/**` negation patterns (these files ARE tracked in git)
    - `git ls-files --cached | grep -c "^dist/"` outputs 0
    - `git count-objects -v` shows fewer loose objects than the starting count of 3809
    - `git status --short` shows no untracked dist/ or build/ files
  </acceptance_criteria>
  <action>
    **Part A: Fix .gitignore redundant patterns**

    The current `.gitignore` lines 30-36 have a contradictory pattern for `.claude/`:
    ```
    .claude/
    !.claude/
    .claude/*
    !.claude/skills/
    !.claude/skills/trapmap-knowledge-workflow/
    !.claude/skills/trapmap-knowledge-workflow/**
    ```

    Lines 31 (`.claude/`) and 32 (`!.claude/`) cancel each other. Replace the entire AI/Code Assistant tools section with this clean version:
    ```
    # AI/Code Assistant tools
    .agent/
    .claude/*
    !.claude/skills/
    !.claude/skills/trapmap-knowledge-workflow/
    !.claude/skills/trapmap-knowledge-workflow/**
    .codex/
    .gemini/
    .kilo/
    .opencode/
    ```

    This preserves the existing intent: ignore everything in `.claude/` EXCEPT the tracked skill workflow files.

    Also check: are there any other redundant patterns? For example, `*.log` appears on line 10 AND line 63. Remove the duplicate on line 10 (keep the one in the Logs section since it is grouped with other log patterns). And `.DS_Store` appears on line 28 AND line 59 -- remove the duplicate on line 59 (keep it in the IDE/Editor section).

    **Part B: Verify no tracked artifacts should be untracked**

    Run these verification commands:
    - `git ls-files --cached | grep -E "^dist/"` -- should output nothing (confirmed already)
    - `git ls-files --cached | grep -E "\.(js|cjs|mjs)$" | grep -v node_modules` -- should output nothing (no compiled JS tracked)
    - `git ls-files --cached | grep -E "\.tsbuildinfo$"` -- should output nothing
    - `git ls-files --cached | grep -E "\.env[^.]"` -- should output nothing

    If any results are found, run `git rm --cached` on those files to untrack them.

    **Part C: Prune git objects**

    Run `git gc --prune=now` to clean up the 22.91 MiB of loose objects. Then run `git prune` and `git repack -a -d` for thorough cleanup. Verify the reduced size with `git count-objects -v`.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && git ls-files --cached | grep -E "^dist/" | wc -l | grep -q "^0$" && echo "PASS: no dist/ tracked" || echo "FAIL"; git count-objects -v | head -4</automated>
  </verify>
  <done>
    - .gitignore has no redundant or conflicting patterns
    - No dist/, build/, compiled JS, .tsbuildinfo, or .env files tracked in git
    - Git loose objects pruned, repository size reduced from 22.91 MiB loose
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/CONTRIBUTING.md with gitignore conventions</name>
  <files>docs/CONTRIBUTING.md</files>
  <read_first>
    docs/CONTRIBUTING.md
    .gitignore
  </read_first>
  <acceptance_criteria>
    - `docs/CONTRIBUTING.md` contains a section titled "Gitignore 与构建产物" (or equivalent Chinese heading matching existing style)
    - The section mentions that `dist/`, `build/`, `coverage/`, `node_modules/` are all gitignored
    - The section instructs contributors to run `git status` before committing to verify no build artifacts are staged
    - The section mentions that `pnpm build` output (dist/) must never be committed
    - File still starts with `# 投稿指南` (existing heading preserved)
    - All existing sections remain intact
  </acceptance_criteria>
  <action>
    Add a new section "## Gitignore 与构建产物" to `docs/CONTRIBUTING.md`, inserted BEFORE the "## Pull Request 流程" section (i.e., after the "## 代码规范" section and its subsections).

    Content for the new section (in Chinese to match existing style):

    ```markdown
    ## Gitignore 与构建产物

    仓库根目录的 `.gitignore` 已配置忽略以下目录和文件：

    | 类别 | 忽略项 |
    |------|--------|
    | 构建输出 | `dist/`、`build/`、`*.tsbuildinfo` |
    | 依赖 | `node_modules/` |
    | 测试覆盖率 | `coverage/`、`*.lcov`、`.nyc_output/` |
    | 环境变量 | `.env`、`.env.*`（保留 `.env.example`） |
    | 运行时数据 | `data/`、`.data/`、`logs/` |

    **注意事项：**

    - `pnpm build` 生成的 `dist/` 目录不得提交到版本控制
    - 提交前运行 `git status` 确认无构建产物被暂存
    - 如需添加新的忽略规则，修改根目录 `.gitignore`（而非各包内的 `.gitignore`）
    - AI 工具目录（`.claude/`、`.agent/` 等）大部分已忽略，仅保留 `trapmap-knowledge-workflow` Skill 文件
    ```

    This preserves all existing content and adds the gitignore documentation section in the appropriate location.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && grep -c "Gitignore 与构建产物" docs/CONTRIBUTING.md && grep -c "dist/" docs/CONTRIBUTING.md && grep -c "pnpm build" docs/CONTRIBUTING.md</automated>
  </verify>
  <done>
    - docs/CONTRIBUTING.md has new section documenting gitignore conventions
    - Contributors are informed about which files are gitignored and why
    - Existing content untouched
  </done>
</task>

</tasks>

<verification>
1. `git ls-files --cached | grep -E "^dist/"` returns nothing
2. `git status --short` shows clean working tree (except planned changes)
3. `git count-objects -v` shows reduced loose object count
4. `grep -c "Gitignore" docs/CONTRIBUTING.md` returns >= 1
5. `.gitignore` has no duplicate patterns (no two lines with identical content)
</verification>

<success_criteria>
- .gitignore is clean with no redundant or conflicting patterns
- No build artifacts are tracked in git
- Git loose objects pruned (count reduced from 3809)
- docs/CONTRIBUTING.md documents gitignore conventions for contributors
- `git status` shows clean state
</success_criteria>

<output>
After completion, create `.planning/phases/86-gitignore-cleanup/86-01-SUMMARY.md`
</output>
