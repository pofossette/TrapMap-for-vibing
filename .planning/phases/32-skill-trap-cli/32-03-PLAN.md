---
wave: 3
depends_on:
  - 32-02
files_modified:
  - packages/cli/src/commands/trap.ts
  - packages/cli/src/commands/knowledge.ts
  - packages/cli/src/index.ts
autonomous: true
---

# PLAN-32-03: Create `trap` CLI Command Group

**Goal:** Add a new `trap` CLI command group that aliases existing knowledge functionality, establishing the domain separation while maintaining backward compatibility with existing `knowledge` commands.

## Threat Model

**Attack Surface:**
- New CLI commands expose existing functionality through different paths
- No new security implications as commands delegate to existing handlers

**Potential Threats:**
1. Command confusion between `trap` and `knowledge` namespaces
2. Deprecation warnings might be missed by users

**Mitigations:**
- Existing `knowledge` commands remain fully functional
- `trap` commands are aliases with clear documentation
- No changes to server-side authorization logic

---

## Task 1: Create trap CLI command module

<read_first>
- packages/cli/src/commands/knowledge.ts
- packages/cli/src/lib/config.ts
- packages/cli/src/lib/http.ts
- packages/cli/src/lib/input.ts
- packages/cli/src/lib/output.ts
</read_first>

<action>
Create `packages/cli/src/commands/trap.ts` as a new command group for trap operations.

The trap command module mirrors the knowledge module but with clearer domain naming:

```typescript
import type {
  KnowledgeEntry,
  KnowledgeEntryResponse,
  KnowledgeHistoryResponse,
} from '@trapmap/contracts';
import {
  knowledgeEntryResponseSchema,
  knowledgeHistoryResponseSchema,
} from '@trapmap/contracts';
import type { Command } from 'commander';

import { loadCliState } from '../lib/config.js';
import { apiRequest, requireSessionToken } from '../lib/http.js';
import { collectValues, resolveTextInput } from '../lib/input.js';
import { printResult } from '../lib/output.js';

interface TrapCommandOptions {
  allowInspect: boolean;
  allowSubmit: boolean;
}

function formatEntry(entry: KnowledgeEntry): string {
  const lines = [
    `${entry.id} [${entry.lifecycleState}]`,
    `Scope: ${entry.scope}`,
    `Required level: ${entry.requiredLevel}`,
    `Owner: ${entry.owner.handle}`,
    `Labels: ${entry.labels.join(', ')}`,
    `Shortcut: ${entry.shortcut}`,
    `History: ${entry.history.length} revision(s)`,
  ];

  if (entry.agentReview) {
    lines.push(`Agent review: ${entry.agentReview.status}`);
    if (entry.agentReview.notes.length > 0) {
      lines.push(`Agent notes: ${entry.agentReview.notes.join(' | ')}`);
    }
  }

  if (entry.reviewHistory.length > 0) {
    const lastDecision = entry.reviewHistory.at(-1);
    if (lastDecision) {
      lines.push(
        `Last decision: ${lastDecision.decision} by ${lastDecision.decidedBy.handle} (${lastDecision.notes})`,
      );
    }
  }

  return lines.join('\n');
}

function formatHistory(items: KnowledgeEntry[]): string {
  if (items.length === 0) {
    return 'No traps found';
  }
  return items.map((entry) => formatEntry(entry)).join('\n\n');
}

export function registerTrapCommands(
  program: Command,
  options: TrapCommandOptions,
): void {
  const trap = program.command('trap').description('Manage trap entries (pitfall/warning knowledge)');

  if (options.allowSubmit) {
    trap
      .command('submit')
      .description('Submit a new trap entry for review')
      .requiredOption('--scope <scope>', 'Trap scope: global or project')
      .requiredOption('--label <label>', 'Trap label', collectValues, [])
      .requiredOption('--shortcut <text>', 'One-line pitfall shortcut')
      .option('--detail <text>', 'Detailed pitfall and fix description')
      .option('--file <path>', 'Read detail text from a file')
      .option('--stdin', 'Read detail text from stdin')
      .option('--required-level <n>', 'Override required security level')
      .option('--json', 'Output JSON')
      .action(
        async (flags: {
          detail?: string;
          file?: string;
          json?: boolean;
          label: string[];
          requiredLevel?: string;
          scope: 'global' | 'project';
          shortcut: string;
          stdin?: boolean;
        }) => {
          const state = await loadCliState();
          requireSessionToken(state);
          const detail = await resolveTextInput(
            {
              ...(flags.detail !== undefined ? { text: flags.detail } : {}),
              ...(flags.file !== undefined ? { file: flags.file } : {}),
              ...(flags.stdin !== undefined ? { stdin: flags.stdin } : {}),
            },
            'detail',
          );
          const response = await apiRequest<KnowledgeEntryResponse>(state, {
            method: 'POST',
            path: '/v1/knowledge',
            body: {
              scope: flags.scope,
              labels: flags.label,
              shortcut: flags.shortcut,
              detail,
              requiredLevel:
                flags.requiredLevel !== undefined ? Number(flags.requiredLevel) : undefined,
            },
          });
          const parsed = knowledgeEntryResponseSchema.parse(response.data);

          printResult(parsed, flags, ({ entry }) =>
            [
              `Submitted ${entry.id}`,
              `Lifecycle: ${entry.lifecycleState}`,
              `Shortcut: ${entry.shortcut}`,
            ].join('\n'),
          );
        },
      );

    trap
      .command('resubmit')
      .description('Resubmit a rejected trap entry')
      .argument('<entryId>', 'Trap entry identifier')
      .requiredOption('--label <label>', 'Trap label', collectValues, [])
      .requiredOption('--shortcut <text>', 'Updated pitfall shortcut')
      .option('--detail <text>', 'Updated detailed explanation')
      .option('--file <path>', 'Read updated detail text from a file')
      .option('--stdin', 'Read updated detail text from stdin')
      .option('--json', 'Output JSON')
      .action(
        async (
          entryId: string,
          flags: {
            detail?: string;
            file?: string;
            json?: boolean;
            label: string[];
            shortcut: string;
            stdin?: boolean;
          },
        ) => {
          const state = await loadCliState();
          requireSessionToken(state);
          const detail = await resolveTextInput(
            {
              ...(flags.detail !== undefined ? { text: flags.detail } : {}),
              ...(flags.file !== undefined ? { file: flags.file } : {}),
              ...(flags.stdin !== undefined ? { stdin: flags.stdin } : {}),
            },
            'detail',
          );
          const response = await apiRequest<KnowledgeEntryResponse>(state, {
            method: 'POST',
            path: `/v1/knowledge/${entryId}/resubmit`,
            body: {
              labels: flags.label,
              shortcut: flags.shortcut,
              detail,
            },
          });
          const parsed = knowledgeEntryResponseSchema.parse(response.data);

          printResult(parsed, flags, ({ entry }) =>
            [
              `Resubmitted ${entry.id}`,
              `Lifecycle: ${entry.lifecycleState}`,
              `Revision: ${entry.latestRevision.revision}`,
            ].join('\n'),
          );
        },
      );
  }

  if (options.allowInspect) {
    trap
      .command('list')
      .description('List your trap submissions')
      .option('--json', 'Output JSON')
      .action(async (flags: { json?: boolean }) => {
        const state = await loadCliState();
        requireSessionToken(state);

        const response = await apiRequest<KnowledgeHistoryResponse>(state, {
          path: '/v1/knowledge/mine',
        });
        const parsed = knowledgeHistoryResponseSchema.parse(response.data);

        printResult(parsed, flags, ({ items }) => formatHistory(items));
      });

    trap
      .command('show')
      .description('Show details of a trap entry')
      .argument('<entryId>', 'Trap entry identifier')
      .option('--json', 'Output JSON')
      .action(async (entryId: string, flags: { json?: boolean }) => {
        const state = await loadCliState();
        requireSessionToken(state);

        const response = await apiRequest<KnowledgeEntryResponse>(state, {
          path: `/v1/knowledge/${entryId}`,
        });
        const parsed = knowledgeEntryResponseSchema.parse(response.data);

        printResult(parsed, flags, ({ entry }) => formatEntry(entry));
      });
  }
}
```

Key points:
- Creates `trap` command group with `submit`, `resubmit`, `list`, `show` subcommands
- Uses same API paths as knowledge commands (`/v1/knowledge/*`)
- Clearer naming: `trap submit` vs `submit`
- Format functions updated for trap terminology
</action>

<acceptance_criteria>
- File `packages/cli/src/commands/trap.ts` exists
- File exports `registerTrapCommands(program, options)` function
- Command group `trap` has subcommands: `submit`, `resubmit`, `list`, `show`
- `trap submit` calls `/v1/knowledge` endpoint (same as existing)
- `trap list` calls `/v1/knowledge/mine` endpoint
- `trap show <entryId>` calls `/v1/knowledge/:entryId` endpoint
- `trap resubmit <entryId>` calls `/v1/knowledge/:entryId/resubmit` endpoint
- Descriptions mention "trap" terminology
</acceptance_criteria>

---

## Task 2: Register trap commands in CLI index

<read_first>
- packages/cli/src/index.ts
- packages/cli/src/commands/trap.ts
- packages/cli/src/commands/knowledge.ts
</read_first>

<action>
Update `packages/cli/src/index.ts` to register the new trap commands alongside existing knowledge commands.

Add import and registration:

```typescript
import { registerTrapCommands } from './commands/trap.js';

// ... existing registrations ...

// Register trap commands (new domain-separated interface)
registerTrapCommands(program, {
  allowInspect: visibility.allowKnowledgeInspect,
  allowSubmit: visibility.allowKnowledgeSubmit,
});

// Keep existing knowledge commands for backward compatibility
registerKnowledgeCommands(program, {
  allowInspect: visibility.allowKnowledgeInspect,
  allowSubmit: visibility.allowKnowledgeSubmit,
});
```

Also update `api:list` command to include new trap commands:

```typescript
const availableCommands = [
  // ... existing ...
  ...(visibility.allowKnowledgeSubmit ? ['trap submit', 'trap resubmit'] : []),
  ...(visibility.allowKnowledgeInspect ? ['trap list', 'trap show'] : []),
  // ... existing knowledge commands remain ...
];
```

The key requirements:
- Import `registerTrapCommands` from `./commands/trap.js`
- Call `registerTrapCommands` with same visibility flags as knowledge commands
- Add trap commands to `api:list` output
- Existing `registerKnowledgeCommands` call remains unchanged (backward compatibility)
</action>

<acceptance_criteria>
- `packages/cli/src/index.ts` imports `registerTrapCommands` from `./commands/trap.js`
- `registerTrapCommands` is called with `allowInspect` and `allowSubmit` options
- `grep -n 'registerTrapCommands' packages/cli/src/index.ts` returns matches
- `api:list` command output includes `trap submit`, `trap resubmit`, `trap list`, `trap show`
- Existing `registerKnowledgeCommands` call still present
</acceptance_criteria>

---

## Verification

After all tasks:

1. `pnpm --filter @trapmap/cli build` - compiles without errors
2. `trapmap trap --help` shows trap command group with subcommands
3. `trapmap api:list` includes trap commands
4. Existing knowledge commands still work: `trapmap submit --help`
