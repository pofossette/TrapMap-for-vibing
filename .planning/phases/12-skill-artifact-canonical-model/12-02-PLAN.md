---
phase: 12-skill-artifact-canonical-model
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/artifacts/model.ts
  - packages/server/src/lib/artifacts/model.test.ts
autonomous: true
requirements:
  - ARTF-02
  - ARTF-03
  - CAPS-02
  - CAPS-03
  - COMP-02
must_haves:
  truths:
    - "The server can persist canonical skill-artifact records beside legacy `knowledgeEntries` without changing public routes."
    - "Artifact revisions keep governance, review, and audit lineage at the artifact boundary instead of inventing per-capsule ACLs."
    - "Assets and scripts are stored as activation metadata and descriptors only, not as model-context payloads."
  artifacts:
    - path: "packages/server/src/lib/store.ts"
      provides: "additive persistent store records for skill artifacts"
      contains: "skillArtifacts"
    - path: "packages/server/src/lib/artifacts/model.ts"
      provides: "artifact record builders and contract mappers"
      contains: "createSkillArtifactRecord"
    - path: "packages/server/src/lib/artifacts/model.test.ts"
      provides: "server-side regression coverage for additive persistence and governance inheritance"
      contains: "createSkillArtifactRecord"
  key_links:
    - from: "packages/server/src/lib/artifacts/model.ts"
      to: "packages/contracts/src/domain/artifacts.ts"
      via: "server record to shared-contract mapper"
      pattern: "skillArtifactSchema"
    - from: "packages/server/src/lib/store.ts"
      to: "packages/server/src/lib/artifacts/model.ts"
      via: "artifact record persistence in JsonStore"
      pattern: "skillArtifacts|SkillArtifactRecord"
    - from: "packages/server/src/lib/artifacts/model.test.ts"
      to: "packages/server/src/lib/artifacts/model.ts"
      via: "governance inheritance and metadata-only script coverage"
      pattern: "requiredLevel|scope|scriptDescriptors"
---

<objective>
Add the server-side storage model for Phase 12 as an additive artifact aggregate beside `KnowledgeEntry`, with governance and audit lineage preserved at the artifact boundary.

Purpose: satisfy ARTF-02, ARTF-03, CAPS-02, CAPS-03, and COMP-02 while staging stable server persistence for later import/export, retrieval, and activation phases without introducing new public routes in this phase.
Output: persistent `SkillArtifactRecord` structures in the JSON store plus a server-internal artifact model module and focused tests.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-skill-artifact-canonical-model/12-RESEARCH.md
@.planning/phases/12-skill-artifact-canonical-model/12-VALIDATION.md
@.planning/phases/05-admin-operations-and-hardening/05-03-SUMMARY.md
@.planning/phases/11-索引生命周期集成/11-01-SUMMARY.md
@packages/contracts/src/domain/artifacts.ts
@packages/server/src/lib/store.ts
@packages/server/src/lib/knowledge.ts
@packages/server/src/lib/context.ts

<interfaces>
From `packages/server/src/lib/store.ts`:
```typescript
export interface StoreData {
  counters: Record<string, number>;
  users: UserRecord[];
  teams: TeamRecord[];
  memberships: MembershipRecord[];
  accessKeys: AccessKeyRecord[];
  sessions: SessionRecord[];
  knowledgeEntries: KnowledgeRecord[];
  auditEvents: AuditEventRecord[];
}
```

From `packages/server/src/lib/knowledge.ts`:
```typescript
export function createKnowledgeEntryRecord(args: {
  store: JsonStore;
  data: StoreData;
  ownerUserId: string;
  teamId: string | null;
  payload: KnowledgeSubmission;
  requiredLevel: number;
  createdAt: string;
  preReview: AgentReviewResult;
}): KnowledgeRecord;
```

From `packages/contracts/src/domain/artifacts.ts` after Plan 12-01:
```typescript
export const skillArtifactSchema;
export const skillArtifactRevisionSchema;
export const skillArtifactFileSchema;
export const clientManifestSchema;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 server tests for additive artifact persistence</name>
  <read_first>packages/contracts/src/domain/artifacts.ts, packages/server/src/lib/store.ts, packages/server/src/lib/knowledge.ts, .planning/phases/12-skill-artifact-canonical-model/12-VALIDATION.md</read_first>
  <files>packages/server/src/lib/artifacts/model.test.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/artifacts/model.test.ts`
    - `rg -n "createSkillArtifactRecord|toSkillArtifact|skillArtifacts" packages/server/src/lib/artifacts/model.test.ts`
    - `rg -n "assets.*activationOnly|scripts.*scriptDescriptors|requiredLevel|scope" packages/server/src/lib/artifacts/model.test.ts`
  </acceptance_criteria>
  <behavior>
    - Test 1: `createSkillArtifactRecord()` appends a new artifact aggregate to `StoreData.skillArtifacts` without mutating `knowledgeEntries`.
    - Test 2: `toSkillArtifact()` serializes the additive server record back through the shared artifact contract and preserves `scope`, `teamId`, `requiredLevel`, review history, and lifecycle history.
    - Test 3: assets remain activation-only manifest entries and scripts remain descriptor-only metadata in persisted revisions.
  </behavior>
  <action>Create `packages/server/src/lib/artifacts/model.test.ts` first. Build store-backed fixtures using the same JSON-store patterns as `createKnowledgeEntryRecord()`, then write failing tests for `createSkillArtifactRecord`, `appendSkillArtifactRevision`, and `toSkillArtifact`. The fixture revision must include one `SKILL.md` manifest entry, one `references/...` entry, one `assets/...` entry marked `activationOnly: true`, and one `scripts/...` entry with descriptor metadata but no script body. Assert that the new aggregate lives under `data.skillArtifacts` and that legacy `data.knowledgeEntries` length is unchanged.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/artifacts/model.test.ts</automated>
  </verify>
  <done>A dedicated server test file exists and pins additive persistence, governance inheritance, and metadata-only treatment for assets/scripts before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive store records and governance-aware artifact mappers</name>
  <read_first>packages/server/src/lib/store.ts, packages/server/src/lib/knowledge.ts, packages/server/src/lib/artifacts/model.test.ts, packages/contracts/src/domain/artifacts.ts, .planning/phases/12-skill-artifact-canonical-model/12-RESEARCH.md</read_first>
  <files>packages/server/src/lib/store.ts, packages/server/src/lib/artifacts/model.ts</files>
  <acceptance_criteria>
    - `rg -n "export interface SkillArtifactRecord" packages/server/src/lib/store.ts`
    - `rg -n "skillArtifacts: SkillArtifactRecord\\[\\]" packages/server/src/lib/store.ts`
    - `rg -n "export function createSkillArtifactRecord" packages/server/src/lib/artifacts/model.ts`
    - `rg -n "export function appendSkillArtifactRevision" packages/server/src/lib/artifacts/model.ts`
    - `rg -n "export function toSkillArtifact" packages/server/src/lib/artifacts/model.ts`
    - `rg -n "scriptDescriptors|activationOnly|sourceHash" packages/server/src/lib/artifacts/model.ts`
  </acceptance_criteria>
  <action>Extend `packages/server/src/lib/store.ts` with concrete record types `SkillArtifactFileRecord`, `SkillScriptDescriptorRecord`, `DerivedSkillProfileRecord`, `DerivedSkillCapsuleRecord`, `ClientManifestRecord`, `SkillArtifactRevisionRecord`, and `SkillArtifactRecord`, then add `skillArtifacts: SkillArtifactRecord[]` to `StoreData` and `EMPTY_STORE`. Create `packages/server/src/lib/artifacts/model.ts` with `createSkillArtifactRecord(args)`, `appendSkillArtifactRevision(args)`, and `toSkillArtifact(data, record)`. The root record must persist `id`, `teamId`, `scope`, `requiredLevel`, `ownerUserId`, `lifecycleState`, `reviewHistory`, `reviewNotes`, `lifecycleHistory`, `createdAt`, and `updatedAt`. The revision record must persist immutable `revision`, `sourceHash`, `submittedAt`, `submittedByUserId`, `files`, `scriptDescriptors`, and `derived` outputs. `toSkillArtifact` must serialize through the new shared schemas from Plan 12-01 and resolve actor refs the same way `toKnowledgeEntry()` does. Keep this module server-internal; do not add routes, CLI commands, or migration logic. When mapping files, enforce Phase 12 boundaries: `assets/` entries always set `activationOnly: true` and `includeInDerivation: false`; `scripts/` entries persist descriptor metadata only; `references/` and `SKILL.md` can remain eligible for derivation in later plans.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/artifacts/model.test.ts</automated>
  </verify>
  <done>The server has a persistent additive artifact aggregate and governance-aware mapper layer that preserves current approval/scope/security/audit semantics without changing any public surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON store -> artifact mappers | persisted records become shared-contract responses for later routes |
| artifact root -> revision-derived outputs | governance is inherited downward from the artifact boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | `packages/server/src/lib/store.ts` | mitigate | add a distinct `skillArtifacts` collection beside `knowledgeEntries` so Phase 12 stays additive and does not corrupt legacy records |
| T-12-06 | Information Disclosure | `packages/server/src/lib/artifacts/model.ts` | mitigate | persist `assets/` as `activationOnly: true` and `scripts/` as descriptor metadata only, never as derived text bodies |
| T-12-07 | Elevation of Privilege | `packages/server/src/lib/artifacts/model.ts` | mitigate | store `scope`, `teamId`, `requiredLevel`, and lifecycle/review history on the artifact root and serialize capsules/manifests through inherited governance only |
| T-12-08 | Repudiation | `packages/server/src/lib/artifacts/model.ts` | mitigate | carry review history, lifecycle history, submitter ids, and timestamps onto the additive aggregate so later phases preserve audit lineage |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/artifacts/model.test.ts`
</verification>

<success_criteria>
- `StoreData` contains additive `skillArtifacts` persistence beside `knowledgeEntries`
- `packages/server/src/lib/artifacts/model.ts` creates, appends, and serializes artifact aggregates through shared contracts
- model tests prove governance inheritance and metadata-only handling for `assets/` and `scripts`
</success_criteria>

<output>
After completion, create `.planning/phases/12-skill-artifact-canonical-model/12-02-SUMMARY.md`
</output>
