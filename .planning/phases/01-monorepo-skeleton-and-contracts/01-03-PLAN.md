---
phase: 01-monorepo-skeleton-and-contracts
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/api-surface.md
  - docs/architecture.md
  - .agents/skills/skill-shareer-knowledge/SKILL.md
  - .agents/skills/skill-shareer-knowledge/templates/submission-template.md
  - packages/cli/src/index.ts
  - packages/server/src/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - A documented API list exists for every planned v1 workflow
    - Claude-compatible project skill scaffolding is present in the repo structure
  artifacts:
    - path: docs/api-surface.md
      provides: v1 HTTP API list
    - path: .agents/skills/skill-shareer-knowledge/SKILL.md
      provides: Claude-compatible project skill scaffold
  key_links:
    - from: docs/api-surface.md
      to: packages/server/src/index.ts
      via: documented routes match bootstrap server registration
---

# Phase 1 Plan 03: API Surface and Skill Scaffolding

## Objective
- **What:** Document the initial server surface and add Claude-compatible project skill scaffolding.
- **Why:** The roadmap requires early API clarity and in-repo skill compatibility.
- **Output:** API documentation plus a discoverable project skill directory and bootstrap docs.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-skeleton-and-contracts/01-CONTEXT.md

## Tasks

<task type="auto">
  <name>Document the v1 API surface</name>
  <files>
    docs/api-surface.md
    docs/architecture.md
  </files>
  <action>
    Create concise project documentation that lists the planned HTTP endpoints, request and response contract references, and package responsibilities for CLI, server, and contracts. Keep the route list aligned with the monorepo layout so later phases can implement against it directly.
  </action>
  <verify>
    test -f docs/api-surface.md && test -f docs/architecture.md
  </verify>
  <done>
    API and package architecture docs exist and describe the planned v1 workflows.
  </done>
</task>

<task type="auto">
  <name>Add Claude-compatible project skill scaffolding</name>
  <files>
    .agents/skills/skill-shareer-knowledge/SKILL.md
    .agents/skills/skill-shareer-knowledge/templates/submission-template.md
    packages/cli/src/index.ts
    packages/server/src/index.ts
  </files>
  <action>
    Add an example project skill directory with valid frontmatter and a local template asset that demonstrates how knowledge could be authored for Skill Shareer. Update the CLI and server placeholders so they reference the documented product purpose rather than empty stubs.
  </action>
  <verify>
    test -f .agents/skills/skill-shareer-knowledge/SKILL.md && pnpm typecheck
  </verify>
  <done>
    Claude-compatible skill scaffolding exists in-repo and the workspace still type-checks.
  </done>
</task>

## Verification
Check that the docs and skill files exist and re-run workspace typecheck.

## Success Criteria
- [ ] v1 API surface documentation is checked into the repo
- [ ] Project skill scaffolding follows Claude-compatible conventions
- [ ] Workspace remains type-safe after docs and bootstrap updates
