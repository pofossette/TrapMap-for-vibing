---
phase: 01-monorepo-skeleton-and-contracts
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/contracts/src/domain/common.ts
  - packages/contracts/src/domain/auth.ts
  - packages/contracts/src/domain/team.ts
  - packages/contracts/src/domain/knowledge.ts
  - packages/contracts/src/domain/review.ts
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/domain/operations.ts
  - packages/contracts/src/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Shared contracts define the core auth, knowledge, review, retrieval, and operations payloads
    - CLI and server can import the same runtime-validated schemas from the contracts package
  artifacts:
    - path: packages/contracts/src/domain/auth.ts
      provides: auth/session payload schemas
    - path: packages/contracts/src/domain/knowledge.ts
      provides: knowledge entity and submission schemas
    - path: packages/contracts/src/domain/retrieval.ts
      provides: retrieval request and response schemas
  key_links:
    - from: packages/contracts/src/index.ts
      to: packages/contracts/src/domain/auth.ts
      via: export surface
    - from: packages/contracts/src/index.ts
      to: packages/contracts/src/domain/knowledge.ts
      via: export surface
---

# Phase 1 Plan 02: Shared Contracts

## Objective
- **What:** Define the shared runtime schemas and contract exports used by both the CLI and server.
- **Why:** All later phases depend on stable payloads and validation logic.
- **Output:** A contracts package with typed Zod schemas covering v1 platform domains.

## Context
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-monorepo-skeleton-and-contracts/01-CONTEXT.md

## Tasks

<task type="auto">
  <name>Model core shared primitives</name>
  <files>
    packages/contracts/src/domain/common.ts
    packages/contracts/src/domain/team.ts
  </files>
  <action>
    Define reusable primitive schemas and enums for identifiers, security levels, timestamps, permissions, scopes, labels, teams, and pagination. These primitives should support the later auth, knowledge, and operations contracts without circular duplication.
  </action>
  <verify>
    pnpm --filter @skill-shareer/contracts typecheck
  </verify>
  <done>
    Common domain schemas are centralized and exported cleanly.
  </done>
</task>

<task type="auto">
  <name>Define domain payload schemas</name>
  <files>
    packages/contracts/src/domain/auth.ts
    packages/contracts/src/domain/knowledge.ts
    packages/contracts/src/domain/review.ts
    packages/contracts/src/domain/retrieval.ts
    packages/contracts/src/domain/operations.ts
    packages/contracts/src/index.ts
  </files>
  <action>
    Add Zod schemas and inferred TypeScript types for auth/session, team membership, knowledge submission and lifecycle, review queue actions, retrieval queries and results, and import/export or audit operations. Export a coherent public API from `packages/contracts/src/index.ts`.
  </action>
  <verify>
    pnpm --filter @skill-shareer/contracts test && pnpm --filter @skill-shareer/contracts typecheck
  </verify>
  <done>
    The contracts package exposes the full v1 schema surface and its tests pass.
  </done>
</task>

## Verification
Type-check and run the contracts package tests.

## Success Criteria
- [ ] Core v1 payload schemas exist in the contracts package
- [ ] Contracts are exported through a single public entrypoint
- [ ] Contracts package tests validate representative payloads
