---
phase: 01-monorepo-skeleton-and-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - biome.json
  - vitest.workspace.ts
  - packages/cli/package.json
  - packages/cli/tsconfig.json
  - packages/cli/src/index.ts
  - packages/server/package.json
  - packages/server/tsconfig.json
  - packages/server/src/index.ts
  - packages/contracts/package.json
  - packages/contracts/tsconfig.json
  - packages/contracts/src/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Repo boots as a pnpm monorepo with separate CLI, server, and contracts packages
    - Shared TypeScript build settings let all workspace packages type-check together
  artifacts:
    - path: package.json
      provides: workspace scripts and root metadata
    - path: pnpm-workspace.yaml
      provides: workspace member registration
    - path: packages/cli/package.json
      provides: CLI package boundary
    - path: packages/server/package.json
      provides: Server package boundary
    - path: packages/contracts/package.json
      provides: Shared contracts package boundary
  key_links:
    - from: package.json
      to: pnpm-workspace.yaml
      via: workspace configuration
    - from: tsconfig.base.json
      to: packages/cli/tsconfig.json
      via: TS config inheritance
    - from: tsconfig.base.json
      to: packages/server/tsconfig.json
      via: TS config inheritance
---

# Phase 1 Plan 01: Workspace Skeleton

## Objective
- **What:** Bootstrap the monorepo and package boundaries for CLI, server, and contracts.
- **Why:** Later phases depend on stable package structure and shared tooling.
- **Output:** A runnable `pnpm` workspace with placeholder CLI/server entrypoints and shared build config.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-skeleton-and-contracts/01-CONTEXT.md

## Tasks

<task type="auto">
  <name>Bootstrap root workspace tooling</name>
  <files>
    package.json
    pnpm-workspace.yaml
    tsconfig.base.json
    biome.json
    vitest.workspace.ts
    .gitignore
  </files>
  <action>
    Create the root workspace manifests and shared TypeScript, Biome, and Vitest configuration for a Node 20+ TypeScript monorepo. Add scripts for build, dev, lint, test, and typecheck that target all workspace packages.
  </action>
  <verify>
    pnpm --version
  </verify>
  <done>
    Root workspace files exist and describe the CLI, server, and contracts packages.
  </done>
</task>

<task type="auto">
  <name>Create workspace package boundaries</name>
  <files>
    packages/cli/package.json
    packages/cli/tsconfig.json
    packages/cli/src/index.ts
    packages/server/package.json
    packages/server/tsconfig.json
    packages/server/src/index.ts
    packages/contracts/package.json
    packages/contracts/tsconfig.json
    packages/contracts/src/index.ts
  </files>
  <action>
    Create `packages/cli`, `packages/server`, and `packages/contracts` with package metadata, TypeScript configs, and minimal source entrypoints so the workspace can build and type-check. Keep dependencies aligned with the approved stack: Commander for CLI, Fastify for server, Zod for contracts.
  </action>
  <verify>
    pnpm install && pnpm typecheck
  </verify>
  <done>
    Each package has a valid manifest, tsconfig, and entrypoint, and the root typecheck passes.
  </done>
</task>

## Verification
Run workspace installation and TypeScript checks from the repository root.

## Success Criteria
- [ ] Root workspace config is present and consistent
- [ ] CLI, server, and contracts packages exist as separate workspace members
- [ ] `pnpm typecheck` succeeds across the monorepo
