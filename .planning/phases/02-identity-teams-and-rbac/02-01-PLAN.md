---
phase: 02-identity-teams-and-rbac
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .gitignore
  - packages/contracts/src/domain/auth.ts
  - packages/server/package.json
  - packages/server/src/app.ts
  - packages/server/src/config.ts
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/seed.ts
  - packages/server/src/lib/session.ts
  - packages/server/src/routes/auth.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - A CLI user can log in, persist a session, and switch active teams
    - System admin key from environment creates a virtual level-10 user
  artifacts:
    - path: packages/server/src/routes/auth.ts
      provides: login, session, and team-selection endpoints
    - path: packages/server/src/lib/session.ts
      provides: persistent session token handling
    - path: .env.example
      provides: documented system admin key configuration
  key_links:
    - from: packages/server/src/config.ts
      to: packages/server/src/routes/auth.ts
      via: system admin key validation
    - from: packages/server/src/lib/store.ts
      to: packages/server/src/lib/session.ts
      via: persisted session records
---

# Phase 2 Plan 01: Auth and Session Foundation

## Objective
- **What:** Implement the server-side auth/session base and the environment-driven system admin identity.
- **Why:** Every later workflow needs authenticated context and team-aware sessions.
- **Output:** Login/session/select-team APIs backed by persistent prototype storage.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-identity-teams-and-rbac/02-CONTEXT.md
@docs/api-surface.md

## Tasks

<task type="auto">
  <name>Create persistent prototype storage and config plumbing</name>
  <files>
    .env.example
    .gitignore
    packages/server/src/config.ts
    packages/server/src/lib/store.ts
    packages/server/src/lib/seed.ts
  </files>
  <action>
    Add server configuration for the system admin key and data file location, document it in `.env.example`, ignore runtime data files in git, and implement a JSON-backed store that can persist users, memberships, access keys, sessions, and future knowledge entities.
  </action>
  <verify>
    pnpm typecheck
  </verify>
  <done>
    Server config and persistent local storage exist and are ready for auth flows.
  </done>
</task>

<task type="auto">
  <name>Implement auth and active-team session routes</name>
  <files>
    packages/contracts/src/domain/auth.ts
    packages/server/src/lib/session.ts
    packages/server/src/routes/auth.ts
    packages/server/src/app.ts
  </files>
  <action>
    Implement `POST /v1/auth/login`, `GET /v1/auth/session`, `POST /v1/auth/logout`, and `POST /v1/teams/select` using the shared contracts. Support both system admin login and access-key login, persist sessions, and expose the active team inside the session payload.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Auth routes authenticate correctly, persist sessions, and expose active-team session state.
  </done>
</task>

## Verification
Build and type-check the workspace, then exercise the auth endpoints with a seeded system admin key.

## Success Criteria
- [ ] System admin login works from env configuration
- [ ] Session state survives across server requests
- [ ] Team selection updates the active session context
