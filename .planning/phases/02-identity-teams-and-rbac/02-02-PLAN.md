---
phase: 02-identity-teams-and-rbac
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/contracts/src/domain/team.ts
  - packages/server/src/lib/rbac.ts
  - packages/server/src/routes/teams.ts
  - packages/server/src/routes/members.ts
  - packages/server/src/routes/access-keys.ts
  - packages/server/src/app.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Higher-level members can create teams, onboard new members at level 0, and modify lower-level members
    - Server authorization blocks protected actions using security-level comparisons plus explicit permissions
  artifacts:
    - path: packages/server/src/lib/rbac.ts
      provides: centralized authorization logic
    - path: packages/server/src/routes/teams.ts
      provides: team listing and creation routes
    - path: packages/server/src/routes/members.ts
      provides: onboarding and membership update routes
    - path: packages/server/src/routes/access-keys.ts
      provides: permanent access-key issuance
  key_links:
    - from: packages/server/src/lib/rbac.ts
      to: packages/server/src/routes/members.ts
      via: security-level enforcement
    - from: packages/server/src/routes/access-keys.ts
      to: packages/server/src/lib/store.ts
      via: persisted permanent access keys
---

# Phase 2 Plan 02: Team and Member Management

## Objective
- **What:** Add team, membership, and access-key management on the server.
- **Why:** Knowledge workflows require a permission-safe organizational model first.
- **Output:** Server endpoints that manage teams and memberships with RBAC enforcement.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-identity-teams-and-rbac/02-CONTEXT.md
@docs/api-surface.md

## Tasks

<task type="auto">
  <name>Centralize authorization rules</name>
  <files>
    packages/contracts/src/domain/team.ts
    packages/server/src/lib/rbac.ts
  </files>
  <action>
    Define the default permission matrix for role templates, the effective-permission resolution rules, and reusable guards for protected routes. Ensure level comparison and explicit permissions both participate in the authorization decision.
  </action>
  <verify>
    pnpm typecheck
  </verify>
  <done>
    Shared authorization utilities exist and are used by server routes.
  </done>
</task>

<task type="auto">
  <name>Implement team, member, and access-key endpoints</name>
  <files>
    packages/server/src/routes/teams.ts
    packages/server/src/routes/members.ts
    packages/server/src/routes/access-keys.ts
    packages/server/src/app.ts
  </files>
  <action>
    Add `GET /v1/teams`, `POST /v1/teams`, `POST /v1/members`, `PATCH /v1/members/:memberId`, and `POST /v1/access-keys`. New members must default to level 0, modifiers cannot raise a member to their own level or higher, and issued access keys must be permanent until revoked manually.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Team, member, and access-key endpoints exist and enforce the RBAC rules.
  </done>
</task>

## Verification
Use the system admin session to create a team, onboard a member, change a lower-level member, and issue an access key.

## Success Criteria
- [ ] Team creation and listing work
- [ ] Member onboarding defaults to level 0
- [ ] Lower-level-only modification rules are enforced
- [ ] Permanent access keys can be issued within the creator's level boundary
