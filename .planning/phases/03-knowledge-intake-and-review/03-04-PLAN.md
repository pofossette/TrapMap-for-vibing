---
phase: 03-knowledge-intake-and-review
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - packages/server/src/routes/review.ts
  - packages/server/src/routes/knowledge.ts
  - packages/server/src/app.ts
  - packages/cli/src/commands/review.ts
  - packages/cli/src/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Only reviewers with level greater than requiredLevel can approve or reject
    - Rejected submissions can be corrected and resubmitted with history preserved
    - Knowledge updates require a member with level greater than the entry's required level
  artifacts:
    - path: packages/server/src/routes/review.ts
      provides: review queue and decision routes
    - path: packages/cli/src/commands/review.ts
      provides: reviewer and resubmission CLI flows
  key_links:
    - from: packages/server/src/routes/review.ts
      to: packages/server/src/lib/rbac.ts
      via: higher-level reviewer checks
    - from: packages/cli/src/commands/review.ts
      to: packages/server/src/routes/review.ts
      via: queue and approve/reject actions
---

# Phase 3 Plan 04: Admin Review and Resubmission

## Objective
- **What:** Complete the review lifecycle with reviewer decisions and submitter resubmission.
- **Why:** Knowledge is only trustworthy once it can be reviewed, rejected with notes, corrected, and approved with preserved history.
- **Output:** Review queue APIs plus CLI workflows for reviewers and submitters.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-intake-and-review/03-CONTEXT.md
@docs/api-surface.md

## Tasks

<task type="auto">
  <name>Implement review queue and decision routes</name>
  <files>
    packages/server/src/routes/review.ts
    packages/server/src/routes/knowledge.ts
    packages/server/src/app.ts
  </files>
  <action>
    Add endpoints for reviewer queue listing, queue filtering by status, approve/reject decisions with notes, and knowledge resubmission or privileged updates. Enforce the rule that only members with level greater than the entry's required level may review or modify the entry.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Reviewer decisions and submitter resubmissions work with preserved lifecycle history.
  </done>
</task>

<task type="auto">
  <name>Add reviewer and resubmission CLI workflows</name>
  <files>
    packages/cli/src/commands/review.ts
    packages/cli/src/index.ts
  </files>
  <action>
    Add CLI commands for review queue inspection, approve/reject decisions, fetching rejected entries, and resubmission. Keep the flows shell-friendly and aligned with the existing permission-aware command registration approach.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Reviewers and submitters can complete the review lifecycle from the CLI.
  </done>
</task>

## Verification
Submit an entry, trigger a rejection with notes, inspect the rejection from the submitter account, resubmit it, and then approve it with a higher-level reviewer.

## Success Criteria
- [ ] Review queue filtering and approve/reject decisions work
- [ ] Rejected submissions can be inspected and resubmitted with preserved history
- [ ] Higher-level-only review and update rules are enforced
