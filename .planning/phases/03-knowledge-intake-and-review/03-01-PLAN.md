---
phase: 03-knowledge-intake-and-review
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/knowledge.ts
  - packages/contracts/src/domain/review.ts
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/knowledge.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Knowledge objects preserve lifecycle states and revision history
    - Global constraints and project knowledge remain distinguishable through scope and metadata
  artifacts:
    - path: packages/contracts/src/domain/knowledge.ts
      provides: canonical knowledge schemas
    - path: packages/contracts/src/domain/review.ts
      provides: review queue and decision schemas
    - path: packages/server/src/lib/store.ts
      provides: persistent lifecycle-aware knowledge storage
  key_links:
    - from: packages/server/src/lib/store.ts
      to: packages/contracts/src/domain/knowledge.ts
      via: stored records align with shared contracts
---

# Phase 3 Plan 01: Knowledge Model and Lifecycle Storage

## Objective
- **What:** Expand the knowledge and review model to support lifecycle history and resubmission.
- **Why:** Submissions, review outcomes, and later retrieval all depend on a stable knowledge object shape.
- **Output:** Shared schemas and persistent store records that capture lifecycle states, revisions, and review metadata.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-intake-and-review/03-CONTEXT.md

## Tasks

<task type="auto">
  <name>Refine knowledge and review contracts</name>
  <files>
    packages/contracts/src/domain/knowledge.ts
    packages/contracts/src/domain/review.ts
  </files>
  <action>
    Expand the knowledge and review contracts to cover submission history, agent-review results, review notes, submission history listing, resubmission payloads, and reviewer decision outputs. Keep the shared contracts aligned with the server and CLI workflows you intend to build in later plans.
  </action>
  <verify>
    pnpm --filter @skill-shareer/contracts test && pnpm typecheck
  </verify>
  <done>
    Shared contracts can represent the full submission and review lifecycle.
  </done>
</task>

<task type="auto">
  <name>Add lifecycle-aware knowledge persistence</name>
  <files>
    packages/server/src/lib/store.ts
    packages/server/src/lib/knowledge.ts
  </files>
  <action>
    Extend the server store with full knowledge-entry persistence including scope, labels, required level, revisions, agent-review state, reviewer decisions, and audit-friendly history. Provide helper functions that later routes can reuse instead of hand-rolling lifecycle transitions.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    The store can persist knowledge entries, revisions, and review metadata without losing history.
  </done>
</task>

## Verification
Rebuild the workspace and confirm the contracts and store compile together.

## Success Criteria
- [ ] Shared contracts model lifecycle state and history
- [ ] The server store can persist knowledge revisions and review metadata
