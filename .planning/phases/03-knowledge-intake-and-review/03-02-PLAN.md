---
phase: 03-knowledge-intake-and-review
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/server/src/routes/knowledge.ts
  - packages/server/src/app.ts
  - packages/cli/src/commands/knowledge.ts
  - packages/cli/src/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - A user can submit structured knowledge and requiredLevel defaults to the submitter's level
    - Submitters can inspect their own submission and review history from the CLI
  artifacts:
    - path: packages/server/src/routes/knowledge.ts
      provides: submission and history endpoints
    - path: packages/cli/src/commands/knowledge.ts
      provides: submit and review-status commands
  key_links:
    - from: packages/cli/src/commands/knowledge.ts
      to: packages/server/src/routes/knowledge.ts
      via: submit and status workflow
---

# Phase 3 Plan 02: Submission APIs and User History

## Objective
- **What:** Add the user-facing knowledge submission and status workflow.
- **Why:** The product only becomes useful when engineers can submit and inspect pitfall knowledge from the terminal.
- **Output:** Server submission/history endpoints and CLI commands for submitters.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-intake-and-review/03-CONTEXT.md
@docs/api-surface.md

## Tasks

<task type="auto">
  <name>Implement submission and user-history endpoints</name>
  <files>
    packages/server/src/routes/knowledge.ts
    packages/server/src/app.ts
  </files>
  <action>
    Implement routes for new knowledge submission, fetching the current user's submissions, and fetching a specific entry or rejected history. Submissions should inherit the submitter's security level by default and must remain team-aware.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Knowledge submission and status routes work for authenticated users.
  </done>
</task>

<task type="auto">
  <name>Add submit and status CLI commands</name>
  <files>
    packages/cli/src/commands/knowledge.ts
    packages/cli/src/index.ts
  </files>
  <action>
    Add `submit`, `review-status`, and related status-inspection commands that call the new knowledge endpoints, support shell-friendly flags, and show submitters their latest review state and notes.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Submitters can create and inspect knowledge entries from the CLI.
  </done>
</task>

## Verification
Submit a sample entry from the CLI and inspect its stored status and history.

## Success Criteria
- [ ] Knowledge submission defaults requiredLevel to the submitter's level
- [ ] Submitters can inspect their submission and review history from the CLI
