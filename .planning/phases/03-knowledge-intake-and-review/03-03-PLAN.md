---
phase: 03-knowledge-intake-and-review
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - packages/server/package.json
  - packages/server/src/lib/pre-review.ts
  - packages/server/src/routes/knowledge.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - Agent pre-review produces agent-pass or agent-rejected
    - Duplicate, correctness, and completeness checks are stored with the submission
  artifacts:
    - path: packages/server/src/lib/pre-review.ts
      provides: LangChain-backed pre-review pipeline
    - path: packages/server/package.json
      provides: LangChain dependency declaration
  key_links:
    - from: packages/server/src/lib/pre-review.ts
      to: packages/server/src/routes/knowledge.ts
      via: submission pre-review before queueing
---

# Phase 3 Plan 03: LangChain Pre-Review

## Objective
- **What:** Add an automated pre-review stage before admin review.
- **Why:** The roadmap requires agent screening for duplicate, correctness, and completeness risk.
- **Output:** A LangChain-backed pre-review service integrated into knowledge submission and resubmission.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-intake-and-review/03-CONTEXT.md

## Tasks

<task type="auto">
  <name>Add LangChain-backed pre-review service</name>
  <files>
    packages/server/package.json
    packages/server/src/lib/pre-review.ts
  </files>
  <action>
    Add the minimum LangChain JS dependency needed for a local pre-review pipeline, then implement duplicate, correctness, and completeness checks that output `agent-pass` or `agent-rejected` plus structured risk notes. The logic may be heuristic-backed, but it must be wrapped through LangChain primitives so the server remains LangChain-powered.
  </action>
  <verify>
    pnpm install && pnpm build && pnpm typecheck
  </verify>
  <done>
    The server can run a LangChain-backed pre-review against knowledge submissions.
  </done>
</task>

<task type="auto">
  <name>Integrate pre-review into submission lifecycle</name>
  <files>
    packages/server/src/routes/knowledge.ts
  </files>
  <action>
    Run pre-review during initial submission and resubmission, persist the result on the knowledge object, and transition lifecycle state to `agent-pass` or `agent-rejected` before the item appears in the admin review queue.
  </action>
  <verify>
    pnpm build && pnpm typecheck
  </verify>
  <done>
    Submission lifecycle now includes persisted agent-review state and notes.
  </done>
</task>

## Verification
Submit both a strong and a weak entry and confirm their pre-review outcomes differ appropriately.

## Success Criteria
- [ ] Pre-review uses LangChain primitives
- [ ] Submissions persist `agent-pass` or `agent-rejected`
- [ ] Pre-review notes are visible in the stored entry history
