---
phase: 43
plan: 03
type: implementation
wave: 3
depends_on: [43-02]
files_modified:
  - packages/server/src/lib/retrieval-workflow.test.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/artifacts/derive.test.ts
  - packages/server/src/lib/artifacts/model.test.ts
  - packages/server/src/lib/artifacts/edit.test.ts
  - packages/server/src/lib/candidates/reconcile.test.ts
  - packages/server/src/lib/indexing/events.test.ts
  - packages/server/src/lib/indexing/reconcile.test.ts
  - packages/server/src/lib/indexing/pipeline.test.ts
  - packages/server/src/lib/indexing/adapters/graph.test.ts
  - packages/server/src/routes/knowledge.test.ts
  - packages/server/src/routes/operations.test.ts
  - packages/server/src/routes/review.test.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/server/src/routes/candidates.test.ts
  - packages/server/src/lib/store.test.ts
autonomous: true
requirements: [P43-03]
must_haves:
  truths:
    - "All server tests that reference the app-level store remain assignable after the shared store contract replaces the concrete JsonStore type at runtime"
    - "Regression coverage proves both store selection and compatibility-store semantics without reintroducing filesystem-only assumptions"
    - "Phase 43 ends with a passing server typecheck over both source and tests"
  artifacts:
    - path: "packages/server/src/lib/store.test.ts"
      provides: "Store-contract and runtime selection coverage for the PostgreSQL-backed store"
    - path: "packages/server/src/routes/operations.test.ts"
      provides: "Representative route-level assignability coverage for app.skillShareer.store"
---

<objective>
Close the test and verification fallout from the shared store migration.

Purpose: update the existing server tests to the new runtime store contract, add any missing selection assertions, and restore a clean package-wide verification surface after the production seam changes.
Output: test files aligned with the shared store contract plus final package-level verification coverage.
</objective>

<tasks>
  <task>
    <name>Task 1: Update existing tests to the shared store contract</name>
    <read_first>
      - packages/server/src/lib/store.ts
      - packages/server/src/lib/context.ts
      - packages/server/src/app.ts
      - packages/server/src/routes/knowledge.test.ts
      - packages/server/src/routes/operations.test.ts
    </read_first>
    <files>packages/server/src/lib/retrieval-workflow.test.ts, packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/artifacts/derive.test.ts, packages/server/src/lib/artifacts/model.test.ts, packages/server/src/lib/artifacts/edit.test.ts, packages/server/src/lib/candidates/reconcile.test.ts, packages/server/src/lib/indexing/events.test.ts, packages/server/src/lib/indexing/reconcile.test.ts, packages/server/src/lib/indexing/pipeline.test.ts, packages/server/src/lib/indexing/adapters/graph.test.ts, packages/server/src/routes/knowledge.test.ts, packages/server/src/routes/operations.test.ts, packages/server/src/routes/review.test.ts, packages/server/src/routes/retrieval.test.ts, packages/server/src/routes/candidates.test.ts</files>
    <action>
      Update the existing tests that still annotate `app.skillShareer.store` or store mocks as `JsonStore` so they compile and execute against the shared store contract. Keep concrete `JsonStore` instantiation where the tests intentionally exercise the file-backed implementation; only change the app-facing or seam-facing type annotations that block package-wide verification.
    </action>
    <acceptance_criteria>
      - route and lib tests no longer assume the app-level store is always typed as `JsonStore`
      - tests that intentionally instantiate `JsonStore` can keep doing so
      - no test-only cast hides an actual contract mismatch introduced by the new store seam
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Finish regression coverage and restore package-wide verification</name>
    <read_first>
      - packages/server/src/lib/store.test.ts
      - packages/server/src/lib/persistence/create-store.ts
      - packages/server/src/lib/persistence/postgres-store.ts
    </read_first>
    <files>packages/server/src/lib/store.test.ts</files>
    <action>
      Expand the store regression suite so it also covers runtime store selection, then run the full server typecheck once the existing tests have been migrated to the shared store contract.
    </action>
    <acceptance_criteria>
      - store tests cover both PostgreSQL-backed behavior and runtime selection behavior
      - `pnpm --filter @trapmap/server typecheck` passes again
      - final verification for the phase can rely on package-wide compilation instead of spot checks only
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/store.test.ts`
  2. `pnpm --filter @trapmap/server typecheck`
</verification>
