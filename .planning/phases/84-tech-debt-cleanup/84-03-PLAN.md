---
wave: 3
depends_on:
  - 84-01-PLAN.md
  - 84-02-PLAN.md
files_modified: []
autonomous: true
---

# Phase 84-03: Final Verification

## Objective

Verify all tech debt cleanup tasks completed successfully: worktrees pruned, duplicate export fixed, and knip warnings reduced.

## Tasks

### Task 1: Verify worktree cleanup

<read_first>
- No file reads needed for this verification task
</read_first>

<action>
Run verification commands to confirm worktree cleanup:

```bash
# Count remaining worktrees (should be 5)
git worktree list | wc -l

# Verify no stale worktrees in .claude/worktrees
ls .claude/worktrees/ 2>/dev/null || echo "Directory empty or removed"
```
</action>

<acceptance_criteria>
- `git worktree list | wc -l` returns 5
- No stale agent worktrees remain in `.claude/worktrees/`
- Only valid worktrees at main location and `/home/wunai/gsd-workspaces/`
</acceptance_criteria>

---

### Task 2: Verify TypeScript compilation

<read_first>
- No file reads needed - verification commands only
</read_first>

<action>
Run TypeScript compilation check across all packages:

```bash
# Check contracts package
cd packages/contracts && npx tsc --noEmit

# Check server package
cd ../server && npx tsc --noEmit

# Check CLI package
cd ../cli && npx tsc --noEmit
```
</action>

<acceptance_criteria>
- `npx tsc --noEmit` passes in packages/contracts
- `npx tsc --noEmit` passes in packages/server
- `npx tsc --noEmit` passes in packages/cli
- No TypeScript errors
</acceptance_criteria>

---

### Task 3: Verify all tests pass

<read_first>
- No file reads needed - verification commands only
</read_first>

<action>
Run full test suite to ensure no regressions from type changes:

```bash
npx vitest run
```
</action>

<acceptance_criteria>
- All tests pass (expect 2400+ tests)
- No test failures
- No test errors
</acceptance_criteria>

---

### Task 4: Verify knip warning reduction

<read_first>
- No file reads needed - verification commands only
</read_first>

<action>
Run knip and count remaining warnings:

```bash
npx knip 2>&1 | head -100
```

Expected improvements:
- Duplicate exports reduced from 1 to 0 (boundary.ts fixed)
- Unused exported types reduced by ~20 (from ~91 to ~70)
- Unused exports reduced by ~15 (from ~75 to ~60)

Note: Many remaining warnings are intentional public API exports or require broader refactoring outside this phase's scope.
</action>

<acceptance_criteria>
- `npx knip 2>&1 | grep -E "Duplicate exports.*boundary"` returns empty
- `npx knip 2>&1 | grep "Duplicate exports" | wc -l` returns 0
- `npx knip 2>&1 | grep -c "packages/server/src/lib/retrieval/graph-extract.ts"` is reduced
- `npx knip 2>&1 | grep -c "packages/server/src/lib/retrieval/merge.ts"` is reduced
- `npx knip 2>&1 | grep -c "packages/server/src/lib/retrieval/rerank.ts"` is reduced
</acceptance_criteria>

---

### Task 5: Update STATE.md with phase completion

<read_first>
- .planning/STATE.md (current state)
</read_first>

<action>
Update STATE.md to mark Phase 84 as complete:
- Update `completed_phases` from 3 to 4
- Update `completed_plans` from 12 to 15
- Update `last_updated` timestamp
- Add decisions section for Phase 84

Add to decisions:
```markdown
### Decisions (Phase 84)

- 84-01: Prune 16 stale agent worktrees from `.claude/worktrees/`
- 84-01: Fix duplicate export by using `export { boundarySchema as boundaryMetaSchema }`
- 84-02: Unexport internal-only types (GraphNode, GraphRelation, TrapGraphExtractionResult, etc.)
- 84-02: Unexport Config interfaces (MergeConfig, RerankConfig, GraphAssistedRecallConfig, PgKeywordRecallConfig)
- 84-02: Unexport batch embedding types (BatchEmbeddingResult, BatchCacheStats, OptimizedSemanticRecallResult)
- 84-02: Unexport internal routing types (RetrievalDecision, RoutingDecision, RetrievalPipelineContext, RetrievalStats)
```
</action>

<acceptance_criteria>
- STATE.md shows `completed_phases: 4`
- STATE.md shows `completed_plans: 15`
- STATE.md has Phase 84 decisions section
</acceptance_criteria>

---

## Verification Summary

All Phase 84 success criteria must be met:

- [ ] `git worktree list` only shows valid worktrees (5 total)
- [ ] `pnpm knip` has no duplicate export warning for boundary.ts
- [ ] Released at least 500 MB disk space (from worktree cleanup)
- [ ] All tests pass
- [ ] TypeScript compiles without errors

## must_haves

These conditions define the goal-backward verification for Phase 84:

1. **Worktree Cleanup**: Exactly 5 worktrees remain (main + 4 gsd-workspaces)
2. **No Duplicate Export**: knip reports 0 duplicate exports
3. **Tests Pass**: Full test suite passes
4. **Type Safety**: TypeScript compilation succeeds in all packages
