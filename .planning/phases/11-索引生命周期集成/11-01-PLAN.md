---
phase: 11-索引生命周期集成
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/app.ts
  - packages/server/src/lib/context.ts
  - packages/server/src/lib/indexing/adapters/index.ts
  - packages/server/src/routes/review.ts
  - packages/server/src/routes/review.test.ts
autonomous: true
requirements:
  - IDX-03
  - IDX-04
must_haves:
  truths:
    - "The server bootstraps a real index-adapter list and exposes it through `app.skillShareer`."
    - "A reviewer approval commits first, then triggers the existing indexing event layer to upsert approved knowledge."
    - "Route coverage proves approval wiring uses post-commit invocation instead of nested transactions."
  artifacts:
    - path: "packages/server/src/lib/context.ts"
      provides: "service contract including registered index adapters"
      contains: "indexAdapters"
    - path: "packages/server/src/app.ts"
      provides: "default adapter registration at server bootstrap"
      contains: "indexAdapters:"
    - path: "packages/server/src/routes/review.ts"
      provides: "post-commit approval indexing trigger"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/review.test.ts"
      provides: "approval-path regression coverage for indexing integration"
      contains: "review routes"
  key_links:
    - from: "packages/server/src/app.ts"
      to: "packages/server/src/lib/context.ts"
      via: "Fastify service decoration"
      pattern: "indexAdapters"
    - from: "packages/server/src/routes/review.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "post-transaction lifecycle event dispatch"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/routes/review.test.ts"
      to: "packages/server/src/routes/review.ts"
      via: "approved transition integration assertions"
      pattern: "approved|runKnowledgeIndexEvent|indexState"
---

<objective>
Register the existing indexing adapters in the server service container and wire reviewer approval to the lifecycle indexing event after the domain transaction commits.

Purpose: satisfy IDX-03 and IDX-04 without redesigning the indexing stack, while enforcing the critical no-nested-transaction constraint.
Output: bootstrap adapter registration, review-route post-commit indexing trigger, and approval-path regression tests.
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-索引生命周期集成/11-RESEARCH.md
@.planning/phases/03-knowledge-intake-and-review/03-01-SUMMARY.md
@.planning/phases/05-admin-operations-and-hardening/05-01-SUMMARY.md
@packages/server/src/app.ts
@packages/server/src/lib/context.ts
@packages/server/src/lib/indexing/adapters/index.ts
@packages/server/src/lib/indexing/events.ts
@packages/server/src/routes/review.ts

<interfaces>
From `packages/server/src/lib/context.ts`:
```typescript
export interface SkillShareerServices {
  config: ServerConfig;
  store: JsonStore;
}
```

From `packages/server/src/lib/indexing/events.ts`:
```typescript
export function determineKnowledgeIndexAction(
  previousState: LifecycleState,
  nextState: LifecycleState,
): 'upsert' | 'remove' | 'noop';

export async function runKnowledgeIndexEvent(args: {
  services: { store: JsonStore; data: StoreData };
  entryId: string;
  previousState: LifecycleState;
  nextState: LifecycleState;
  reason: string;
  adapters: IndexAdapter[];
}): Promise<void>;
```

From `packages/server/src/lib/indexing/adapters/index.ts`:
```typescript
export { vectorIndexAdapter } from './vector.js';
export { keywordIndexAdapter } from './keyword.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create approval-path regression coverage and expose adapter registration seam</name>
  <files>packages/server/src/lib/context.ts, packages/server/src/lib/indexing/adapters/index.ts, packages/server/src/app.ts, packages/server/src/routes/review.test.ts</files>
  <behavior>
    - Test 1: the Fastify service container exposes a stable `indexAdapters` array built from the existing vector and keyword adapters.
    - Test 2: approving a reviewable entry causes index state to exist only after the route completes, proving the integration is observable through the existing store.
    - Test 3: rejected decisions remain indexing no-ops.
  </behavior>
  <action>Create `packages/server/src/routes/review.test.ts` first because this route currently has no coverage. Extend `SkillShareerServices` with `indexAdapters`, add a bootstrap helper in `packages/server/src/lib/indexing/adapters/index.ts` if needed to return the default adapter list, and decorate `app.skillShareer` in `packages/server/src/app.ts` with that list. Keep this bootstrap server-internal per the phase constraint; do not touch CLI or shared contracts. The new test should boot the real server, drive `POST /v1/knowledge/review`, and assert against store/index state rather than mocking route-local branches.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/review.test.ts src/lib/indexing/events.test.ts</automated>
  </verify>
  <done>The server has a concrete adapter-registration seam and a failing-or-pending approval integration test exists before route wiring changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire reviewer approval to post-commit indexing events</name>
  <files>packages/server/src/routes/review.ts, packages/server/src/routes/review.test.ts</files>
  <behavior>
    - Test 1: `approve` transitions trigger `runKnowledgeIndexEvent(...)` after the outer mutation transaction returns and persist vector/keyword index state for the approved entry.
    - Test 2: `reject` transitions do not create index state or adapter payloads.
    - Test 3: the route continues to preserve existing review audit and serialization behavior.
  </behavior>
  <action>Capture the `entryId`, `previousState`, and committed `nextState` inside the existing `store.transact(...)` block in `packages/server/src/routes/review.ts`, but invoke `runKnowledgeIndexEvent(...)` only after that transaction resolves. Pass `app.skillShareer.store`, a fresh committed snapshot, and `app.skillShareer.indexAdapters`. Do not call the event layer from inside the transaction, and do not reimplement lifecycle mapping in the route; reuse `runKnowledgeIndexEvent(...)` exactly as the centralized transition seam for IDX-03 per the phase research.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/review.test.ts src/lib/indexing/events.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>Reviewer approval performs automatic indexing after commit, rejection stays a no-op, and the bootstrap/service seam is ready for the remaining mutation routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review mutation -> indexing event | approved lifecycle changes become index-maintenance side effects |
| app bootstrap -> route handlers | every mutation route depends on one shared adapter registration surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Denial of Service | `packages/server/src/routes/review.ts` | mitigate | invoke `runKnowledgeIndexEvent(...)` only after the outer `store.transact(...)` completes so nested transactions cannot deadlock |
| T-11-02 | Tampering | `packages/server/src/lib/context.ts` / `packages/server/src/app.ts` | mitigate | register adapters once at bootstrap and pass them through `app.skillShareer` rather than rebuilding route-local lists |
| T-11-03 | Information Disclosure | `packages/server/src/routes/review.ts` | mitigate | keep approval/RBAC enforcement in the existing review route and only trigger indexing from committed lifecycle state |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/routes/review.test.ts src/lib/indexing/events.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- `SkillShareerServices` includes a concrete `indexAdapters` field initialized by `buildServer()`
- `POST /v1/knowledge/review` triggers indexing only for committed approval transitions
- review-route regression coverage exists and passes against the real store + indexing modules
</success_criteria>

<output>
After completion, create `.planning/phases/11-索引生命周期集成/11-01-SUMMARY.md`
</output>
