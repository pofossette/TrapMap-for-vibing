---
phase: 11-索引生命周期集成
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - packages/server/src/routes/knowledge.ts
  - packages/server/src/routes/knowledge.test.ts
  - packages/server/src/routes/operations.ts
  - packages/server/src/routes/operations.test.ts
autonomous: true
requirements:
  - IDX-05
  - IDX-06
must_haves:
  truths:
    - "Approved knowledge updates refresh persisted index state after the mutation commits."
    - "Non-approved updates do not upsert index artifacts and therefore preserve the approval-first boundary."
    - "Knowledge deactivation removes persisted index artifacts after commit."
  artifacts:
    - path: "packages/server/src/routes/knowledge.ts"
      provides: "post-commit approved-update indexing trigger"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/knowledge.test.ts"
      provides: "approved-update and non-approved-update regression coverage"
      contains: "knowledge routes"
    - path: "packages/server/src/routes/operations.ts"
      provides: "post-commit deactivate indexing trigger"
      contains: "runKnowledgeIndexEvent"
    - path: "packages/server/src/routes/operations.test.ts"
      provides: "deactivate-trigger regression coverage"
      contains: "deactivate"
  key_links:
    - from: "packages/server/src/routes/knowledge.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "approved -> approved event dispatch after patch commit"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/routes/operations.ts"
      to: "packages/server/src/lib/indexing/events.ts"
      via: "approved -> deactivated event dispatch after deactivate commit"
      pattern: "runKnowledgeIndexEvent"
    - from: "packages/server/src/routes/knowledge.test.ts"
      to: "packages/server/src/routes/operations.test.ts"
      via: "route-level regression proof for refresh and remove lifecycle hooks"
      pattern: "approved|deactivated|indexState"
---

<objective>
Finish Phase 11 by wiring post-commit indexing refresh/removal into the knowledge update and deactivate mutation paths, with focused route regression coverage.

Purpose: satisfy IDX-05 and IDX-06 while preserving approval-first lifecycle boundaries and reusing the adapter-registration seam from 11-01.
Output: update/deactivate route integration and route tests proving refresh/remove behavior.
</objective>

<execution_context>
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/gsd-workspaces/rag-enhance/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-索引生命周期集成/11-RESEARCH.md
@.planning/phases/03-knowledge-intake-and-review/03-01-SUMMARY.md
@.planning/phases/05-admin-operations-and-hardening/05-01-SUMMARY.md
@packages/server/src/lib/indexing/events.ts
@packages/server/src/routes/knowledge.ts
@packages/server/src/routes/operations.ts
@packages/server/src/routes/operations.test.ts

<interfaces>
From `packages/server/src/lib/indexing/events.ts`:
```typescript
export async function runKnowledgeIndexEvent(args: {
  services: { store: JsonStore; data: StoreData };
  entryId: string;
  previousState: LifecycleState;
  nextState: LifecycleState;
  reason: string;
  adapters: IndexAdapter[];
}): Promise<void>;
```

From `packages/server/src/routes/knowledge.ts`:
```typescript
app.patch('/v1/knowledge/:entryId', async (request) => {
  // existing privileged update path
});
```

From `packages/server/src/routes/operations.ts`:
```typescript
app.post('/v1/operations/knowledge/:entryId/deactivate', async (request) => {
  // existing deactivation path
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route-level tests for approved refresh and deactivate removal</name>
  <files>packages/server/src/routes/knowledge.test.ts, packages/server/src/routes/operations.test.ts</files>
  <behavior>
    - Test 1: patching an `approved` entry updates its indexed content hash or adapter state only after the route succeeds.
    - Test 2: patching a non-approved entry does not create vector/keyword index state.
    - Test 3: deactivating an indexed entry clears persisted `indexState` and removes adapter artifacts after the response completes.
  </behavior>
  <action>Create `packages/server/src/routes/knowledge.test.ts` because this route has no focused tests today, and extend `packages/server/src/routes/operations.test.ts` beyond schema/auth smoke checks to cover deactivate indexing. Reuse the real Fastify app plus JSON store setup, seed entries in both approved and non-approved states, and assert on persisted `indexState` changes rather than route-local mocks. These tests should encode the no-nested-transaction expectation indirectly by asserting on post-response committed state only.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/knowledge.test.ts src/routes/operations.test.ts</automated>
  </verify>
  <done>The missing knowledge route test file exists, operations tests cover indexing removal, and both files pin refresh/remove behavior before route code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire update and deactivate routes to post-commit index refresh/remove events</name>
  <files>packages/server/src/routes/knowledge.ts, packages/server/src/routes/knowledge.test.ts, packages/server/src/routes/operations.ts, packages/server/src/routes/operations.test.ts</files>
  <behavior>
    - Test 1: updating an approved entry emits `approved -> approved` and refreshes index state through the existing event/pipeline modules.
    - Test 2: updating a non-approved entry remains an indexing no-op.
    - Test 3: deactivation emits `previousState -> deactivated` after commit and clears persisted index state.
  </behavior>
  <action>In `packages/server/src/routes/knowledge.ts`, capture the `previousState`, `entryId`, and committed `nextState` inside the patch transaction, then after commit call `runKnowledgeIndexEvent(...)` only when `nextState === 'approved'`. In `packages/server/src/routes/operations.ts`, do the same for deactivate and always dispatch the committed transition to `deactivated` after the transaction resolves. Reuse `app.skillShareer.indexAdapters` from 11-01 and always pass a fresh committed store snapshot into the event layer. Do not add new event-mapping logic, do not trigger indexing inside the transaction, and do not change any CLI or contract surface.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/knowledge.test.ts src/routes/operations.test.ts src/lib/indexing/events.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>Approved updates refresh indexes, deactivation removes indexes, non-approved updates stay no-op, and all route behavior is covered by passing regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| knowledge patch -> indexing event | mutable entry content can become refreshed index artifacts only after approval-gated commit |
| deactivate mutation -> adapter removal | lifecycle state transition removes persisted search artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | `packages/server/src/routes/knowledge.ts` | mitigate | dispatch refresh only when the committed lifecycle state remains `approved`, so draft/rejected content is never indexed |
| T-11-05 | Denial of Service | `packages/server/src/routes/knowledge.ts` / `packages/server/src/routes/operations.ts` | mitigate | keep `runKnowledgeIndexEvent(...)` outside the outer mutation transaction and reuse the centralized event layer instead of opening nested route-local transactions |
| T-11-06 | Tampering | `packages/server/src/routes/operations.ts` | mitigate | remove index artifacts through the existing event/pipeline path so persisted `indexState` and adapter state are cleared together |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/routes/knowledge.test.ts src/routes/operations.test.ts src/lib/indexing/events.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- `PATCH /v1/knowledge/:entryId` refreshes indexes only for approved entries and only after commit
- `POST /v1/operations/knowledge/:entryId/deactivate` removes index state after commit
- route regression coverage exists for both knowledge update and deactivate integration paths
</success_criteria>

<output>
After completion, create `.planning/phases/11-索引生命周期集成/11-02-SUMMARY.md`
</output>
