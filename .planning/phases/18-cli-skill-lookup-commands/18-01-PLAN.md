---
phase: 18-cli-skill-lookup-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.test.ts
autonomous: true
requirements:
  - SKED-01
must_haves:
  truths:
    - "The codebase exposes a shared artifact-first lookup contract for searching skills by content text."
    - "The lookup contract returns skill IDs with brief metadata instead of capsule-native retrieval payloads."
    - "The shared schema supports the JSON shape the CLI and server will both enforce for `skill search-by-content`."
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "Skill lookup request/result/response schemas and exported types"
      contains: "artifact-first search-by-content contract for skills"
    - path: "packages/contracts/src/index.test.ts"
      provides: "Contract regression coverage for the new lookup schemas"
      contains: "schema acceptance and rejection cases for SKED-01"
  key_links:
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "shared Zod request/response parsing"
      pattern: "skill.*search.*Schema"
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/cli/src/commands/skill.ts"
      via: "CLI response parsing before printResult"
      pattern: "skill.*lookup.*ResponseSchema"
---

<objective>
Define the shared contract for Phase 18 skill lookup so the server and CLI implement one artifact-first schema for `skill search-by-content <text>`.

Purpose: Lock the Phase 18 interface before wiring behavior so the server route and CLI formatter stay aligned on SKED-01 and do not drift back toward capsule-native responses.
Output: Additive retrieval-domain schemas and tests for skill lookup by content.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-cli-skill-lookup-commands/18-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts

<interfaces>
From `packages/contracts/src/domain/retrieval.ts`:

```ts
export const retrievalFiltersSchema = z.object({
  teamId: entityIdSchema.nullable().optional(),
  labels: z.array(labelSchema).default([]),
  scopes: z.array(scopeSchema).default([]),
});

export const retrievalV2QuerySchema = z.object({
  seed: z.string().min(1).max(2000),
  filters: retrievalFiltersSchema.default({ labels: [], scopes: [] }),
  maxResults: z.number().int().min(1).max(50).default(10),
});

export const profileHintSchema = z.object({
  artifactId: entityIdSchema,
  title: z.string().min(1).max(280),
  slug: z.string().min(1).max(160),
  labels: z.array(labelSchema).min(1),
});
```

Use the same retrieval-domain conventions and limits where they fit, but do not reuse the capsule-native response shape. The new response must be artifact-first and purpose-built for Phase 18.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add artifact-first skill lookup schemas to retrieval contracts</name>
  <files>packages/contracts/src/domain/retrieval.ts</files>
  <behavior>
    - Test 1: A valid lookup query with `text` and defaulted `maxResults` parses successfully.
    - Test 2: A valid lookup response parses a `matches` array whose items expose skill ID plus brief metadata, not capsule content.
    - Test 3: Empty text, oversized result counts, or capsule-native-only fields are rejected or omitted from the parsed contract.
  </behavior>
  <action>Create additive schemas and exported types for the Phase 18 lookup flow in `packages/contracts/src/domain/retrieval.ts`. Use an artifact-first request/response shape for `skill search-by-content <text>` per SKED-01 and the phase constraints: return unique skill/artifact IDs with brief metadata such as title, slug, labels, scope, requiredLevel, sourceKind, score, and reason; do not model capsules or activation hints in this contract. Keep the schema in the retrieval domain so the new route can live beside existing retrieval endpoints, and preserve all existing legacy/v2 retrieval exports unchanged.</action>
  <verify>
    <automated>pnpm --filter @trapmap/contracts test -- --run packages/contracts/src/index.test.ts</automated>
  </verify>
  <done>The contracts package exports a request schema, result item schema, response schema, and inferred types for artifact-first skill lookup without changing existing retrieval surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add contract regression coverage for skill lookup request and response shapes</name>
  <files>packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: The query schema accepts a normal search text and applies defaults.
    - Test 2: The response schema accepts artifact-first matches with metadata-only fields.
    - Test 3: The response schema remains distinct from `retrievalV2ResponseWithHintsSchema` so capsule-first payloads do not become the lookup API by accident.
  </behavior>
  <action>Extend `packages/contracts/src/index.test.ts` with a new Phase 18 section that exercises the new schemas as the single source of truth for SKED-01. Cover valid parsing, default behavior, and negative or regression cases that would accidentally turn the response into a capsule/native export payload. Keep the tests close to the existing retrieval contract sections so later phases can discover the lookup contract alongside the existing retrieval families.</action>
  <verify>
    <automated>pnpm --filter @trapmap/contracts test -- --run packages/contracts/src/index.test.ts</automated>
  </verify>
  <done>The contract tests prove the lookup schema is metadata-only, artifact-first, and safe for both text and JSON CLI consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/server caller -> shared contract | Untrusted search text and result limits cross into validated request schemas |
| Server helper -> CLI output parser | The response contract constrains what metadata may be emitted to human or JSON output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | `packages/contracts/src/domain/retrieval.ts` | mitigate | Bound query text and result counts with Zod, and keep the response metadata-only so later code cannot smuggle raw capsule/file payloads through this API. |
| T-18-02 | I | skill lookup response schema | mitigate | Define a purpose-built artifact-first result item instead of reusing `retrievalV2ResponseWithHintsSchema`; exclude capsule content, activation hints, and file bodies from the contract. |
| T-18-03 | D | lookup query parsing | accept | Result-limit abuse is low-risk at contract level once `maxResults` is capped to the same bounded range used elsewhere; server-side tests in Plan 18-02 will cover full enforcement. |
</threat_model>

<verification>
Run the contracts test file after schema additions and confirm the new section passes without regressing existing retrieval contract coverage.
</verification>

<success_criteria>
The shared contracts define one artifact-first lookup API for SKED-01, the response shape is clearly distinct from capsule-native retrieval, and the schema tests document the accepted JSON surface for the Phase 18 implementation plan to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cli-skill-lookup-commands/18-01-SUMMARY.md`
</output>
