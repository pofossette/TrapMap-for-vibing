---
phase: 42
plan: 02
type: implementation
wave: 2
depends_on: [01]
files_modified:
  - packages/server/src/lib/retrieval/recall/graph-assisted.ts
  - packages/server/src/lib/retrieval/recall/graph-assisted.test.ts
autonomous: true
requirements: [P42-03]
must_haves:
  truths:
    - "Graph-assisted recall uses graphology-backed helpers for both cached and store-backed graph reads"
    - "Governance filtering remains outside the graph runtime"
    - "Direct matches still outrank relation-only matches after the runtime migration"
  artifacts:
    - path: "packages/server/src/lib/retrieval/recall/graph-assisted.ts"
      provides: "Graphology-backed one-hop recall and scoring"
    - path: "packages/server/src/lib/retrieval/recall/graph-assisted.test.ts"
      provides: "Regression coverage for graph-document-based recall behavior"
---

<objective>
Finish the Phase 42 runtime migration in the recall path.

Purpose: remove the remaining hand-rolled traversal and scoring implementation from graph-assisted recall and switch it to the graphology runtime introduced in Plan 01.
Output: graph-assisted recall backed by graphology helpers, plus regression coverage proving one-hop discovery and authorization safety still hold.
</objective>

<tasks>
  <task>
    <name>Task 1: Migrate graph-assisted recall to graphology runtime helpers</name>
    <read_first>
      - packages/server/src/lib/retrieval/recall/graph-assisted.ts
      - packages/server/src/lib/indexing/graph-lite/graphology.ts
      - packages/server/src/lib/indexing/adapters/graph.ts
      - packages/server/src/lib/retrieval/plan-compiler.ts
    </read_first>
    <files>packages/server/src/lib/retrieval/recall/graph-assisted.ts</files>
    <action>
      Remove the legacy synthetic graph-index builder from graph-assisted recall. Build a graph runtime snapshot from either store-backed documents or cached graph documents, then use the shared helper APIs for one-hop expansion and relation-strength scoring while preserving existing scoring semantics and eligible-entry intersection.
    </action>
    <acceptance_criteria>
      - graph-assisted recall no longer imports the old in-memory graph index
      - store-backed and cached paths both use the same graphology-backed runtime
      - scoring still keeps direct matches above relation-only matches
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Rewrite recall tests around graph documents</name>
    <read_first>
      - packages/server/src/lib/retrieval/recall/graph-assisted.test.ts
      - packages/server/src/lib/indexing/graph-lite/documents.ts
      - packages/server/src/lib/indexing/adapters/graph.ts
    </read_first>
    <files>packages/server/src/lib/retrieval/recall/graph-assisted.test.ts</files>
    <action>
      Replace the old persisted-state fixtures with `GraphIndexDocumentRecord` fixtures loaded through the adapter’s cached-document helper. Keep coverage for direct matches, one-hop expansion, authorization-safe filtering, and direct-vs-relation score ordering.
    </action>
    <acceptance_criteria>
      - recall tests no longer depend on legacy entity/relation fixture types
      - tests prove one-hop expansion still finds related eligible entries
      - tests prove unauthorized entries are still excluded after expansion
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/retrieval/recall/graph-assisted.test.ts`
  2. `pnpm --filter @trapmap/server typecheck`
</verification>
