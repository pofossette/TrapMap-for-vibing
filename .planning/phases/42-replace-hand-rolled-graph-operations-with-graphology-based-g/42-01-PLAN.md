---
phase: 42
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - .planning/phases/42-replace-hand-rolled-graph-operations-with-graphology-based-g/42-RESEARCH.md
  - packages/server/src/lib/indexing/graph-lite/graphology.ts
  - packages/server/src/lib/indexing/graph-lite/graphology.test.ts
  - packages/server/src/lib/indexing/adapters/graph.ts
  - packages/server/src/lib/indexing/adapters/index.ts
autonomous: true
requirements: [P42-01, P42-02]
must_haves:
  truths:
    - "The transition runtime caches graph documents rather than hand-maintained entity/relation maps"
    - "Graphology owns one-hop expansion primitives needed by graph-assisted recall"
    - "Phase 42 stays inside runtime migration and does not change persistence contracts"
  artifacts:
    - path: "packages/server/src/lib/indexing/graph-lite/graphology.ts"
      provides: "Graphology-backed runtime snapshot and traversal helpers"
    - path: "packages/server/src/lib/indexing/adapters/graph.ts"
      provides: "Document-cache transition path without bespoke graph maps"
---

<objective>
Lay down the graphology-backed runtime layer for Phase 42.

Purpose: replace the last hand-maintained in-memory graph runtime with cached graph documents plus reusable graphology helpers, while preserving indexing behavior and transitional in-memory availability.
Output: a graphology runtime helper surface, adapter cache migration, and focused runtime tests.
</objective>

<tasks>
  <task>
    <name>Task 1: Add graphology runtime snapshot helpers</name>
    <read_first>
      - packages/server/src/lib/indexing/graph-lite/graphology.ts
      - packages/server/src/lib/indexing/graph-lite/graphology.test.ts
      - .planning/phases/42-replace-hand-rolled-graph-operations-with-graphology-based-g/42-RESEARCH.md
    </read_first>
    <files>packages/server/src/lib/indexing/graph-lite/graphology.ts, packages/server/src/lib/indexing/graph-lite/graphology.test.ts</files>
    <action>
      Extend the graphology wrapper with a runtime snapshot that derives node-to-source mappings, normalized-label lookups, one-hop candidate expansion, and relation-strength scoring from `GraphIndexDocumentRecord[]`. Add tests that verify the helper returns direct and one-hop source IDs deterministically.
    </action>
    <acceptance_criteria>
      - graphology wrapper exposes runtime helpers for query labels and candidate traversal
      - helper behavior is covered by focused tests
      - no public contract files are changed
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Replace the adapter's legacy cache shape</name>
    <read_first>
      - packages/server/src/lib/indexing/adapters/graph.ts
      - packages/server/src/lib/indexing/adapters/index.ts
      - packages/server/src/lib/indexing/graph-lite/store.ts
    </read_first>
    <files>packages/server/src/lib/indexing/adapters/graph.ts, packages/server/src/lib/indexing/adapters/index.ts</files>
    <action>
      Replace the legacy in-memory `{ entities, relations }` cache with a cached graph-document list keyed by source. Keep idempotency behavior intact, update sync/remove to mutate that document cache, and expose only document-cache helpers needed by recall/tests.
    </action>
    <acceptance_criteria>
      - adapter no longer maintains hand-built entity/relation maps
      - cache clearing still works for tests
      - transitional runtime remains usable without a store snapshot
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/indexing/graph-lite/graphology.test.ts src/lib/indexing/adapters/graph.test.ts`
  2. `pnpm --filter @trapmap/server typecheck`
</verification>
