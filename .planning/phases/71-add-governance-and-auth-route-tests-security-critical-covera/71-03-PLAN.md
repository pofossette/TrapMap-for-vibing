---
phase: 71-add-governance-and-auth-route-tests-security-critical-covera
plan: 03
type: testing
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/knowledge.test.ts
  - packages/contracts/src/domain/retrieval.test.ts
  - package.json
  - vitest.config.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-04
  - TEST-05
must_haves:
  truths:
    - Contracts use Zod schemas for validation
    - Knowledge schemas define entries, revisions, submissions
    - Retrieval schemas define queries, responses, citations
    - Vitest coverage requires @vitest/coverage-v8 package
  artifacts:
    - packages/contracts/src/domain/knowledge.ts (source)
    - packages/contracts/src/domain/retrieval.ts (source)
    - packages/contracts/src/domain/boundary.test.ts (existing test pattern)
  key_links:
    - vitest.config.ts (coverage config location)
---

# Plan 71-03: Contracts Schema Tests + Coverage Tooling

## Objective

Create unit tests for knowledge and retrieval schema contracts, and integrate Vitest coverage tooling.

## Scope

**Test files to create:**
- `packages/contracts/src/domain/knowledge.test.ts`
- `packages/contracts/src/domain/retrieval.test.ts`

**Configuration to update:**
- `package.json` (add test:coverage script, @vitest/coverage-v8)
- `vitest.config.ts` (add coverage configuration)
- `.github/workflows/ci.yml` (add coverage step)

## Test Cases

### knowledge.test.ts

**reviewRiskSchema (2 tests):**
1. Accepts valid risk levels (low, medium, high)
2. Rejects invalid risk level

**agentReviewStatusSchema (2 tests):**
3. Accepts valid statuses (agent-pass, agent-rejected)
4. Rejects invalid status

**agentReviewResultSchema (5 tests):**
5. Accepts complete result with all fields
6. Defaults notes to empty array
7. Accepts null boundary
8. Rejects missing required fields
9. Rejects invalid risk values

**reviewDecisionSchema (4 tests):**
10. Accepts approve decision with notes
11. Accepts reject decision with notes
12. Rejects empty notes
13. Rejects invalid decision

**reviewNoteSchema (5 tests):**
14. Accepts note with all fields
15. Defaults author to null
16. Accepts all authorType values
17. Rejects empty message
18. Rejects message over 2000 chars

**knowledgeRevisionSchema (5 tests):**
19. Accepts valid revision
20. Requires at least one label
21. Rejects shortcut over 280 chars
22. Rejects detail over 10000 chars
23. Defaults reviewNotes to empty array

**knowledgeEntrySchema (8 tests):**
24. Accepts complete entry with all fields
25. Requires at least one label
26. Requires non-empty history
27. Defaults optional fields (boundary, evidenceMeta, maintenanceMeta)
28. Rejects invalid lifecycleState
29. Rejects mismatched revision in latestRevision
30. Accepts entry with agentReview null
31. Merges auditMetadata fields

**knowledgeSubmissionSchema (5 tests):**
32. Accepts valid submission
33. Makes teamId optional
34. Makes requiredLevel optional
35. Makes boundary optional
36. Requires at least one label

**knowledgeListItemSchema (4 tests):**
37. Accepts valid list item
38. Defaults evidenceMeta to null
39. Requires valid lifecycleState
40. Accepts empty labels array

### retrieval.test.ts

**retrievalQueryModeSchema (2 tests):**
1. Accepts valid modes (semantic, hybrid, graph-assisted)
2. Rejects invalid mode

**retrievalFiltersSchema (4 tests):**
3. Accepts empty filters
4. Accepts filters with teamId
5. Defaults labels and scopes to empty arrays
6. Accepts null teamId

**retrievalCitationSchema (5 tests):**
7. Accepts complete citation
8. Requires non-empty snippet
9. Requires at least one recallChannel
10. Accepts null semantic/keyword/graph scores
11. Rejects scores outside 0-1 range

**retrievalQuerySchema (6 tests):**
12. Accepts minimal query (seed only)
13. Defaults maxResults to 10
14. Defaults mode to 'semantic'
15. Rejects seed over 2000 chars
16. Rejects maxResults over 50
17. Accepts boundaryContext option

**retrievalMatchSchema (5 tests):**
18. Accepts complete match
19. Makes citation optional
20. Makes conflicts optional
21. Makes boundaryExplanation optional
22. Requires valid securityLevel

**capsuleMatchSchema (6 tests):**
23. Accepts complete capsule match
24. Requires at least one label
25. Makes errorText optional
26. Makes conflicts optional
27. Validates content max 5000 chars
28. Validates score in 0-1 range

**retrievalV2QuerySchema (4 tests):**
29. Accepts seed-only query
30. Defaults maxResults to 10
31. Defaults includeSummary to false
32. Rejects empty seed

**retrievalV2ResponseSchema (4 tests):**
33. Defaults capsules to empty array
34. Defaults profileHints to empty array
35. Defaults summary to null
36. Accepts complete response

**retrievalStrategySchema (2 tests):**
37. Accepts valid strategies (naive, local, global, hybrid, mix, auto)
38. Rejects invalid strategy

**routingTraceSchema (5 tests):**
39. Accepts complete trace
40. Defaults channelsUsed to empty array
41. Defaults fallbackApplied to false
42. Makes confidenceScore nullable
43. Requires valid routingReason

## Coverage Tooling

### package.json updates

```json
{
  "scripts": {
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4"
  }
}
```

### vitest.config.ts updates

Add coverage configuration to each project:

```typescript
test: {
  // ... existing config
  coverage: {
    provider: 'v8',
    reporter: ['text', 'html', 'lcov'],
    exclude: [
      '**/*.test.ts',
      '**/*.d.ts',
      '**/dist/**',
      '**/node_modules/**'
    ],
    thresholds: {
      lines: 70,
      functions: 70,
      branches: 60,
      statements: 70
    }
  }
}
```

### .github/workflows/ci.yml updates

Add coverage step after test step:

```yaml
coverage:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: '20'

    - uses: pnpm/action-setup@v3
      with:
        version: 10.33.0

    - run: pnpm install --frozen-lockfile

    - run: pnpm test:coverage
      env:
        NODE_ENV: test

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coverage/
```

## Estimated Tests

~48 tests (knowledge: ~24, retrieval: ~24)

## Verification

```bash
# Install coverage package
pnpm add -D @vitest/coverage-v8

# Run tests
pnpm test packages/contracts/src/domain/knowledge.test.ts
pnpm test packages/contracts/src/domain/retrieval.test.ts

# Run coverage
pnpm test:coverage

# Verify coverage report generated
ls coverage/

# Typecheck
pnpm typecheck
```
