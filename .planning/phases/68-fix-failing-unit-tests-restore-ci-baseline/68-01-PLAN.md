---
phase: 68-fix-failing-unit-tests-restore-ci-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/routes/review.test.ts
  - packages/server/src/lib/artifacts/derive.test.ts
  - packages/server/src/lib/knowledge.ts
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/artifacts/model.ts
  - packages/server/src/lib/artifacts/pg-repository.ts
  - packages/server/src/lib/knowledge/pg-repository.ts
  - packages/server/src/lib/feedback/batch.ts
  - packages/server/src/lib/feedback/quality-score.ts
  - packages/server/src/lib/candidates/reconcile.ts
  - packages/cli/src/commands/review.ts
  - packages/cli/src/commands/review.test.ts
  - packages/contracts/src/domain/knowledge.ts
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "pnpm test exits with code 0 -- no failures"
    - "pnpm typecheck exits with code 0 -- no type errors"
    - "All 1725+ tests pass (0 failures)"
  artifacts:
    - path: "packages/server/src/routes/review.test.ts"
      provides: "Review route integration tests with correct lifecycle state fixtures"
      contains: "lifecycleState: 'agent-pass'"
    - path: "packages/server/src/lib/artifacts/derive.test.ts"
      provides: "Artifact derive tests with async/await fixes"
      contains: "async"
  key_links:
    - from: "packages/server/src/routes/review.test.ts"
      to: "packages/server/src/lib/lifecycle/state-machine.ts"
      via: "lifecycle state transition in applyReviewDecision"
      pattern: "lifecycleState.*agent-pass"
---

<objective>
Verify that all previously failing unit tests are now passing and CI baseline is restored.

Purpose: Phase 68's fix was already committed in `3fb096a` (fix: resolve typecheck, lint, and test errors across server and CLI). This plan verifies the fix is complete and documents what was done. The executor must confirm the CI baseline is green by running the full test suite and typecheck.

Output: Verified green CI baseline, summary documenting the fix.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-fix-failing-unit-tests-restore-ci-baseline/68-CONTEXT.md
@.planning/phases/68-fix-failing-unit-tests-restore-ci-baseline/68-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CI baseline is restored</name>
  <files>packages/server/src/routes/review.test.ts, packages/server/src/lib/artifacts/derive.test.ts</files>
  <read_first>
    packages/server/src/routes/review.test.ts
    packages/server/src/lib/artifacts/derive.test.ts
  </read_first>
  <action>
The test fixes were committed in `3fb096a`. Run verification commands to confirm everything passes:

1. Run `pnpm typecheck` -- must exit 0 with no errors.
2. Run `pnpm test` -- must exit 0 with 0 failed tests.
3. If any test fails, read the failing test file and corresponding production code to diagnose. The RESEARCH.md documents two root causes:
   - **Lifecycle state machine mismatch:** Tests used `lifecycleState: 'submitted'` but the state machine (Phase 62) requires `agent-pass` before approval. Fix: change to `lifecycleState: 'agent-pass'` in all test fixtures that will be approved.
   - **Missing required fields:** KnowledgeRecord now requires `decayMeta`, `evidenceMeta`, `maintenanceMeta`, `boundary`. Fix: add these fields (all `null`) to test fixtures.
4. If any fix is needed beyond what is already committed, apply the fix and re-run tests.

The following files were modified in commit `3fb096a` to fix the tests:
- `packages/server/src/routes/review.test.ts` -- lifecycleState: 'agent-pass', missing fields added
- `packages/server/src/lib/artifacts/derive.test.ts` -- async/await fixes
- `packages/server/src/lib/knowledge.ts` -- MaintenanceMetaRecord import, toKnowledgeEntry conversion
- `packages/server/src/lib/store.ts` -- MaintenanceMetaRecord type export
- `packages/server/src/lib/artifacts/model.ts` -- type fix
- `packages/server/src/lib/artifacts/pg-repository.ts` -- decayMeta/evidenceMeta/maintenanceMeta row mappings
- `packages/server/src/lib/knowledge/pg-repository.ts` -- decayMeta/evidenceMeta/maintenanceMeta row mappings
- `packages/server/src/lib/feedback/batch.ts` -- import fix
- `packages/server/src/lib/feedback/quality-score.ts` -- import/field fixes
- `packages/server/src/lib/candidates/reconcile.ts` -- import fix
- `packages/cli/src/commands/review.ts` -- --source-type, --source-ref, --evidence-level flags
- `packages/cli/src/commands/review.test.ts` -- test updates for new CLI flags
- `packages/contracts/src/domain/knowledge.ts` -- maintenanceMeta in schemas
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test</automated>
  </verify>
  <done>
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0
    - 0 test failures in output
    - All 1725+ tests pass
  </done>
  <acceptance_criteria>
    - `pnpm typecheck` command exits with code 0
    - `pnpm test` output contains "0 failed"
    - `pnpm test` output does NOT contain "failed" in any test file line
    - `packages/server/src/routes/review.test.ts` contains `lifecycleState: 'agent-pass'`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `pnpm typecheck` -- exits 0
2. `pnpm test` -- exits 0, 0 failures
3. Grep verification: `grep -c "lifecycleState: 'agent-pass'" packages/server/src/routes/review.test.ts` returns count >= 5
</verification>

<success_criteria>
- All tests pass (1725+ tests, 0 failures)
- Typecheck clean (exit 0)
- CI baseline fully restored
</success_criteria>

<output>
After completion, create `.planning/phases/68-fix-failing-unit-tests-restore-ci-baseline/68-01-SUMMARY.md`
</output>
