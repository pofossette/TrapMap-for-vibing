---
phase: 74-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: [73]
files_modified: []
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "pnpm test exits with code 0 -- no failures"
    - "pnpm typecheck exits with code 0 -- no type errors"
    - "Unused code removed without breaking functionality"
  artifacts: []
  key_links: []
---

<objective>
Remove unused functions, imports, and files across the codebase.

Purpose: Clean up dead code to improve maintainability and reduce bundle size.

Output: Cleaner codebase with no unused exports.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-dead-code-removal/74-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused files</name>
  <files>packages/server/src/lib/config/feature-flags.ts, packages/server/src/lib/feedback/batch.ts, packages/server/src/lib/feedback/quality-score.ts, packages/server/src/lib/lifecycle/index.ts, packages/server/src/lib/retrieval/recall/hybrid-recall.ts, packages/server/src/lib/retrieval/recall/pg-vector.ts</files>
  <action>
1. Verify each file has no imports using grep
2. Delete the unused files
3. Run `pnpm test` to verify no regressions
4. Run `pnpm typecheck` to verify no type errors
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <done>
    - All 6 unused files deleted
    - Tests pass
    - Typecheck passes
  </done>
  <acceptance_criteria>
    - `packages/server/src/lib/config/feature-flags.ts` deleted
    - `packages/server/src/lib/feedback/batch.ts` deleted
    - `packages/server/src/lib/feedback/quality-score.ts` deleted
    - `packages/server/src/lib/lifecycle/index.ts` deleted
    - `packages/server/src/lib/retrieval/recall/hybrid-recall.ts` deleted
    - `packages/server/src/lib/retrieval/recall/pg-vector.ts` deleted
    - `pnpm test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove unused exports from indexing adapters</name>
  <files>packages/server/src/lib/indexing/adapters/index.ts, packages/server/src/lib/indexing/adapters/keyword.ts, packages/server/src/lib/indexing/adapters/vector.ts</files>
  <action>
1. Read the adapter index file
2. Remove unused exports that are re-exports from other files
3. Keep exports that are used in production code
4. Run tests after changes
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>
    - Unused exports removed
    - Tests pass
  </done>
</task>

</tasks>

<verification>
1. `pnpm test` -- exits 0
2. `pnpm typecheck` -- exits 0
3. `pnpm knip` -- reduced unused exports count
</verification>

<success_criteria>
- Unused files removed
- Unused exports reduced
- All tests pass
- Typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/74-dead-code-removal/74-01-SUMMARY.md`
</output>
