---
phase: 14-seed-intent-retrieval-and-capsule-ranking
plan: "03"
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/assembly.ts
  - packages/server/src/lib/retrieval/assembly.test.ts
  - packages/server/src/lib/retrieval/summary.ts
  - packages/server/src/lib/retrieval/summary.test.ts
autonomous: true
requirements:
  - RETR-04
  - COMP-01
must_haves:
  truths:
    - "Default retrieval output is capsule-first and distilled rather than a raw skill bundle."
    - "Response shaping stays pure and contract-driven, separate from ranking and route logic."
    - "Optional summary behavior still summarizes only already-filtered distilled hits."
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "Final capsule-first v2 response schema used by server and CLI"
    - path: "packages/server/src/lib/retrieval/assembly.ts"
      provides: "Pure capsule-first response assembly helpers"
    - path: "packages/server/src/lib/retrieval/summary.ts"
      provides: "Pure summary builder for distilled capsule hits"
    - path: "packages/server/src/lib/retrieval/assembly.test.ts"
      provides: "Focused regression tests for response shaping"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "capsule hit shaping"
      pattern: "build.*RetrievalResponse"
    - from: "packages/server/src/lib/retrieval/assembly.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "response schema parse"
      pattern: "retrieval.*ResponseSchema"
    - from: "packages/server/src/lib/retrieval/summary.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "distilled hit summary inputs"
      pattern: "summary"
---

<objective>
Shape the v2 retrieval response around distilled capsule results and keep the response/summary layer pure.

Purpose: Deliver RETR-04 while preserving the contract-first boundary and the Phase 10 summary purity pattern.
Output: Capsule-first response contracts, pure assembly helpers, and focused tests for distilled response shaping.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-RESEARCH.md
@.planning/phases/10-回答与引用/10-04-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/assembly.ts
@packages/server/src/lib/retrieval/summary.ts

<interfaces>
From packages/server/src/lib/retrieval/assembly.ts:
```ts
export function buildRetrievalResponse(
  globalConstraints: RetrievalMatch[],
  projectKnowledge: RetrievalMatch[],
  refinementSummary: string | null,
  summary: RetrievalSummary | null = null,
): RetrievalResponse
```

From packages/server/src/lib/retrieval/summary.ts:
```ts
export function buildSummary(options: BuildSummaryOptions): RetrievalSummary | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize the shared capsule-first v2 response contract</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: v2 response schema accepts distilled capsule matches with artifact/profile metadata and optional summary.
    - Test 2: default retrieval payloads do not require bundle file contents.
    - Test 3: legacy response schemas remain available for coexistence during migration.
  </behavior>
  <action>Refine the additive contract work from 14-01 into the final capsule-first v2 response schema now that ranking inputs are known. Keep the default output distilled per RETR-04, reuse artifact-domain types where appropriate, and leave legacy retrieval schemas untouched for coexistence. Update contract tests to pin the distilled response shape.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>The shared contract exposes the final v2 distilled response shape, proves bundle payloads are not part of default search output, and preserves legacy schema availability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure capsule-first assembly and summary shaping</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/assembly.ts, packages/server/src/lib/retrieval/assembly.test.ts, packages/server/src/lib/retrieval/summary.ts, packages/server/src/lib/retrieval/summary.test.ts</files>
  <behavior>
    - Test 1: assembly groups distilled capsule results into stable response sections without exposing raw file bodies.
    - Test 2: match reasons and citations remain derivable from filtered capsule hits.
    - Test 3: summary generation only consumes already-filtered distilled hits and returns null when citations are absent.
  </behavior>
  <action>Rewrite `assembly.ts` around capsule-first distilled results and add a dedicated `assembly.test.ts` if none exists yet. Update `orchestrator.ts` in the same slice so the ranked capsule hits from 14-02 are explicitly handed off to the new v2 assembly builder before optional summary generation. Preserve the Phase 10 pure-function pattern in `summary.ts`: it should summarize only the distilled hits already chosen by the orchestrator, never re-fetch store data, and never reconstruct bundle payloads. Keep response shaping pure and reusable by route/CLI consumers.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/assembly.test.ts src/lib/retrieval/summary.test.ts src/lib/retrieval.test.ts</automated>
  </verify>
  <done>Pure assembly/summary helpers exist for capsule-first retrieval output, `orchestrator.ts` explicitly routes ranked capsule hits into the new v2 builder, and focused tests prove the response stays distilled and store-independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ranked capsule hits -> API response | Internal ranking output becomes externally visible here |
| filtered hits -> summary builder | Already-filtered data is condensed into user-facing summary text here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | I | `packages/server/src/lib/retrieval/assembly.ts` | mitigate | Emit distilled capsule/profile metadata only; do not include raw bundle file contents or activation-only payloads. |
| T-14-08 | T | `packages/server/src/lib/retrieval/summary.ts` | mitigate | Keep summary builder pure and limited to filtered distilled hits/citations so it cannot bypass governance filters. |
| T-14-09 | R | `packages/contracts/src/domain/retrieval.ts` | mitigate | Validate final v2 responses against shared schemas so CLI/server output remains auditable and consistent. |
</threat_model>

<verification>
Run focused shaping tests:
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/assembly.test.ts src/lib/retrieval/summary.test.ts src/lib/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- The default v2 search payload is capsule-first and distilled.
- Assembly and summary remain pure helpers with no store access.
- Contracts remain the only shared truth for the new response shape.
</success_criteria>

<output>
After completion, create `.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-03-SUMMARY.md`
</output>
