---
phase: 14-seed-intent-retrieval-and-capsule-ranking
plan: "04"
type: execute
wave: 4
depends_on:
  - 14-01
  - 14-03
files_modified:
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/server/src/lib/retrieval.ts
  - packages/cli/src/commands/retrieval.ts
  - packages/cli/src/commands/retrieval.test.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-03
  - RETR-01
must_haves:
  truths:
    - "CLI still accepts a single seed and can consume the capsule-first v2 response."
    - "A compatibility path preserves legacy retrieval during migration instead of forcing an in-place break."
    - "Routes stay thin and contract-driven while server retrieval internals evolve."
  artifacts:
    - path: "packages/server/src/routes/retrieval.ts"
      provides: "Thin legacy/v2 retrieval routing"
    - path: "packages/server/src/lib/retrieval.ts"
      provides: "Compatibility facade exposing stable retrieval entrypoints"
    - path: "packages/cli/src/commands/retrieval.ts"
      provides: "Seed-only CLI formatter for capsule-first output"
    - path: "packages/cli/src/commands/retrieval.test.ts"
      provides: "CLI regression coverage for v2 output and migration coexistence"
  key_links:
    - from: "packages/cli/src/commands/retrieval.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "POST request to selected retrieval path"
      pattern: "/v(1|2)/retrieval/search"
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "shared schema parse/response validation"
      pattern: "retrieval.*Schema"
    - from: "packages/server/src/lib/retrieval.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "compatibility facade for legacy/v2 entrypoints"
      pattern: "searchKnowledge"
---

<objective>
Wire the v2 retrieval contract through the server route and CLI while preserving legacy retrieval coexistence during migration.

Purpose: Deliver the user-visible Phase 14 surface without breaking the single-seed UX or the legacy compatibility window.
Output: Thin route integration, CLI v2 formatting, and targeted compatibility regressions for legacy + v2 retrieval paths.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-RESEARCH.md
@.planning/phases/10-回答与引用/10-04-SUMMARY.md
@packages/server/src/routes/retrieval.ts
@packages/server/src/lib/retrieval.ts
@packages/cli/src/commands/retrieval.ts
@packages/server/src/routes/retrieval.test.ts
@packages/cli/src/commands/retrieval.test.ts

<interfaces>
From packages/server/src/routes/retrieval.ts:
```ts
app.post('/v1/retrieval/search', async (request) => {
  const auth = await resolveAuthContext(app.skillShareer, request);
  requirePermission(auth, 'knowledge:search');
  const query = retrievalQuerySchema.parse(request.body);
  const result = await searchKnowledge(app.skillShareer, auth, query);
  return retrievalResponseSchema.parse(result);
});
```

From packages/cli/src/commands/retrieval.ts:
```ts
program
  .command('search')
  .argument('[seed]', 'Search seed text or query')
  .option('--json', 'Output JSON')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thin compatibility routing for legacy and v2 retrieval</name>
  <files>packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts, packages/server/src/lib/retrieval.ts</files>
  <behavior>
    - Test 1: the legacy `/v1/retrieval/search` contract remains reachable during migration.
    - Test 2: the new capsule-native retrieval path validates the v2 contract and delegates without route-layer business logic.
    - Test 3: route tests continue to prove auth and permission checks are enforced before delegation.
  </behavior>
  <action>Keep `routes/retrieval.ts` thin and contract-driven while introducing `POST /v2/retrieval/search` as the additive capsule-native path per COMP-03. Leave `POST /v1/retrieval/search` intact for legacy flat-entry retrieval during coexistence. Use `packages/server/src/lib/retrieval.ts` as the facade boundary so route code only chooses the correct schema/entrypoint and delegates; do not move ranking, parsed-intent, or assembly logic into the route layer.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/routes/retrieval.test.ts</automated>
  </verify>
  <done>Legacy `/v1/retrieval/search` remains reachable, the new `/v2/retrieval/search` path is thin and schema-driven, and focused route tests prove both paths preserve auth/permission behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve the single-seed CLI UX while rendering capsule-first output</name>
  <files>packages/cli/src/commands/retrieval.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <behavior>
    - Test 1: `search [seed]` still sends only the seed plus existing optional filters/flags.
    - Test 2: text output renders capsule-first distilled sections without exposing bundle payloads.
    - Test 3: JSON mode prints the full v2 contract shape unchanged for agent consumption.
  </behavior>
  <action>Update the CLI retrieval command to call the selected v2 path while preserving the same single-seed argument/stdin UX per RETR-01. Refresh text formatting for capsule-first results and keep JSON mode contract-faithful. Do not add new required client-side intent flags, and do not edit unrelated dirty import/export command files.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>The CLI still exposes a single-seed retrieval UX, text output is capsule-first and distilled, and JSON mode mirrors the shared v2 response contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI request -> retrieval routes | User-controlled seed input crosses the network boundary here |
| route facade -> retrieval internals | Thin route delegates to either legacy or v2 pipeline here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | S | `packages/server/src/routes/retrieval.ts` | mitigate | Keep existing auth resolution and `knowledge:search` permission checks on both legacy and v2 retrieval routes. |
| T-14-11 | T | `packages/cli/src/commands/retrieval.ts` | mitigate | Preserve seed-only UX and shared schema parsing so clients cannot drift from the contract during migration. |
| T-14-12 | R | `packages/server/src/lib/retrieval.ts` | mitigate | Centralize legacy/v2 delegation in the facade so compatibility behavior is auditable and route logic stays minimal. |
</threat_model>

<verification>
Run targeted integration checks:
- `pnpm --filter @skill-shareer/server test -- src/routes/retrieval.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
- `pnpm --filter @skill-shareer/cli exec tsc --noEmit`
</verification>

<success_criteria>
- The CLI still feels identical at input time: one seed, optional filters, optional JSON.
- A legacy-compatible retrieval path remains available during migration.
- Route code stays thin and contract-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-04-SUMMARY.md`
</output>
