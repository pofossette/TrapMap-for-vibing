---
phase: 14-seed-intent-retrieval-and-capsule-ranking
plan: "02"
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - packages/server/src/lib/artifacts/derive.ts
  - packages/server/src/lib/artifacts/derive.test.ts
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/artifacts/model.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/capsule-recall.ts
  - packages/server/src/lib/retrieval/capsule-recall.test.ts
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/routes/operations.ts
autonomous: true
requirements:
  - RETR-03
  - CAPS-04
  - COMP-02
must_haves:
  truths:
    - "Approved artifact-derived profiles and capsules become the main retrieval candidates instead of flat knowledge entries."
    - "Ranking considers parsed problem/situation signals plus stack/path boosts while still respecting team, level, and approval gates."
    - "The Phase 12 derivation seam remains the source of profile/capsule data; retrieval does not bypass it."
  artifacts:
    - path: "packages/server/src/lib/artifacts/derive.ts"
      provides: "Text-backed profile/capsule derivation suitable for ranking"
    - path: "packages/server/src/lib/store.ts"
      provides: "Artifact payload lookup for derivation-eligible text"
    - path: "packages/server/src/lib/retrieval/capsule-recall.ts"
      provides: "Profile shortlist and capsule ranking helpers"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "Governed v2 recall pipeline over artifact-derived data"
    - path: "packages/server/src/lib/retrieval.test.ts"
      provides: "End-to-end retrieval regression coverage for capsule recall"
  key_links:
    - from: "packages/server/src/lib/artifacts/derive.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "latestRevision.derived.profile/capsules"
      pattern: "derived\\.(profile|capsules)"
    - from: "packages/server/src/lib/store.ts"
      to: "packages/server/src/lib/artifacts/derive.ts"
      via: "artifactFilePayloads resolved into derivation-eligible text inputs"
      pattern: "artifactFilePayloads"
    - from: "packages/server/src/lib/retrieval/intent.ts"
      to: "packages/server/src/lib/retrieval/capsule-recall.ts"
      via: "parsed intent scoring inputs"
      pattern: "ParsedIntent"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/store.ts"
      via: "pre-filtered governed artifact snapshot"
      pattern: "skillArtifacts"
---

<objective>
Convert retrieval from flat-entry recall to artifact-profile shortlist plus capsule ranking, while keeping governance and the Phase 12 derivation boundary intact.

Purpose: Deliver RETR-03 and CAPS-04 without regressing auth, team, approval, or security-level enforcement.
Output: Upgraded derivation content, capsule recall helpers, and orchestrator coverage proving governed capsule-native search works.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-RESEARCH.md
@.planning/phases/12-skill-artifact-canonical-model/12-03-SUMMARY.md
@packages/contracts/src/domain/artifacts.ts
@packages/server/src/lib/artifacts/derive.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/retrieval.test.ts

<interfaces>
From packages/contracts/src/domain/artifacts.ts:
```ts
export type SkillProfile = z.infer<typeof skillProfileSchema>;
export type SkillCapsule = z.infer<typeof skillCapsuleSchema>;

export const skillCapsuleSchema = z.object({
  capsuleId: entityIdSchema,
  artifactId: entityIdSchema,
  revision: z.number().int().min(1),
  sourcePaths: z.array(z.string().max(512)).min(1),
  content: z.string().min(1).max(5000),
  situation: z.string().min(1).max(1000),
  problem: z.string().min(1).max(1000),
  goal: z.string().min(1).max(1000),
  errorText: z.string().max(500).optional(),
  labels: z.array(labelSchema).min(1),
  scope: scopeSchema,
  requiredLevel: securityLevelSchema,
});
```

From packages/server/src/lib/retrieval/orchestrator.ts:
```ts
const data = await services.store.snapshot();
const eligibleEntries = filterEligibleEntries(data.knowledgeEntries, auth, parsed.filters);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder derivation text with retrieval-grade profile and capsule content</name>
  <files>packages/server/src/lib/artifacts/derive.ts, packages/server/src/lib/artifacts/derive.test.ts, packages/server/src/lib/store.ts, packages/server/src/lib/artifacts/model.ts, packages/server/src/routes/operations.ts</files>
  <behavior>
    - Test 1: derived profile summary/keywords are built from SKILL.md and reference text, not only title/labels placeholders.
    - Test 2: multiple capsules can be produced from meaningful text sections with situation/problem/goal/errorText fields populated when source text supports them.
    - Test 3: assets and scripts remain excluded from profile/capsule content per the Phase 12 boundary.
  </behavior>
  <action>Upgrade `derive.ts` so Phase 12 placeholders become text-backed profile/capsule outputs suitable for retrieval ranking. Make the payload-to-derivation seam explicit: add a store/model helper that resolves derivation-eligible SKILL.md/reference payload text from `artifactFilePayloads`, then thread that helper through the import-time derivation wiring so cached `latestRevision.derived.*` is computed from governed stored text instead of metadata-only placeholders. Because `packages/server/src/routes/operations.ts` is currently dirty, execution must keep that edit narrowly scoped to the derivation call site, preserve surrounding user changes, and verify against the existing operations tests before continuing. Do not introduce an alternate retrieval cache or direct bundle reparsing inside search-time code.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/artifacts/derive.test.ts src/routes/operations.test.ts</automated>
  </verify>
  <done>Derived profiles and capsules contain retrieval-useful text from SKILL.md and references, with tests proving the derivation boundary still excludes activation-only file content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement governed profile shortlist and capsule ranking</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/capsule-recall.ts, packages/server/src/lib/retrieval/capsule-recall.test.ts, packages/server/src/lib/retrieval.test.ts</files>
  <behavior>
    - Test 1: only approved, in-scope, within-level artifact revisions enter the shortlist.
    - Test 2: parsed problem/situation/error signals and stack/path boosts affect capsule ordering.
    - Test 3: when both legacy entries and artifact capsules exist during coexistence, the v2 pipeline reads derived artifact outputs without mutating the legacy path.
  </behavior>
  <action>Create a dedicated `capsule-recall.ts` helper for profile shortlist and capsule ranking, then rework `orchestrator.ts` to use parsed intent plus `latestRevision.derived.profile/capsules` as the primary recall path per RETR-03 and CAPS-04. Preserve server-side governance filtering before ranking, reuse the shared/internal types from 14-01, and keep legacy `/v1` retrieval behavior isolated rather than partially rewriting it in place.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/capsule-recall.test.ts src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts</automated>
  </verify>
  <done>The v2 retrieval pipeline recalls governed artifact profiles first, ranks capsules second, and regression tests cover governance filters plus capsule scoring signals while the existing `retrieval-workflow.test.ts` seam stays green as an unchanged integration gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store snapshot -> retrieval shortlist | Persisted artifact data becomes ranked retrieval candidates here |
| parsed intent -> ranking | Untrusted seed-derived signals influence ordering here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | E | `packages/server/src/lib/retrieval/orchestrator.ts` | mitigate | Preserve approval/team/level filtering before any shortlist or ranking work so unauthorized artifacts never enter scoring. |
| T-14-05 | T | `packages/server/src/lib/artifacts/derive.ts` | mitigate | Keep derivation inputs limited to SKILL.md and references; exclude assets/scripts from retrieval content to preserve the Phase 12 boundary. |
| T-14-06 | I | `packages/server/src/lib/retrieval/capsule-recall.ts` | mitigate | Rank only distilled profile/capsule text and metadata; do not inline raw file payloads or script bodies into retrieval results. |
</threat_model>

<verification>
Run targeted tests instead of the full server suite:
- `pnpm --filter @skill-shareer/server test -- src/lib/artifacts/derive.test.ts src/routes/operations.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/capsule-recall.test.ts src/lib/retrieval.test.ts src/lib/retrieval-workflow.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- Retrieval-grade profile/capsule derivation exists on the Phase 12 seam.
- The v2 server pipeline uses artifact-derived profiles/capsules as the primary recall unit.
- Governance filters are still enforced before ranking.
</success_criteria>

<output>
After completion, create `.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-02-SUMMARY.md`
</output>
