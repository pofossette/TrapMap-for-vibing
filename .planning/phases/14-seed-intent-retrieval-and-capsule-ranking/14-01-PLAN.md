---
phase: 14-seed-intent-retrieval-and-capsule-ranking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/index.test.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/intent.ts
  - packages/server/src/lib/retrieval/intent.test.ts
autonomous: true
requirements:
  - RETR-01
  - RETR-02
  - COMP-01
must_haves:
  truths:
    - "CLI-facing retrieval requests still require only one seed string."
    - "The server can derive internal situation, problem, goal, and errorText fields from one seed before recall begins."
    - "The shared retrieval contract exposes a capsule-native v2 response shape without forcing the client to send structured intent fields."
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "Seed-only v2 retrieval request/response schemas"
    - path: "packages/server/src/lib/retrieval/types.ts"
      provides: "Internal parsed-intent and capsule retrieval pipeline types"
    - path: "packages/server/src/lib/retrieval/intent.ts"
      provides: "Pure intent parsing helpers kept inside the server boundary"
    - path: "packages/server/src/lib/retrieval/intent.test.ts"
      provides: "Focused regression coverage for parsed-intent behavior"
  key_links:
    - from: "packages/contracts/src/domain/retrieval.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "shared zod schemas for request/response validation"
      pattern: "retrieval(V2)?(Query|Response)Schema"
    - from: "packages/server/src/lib/retrieval/intent.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "internal parsed-intent handoff before recall"
      pattern: "parseSeedIntent"
---

<objective>
Define the seed-only retrieval v2 contract and the server-only parsed-intent seam that later Phase 14 plans build on.

Purpose: Lock RETR-01/RETR-02 and COMP-01 before any ranking or CLI wiring changes.
Output: Shared v2 retrieval schemas, internal parsed-intent types/helpers, and focused tests proving the client contract stays seed-only.
</objective>

<execution_context>
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/Disks/Data/my-project/Trap-Map/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-RESEARCH.md
@.planning/phases/12-skill-artifact-canonical-model/12-03-SUMMARY.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/retrieval/orchestrator.ts

<interfaces>
From packages/contracts/src/domain/retrieval.ts:
```ts
export const retrievalQuerySchema = z.object({
  seed: z.string().min(1).max(2000),
  filters: retrievalFiltersSchema.default({ labels: [], scopes: [] }),
  maxResults: z.number().int().min(1).max(50).default(10),
  includeRefinement: z.boolean().default(true),
  includeSummary: z.boolean().default(false),
  mode: retrievalQueryModeSchema.default('semantic'),
});

export const retrievalResponseSchema = z.object({
  globalConstraints: z.array(retrievalMatchSchema),
  projectKnowledge: z.array(retrievalMatchSchema),
  refinementSummary: z.string().nullable(),
  summary: retrievalSummarySchema.nullable(),
});
```

From packages/server/src/lib/retrieval/types.ts:
```ts
export interface RetrievalPipelineContext {
  auth: ResolvedAuthContext;
  query: RetrievalQuery;
  dataSnapshot: {
    knowledgeEntries: KnowledgeRecord[];
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seed-only v2 retrieval schemas to contracts</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/index.test.ts</files>
  <behavior>
    - Test 1: v2 query schema accepts `seed` plus existing optional filters/flags and rejects required structured intent fields.
    - Test 2: v2 response schema accepts capsule-first distilled results and coexists with the legacy response schema.
    - Test 3: shared contract exports remain the only cross-package truth for retrieval v2 shapes per COMP-01.
  </behavior>
  <action>Extend `packages/contracts/src/domain/retrieval.ts` with additive v2 schemas and types for seed-based capsule retrieval, keeping `seed` as the only required client input per RETR-01 and keeping legacy schemas intact for coexistence. Reuse artifact-domain types by reference where practical instead of duplicating capsule/profile concepts. Update `packages/contracts/src/index.test.ts` with focused schema coverage for the new request/response variants.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/contracts test -- src/index.test.ts</automated>
  </verify>
  <done>Contracts expose additive v2 retrieval schemas, tests prove no structured intent fields became required on the client contract, and legacy retrieval schemas still parse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create internal parsed-intent types and pure parser</name>
  <files>packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/intent.ts, packages/server/src/lib/retrieval/intent.test.ts</files>
  <behavior>
    - Test 1: a freeform seed yields normalized tokens plus nullable `situation`, `problem`, `goal`, and `errorText`.
    - Test 2: error-like seeds surface `errorText` without leaking new fields into the shared client contract.
    - Test 3: stack/path hints are extracted deterministically for later ranking inputs.
  </behavior>
  <action>Create `packages/server/src/lib/retrieval/intent.ts` as a pure helper module and expand `packages/server/src/lib/retrieval/types.ts` with parsed-intent and capsule-recall types. Keep this seam server-internal per RETR-02: do not export parsed-intent fields through `packages/contracts`, do not push parsing into the CLI, and do not modify unrelated import/export modules. The parser baseline must be deterministic and heuristic-driven so it runs without `OPENAI_API_KEY`; any future model assistance must stay optional behind the same `ParsedIntent` interface.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/intent.test.ts</automated>
  </verify>
  <done>`parseSeedIntent()` exists with focused tests, retrieval internal types cover parsed intent and upcoming capsule candidates, and no client-facing schema requires structured intent input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI seed -> retrieval contract | Untrusted natural-language input enters the system here |
| retrieval route -> orchestrator internals | Contract-validated request becomes internal parsed-intent state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T | `packages/contracts/src/domain/retrieval.ts` | mitigate | Keep v2 request schema seed-only so clients cannot spoof privileged internal intent fields; server derives parsed intent itself. |
| T-14-02 | I | `packages/server/src/lib/retrieval/intent.ts` | mitigate | Keep parsed-intent model server-internal and avoid surfacing raw parser internals through CLI/API contracts. |
| T-14-03 | D | `packages/server/src/lib/retrieval/intent.ts` | mitigate | Use deterministic string parsing/token normalization only; no blocking network dependency for baseline intent extraction. |
</threat_model>

<verification>
Run focused contract and intent tests only:
- `pnpm --filter @skill-shareer/contracts test -- src/index.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/intent.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- A v2 retrieval contract exists for `/v2/retrieval/search` with seed-only input and capsule-first response types.
- Internal parsed-intent helpers exist only on the server side.
- No Phase 14 work in this plan touches the dirty import/export `operations` files.
</success_criteria>

<output>
After completion, create `.planning/phases/14-seed-intent-retrieval-and-capsule-ranking/14-01-SUMMARY.md`
</output>
