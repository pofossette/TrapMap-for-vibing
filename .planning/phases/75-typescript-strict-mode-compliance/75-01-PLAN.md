---
phase: 75-typescript-strict-mode-compliance
plan: 01
type: execute
wave: 1
depends_on: [74]
files_modified:
  - packages/server/src/lib/retrieval/benchmark.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "pnpm typecheck exits with code 0 -- no type errors"
    - "pnpm test exits with code 0 -- no failures"
    - "TypeScript strict mode already enabled in tsconfig.base.json"
  artifacts: []
  key_links: []
---

<objective>
Fix all TypeScript type errors in strict mode.

Purpose: Ensure codebase is fully type-safe with strict mode enabled.

Output: Clean typecheck with 0 errors.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-typescript-strict-mode-compliance/75-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix type errors in benchmark.ts and orchestrator.ts</name>
  <files>packages/server/src/lib/retrieval/benchmark.ts, packages/server/src/lib/retrieval/orchestrator.ts</files>
  <action>
1. Run `pnpm typecheck` to identify errors
2. Fix each error:
   - benchmark.ts: requiredLevel is number, not string
   - orchestrator.ts: use scopes array instead of scope property
   - Handle exactOptionalPropertyTypes with spread operator
3. Run `pnpm typecheck` to verify fixes
4. Run `pnpm test` to verify no regressions
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test</automated>
  </verify>
  <done>
    - Typecheck passes with 0 errors
    - Tests pass with 0 failures
  </done>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `pnpm typecheck` -- exits 0
2. `pnpm test` -- exits 0
</verification>

<success_criteria>
- Typecheck clean
- All tests pass
- Strict mode already enabled
</success_criteria>

<output>
After completion, create `.planning/phases/75-typescript-strict-mode-compliance/75-01-SUMMARY.md`
</output>
