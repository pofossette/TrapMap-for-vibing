---
phase: 38
plan: 02
type: tdd
wave: 2
depends_on: [38-01]
files_modified:
  - packages/server/src/lib/rag-log.ts
  - packages/server/src/lib/retrieval/graph-plan-search.ts
  - packages/server/src/lib/retrieval/graph-plan-search.test.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements: [P38-03, P38-04]
must_haves:
  truths:
    - "GraphRAG-lite selection uses deterministic confidence scoring rather than opaque heuristics"
    - "Low-confidence graph plans fall back to governed v2 capsule or v1 graph-assisted retrieval"
    - "The additive /v3/retrieval/search endpoint preserves the raw /v3/retrieval/plan compiler surface"
    - "RAG logs and route responses expose auditable routing metadata for selected and fallback outcomes"
  artifacts:
    - path: "packages/server/src/lib/retrieval/graph-plan-search.ts"
      provides: "Wrapper service that compiles, scores confidence, and executes fallback"
    - path: "packages/server/src/routes/retrieval.ts"
      provides: "New /v3/retrieval/search route"
    - path: "packages/server/src/lib/retrieval/graph-plan-search.test.ts"
      provides: "Deterministic confidence and fallback tests"
  key_links:
    - from: "packages/server/src/lib/retrieval/graph-plan-search.ts"
      to: "packages/server/src/lib/retrieval/plan-compiler.ts"
      via: "compileTrapFirstPlan"
      pattern: "compileTrapFirstPlan"
    - from: "packages/server/src/lib/retrieval/graph-plan-search.ts"
      to: "packages/server/src/lib/retrieval.js"
      via: "searchKnowledge and searchKnowledgeV2 fallback paths"
      pattern: "searchKnowledgeV2"
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/graph-plan-search.ts"
      via: "searchKnowledgeGraphPlan"
      pattern: "v3/retrieval/search"
---

<objective>
Implement the GraphRAG-lite wrapper retrieval path with deterministic confidence routing and governed fallback behavior.

Purpose: turn the raw Phase 37 compiler into an additive retrieval option that is safe to enable without destabilizing legacy retrieval.
Output: new wrapper service, route, logging updates, and tests.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-CONTEXT.md
@.planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-RESEARCH.md
@packages/server/src/lib/retrieval/plan-compiler.ts
@packages/server/src/lib/retrieval.ts
@packages/server/src/routes/retrieval.ts
@packages/server/src/lib/rag-log.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement confidence scoring and fallback execution</name>
  <files>packages/server/src/lib/retrieval/graph-plan-search.ts, packages/server/src/lib/retrieval/graph-plan-search.test.ts, packages/server/src/lib/rag-log.ts</files>
  <read_first>
    packages/server/src/lib/retrieval/plan-compiler.ts
    packages/server/src/lib/retrieval.ts
    packages/server/src/lib/rag-log.ts
    .planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-RESEARCH.md
  </read_first>
  <behavior>
    - high-confidence plans are returned directly
    - blocker-only/no-skill outcomes fall back to v1 graph-assisted
    - weak-trap or empty-plan outcomes fall back to v2 capsule retrieval
    - routing trace records reason, fallback target, confidence bucket, and channels used
    - log typing accepts the GraphRAG-lite route family and metadata
  </behavior>
  <action>
    Create a dedicated wrapper service, for example `searchKnowledgeGraphPlan()`, that calls `compileTrapFirstPlan()`, computes confidence with deterministic count/edge heuristics, and then either returns the selected plan or executes the chosen fallback path.
  </action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/retrieval/graph-plan-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - wrapper service uses `compileTrapFirstPlan` first
    - confidence/fallback logic is deterministic and unit-tested
    - v1/v2 fallback searches reuse existing governed services
    - log typings include GraphRAG-lite route metadata
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the additive /v3/retrieval/search route</name>
  <files>packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts</files>
  <read_first>
    packages/server/src/routes/retrieval.ts
    packages/server/src/routes/retrieval.test.ts
    packages/server/src/lib/retrieval/graph-plan-search.ts
  </read_first>
  <behavior>
    - route enforces the same `knowledge:search` permission gate as v1/v2
    - route validates input and output with graph-plan wrapper schemas
    - route logs endpoint, route family, fallback target, and result counts
    - existing `/v3/retrieval/plan` route remains present
  </behavior>
  <action>
    Wire the new wrapper service into `POST /v3/retrieval/search` without altering the raw Phase 37 plan route.
  </action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/routes/retrieval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `/v3/retrieval/search` is registered
    - `/v3/retrieval/plan` is still registered
    - route requires auth and `knowledge:search`
    - route validates against `graphPlanSearchQuerySchema` and `graphPlanSearchResponseSchema`
    - route tests cover auth/schema/registration expectations
  </acceptance_criteria>
</task>

</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/retrieval/graph-plan-search.test.ts src/routes/retrieval.test.ts`
  2. `pnpm --filter @trapmap/server exec tsc --noEmit`
</verification>

<success_criteria>
GraphRAG-lite is available as a safe additive retrieval route with deterministic fallback and auditable traces.
</success_criteria>
