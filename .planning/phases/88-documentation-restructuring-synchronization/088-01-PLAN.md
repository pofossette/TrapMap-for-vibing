---
phase: 88
plan: "01"
type: docs
wave: 1
depends_on: []
files_modified:
  - docs/architecture.md (DELETE)
  - docs/architecture/ARCHITECTURE_en.md (DELETE)
  - docs/retrieval-structure-adjustment.md (MOVE to docs/archived/)
  - docs/archived-plans/plan.md (MOVE to docs/archived/)
  - docs/archived/README.md (CREATE)
  - architecture.md (UPDATE - add pointer to detailed version)
autonomous: true
---

# Plan 88-01: Eliminate Duplicate Architecture Files & Archive Outdated Docs

## Context

Documentation audit identified:
1. Three layers of architecture.md duplication (root, docs/, docs/architecture/)
2. ARCHITECTURE_en.md is incomplete English translation - maintenance burden
3. `docs/retrieval-structure-adjustment.md` is historical document from v1.x
4. `docs/archived-plans/plan.md` is old planning document

## Task

### Step 1: Delete duplicate `docs/architecture.md`

<read_first>
- docs/architecture.md (current content to verify it's a duplicate)
- architecture.md (root version to compare)
</read_first>

<action>
Delete the file `docs/architecture.md` using `git rm docs/architecture.md`. This file duplicates content from the root `architecture.md`.
</action>

<acceptance_criteria>
- `docs/architecture.md` does not exist
- Git shows the file as deleted
</acceptance_criteria>

---

### Step 2: Delete incomplete English translation `docs/architecture/ARCHITECTURE_en.md`

<read_first>
- docs/architecture/ARCHITECTURE_en.md (verify it's incomplete translation)
- docs/architecture/ARCHITECTURE.md (Chinese authoritative version)
</read_first>

<action>
Delete the file `docs/architecture/ARCHITECTURE_en.md` using `git rm docs/architecture/ARCHITECTURE_en.md`.

Reason: The English version is incomplete and creates maintenance burden. The Chinese version is authoritative. If English translation is needed later, it should be done systematically with tooling support.
</action>

<acceptance_criteria>
- `docs/architecture/ARCHITECTURE_en.md` does not exist
- Git shows the file as deleted
</acceptance_criteria>

---

### Step 3: Create `docs/archived/` directory and README

<read_first>
- docs/README.md (to understand current structure)
</read_first>

<action>
1. Create directory `docs/archived/` if it doesn't exist
2. Create `docs/archived/README.md` with the following content:

```markdown
# 归档文档

本目录存放已过时但保留作历史参考的文档。

## 归档文件

| 文件 | 归档日期 | 归档原因 |
|------|----------|----------|
| `retrieval-structure-adjustment.md` | 2026-05-06 | v1.x 早期检索架构调整决策背景，已整合入主架构文档 `docs/architecture/RETRIEVAL.md` |
| `archived-plans/plan.md` | 2026-05-06 | 短期库化改造计划，已整合入 `.planning/ROADMAP.md` |

## 归档原则

文档归档而非删除，保留历史决策背景供参考。归档文档不应被其他文档引用。
```
</action>

<acceptance_criteria>
- `docs/archived/README.md` exists with the specified content
- File contains table with both archived files listed
</acceptance_criteria>

---

### Step 4: Move `docs/retrieval-structure-adjustment.md` to archive

<read_first>
- docs/retrieval-structure-adjustment.md (content to understand what's being archived)
</read_first>

<action>
Move the file from `docs/retrieval-structure-adjustment.md` to `docs/archived/retrieval-structure-adjustment.md` using `git mv docs/retrieval-structure-adjustment.md docs/archived/retrieval-structure-adjustment.md`.
</action>

<acceptance_criteria>
- `docs/retrieval-structure-adjustment.md` no longer exists at original location
- `docs/archived/retrieval-structure-adjustment.md` exists
- Git shows the file as renamed
</acceptance_criteria>

---

### Step 5: Move `docs/archived-plans/` directory to archive

<read_first>
- docs/archived-plans/plan.md (content to understand what's being archived)
</read_first>

<action>
Move the entire directory from `docs/archived-plans/` to `docs/archived/archived-plans/` using:
```bash
git mv docs/archived-plans docs/archived/archived-plans
```
</action>

<acceptance_criteria>
- `docs/archived-plans/` directory no longer exists at original location
- `docs/archived/archived-plans/plan.md` exists
- Git shows the directory as renamed
</acceptance_criteria>

---

### Step 6: Update root `architecture.md` to point to detailed version

<read_first>
- architecture.md (current content)
</read_first>

<action>
Update the root `architecture.md` file. Change the "完整架构文档" reference line to make it clearer that the root version is a brief overview:

Find the section:
```markdown
## 文档导航

完整架构文档（包含模块详解、流程图、技术细节）请参阅：

**[docs/architecture/ARCHITECTURE.md](./docs/architecture/ARCHITECTURE.md)**
```

Replace with:
```markdown
## 文档导航

> **说明**：本文档为架构简洁概览。完整架构文档（包含模块详解、流程图、技术细节）请参阅下方链接。

**[docs/architecture/ARCHITECTURE.md](./docs/architecture/ARCHITECTURE.md)** — 完整架构文档

其他架构文档：
```
</action>

<acceptance_criteria>
- `architecture.md` contains the text "本文档为架构简洁概览"
- The link to detailed ARCHITECTURE.md is preserved
</acceptance_criteria>

---

### Step 7: Update docs/README.md to remove deleted file references

<read_first>
- docs/README.md (current content)
</read_first>

<action>
Update `docs/README.md` to:
1. Remove reference to `architecture/ARCHITECTURE_en.md`
2. Add reference to the new `archived/` directory

Find and remove this line:
```markdown
- [架构详解（英文）](architecture/ARCHITECTURE_en.md) - English translation
```

After the architecture links section, add:
```markdown
### 归档文档
- [归档文档](archived/) - 历史参考文档
```
</action>

<acceptance_criteria>
- `docs/README.md` does not contain "ARCHITECTURE_en.md"
- `docs/README.md` contains link to `archived/`
</acceptance_criteria>

---

## Verification

After all steps:
1. Run `ls docs/architecture/` - should NOT include `ARCHITECTURE_en.md`
2. Run `ls docs/` - should NOT include `architecture.md` or `retrieval-structure-adjustment.md`
3. Run `ls docs/archived/` - should include `README.md`, `retrieval-structure-adjustment.md`, `archived-plans/`
4. Run `grep -r "ARCHITECTURE_en" docs/` - should return no results
5. Run `grep -r "retrieval-structure-adjustment" docs/ --include="*.md"` - should only find reference in archived/README.md
