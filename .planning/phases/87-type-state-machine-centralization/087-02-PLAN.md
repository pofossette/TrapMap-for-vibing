---
phase: 87-type-state-machine-centralization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/state-machines/index.ts
autonomous: true
requirements:
  - "87-req-3"

must_haves:
  truths:
    - "Decay state machine functions are importable from lib/state-machines/index.js"
    - "Lifecycle state machine functions are importable from lib/state-machines/index.js"
    - "Original import paths (decay/state-machine.js and lifecycle/state-machine.js) still work"
  artifacts:
    - path: "packages/server/src/lib/state-machines/index.ts"
      provides: "Unified barrel re-export for decay and lifecycle state machines"
      contains: "export * from '../decay/state-machine.js'"
  key_links:
    - from: "packages/server/src/lib/state-machines/index.ts"
      to: "packages/server/src/lib/decay/state-machine.ts"
      via: "export * from '../decay/state-machine.js'"
      pattern: "export \\* from '\\.\\./decay/state-machine\\.js'"
    - from: "packages/server/src/lib/state-machines/index.ts"
      to: "packages/server/src/lib/lifecycle/state-machine.ts"
      via: "export * from '../lifecycle/state-machine.js'"
      pattern: "export \\* from '\\.\\./lifecycle/state-machine\\.js'"
---

<objective>
Create a unified barrel export for the decay and lifecycle state machines.

Purpose: Currently, consumers must import from separate paths (`decay/state-machine.js` and `lifecycle/state-machine.js`). A unified `state-machines/index.ts` provides a single import point, consistent with the barrel pattern used across the codebase (ai/index.ts, governance/index.ts, etc.).

Output: `lib/state-machines/index.ts` that re-exports everything from both state machine modules.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/87-type-state-machine-centralization/087-RESEARCH.md

Source files being re-exported:
@packages/server/src/lib/decay/state-machine.ts
@packages/server/src/lib/lifecycle/state-machine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create state-machines/index.ts barrel export</name>
  <files>
    packages/server/src/lib/state-machines/index.ts
  </files>
  <read_first>
    packages/server/src/lib/decay/state-machine.ts
    packages/server/src/lib/lifecycle/state-machine.ts
  </read_first>
  <action>
    Create the `packages/server/src/lib/state-machines/` directory and its `index.ts` file.

    The file should contain exactly:
    ```typescript
    /**
     * Unified barrel export for state machine modules.
     * Re-exports all public APIs from decay and lifecycle state machines.
     */

    // Decay state machine: computeDecayState, isTerminalDecayState, requiresAttention, validateDecayConfig, DecayableEntry, DEFAULT_DECAY_CONFIG
    export * from '../decay/state-machine.js';

    // Lifecycle state machine: isValidTransition, getValidTransitions, isTerminalState, transitionLifecycleState
    export * from '../lifecycle/state-machine.js';
    ```

    Verify there are no naming conflicts between the two modules:
    - Decay exports: `DecayableEntry`, `DEFAULT_DECAY_CONFIG`, `computeDecayState`, `isTerminalDecayState`, `requiresAttention`, `validateDecayConfig`
    - Lifecycle exports: `isValidTransition`, `getValidTransitions`, `isTerminalState`, `transitionLifecycleState`
    - No overlapping names, so `export *` is safe.

    Run `pnpm typecheck` to confirm no errors.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <done>
    - packages/server/src/lib/state-machines/index.ts exists
    - Re-exports all 6 decay state machine exports
    - Re-exports all 4 lifecycle state machine exports
    - pnpm typecheck passes
    - Original import paths (decay/state-machine.js, lifecycle/state-machine.js) still work
  </done>
  <acceptance_criteria>
    - `grep "export \* from '../decay/state-machine.js'" packages/server/src/lib/state-machines/index.ts` returns match
    - `grep "export \* from '../lifecycle/state-machine.js'" packages/server/src/lib/state-machines/index.ts` returns match
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
pnpm typecheck passes. File exists with two re-export lines. No naming conflicts between the two source modules.
</verification>

<success_criteria>
- state-machines/index.ts created and re-exports both state machines
- pnpm typecheck passes
- Original direct imports still work
</success_criteria>

<output>
After completion, create `.planning/phases/87-type-state-machine-centralization/087-02-SUMMARY.md`
</output>
