---
phase: 87-type-state-machine-centralization
plan: 03
type: execute
wave: 2
depends_on:
  - "087-01-PLAN.md"
  - "087-02-PLAN.md"
files_modified:
  - packages/server/src/lib/types.ts
  - packages/server/src/lib/__tests__/types-export.test.ts
autonomous: true
requirements:
  - "87-req-2"
  - "87-req-5"

must_haves:
  truths:
    - "All server package types are importable from a single entry point: lib/types.ts"
    - "Compile verification test proves all types can be imported without runtime errors"
    - "Backward compatibility is preserved: all existing import paths continue to work"
  artifacts:
    - path: "packages/server/src/lib/types.ts"
      provides: "Unified barrel re-export for all server package types"
      contains: "export * from './store/types/index.js'"
    - path: "packages/server/src/lib/__tests__/types-export.test.ts"
      provides: "Compile-time verification that all types are correctly exported"
      contains: "import type { UserRecord, KnowledgeRecord"
  key_links:
    - from: "packages/server/src/lib/types.ts"
      to: "packages/server/src/lib/store/types/index.ts"
      via: "export * from './store/types/index.js'"
      pattern: "export \\* from '\\./store/types/index\\.js'"
    - from: "packages/server/src/lib/types.ts"
      to: "packages/server/src/lib/state-machines/index.ts"
      via: "export * from './state-machines/index.js'"
      pattern: "export \\* from '\\./state-machines/index\\.js'"
    - from: "packages/server/src/lib/types.ts"
      to: "packages/server/src/lib/ai/types.ts"
      via: "export * from './ai/types.js'"
      pattern: "export \\* from '\\./ai/types\\.js'"
---

<objective>
Create the unified `lib/types.ts` entry point and a compile verification test.

Purpose: Establish a single import path (`lib/types.ts`) for all server package types, enabling cleaner imports and preparing for future type-only package extraction. The compile verification test ensures all types are correctly exported and prevents accidental type omissions.

Output: `lib/types.ts` that re-exports from all type-bearing sub-modules, and `lib/__tests__/types-export.test.ts` that verifies type importability.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/87-type-state-machine-centralization/087-RESEARCH.md

Existing type files to aggregate:
@packages/server/src/lib/store/types/index.ts (created in Plan 01)
@packages/server/src/lib/state-machines/index.ts (created in Plan 02)
@packages/server/src/lib/ai/types.ts
@packages/server/src/lib/governance/types.ts
@packages/server/src/lib/indexing/types.ts
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/candidates/types.ts
@packages/server/src/lib/context.ts (ResolvedAuthContext)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/types.ts unified barrel export</name>
  <files>
    packages/server/src/lib/types.ts
  </files>
  <read_first>
    packages/server/src/lib/store/types/index.ts
    packages/server/src/lib/state-machines/index.ts
    packages/server/src/lib/ai/types.ts
    packages/server/src/lib/governance/types.ts
    packages/server/src/lib/indexing/types.ts
    packages/server/src/lib/retrieval/types.ts
    packages/server/src/lib/candidates/types.ts
    packages/server/src/lib/context.ts
  </read_first>
  <action>
    Create `packages/server/src/lib/types.ts` as the unified entry point for all server package types.

    The file should re-export from each type-bearing sub-module:
    ```typescript
    /**
     * Unified barrel export for all server package types.
     *
     * This file provides a single import point for all type definitions
     * across the server package, enabling cleaner imports and preparing
     * for potential future type-only package extraction.
     *
     * Usage:
     *   import type { KnowledgeRecord, UserRecord } from '../types.js';
     *
     * Backward compatibility: All existing import paths continue to work.
     * This file is additive, not replacing any existing module paths.
     */

    // Store record types (system, knowledge, artifact, candidate, feedback)
    export * from './store/types/index.js';

    // Store interfaces and utilities
    export { type StoreData, type SkillShareerStore, JsonStore } from './store/index.js';
    export { createEmptyStoreData, cloneStoreData, nowIso, hashSecret, createOpaqueToken, createSlug } from './store/index.js';

    // State machines
    export * from './state-machines/index.js';

    // AI types
    export * from './ai/types.js';

    // Governance types
    export * from './governance/types.js';

    // Indexing types
    export * from './indexing/types.js';

    // Retrieval types
    export * from './retrieval/types.js';

    // Candidate types
    export * from './candidates/types.js';

    // Auth context
    export { type ResolvedAuthContext } from './context.js';
    ```

    NOTES:
    - Use `export *` for modules that define pure type interfaces
    - Use `export { type X }` syntax for named type re-exports from modules that also contain implementations
    - JsonStore is a class (not just a type), so it's exported without `type`
    - Utility functions (nowIso, etc.) are exported without `type`

    Run `pnpm typecheck` to verify no conflicts or missing imports.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - lib/types.ts exists and re-exports from all 9 type sources
    - All store record types are accessible
    - All state machine functions and types are accessible
    - All sub-module types (ai, governance, indexing, retrieval, candidates) are accessible
    - ResolvedAuthContext is accessible
    - pnpm typecheck passes
  </done>
  <acceptance_criteria>
    - `grep "export \* from './store/types/index.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './state-machines/index.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './ai/types.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './governance/types.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './indexing/types.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './retrieval/types.js'" packages/server/src/lib/types.ts` returns match
    - `grep "export \* from './candidates/types.js'" packages/server/src/lib/types.ts` returns match
    - `grep "ResolvedAuthContext" packages/server/src/lib/types.ts` returns match
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create compile verification test for type exports</name>
  <files>
    packages/server/src/lib/__tests__/types-export.test.ts
  </files>
  <read_first>
    packages/server/src/lib/types.ts
    packages/server/src/lib/store/types/index.ts
    packages/server/src/lib/state-machines/index.ts
  </read_first>
  <action>
    Create `packages/server/src/lib/__tests__/types-export.test.ts` to verify that all types are correctly exported and importable.

    The test file should use `import type` to verify compile-time accessibility:
    ```typescript
    /**
     * Compile verification test for type exports.
     *
     * This test verifies that all types exported from lib/types.ts
     * are correctly importable. The test passes if TypeScript compiles
     * without errors -- runtime assertions are not needed since this
     * is a compile-time verification.
     *
     * Phase: 87 (Type & State Machine Centralization)
     */

    import { describe, it, expect } from 'vitest';

    // Import all types from the unified entry point
    import type {
      // Store record types
      UserRecord,
      TeamRecord,
      MembershipRecord,
      AccessKeyRecord,
      SessionRecord,
      AuditEventRecord,
      KnowledgeRecord,
      KnowledgeRevisionRecord,
      KnowledgeSubmissionRecord,
      SkillArtifactRecord,
      SkillArtifactRevisionRecord,
      CandidateSubmissionRecord,
      DuplicateCaseRecord,
      EntityLineageRecord,
      FeedbackQueueRecord,
      StoreData,
      SkillShareerStore,
    } from '../types.js';

    // Import state machine functions
    import {
      computeDecayState,
      isTerminalDecayState,
      isValidTransition,
      getValidTransitions,
      isTerminalState,
    } from '../types.js';

    // Import utility functions
    import {
      nowIso,
      hashSecret,
      createOpaqueToken,
      createSlug,
    } from '../types.js';

    // Import JsonStore class
    import { JsonStore } from '../types.js';

    // Import sub-module types
    import type {
      EmbeddingsProvider,
      ChatProvider,
    } from '../types.js';

    import type {
      GovernanceContext,
      GovernedEntity,
    } from '../types.js';

    import type {
      NormalizedIndexDocument,
      AdapterSyncState,
      KnowledgeIndexStateRecord,
    } from '../types.js';

    import type {
      RecallCandidate,
      MergedCandidate,
    } from '../types.js';

    import type {
      CandidateFingerprintInput,
      DuplicateDetectionInput,
    } from '../types.js';

    import type { ResolvedAuthContext } from '../types.js';

    describe('Type Export Verification', () => {
      it('verifies record types are importable', () => {
        // Type assertions -- these compile if imports are valid
        const _user: UserRecord = {} as UserRecord;
        const _team: TeamRecord = {} as TeamRecord;
        const _membership: MembershipRecord = {} as MembershipRecord;
        const _knowledge: KnowledgeRecord = {} as KnowledgeRecord;
        const _artifact: SkillArtifactRecord = {} as SkillArtifactRecord;
        const _candidate: CandidateSubmissionRecord = {} as CandidateSubmissionRecord;
        const _feedback: FeedbackQueueRecord = {} as FeedbackQueueRecord;
        const _storeData: StoreData = {} as StoreData;

        // Dummy assertion to satisfy test framework
        expect(true).toBe(true);
      });

      it('verifies state machine functions are callable', () => {
        // Verify function signatures match expected types
        expect(typeof computeDecayState).toBe('function');
        expect(typeof isTerminalDecayState).toBe('function');
        expect(typeof isValidTransition).toBe('function');
        expect(typeof getValidTransitions).toBe('function');
        expect(typeof isTerminalState).toBe('function');
      });

      it('verifies utility functions are callable', () => {
        expect(typeof nowIso).toBe('function');
        expect(typeof hashSecret).toBe('function');
        expect(typeof createOpaqueToken).toBe('function');
        expect(typeof createSlug).toBe('function');
      });

      it('verifies JsonStore is a class', () => {
        expect(JsonStore).toBeInstanceOf(Function);
      });

      it('verifies sub-module types are importable', () => {
        // Type assertions for sub-module types
        const _embeddings: EmbeddingsProvider = {} as EmbeddingsProvider;
        const _governance: GovernanceContext = {} as GovernanceContext;
        const _index: NormalizedIndexDocument = {} as NormalizedIndexDocument;
        const _recall: RecallCandidate = {} as RecallCandidate;
        const _candidate: CandidateFingerprintInput = {} as CandidateFingerprintInput;
        const _auth: ResolvedAuthContext = {} as ResolvedAuthContext;

        expect(true).toBe(true);
      });
    });
    ```

    Run `pnpm test -- packages/server/src/lib/__tests__/types-export.test.ts` to verify the test passes.
  </action>
  <verify>
    <automated>cd /home/wunai/project/TrapMap-for-vibing && pnpm test -- packages/server/src/lib/__tests__/types-export.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - lib/__tests__/types-export.test.ts exists
    - Imports all major type categories from lib/types.ts
    - Test file compiles without TypeScript errors
    - All tests pass
  </done>
  <acceptance_criteria>
    - `grep "import type.*UserRecord.*KnowledgeRecord" packages/server/src/lib/__tests__/types-export.test.ts` returns match
    - `grep "import.*computeDecayState.*isValidTransition" packages/server/src/lib/__tests__/types-export.test.ts` returns match
    - `grep "import.*JsonStore" packages/server/src/lib/__tests__/types-export.test.ts` returns match
    - `grep "import type.*ResolvedAuthContext" packages/server/src/lib/__tests__/types-export.test.ts` returns match
    - `pnpm test -- packages/server/src/lib/__tests__/types-export.test.ts` exits with code 0
    - `pnpm typecheck` exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
pnpm typecheck passes. All tests pass. types.ts re-exports from 9 source modules. Compile verification test exists and passes.
</verification>

<success_criteria>
- lib/types.ts created as unified type entry point
- lib/__tests__/types-export.test.ts created and passing
- All store record types importable from lib/types.ts
- All state machine functions importable from lib/types.ts
- All sub-module types importable from lib/types.ts
- pnpm typecheck passes
- pnpm test passes
</success_criteria>

<output>
After completion, create `.planning/phases/87-type-state-machine-centralization/087-03-SUMMARY.md`
</output>
