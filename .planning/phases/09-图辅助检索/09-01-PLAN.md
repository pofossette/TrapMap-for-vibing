---
phase: 09-图辅助检索
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/indexing/types.ts
  - packages/server/src/lib/indexing/pipeline.ts
  - packages/server/src/lib/indexing/adapters/vector.ts
  - packages/server/src/lib/indexing/adapters/keyword.ts
  - packages/server/src/lib/indexing/adapters/graph.ts
  - packages/server/src/lib/indexing/adapters/vector.test.ts
  - packages/server/src/lib/indexing/adapters/keyword.test.ts
  - packages/server/src/lib/indexing/adapters/graph.test.ts
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-07
must_haves:
  truths:
    - "The indexing subsystem supports a third internal adapter kind named `graph` without changing public retrieval contracts."
    - "The repo has concrete adapter implementations for `vector`, `keyword`, and `graph`, so adapter-focused tests stop failing due to missing files."
    - "Lifecycle-driven indexing can sync and remove graph state through the same pipeline seam created in Phase 8."
  artifacts:
    - path: "packages/server/src/lib/indexing/types.ts"
      provides: "internal adapter contracts widened for graph indexing"
      contains: "kind: 'vector' | 'keyword' | 'graph'"
    - path: "packages/server/src/lib/indexing/adapters/graph.ts"
      provides: "graph adapter sync/remove seam"
      contains: "graphIndexAdapter"
    - path: "packages/server/src/lib/indexing/adapters/graph.test.ts"
      provides: "adapter-level verification for graph sync/remove behavior"
      contains: "graphIndexAdapter"
  key_links:
    - from: "packages/server/src/lib/indexing/pipeline.ts"
      to: "packages/server/src/lib/indexing/adapters/graph.ts"
      via: "adapter fan-out"
      pattern: "adapter.kind === 'graph'|graphIndexAdapter"
    - from: "packages/server/src/lib/store.ts"
      to: "packages/server/src/lib/indexing/types.ts"
      via: "persisted graph sync state"
      pattern: "graph:"
---

<objective>
Create the internal graph indexing seam required for Phase 9 by widening Phase 8's adapter contracts, restoring missing adapter implementation files, and adding a concrete graph adapter/test surface.

Planning basis: no `CONTEXT.md` exists for Phase 9. This plan is based on roadmap, requirements, current code, and prior Phase 6-8 artifacts only.

Output: graph-capable internal indexing contracts, concrete adapter files for all indexed channels, and adapter-focused tests that can serve as a trustworthy baseline for later graph retrieval work.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-图辅助检索/09-RESEARCH.md
@.planning/phases/09-图辅助检索/09-VALIDATION.md
@packages/server/src/lib/store.ts
@packages/server/src/lib/indexing/types.ts
@packages/server/src/lib/indexing/pipeline.ts
@packages/server/src/lib/indexing/adapters/vector.test.ts
@packages/server/src/lib/indexing/adapters/keyword.test.ts

<interfaces>
From `packages/server/src/lib/indexing/types.ts`:
```ts
export interface IndexAdapter {
  kind: 'vector' | 'keyword';
  sync(document: NormalizedIndexDocument): Promise<IndexSyncResult>;
  remove(ref: { entryId: string; revision: number }): Promise<void>;
}
```

From `packages/server/src/lib/store.ts`:
```ts
export interface KnowledgeIndexStateRecord {
  contentHash: string;
  normalizedAt: string;
  vector: AdapterSyncState;
  keyword: AdapterSyncState;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 adapter coverage for graph indexing and baseline adapter behavior</name>
  <read_first>packages/server/src/lib/indexing/adapters/vector.test.ts, packages/server/src/lib/indexing/adapters/keyword.test.ts, packages/server/src/lib/indexing/types.ts, .planning/phases/09-图辅助检索/09-VALIDATION.md</read_first>
  <files>packages/server/src/lib/indexing/adapters/graph.test.ts, packages/server/src/lib/indexing/adapters/vector.test.ts, packages/server/src/lib/indexing/adapters/keyword.test.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/indexing/adapters/graph.test.ts`
    - `rg -n "graphIndexAdapter" packages/server/src/lib/indexing/adapters/graph.test.ts`
    - `rg -n "vectorIndexAdapter" packages/server/src/lib/indexing/adapters/vector.test.ts`
    - `rg -n "keywordIndexAdapter" packages/server/src/lib/indexing/adapters/keyword.test.ts`
  </acceptance_criteria>
  <action>Create `packages/server/src/lib/indexing/adapters/graph.test.ts` first with concrete cases for `graphIndexAdapter.sync(...)` and `graphIndexAdapter.remove(...)`, covering idempotent sync, entry removal, and graph-specific payload persistence keyed by `entryId`, `revision`, and `contentHash`. Update the existing `vector.test.ts` and `keyword.test.ts` only where needed so their expectations match the real adapter signatures that will exist after implementation. Keep all tests focused on store-backed adapter behavior, not end-to-end retrieval.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts</automated>
  </verify>
  <done>Adapter tests exist for vector, keyword, and graph channels and fail for missing or incorrect adapter implementations instead of failing due to absent files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen internal index-state contracts to include graph sync state</name>
  <read_first>packages/server/src/lib/store.ts, packages/server/src/lib/indexing/types.ts, packages/server/src/lib/indexing/pipeline.ts, packages/server/src/lib/indexing/adapters/graph.test.ts</read_first>
  <files>packages/server/src/lib/store.ts, packages/server/src/lib/indexing/types.ts, packages/server/src/lib/indexing/pipeline.ts</files>
  <acceptance_criteria>
    - `rg -n "kind: 'vector' \\| 'keyword' \\| 'graph'" packages/server/src/lib/indexing/types.ts`
    - `rg -n "graph: AdapterSyncState|graph:" packages/server/src/lib/store.ts packages/server/src/lib/indexing/types.ts`
    - `rg -n "graph" packages/server/src/lib/indexing/pipeline.ts`
  </acceptance_criteria>
  <action>Update `packages/server/src/lib/indexing/types.ts` so `IndexAdapter.kind` supports `'graph'`, `IndexSyncResult.adapterKind` supports `'graph'`, and any graph-specific payload types needed by later plans are defined server-internally. Update `packages/server/src/lib/store.ts` so `KnowledgeIndexStateRecord` persists `graph: AdapterSyncState` beside `vector` and `keyword`. Update `packages/server/src/lib/indexing/pipeline.ts` so initialization, sync checks, and reconciliation logic treat `graph` as a first-class adapter rather than a special case.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/indexing/pipeline.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>The indexing pipeline and persisted store schema support graph sync state without changing public contracts or bypassing the Phase 8 lifecycle seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement concrete vector, keyword, and graph adapter modules</name>
  <read_first>packages/server/src/lib/indexing/types.ts, packages/server/src/lib/indexing/pipeline.ts, packages/server/src/lib/indexing/adapters/vector.test.ts, packages/server/src/lib/indexing/adapters/keyword.test.ts, packages/server/src/lib/indexing/adapters/graph.test.ts, packages/server/src/lib/embeddings.ts</read_first>
  <files>packages/server/src/lib/indexing/adapters/vector.ts, packages/server/src/lib/indexing/adapters/keyword.ts, packages/server/src/lib/indexing/adapters/graph.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/indexing/adapters/vector.ts`
    - `test -f packages/server/src/lib/indexing/adapters/keyword.ts`
    - `test -f packages/server/src/lib/indexing/adapters/graph.ts`
    - `rg -n "export const vectorIndexAdapter" packages/server/src/lib/indexing/adapters/vector.ts`
    - `rg -n "export const keywordIndexAdapter" packages/server/src/lib/indexing/adapters/keyword.ts`
    - `rg -n "export const graphIndexAdapter" packages/server/src/lib/indexing/adapters/graph.ts`
  </acceptance_criteria>
  <action>Implement `vector.ts` so it generates embeddings from `document.canonicalText`, persists compatibility data needed by retrieval, and skips work when `revision` and `contentHash` already match. Implement `keyword.ts` so it persists canonical token arrays and field-token groupings derived from `shortcut`, `detail`, and `labels`. Implement `graph.ts` so it persists extracted-entity payloads and adjacency-ready relation data keyed by `entryId`, `revision`, and `contentHash`; keep the graph adapter deterministic and local to the JSON-store architecture. All three adapters must expose a consistent object shape with `kind`, `sync(document, ...)`, and `remove(ref, ...)` semantics that Phase 8's pipeline can call.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>All adapter implementation files exist, adapter-focused tests can run against real code, and graph indexing has a concrete implementation seam for the next plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalization -> adapter fan-out | one canonical document feeds all adapters, including graph |
| adapter implementation -> JSON store | adapter payload writes must remain server-internal and revision/content-hash aware |
| pipeline reconciliation -> adapter remove | stale graph state must be removable through the same lifecycle seam as other adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `graph.ts` / `pipeline.ts` | mitigate | keep graph payloads internal to the store and do not expose them through contracts or CLI output |
| T-09-02 | Tampering | adapter sync state | mitigate | key all adapter writes by `entryId`, `revision`, and `contentHash`, and cover idempotent no-op behavior with adapter tests |
| T-09-03 | Denial of Service | reconciliation path | mitigate | reuse the existing pipeline fan-out seam instead of adding a second graph-only sync system |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/lib/indexing/pipeline.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- Internal indexing contracts support `graph` beside `vector` and `keyword`
- Concrete adapter implementation files exist for all three channels
- Adapter-focused tests can validate graph sync/remove behavior without missing-file failures
</success_criteria>

<output>
After completion, create `.planning/phases/09-图辅助检索/09-01-SUMMARY.md`
</output>
