---
phase: 09-图辅助检索
plan: "03"
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - packages/server/src/lib/retrieval/recall/graph-assisted.ts
  - packages/server/src/lib/retrieval/recall/graph-assisted.test.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/types.ts
  - packages/server/src/lib/retrieval/merge.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/cli/src/commands/retrieval.test.ts
autonomous: true
requirements:
  - GRAPH-02
  - GRAPH-04
  - GRAPH-05
  - GRAPH-06
  - BOUND-01
  - BOUND-02
  - BOUND-03
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "`mode: 'graph-assisted'` executes a real retrieval path instead of returning 501."
    - "Graph-assisted recall augments the existing retrieval pipeline and preserves the current response shape."
    - "Graph-derived candidates are constrained by the already eligible entry set, preserving the approval/team/security order."
  artifacts:
    - path: "packages/server/src/lib/retrieval/recall/graph-assisted.ts"
      provides: "query entity extraction, bounded expansion, and graph candidate scoring"
      contains: "graphAssistedRecall"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "graph-assisted mode dispatch"
      contains: "case 'graph-assisted'"
    - path: "packages/server/src/lib/retrieval/recall/graph-assisted.test.ts"
      provides: "bounded expansion and authorization-safe graph recall coverage"
      contains: "graph-assisted"
  key_links:
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/recall/graph-assisted.ts"
      via: "mode dispatch"
      pattern: "graphAssistedRecall"
    - from: "packages/server/src/lib/retrieval/recall/graph-assisted.ts"
      to: "packages/server/src/lib/retrieval/merge.ts"
      via: "internal candidate handoff"
      pattern: "mergeCandidates|createSemanticCandidate"
---

<objective>
Implement actual graph-assisted retrieval by adding bounded query expansion and relationship-assisted recall, then wire it into the orchestrator without changing the existing response contract or authorization order.

Planning basis: no `CONTEXT.md` exists for Phase 9. This plan is based on roadmap, requirements, current code, and prior Phase 6-8 artifacts only.

Output: a graph-assisted recall module, orchestrator support for `mode: 'graph-assisted'`, and regression tests proving the 501 placeholder is gone while business boundaries remain intact.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-图辅助检索/09-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/types.ts
@packages/server/src/lib/retrieval/merge.ts
@packages/server/src/lib/retrieval/rerank.ts
@packages/server/src/lib/retrieval/graph-extract.ts
@packages/server/src/routes/retrieval.test.ts
@packages/cli/src/commands/retrieval.test.ts

<interfaces>
From `packages/contracts/src/domain/retrieval.ts`:
```ts
export const retrievalQueryModeSchema = z.enum(['semantic', 'hybrid', 'graph-assisted']);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graph-assisted recall tests for bounded expansion and hidden-match discovery</name>
  <read_first>.planning/phases/09-图辅助检索/09-RESEARCH.md, .planning/phases/09-图辅助检索/09-VALIDATION.md, packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/rerank.ts</read_first>
  <files>packages/server/src/lib/retrieval/recall/graph-assisted.test.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/retrieval/recall/graph-assisted.test.ts`
    - `rg -n "graphAssistedRecall|eligibleEntries|one hop|relationship" packages/server/src/lib/retrieval/recall/graph-assisted.test.ts`
  </acceptance_criteria>
  <action>Create `packages/server/src/lib/retrieval/recall/graph-assisted.test.ts` with deterministic fixtures that prove: query entities are extracted from the search seed, expansion is limited to one hop, graph-derived entry IDs are intersected with the eligible entry set, and indirectly related entries can outrank or supplement purely direct matches when the relationship signal is stronger. Include at least one test ensuring unauthorized or non-approved entries do not appear even if their graph links are strong.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/graph-assisted.test.ts</automated>
  </verify>
  <done>The repo has a dedicated test harness that locks in the intended graph-assisted retrieval behavior before orchestrator wiring changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement graph-assisted recall as a bounded internal channel</name>
  <read_first>packages/server/src/lib/retrieval/recall/graph-assisted.test.ts, packages/server/src/lib/retrieval/graph-extract.ts, packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/merge.ts, packages/server/src/lib/retrieval/rerank.ts</read_first>
  <files>packages/server/src/lib/retrieval/recall/graph-assisted.ts, packages/server/src/lib/retrieval/types.ts, packages/server/src/lib/retrieval/merge.ts</files>
  <acceptance_criteria>
    - `test -f packages/server/src/lib/retrieval/recall/graph-assisted.ts`
    - `rg -n "export async function graphAssistedRecall|extractGraphEntities" packages/server/src/lib/retrieval/recall/graph-assisted.ts`
    - `rg -n "graph" packages/server/src/lib/retrieval/types.ts packages/server/src/lib/retrieval/merge.ts`
  </acceptance_criteria>
  <action>Implement `packages/server/src/lib/retrieval/recall/graph-assisted.ts` with `graphAssistedRecall(...)` that takes the search seed, an `eligibleEntries` list or lookup map, and graph adapter payloads from the store. Extract entities from the query using `extractGraphEntities(...)`, expand only one hop through typed entity/relationship links, score candidates using direct entity hits plus relation support, and return internal candidates that can be merged or reranked alongside the existing hybrid pipeline. Update internal retrieval types and merge helpers only as needed so graph evidence remains server-internal.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/graph-assisted.test.ts && pnpm --filter @skill-shareer/server exec tsc --noEmit</automated>
  </verify>
  <done>Graph-assisted recall exists as a real, bounded, authorization-safe internal channel that can feed the orchestrator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace the graph-assisted 501 path with orchestrated graph-assisted retrieval</name>
  <read_first>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/recall/graph-assisted.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</read_first>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <acceptance_criteria>
    - `rg -n "case 'graph-assisted'" packages/server/src/lib/retrieval/orchestrator.ts`
    - `rg -n "graph-assisted" packages/server/src/routes/retrieval.test.ts packages/cli/src/commands/retrieval.test.ts`
    - `pnpm --filter @skill-shareer/server test -- src/routes/retrieval.test.ts`
  </acceptance_criteria>
  <action>Update `packages/server/src/lib/retrieval/orchestrator.ts` so `case 'graph-assisted'` no longer throws a 501. The graph-assisted branch must preserve the current filter-first order, use the real graph recall module, keep the same response contract (`globalConstraints`, `projectKnowledge`, `refinementSummary`), and leave `semantic` as the default when `mode` is omitted. Extend route and CLI tests so they prove graph-assisted mode works through the existing API and CLI boundaries without exposing internal graph payloads or changing human-readable output responsibilities.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/graph-assisted.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>`graph-assisted` is a real supported retrieval mode, the public response shape stays unchanged, and route/CLI regression coverage proves the boundary remains contract-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eligible entries -> graph recall | graph traversal must never expand the authorization universe |
| graph candidates -> merge/rerank | graph evidence must stay internal while influencing final ranking |
| API/CLI mode boundary | graph-assisted must remain a contract-driven mode, not a CLI-specific feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | `recall/graph-assisted.ts` | mitigate | intersect graph-derived entry refs with `eligibleEntries` before scoring or merge |
| T-09-08 | Elevation of Privilege | orchestrator mode dispatch | mitigate | keep `filterEligibleEntries(...)` ahead of any graph recall work and cover it with tests |
| T-09-09 | Tampering | merge/rerank inputs | mitigate | keep graph evidence internal and bounded so public contracts do not encode raw graph state |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval/recall/graph-assisted.test.ts`
- `pnpm --filter @skill-shareer/server test -- src/routes/retrieval.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit`
</verification>

<success_criteria>
- `mode: 'graph-assisted'` no longer returns a placeholder error
- Graph-assisted recall can surface indirect but authorized matches
- Public response shape and business scope semantics remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/09-图辅助检索/09-03-SUMMARY.md`
</output>
