---
phase: 09-图辅助检索
plan: "04"
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - packages/server/src/lib/retrieval.test.ts
  - packages/server/src/lib/indexing/adapters/vector.test.ts
  - packages/server/src/lib/indexing/adapters/keyword.test.ts
  - packages/server/src/lib/indexing/adapters/graph.test.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/cli/src/commands/retrieval.test.ts
  - .planning/phases/09-图辅助检索/09-VALIDATION.md
autonomous: true
requirements:
  - GRAPH-05
  - GRAPH-06
  - GRAPH-07
  - BOUND-01
  - BOUND-02
  - BOUND-03
  - BOUND-04
  - BOUND-05
must_haves:
  truths:
    - "Phase 9 has a defensible green verification story instead of inheriting a silently red retrieval/indexing baseline."
    - "Graph-assisted improvements are proven by deterministic tests and documented manual checks."
    - "The validation artifact accurately reflects the commands and caveats needed to execute and verify the phase."
  artifacts:
    - path: "packages/server/src/lib/retrieval.test.ts"
      provides: "end-to-end retrieval regression coverage including graph-assisted behavior"
      contains: "graph-assisted"
    - path: ".planning/phases/09-图辅助检索/09-VALIDATION.md"
      provides: "phase-specific verification contract after implementation reality is known"
      contains: "Phase 09"
  key_links:
    - from: "packages/server/src/lib/retrieval.test.ts"
      to: "packages/server/src/lib/retrieval/recall/graph-assisted.ts"
      via: "behavioral graph-assisted regression coverage"
      pattern: "graph-assisted"
---

<objective>
Close the loop on Phase 9 by making the retrieval/indexing verification story trustworthy, proving graph-assisted behavior with deterministic regression coverage, and updating the validation contract to match what was actually built.

Planning basis: no `CONTEXT.md` exists for Phase 9. This plan is based on roadmap, requirements, current code, and prior Phase 6-8 artifacts only.

Output: a clean or explicitly bounded verification baseline, graph-assisted regression tests, and an updated Phase 9 validation strategy that execution can follow without ambiguity.
</objective>

<context>
@.planning/phases/09-图辅助检索/09-RESEARCH.md
@.planning/phases/09-图辅助检索/09-VALIDATION.md
@packages/server/src/lib/retrieval.test.ts
@packages/server/src/routes/retrieval.test.ts
@packages/cli/src/commands/retrieval.test.ts
@packages/server/src/lib/indexing/adapters/vector.test.ts
@packages/server/src/lib/indexing/adapters/keyword.test.ts
@packages/server/src/lib/indexing/adapters/graph.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair or isolate the current red retrieval/indexing baseline</name>
  <read_first>.planning/phases/09-图辅助检索/09-RESEARCH.md, .planning/phases/09-图辅助检索/09-VALIDATION.md, packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/indexing/adapters/vector.test.ts, packages/server/src/lib/indexing/adapters/keyword.test.ts</read_first>
  <files>packages/server/src/lib/retrieval.test.ts, packages/server/src/lib/indexing/adapters/vector.test.ts, packages/server/src/lib/indexing/adapters/keyword.test.ts, packages/server/src/lib/indexing/adapters/graph.test.ts</files>
  <acceptance_criteria>
    - `pnpm --filter @skill-shareer/server test -- src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts`
    - `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts`
  </acceptance_criteria>
  <action>Resolve the known baseline blockers documented in the Phase 9 research: either repair the persisted-index-state retrieval tests in `packages/server/src/lib/retrieval.test.ts` so they use defined fixtures/services, or narrow and document them so the Phase 9 verification path no longer depends on broken unrelated cases. Keep adapter tests aligned with the real implementation shape created by Plans 01-03. The final state must leave execution with a trustworthy focused server verification command, not a hand-waved red baseline.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts</automated>
  </verify>
  <done>The focused retrieval/indexing baseline is either green or explicitly, defensibly scoped so Phase 9 verification can rely on it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add end-to-end graph-assisted regression coverage across server and CLI boundaries</name>
  <read_first>packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts, packages/server/src/lib/retrieval/recall/graph-assisted.ts</read_first>
  <files>packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</files>
  <acceptance_criteria>
    - `rg -n "graph-assisted" packages/server/src/lib/retrieval.test.ts packages/server/src/routes/retrieval.test.ts packages/cli/src/commands/retrieval.test.ts`
    - `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts`
    - `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
  </acceptance_criteria>
  <action>Extend the retrieval and route/CLI regression suites so they prove graph-assisted mode preserves response shape, respects business scope buckets, stays authorization-safe, and improves hidden-match discovery on deterministic fixtures. Keep the CLI tests focused on passthrough and output stability rather than internal graph payload details. Use direct fixture comparisons between `semantic`, `hybrid`, and `graph-assisted` modes where that creates concrete evidence for the roadmap success criteria.</action>
  <verify>
    <automated>pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts && pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts</automated>
  </verify>
  <done>The repo has contract-safe, deterministic evidence that graph-assisted retrieval behaves as intended across server and CLI boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Update the Phase 9 validation contract to reflect implemented reality</name>
  <read_first>.planning/phases/09-图辅助检索/09-VALIDATION.md, packages/server/src/lib/retrieval.test.ts, packages/server/src/routes/retrieval.test.ts, packages/cli/src/commands/retrieval.test.ts</read_first>
  <files>.planning/phases/09-图辅助检索/09-VALIDATION.md</files>
  <acceptance_criteria>
    - `rg -n "graph-assisted|graph.test|graph-extract" .planning/phases/09-图辅助检索/09-VALIDATION.md`
    - `rg -n "Approval:" .planning/phases/09-图辅助检索/09-VALIDATION.md`
  </acceptance_criteria>
  <action>Revise `.planning/phases/09-图辅助检索/09-VALIDATION.md` so its per-task verification map, quick commands, plan coverage, manual checks, and baseline caveats reflect the real Phase 9 implementation and final verification commands. If the phase successfully repairs the red baseline, remove the caveat and mark the relevant Wave 0 items complete. If any focused caveat remains, describe it precisely and tie it to the commands the verifier should trust.</action>
  <verify>
    <automated>rg -n "graph-assisted|graph.test|graph-extract|Approval:" .planning/phases/09-图辅助检索/09-VALIDATION.md</automated>
  </verify>
  <done>The validation artifact is accurate enough for `/gsd-execute-phase` and later verification work to rely on without reinterpretation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> tests | regression coverage is the proof that graph-assisted mode stayed inside contract and security boundaries |
| focused verification -> full workspace verification | Phase 9 needs a defensible path from task-local checks to broader workspace confidence |
| validation artifact -> executor/verifier | incorrect commands or stale caveats would create false confidence during execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Repudiation | verification baseline | mitigate | repair or precisely isolate known red tests so execution does not claim green status from a broken suite |
| T-09-11 | Information Disclosure | graph-assisted regressions | mitigate | retain authorization-safe tests in server retrieval coverage and keep CLI output free of internal graph payloads |
| T-09-12 | Tampering | validation artifact | mitigate | update Phase 9 validation commands and caveats to match implemented reality before sign-off |
</threat_model>

<verification>
- `pnpm --filter @skill-shareer/server test -- src/lib/retrieval.test.ts src/routes/retrieval.test.ts src/lib/indexing/adapters/vector.test.ts src/lib/indexing/adapters/keyword.test.ts src/lib/indexing/adapters/graph.test.ts`
- `pnpm --filter @skill-shareer/cli test -- src/commands/retrieval.test.ts`
- `pnpm --filter @skill-shareer/server exec tsc --noEmit && pnpm --filter @skill-shareer/cli exec tsc --noEmit`
</verification>

<success_criteria>
- Phase 9 has a trustworthy focused verification baseline
- Graph-assisted behavior is covered by deterministic server and CLI regressions
- `09-VALIDATION.md` accurately reflects the commands and caveats needed to verify the phase
</success_criteria>

<output>
After completion, create `.planning/phases/09-图辅助检索/09-04-SUMMARY.md`
</output>
