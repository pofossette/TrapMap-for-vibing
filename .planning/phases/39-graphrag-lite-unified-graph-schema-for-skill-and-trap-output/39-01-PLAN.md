---
phase: 39
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/plans.ts
  - packages/contracts/src/domain/plans.test.ts
autonomous: true
requirements: [P39-01, P39-02]
must_haves:
  truths:
    - "Trap and skill outputs share one additive public graph schema without removing existing trap-first arrays"
    - "Selected skill nodes expose metadata-only activation references derived from governed client manifests"
  artifacts:
    - path: "packages/contracts/src/domain/plans.ts"
      provides: "Unified graph node, edge, focus, and activation-reference contracts"
    - path: "packages/contracts/src/domain/plans.test.ts"
      provides: "Contract coverage for the additive graph plan shape"
---

<objective>
Define the public additive graph schema for Phase 39.

Purpose: give `/v3/retrieval/plan` and routed graph-plan responses one explicit node/edge surface that can represent both traps and skills while preserving backward compatibility.
Output: shared contracts and tests for unified graph nodes, graph edges, focus metadata, and skill activation references.
</objective>

<tasks>
  <task>
    <name>Task 1: Add unified graph-plan contracts</name>
    <files>packages/contracts/src/domain/plans.ts, packages/contracts/src/domain/plans.test.ts</files>
    <action>
      Add graph node kinds, additive graph edge vocabulary, focus metadata, and metadata-only activation references. Extend `trapFirstPlanSchema` with a new `graph` field while keeping `blockingTraps`, `recommendedSkills`, `edges`, and `citations` intact.
    </action>
    <acceptance_criteria>
      - `trapFirstPlanSchema` includes additive `graph`
      - unified graph nodes discriminate between `trap` and `skill`
      - skill nodes expose manifest-backed metadata references only
      - schema tests cover defaults and additive edge vocabulary
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/contracts test -- src/domain/plans.test.ts src/index.test.ts`
</verification>
