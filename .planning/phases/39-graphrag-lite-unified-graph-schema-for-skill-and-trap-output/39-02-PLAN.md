---
phase: 39
plan: 02
type: implementation
wave: 1
depends_on: [39-01]
files_modified:
  - packages/server/src/lib/retrieval/plan-compiler.ts
  - packages/server/src/lib/retrieval/graph-plan-search.test.ts
  - evals/retrieval/lib/normalize.ts
  - evals/retrieval/lib/normalize.test.ts
  - packages/contracts/src/index.test.ts
autonomous: true
requirements: [P39-02, P39-03]
must_haves:
  truths:
    - "The compiler populates the unified graph from the selected traps, skills, and relevant graph relations"
    - "Downstream v3 consumers can read the unified graph without regressing existing selection behavior"
  artifacts:
    - path: "packages/server/src/lib/retrieval/plan-compiler.ts"
      provides: "Unified graph assembly and skill activation metadata"
    - path: "evals/retrieval/lib/normalize.ts"
      provides: "v3 normalization driven by unified graph focus metadata"
---

<objective>
Wire the new unified graph shape into the live retrieval path.

Purpose: populate the new graph contract from the plan compiler, keep routed search tests green, and move eval normalization to the new public surface instead of depending only on legacy split arrays.
Output: populated graph payloads, refreshed tests, and stale v3 endpoint expectations removed from contracts tests.
</objective>

<tasks>
  <task>
    <name>Task 1: Populate unified graph outputs in the compiler</name>
    <files>packages/server/src/lib/retrieval/plan-compiler.ts, packages/server/src/lib/retrieval/graph-plan-search.test.ts</files>
    <action>
      Assemble unified nodes, edges, focus metadata, and metadata-only skill activation references in `compileTrapFirstPlan()`. Update GraphRAG-lite search tests to use the new shape.
    </action>
  </task>
  <task>
    <name>Task 2: Switch v3 normalization to the unified graph surface</name>
    <files>evals/retrieval/lib/normalize.ts, evals/retrieval/lib/normalize.test.ts, packages/contracts/src/index.test.ts</files>
    <action>
      Normalize selected graph-plan hits from `plan.graph` focus metadata and update stale contracts tests so `/v3/retrieval/search` is treated as a valid eval endpoint.
    </action>
    <acceptance_criteria>
      - selected v3 normalization reads unified graph nodes
      - server and eval tests pass with the additive graph surface
      - contracts tests align with the Phase 38+ v3 endpoint reality
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/retrieval/plan-compiler.test.ts src/lib/retrieval/graph-plan-search.test.ts`
  2. `pnpm test -- evals/retrieval/lib/normalize.test.ts`
</verification>
