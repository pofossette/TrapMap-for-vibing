---
phase: 18-cli-skill-lookup-commands
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - packages/server/src/app.ts
  - packages/server/src/lib/retrieval/skill-lookup.ts
  - packages/server/src/lib/retrieval/skill-lookup.test.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/routes/retrieval.test.ts
  - packages/cli/src/commands/skill.ts
  - packages/cli/src/commands/skill.test.ts
  - packages/cli/src/index.ts
autonomous: true
requirements:
  - SKED-01
must_haves:
  truths:
    - "A caller with `knowledge:search` can run `trapmap skill search-by-content <text>` and receive matching skill IDs."
    - "Lookup results are permission-filtered by active team and security level before they reach text or JSON output."
    - "The CLI exposes the new additive `skill` namespace without removing or renaming legacy top-level commands."
    - "JSON output returns the shared artifact-first contract; text output renders the same matches in a concise terminal format."
  artifacts:
    - path: "packages/server/src/lib/retrieval/skill-lookup.ts"
      provides: "Governed artifact-first search helper built on capsule ranking"
      contains: "artifact dedupe, metadata shaping, and SKED-01 lookup logic"
    - path: "packages/server/src/routes/retrieval.ts"
      provides: "Thin POST endpoint for skill lookup"
      exports: "route registration for search-by-content"
    - path: "packages/server/src/app.ts"
      provides: "Route discoverability metadata"
      contains: "documentedRoutes entry for POST /v1/retrieval/skills/search-by-content"
    - path: "packages/cli/src/commands/skill.ts"
      provides: "Nested skill command group with search-by-content subcommand"
      contains: "text/json output wiring for artifact-first results"
    - path: "packages/cli/src/index.ts"
      provides: "CLI registration and discoverability"
      contains: "additive skill namespace and `api:list` exposure"
  key_links:
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/skill-lookup.ts"
      via: "delegation after auth and schema parsing"
      pattern: "searchSkillsByContent"
    - from: "packages/server/src/app.ts"
      to: "packages/server/src/routes/retrieval.ts"
      via: "documentedRoutes exposure for /meta/routes"
      pattern: "POST /v1/retrieval/skills/search-by-content"
    - from: "packages/server/src/lib/retrieval/skill-lookup.ts"
      to: "packages/server/src/lib/retrieval/capsule-recall.ts"
      via: "rank governed capsules then collapse to unique artifacts"
      pattern: "rankCapsules|isArtifactGovernanceEligible"
    - from: "packages/cli/src/commands/skill.ts"
      to: "/v1/retrieval/skills/search-by-content"
      via: "apiRequest POST"
      pattern: "skill.*search-by-content"
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/commands/skill.ts"
      via: "registerSkillCommands and api:list visibility"
      pattern: "skill search-by-content"
---

<objective>
Implement the server and CLI slice for Phase 18 so users can search governed skill artifacts by content through the new additive `skill` namespace.

Purpose: Deliver the actual SKED-01 workflow using existing retrieval governance and ranking seams while keeping the response artifact-first and terminal-friendly.
Output: Search helper, retrieval route, CLI command group, and tests for governance plus text/JSON output.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-cli-skill-lookup-commands/18-RESEARCH.md
@.planning/phases/18-cli-skill-lookup-commands/18-01-PLAN.md
@packages/server/src/routes/retrieval.ts
@packages/server/src/lib/retrieval/capsule-recall.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/cli/src/index.ts
@packages/cli/src/commands/team.ts
@packages/cli/src/commands/retrieval.ts

<interfaces>
From `packages/server/src/routes/retrieval.ts`:

```ts
app.post('/v2/retrieval/search', async (request) => {
  const auth = await resolveAuthContext(app.skillShareer, request);
  requirePermission(auth, 'knowledge:search');
  const query = retrievalV2QuerySchema.parse(request.body);
  const result = await searchKnowledgeV2(app.skillShareer, auth, query);
  return retrievalV2ResponseWithHintsSchema.parse(result);
});
```

From `packages/server/src/lib/retrieval/capsule-recall.ts`:

```ts
export function isArtifactGovernanceEligible(
  artifact: SkillArtifactRecord,
  filters: ArtifactGovernanceFilters,
): boolean;

export function rankCapsules(
  artifacts: SkillArtifactRecord[],
  intent: ParsedIntent,
  filters: ArtifactGovernanceFilters,
  maxResults: number,
): CapsuleCandidate[];
```

From `packages/cli/src/index.ts`:

```ts
const visibility = {
  allowKnowledgeSearch: hasPermission(effectivePermissions, 'knowledge:search'),
};

registerRetrievalCommands(program, {
  allowSearch: visibility.allowKnowledgeSearch,
});
```

From Plan `18-01`:

```ts
skillLookupQuerySchema
skillLookupMatchSchema
skillLookupResponseSchema
type SkillLookupResponse = z.infer<typeof skillLookupResponseSchema>
```

Use the existing `knowledge:search` permission and the same team/security governance inputs. Do not add edit/history/review behavior from Phases 19-20, and do not remove any legacy top-level commands.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement governed artifact lookup endpoint on the retrieval surface</name>
  <files>packages/server/src/app.ts, packages/server/src/lib/retrieval/skill-lookup.ts, packages/server/src/lib/retrieval/skill-lookup.test.ts, packages/server/src/routes/retrieval.ts, packages/server/src/routes/retrieval.test.ts</files>
  <behavior>
    - Test 1: Matching artifacts are ranked from content-bearing capsules, deduped to one row per artifact, and returned as artifact-first matches.
    - Test 2: Artifacts from another team or above the caller security level are excluded, while system-admin access still sees eligible records.
    - Test 3: `POST /v1/retrieval/skills/search-by-content` stays a thin route that enforces `knowledge:search`, parses the shared contract, returns the shared response schema, and is documented in `/meta/routes`.
  </behavior>
  <action>Create a dedicated helper in `packages/server/src/lib/retrieval/skill-lookup.ts` that reuses the capsule recall pipeline from `packages/server/src/lib/retrieval/capsule-recall.ts` per the phase constraint: build governance filters from auth, rank capsules from approved artifacts, collapse ranked candidates to unique artifacts by `artifactId`, and emit the Phase 18 artifact-first matches using the contract from Plan 18-01. Reuse artifact governance filtering patterns from `capsule-recall.ts`, but do not reuse the capsule-native response shape directly. Then add a thin retrieval route in `packages/server/src/routes/retrieval.ts` at `POST /v1/retrieval/skills/search-by-content` that resolves auth, requires `knowledge:search`, parses the new query schema, delegates to the helper, and validates the response with the new contract. Update `packages/server/src/app.ts` so `/meta/routes` documents the new endpoint. Add focused helper tests for team and security filtering plus dedupe behavior, and extend route tests to cover unauthenticated, unauthorized, successful contract-shaped responses, and route documentation exposure.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- --run packages/server/src/lib/retrieval/skill-lookup.test.ts packages/server/src/routes/retrieval.test.ts && pnpm --filter @trapmap/server typecheck</automated>
  </verify>
  <done>The server exposes a governed artifact-first lookup endpoint that returns only approved, team-visible, security-eligible skill matches and proves it with automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the additive `skill search-by-content` CLI command and output coverage</name>
  <files>packages/cli/src/commands/skill.ts, packages/cli/src/commands/skill.test.ts, packages/cli/src/index.ts</files>
  <behavior>
    - Test 1: `trapmap skill search-by-content <text>` POSTs to the new endpoint and prints concise text output containing the skill ID, title, and brief metadata.
    - Test 2: `--json` prints the raw shared contract so agents can parse it reliably.
    - Test 3: The new command appears in `api:list` when `knowledge:search` is allowed, while existing top-level commands remain registered and unchanged.
  </behavior>
  <action>Add `packages/cli/src/commands/skill.ts` to establish the nested `skill` namespace now, with a `search-by-content` subcommand that requires a session, accepts `<text>`, supports `--max-results` and `--json`, calls `POST /v1/retrieval/skills/search-by-content`, parses the shared response schema, and formats human-readable rows without hiding the skill ID. Wire the command into `packages/cli/src/index.ts` as an additive group and surface `skill search-by-content` in `api:list` under the existing `knowledge:search` visibility gate. Preserve all existing top-level commands unchanged. Add CLI tests for request wiring, text output, JSON output, and discoverability.</action>
  <verify>
    <automated>pnpm --filter @trapmap/cli test -- --run packages/cli/src/commands/skill.test.ts && pnpm --filter @trapmap/cli typecheck</automated>
  </verify>
  <done>The CLI exposes a stable `skill` namespace with a working `search-by-content` command, JSON mode, and discoverability, without regressing the legacy command surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI caller -> server route | Untrusted search text and flags enter the authenticated lookup endpoint |
| Route -> retrieval helper | Authorized requests cross from HTTP/auth parsing into ranking and governance logic |
| Server response -> CLI formatter | Only filtered metadata should reach human or JSON output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | S | `POST /v1/retrieval/skills/search-by-content` | mitigate | Resolve auth from the saved session and require `knowledge:search` before query parsing or helper execution, matching the existing retrieval route guard. |
| T-18-05 | I | `packages/server/src/lib/retrieval/skill-lookup.ts` | mitigate | Reuse `isArtifactGovernanceEligible()` inputs and team/security tests so artifacts from other teams or above the caller level never enter lookup results. |
| T-18-06 | T | artifact dedupe and shaping | mitigate | Collapse ranked capsule candidates to unique `artifactId` rows and emit only contract-approved metadata fields, preventing duplicate or overbroad result rows. |
| T-18-07 | R | CLI discoverability and output | accept | This phase does not add audit logging; preserve deterministic text/JSON output and rely on existing auth/session behavior until logging phases 21-22. |
| T-18-08 | D | lookup query execution | accept | `maxResults` remains bounded by the shared schema and the helper ranks only governed artifacts already held in the store snapshot. |
</threat_model>

<verification>
Run the targeted server and CLI tests, then optionally smoke-check the help surface with `pnpm --filter @trapmap/cli dev -- skill --help` once implementation is complete.
</verification>

<success_criteria>
Users with `knowledge:search` can execute `skill search-by-content <text>` through the new additive namespace, receive artifact-first matches in text or JSON form, and automated tests prove the results are filtered by team and security level.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cli-skill-lookup-commands/18-02-SUMMARY.md`
</output>
