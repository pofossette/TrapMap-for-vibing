---
phase: 23
plan: 02
type: verification
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-deployment-scripts/VERIFICATION.md
  - .planning/phases/21-user-operations-logger/VERIFICATION.md
  - .planning/phases/22-rag-logger-with-file-rotation/VERIFICATION.md
  - packages/contracts/tsconfig.json
requirements: [LOG-01, LOG-02, LOG-03, LOG-04]
autonomous: true
must_haves:
  - LOG-01: 15 routes instrumented with user ops logging
  - LOG-02: RAG logging captures pipeline timing and strategy
  - LOG-03: Independent env toggles for each log layer
  - LOG-04: Size-based and time-based file rotation
---

# Plan 23-02: Verify LOG Requirements (Logging) + Deployment

**Goal:** Goal-backward verification of LOG-01, LOG-02, LOG-03, LOG-04 requirements across Phases 17, 21, 22. Fix contracts build issue causing TypeScript errors.

---

## Task 1: Verify Phase 17 (Deployment Scripts) -- No Formal Requirements

<read_first>
- .planning/phases/17-deployment-scripts/17-01-SUMMARY.md
- packages/server/Dockerfile
- docker-compose.yml
- scripts/deploy.sh
</read_first>

<action>
Create VERIFICATION.md file at `.planning/phases/17-deployment-scripts/VERIFICATION.md` with goal-backward analysis:

1. Document phase goal: "Provide quick deployment tooling for server setup"
2. Verify must-haves:
   - `packages/server/Dockerfile` exists with multi-stage build
   - `packages/server/.dockerignore` exists
   - `.dockerignore` at root exists
   - `docker-compose.yml` exists with healthcheck
   - `scripts/deploy.sh` exists
   - `scripts/deploy-quick.sh` exists
3. Document key decisions:
   - Node.js 22 LTS Alpine for minimal image
   - pnpm via corepack for monorepo support
   - Volume mount for .data persistence
4. Note gap: LOG_* env vars and log volume not configured (Phase 24 scope)
5. Mark status as PASSED if all must-haves verified
</action>

<acceptance_criteria>
- File `.planning/phases/17-deployment-scripts/VERIFICATION.md` exists
- File contains "Dockerfile" evidence
- File contains "docker-compose.yml" evidence
- File contains "deploy.sh" evidence
- File contains status "PASSED" or "GOAL ACHIEVED"
</acceptance_criteria>

---

## Task 2: Verify LOG-01 and LOG-03 (User Ops Logger) -- Phase 21

<read_first>
- .planning/phases/21-user-operations-logger/VERIFICATION.md (existing)
- .planning/phases/21-user-operations-logger/21-01-SUMMARY.md
- .planning/phases/21-user-operations-logger/21-02-SUMMARY.md
- packages/server/src/lib/user-ops-log.ts
- packages/server/src/routes/retrieval.ts
- packages/server/src/routes/operations.ts
</read_first>

<action>
Review and validate existing VERIFICATION.md at `.planning/phases/21-user-operations-logger/VERIFICATION.md`:

1. Confirm LOG-01 verification is complete:
   - 15 route handlers instrumented with logUserOperation calls
   - Each entry includes actorId, actorHandle, action, targetId, teamId, timestamp, metadata
   - Actions covered: search, submit, edit, review, import, export
2. Confirm LOG-03 verification is complete:
   - LOG_USER_OPS_ENABLED env var exists with disabled default
   - Independent from LOG_RAG_ENABLED
3. Add Nyquist compliance section if missing:
   - Sampling rate: After every plan wave
   - Test infrastructure: vitest
   - Wave 0 complete: Yes (tests existed before implementation)
4. Verify evidence by running targeted grep commands:
   - `grep -c "logUserOperation" packages/server/src/routes/*.ts` should return 15+
</action>

<acceptance_criteria>
- File `.planning/phases/21-user-operations-logger/VERIFICATION.md` exists with LOG-01 and LOG-03 verification
- File contains "LOG-01" and "LOG-03" requirement traceability
- File contains "logUserOperation" evidence
- File contains "LOG_USER_OPS_ENABLED" evidence
- Command `grep -c "logUserOperation" packages/server/src/routes/*.ts` returns 15 or higher
</acceptance_criteria>

---

## Task 3: Verify LOG-02, LOG-03, LOG-04 (RAG Logger + Rotation) -- Phase 22

<read_first>
- .planning/phases/22-rag-logger-with-file-rotation/VERIFICATION.md (existing)
- .planning/phases/22-rag-logger-with-file-rotation/22-01-SUMMARY.md
- .planning/phases/22-rag-logger-with-file-rotation/22-02-SUMMARY.md
- packages/server/src/lib/rag-log.ts
- packages/server/src/lib/log-rotation.ts
- packages/server/src/lib/retrieval/orchestrator.ts
</read_first>

<action>
Review and validate existing VERIFICATION.md at `.planning/phases/22-rag-logger-with-file-rotation/VERIFICATION.md`:

1. Confirm LOG-02 verification is complete:
   - RagLogEntry captures mode, pipelineSteps, totalLatencyMs
   - RAG logging integrated into orchestrator (searchKnowledge, searchKnowledgeV2)
   - timedStep helper for pipeline timing
2. Confirm LOG-03 verification is complete:
   - LOG_RAG_ENABLED env var exists with disabled default
   - Independent from LOG_USER_OPS_ENABLED
3. Confirm LOG-04 verification is complete:
   - Size-based rotation with LOG_MAX_FILE_SIZE_MB (default 10MB)
   - Time-based rotation via daily files (YYYY-MM-DD.log)
   - Numbered backups (.1, .2, etc.) up to maxBackupFiles (default 5)
4. Add Nyquist compliance section if missing
5. Verify evidence by running targeted grep commands:
   - `grep -c "logRagRetrieval" packages/server/src/lib/retrieval/orchestrator.ts` should return 6+
   - `grep "appendWithRotation" packages/server/src/lib/user-ops-log.ts packages/server/src/lib/rag-log.ts` should show both files
</action>

<acceptance_criteria>
- File `.planning/phases/22-rag-logger-with-file-rotation/VERIFICATION.md` exists with LOG-02, LOG-03, LOG-04 verification
- File contains "LOG-02", "LOG-03", "LOG-04" requirement traceability
- File contains "logRagRetrieval" evidence
- File contains "appendWithRotation" evidence
- Command `grep -c "logRagRetrieval" packages/server/src/lib/retrieval/orchestrator.ts` returns 6 or higher
</acceptance_criteria>

---

## Task 4: Fix Contracts Build -- dist/index.d.ts Generation

<read_first>
- packages/contracts/tsconfig.json
- tsconfig.base.json
</read_first>

<action>
Fix the contracts package build to generate `dist/index.d.ts`. The current build produces `.js` and `.d.ts.map` but not the actual `.d.ts` file for `index.ts`.

Option A -- Add `declaration` explicitly to contracts tsconfig.json:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "declaration": true
  },
  "include": ["src/**/*.ts"]
}
```

Option B -- If Option A doesn't work, add a separate build step or use `tsc --build` mode.

After fix:
1. Run `pnpm --filter @trapmap/contracts build`
2. Verify `ls packages/contracts/dist/index.d.ts` succeeds
3. Run `pnpm --filter @trapmap/contracts typecheck` to confirm no errors
</action>

<acceptance_criteria>
- File `packages/contracts/dist/index.d.ts` exists after running `pnpm --filter @trapmap/contracts build`
- Command `pnpm --filter @trapmap/contracts typecheck` exits with code 0
- The tsconfig.json modification is minimal (only adds `declaration: true` if needed)
</acceptance_criteria>

---

## Verification Criteria

### Must-Haves (Goal-Backward)

| Requirement | Phase | Must Have | Status |
|-------------|-------|-----------|--------|
| LOG-01 | 21 | 15 routes instrumented with user ops logging | must verify |
| LOG-02 | 22 | RAG logging captures pipeline timing and strategy | must verify |
| LOG-03 | 21, 22 | Independent env toggles for each log layer | must verify |
| LOG-04 | 22 | Size-based and time-based file rotation | must verify |

### Test Gates

- [ ] 10+ unit tests for user-ops-log pass
- [ ] 13+ unit tests for rag-log pass
- [ ] 10+ unit tests for log-rotation pass
- [ ] Contracts build produces dist/index.d.ts

### Documents Created/Updated

- [ ] `.planning/phases/17-deployment-scripts/VERIFICATION.md` (created)
- [ ] `.planning/phases/21-user-operations-logger/VERIFICATION.md` (validated)
- [ ] `.planning/phases/22-rag-logger-with-file-rotation/VERIFICATION.md` (validated)