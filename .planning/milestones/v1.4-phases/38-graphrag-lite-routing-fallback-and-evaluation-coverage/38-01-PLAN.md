---
phase: 38
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/contracts/src/domain/evals/retrieval.ts
  - packages/contracts/src/domain/evals/report.ts
  - evals/retrieval/lib/types.ts
  - evals/retrieval/lib/normalize.ts
  - evals/retrieval/lib/normalize.test.ts
autonomous: true
requirements: [P38-01, P38-02]
must_haves:
  truths:
    - "GraphRAG-lite is a first-class route family in retrieval routing and evaluation contracts"
    - "A graph-plan wrapper response can represent either a selected plan or a governed fallback payload"
    - "The retrieval eval normalizer can convert /v3/retrieval/search responses into ranked hits and routing metadata"
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "Graph-plan route family, routing trace, query, and response contracts"
    - path: "packages/contracts/src/domain/evals/retrieval.ts"
      provides: "Retrieval eval endpoint contract extended to /v3/retrieval/search"
    - path: "evals/retrieval/lib/normalize.ts"
      provides: "v3 graph-plan response normalization into common eval result shape"
  key_links:
    - from: "evals/retrieval/lib/normalize.ts"
      to: "packages/contracts/src/domain/retrieval.ts"
      via: "graphPlanSearchResponseSchema-compatible normalization"
      pattern: "routingTrace"
    - from: "evals/retrieval/lib/types.ts"
      to: "packages/contracts/src/domain/evals/retrieval.ts"
      via: "endpoint and route-family derivation"
      pattern: "/v3/retrieval/search"
---

<objective>
Create the contract and normalization layer for GraphRAG-lite rollout.

Purpose: define a stable wrapper response for GraphRAG-lite selection vs fallback, extend retrieval evaluation to understand a third endpoint, and keep route-family reporting deterministic.
Output: updated retrieval contracts plus v3 normalization tests.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-CONTEXT.md
@.planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-RESEARCH.md
@packages/contracts/src/domain/retrieval.ts
@packages/contracts/src/domain/evals/retrieval.ts
@packages/contracts/src/domain/evals/report.ts
@evals/retrieval/lib/types.ts
@evals/retrieval/lib/normalize.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define graph-plan route and wrapper contracts</name>
  <files>packages/contracts/src/domain/retrieval.ts, packages/contracts/src/domain/evals/retrieval.ts, packages/contracts/src/domain/evals/report.ts</files>
  <read_first>
    packages/contracts/src/domain/retrieval.ts
    packages/contracts/src/domain/evals/retrieval.ts
    packages/contracts/src/domain/evals/report.ts
    .planning/phases/38-graphrag-lite-routing-fallback-and-evaluation-coverage/38-RESEARCH.md
  </read_first>
  <action>
    Extend retrieval contracts without breaking the Phase 37 raw compiler endpoint.

    Required contract changes:
    - add `graph-plan` to `routeFamilySchema`
    - extend routing reason vocabulary with graph-plan selection/low-confidence reasons
    - add `plan` to the routing channel enum used by routing trace
    - add `graphPlanFallbackTargetSchema` with `v2-capsule` and `v1-graph-assisted`
    - add `graphPlanConfidenceBucketSchema`
    - add `graphPlanSearchQuerySchema` with `seed`, `skillBudget`, `maxDepth`, and optional `fallbackMode`
    - add `graphPlanRoutingTraceSchema`
    - add `graphPlanSearchResponseSchema` that wraps either `plan` or `fallback`
    - extend retrieval eval endpoint enums/report schemas to include `/v3/retrieval/search`
  </action>
  <verify>
    <automated>pnpm --filter @trapmap/contracts exec tsc --noEmit</automated>
    <grep_check file="packages/contracts/src/domain/retrieval.ts">
      <pattern>graph-plan</pattern>
      <pattern>graphPlanSearchQuerySchema</pattern>
      <pattern>graphPlanSearchResponseSchema</pattern>
      <pattern>graphPlanRoutingTraceSchema</pattern>
    </grep_check>
    <grep_check file="packages/contracts/src/domain/evals/retrieval.ts">
      <pattern>/v3/retrieval/search</pattern>
    </grep_check>
  </verify>
  <acceptance_criteria>
    - `routeFamilySchema` includes `graph-plan`
    - retrieval contracts export query/response schemas for `graphPlanSearch`
    - retrieval eval endpoint enums accept `/v3/retrieval/search`
    - report schemas continue to validate route-family cohorts with `graph-plan`
    - contracts typecheck passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize v3 graph-plan responses for the eval runner</name>
  <files>evals/retrieval/lib/types.ts, evals/retrieval/lib/normalize.ts, evals/retrieval/lib/normalize.test.ts</files>
  <read_first>
    evals/retrieval/lib/types.ts
    evals/retrieval/lib/normalize.ts
    evals/retrieval/lib/normalize.test.ts
    packages/contracts/src/domain/retrieval.ts
  </read_first>
  <behavior>
    - selected graph-plan responses normalize `recommendedSkills` into ranked hits
    - fallback-v2 responses normalize capsule hits
    - fallback-v1 responses normalize entry hits
    - routing trace metadata survives normalization
    - `deriveRouteFamily('/v3/retrieval/search')` returns `graph-plan`
  </behavior>
  <action>
    Update the eval normalization layer to support `/v3/retrieval/search` and add tests for all three outcomes: selected plan, v2 fallback, and v1 fallback.
  </action>
  <verify>
    <automated>pnpm exec vitest run evals/retrieval/lib/normalize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `normalizeResponse()` accepts `/v3/retrieval/search`
    - normalized v3 results populate `routingTrace`
    - `deriveRouteFamily()` reports `graph-plan` for v3
    - normalization tests cover selected and fallback shapes
  </acceptance_criteria>
</task>

</tasks>

<verification>
  1. `pnpm --filter @trapmap/contracts exec tsc --noEmit`
  2. `pnpm exec vitest run evals/retrieval/lib/normalize.test.ts`
</verification>

<success_criteria>
GraphRAG-lite has a stable contract and eval normalization surface before any server routing logic is added.
</success_criteria>
