---
phase: 29-rag-mode-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/assembly.ts
  - packages/server/src/lib/retrieval/capsule-recall.ts
  - packages/server/src/routes/retrieval.ts
  - packages/server/src/lib/retrieval/**/*.test.ts
  - packages/server/src/routes/retrieval.test.ts
autonomous: true
requirements:
  - EOPS-03
must_haves:
  truths:
    - "Governance filtering remains a precondition across every routed retrieval strategy."
    - "v1 and v2 keep their existing route-family response shapes while sharing one routing decision layer."
    - "Any new mode parity channels for v2 are introduced behind governed execution paths and visible trace metadata."
  artifacts:
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "governance-safe shared execution entrypoints"
      contains: "router selection, executor dispatch, and trace propagation"
    - path: "packages/server/src/routes/retrieval.ts"
      provides: "backward-compatible route behavior"
      contains: "v1/v2 request handling that preserves compatibility while exposing new trace-aware execution"
    - path: "packages/server/src/lib/retrieval/assembly.ts"
      provides: "response shaping"
      contains: "trace-safe assembly without collapsing v1 and v2 response families"
  key_links:
    - from: "packages/server/src/routes/retrieval.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "route-family execution"
      pattern: "searchKnowledge|searchKnowledgeV2|routing"
    - from: "packages/server/src/lib/retrieval/orchestrator.ts"
      to: "packages/server/src/lib/retrieval/assembly.ts"
      via: "route-specific result shaping"
      pattern: "buildRetrievalResponse|buildV2RetrievalResponse"
---

<objective>
Integrate the shared router into server execution while preserving governance-first behavior and route-family compatibility.

Purpose: make the mode-routing layer real in server code by connecting routing decisions to governed recall channels, response shaping, and retrieval-route compatibility tests.
</objective>

<context>
@.planning/phases/29-rag-mode-routing/29-CONTEXT.md
@.planning/phases/29-rag-mode-routing/29-RESEARCH.md
@.planning/phases/29-rag-mode-routing/29-PATTERNS.md
@.planning/phases/29-rag-mode-routing/29-01-PLAN.md
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/assembly.ts
@packages/server/src/lib/retrieval/capsule-recall.ts
@packages/server/src/routes/retrieval.ts
@packages/server/src/routes/retrieval.test.ts
</context>

<tasks>

<task type="execute">
  <name>Task 29-02-01: Apply shared routing to governed v1 and v2 execution paths</name>
  <files>packages/server/src/lib/retrieval/orchestrator.ts, packages/server/src/lib/retrieval/capsule-recall.ts, packages/server/src/lib/retrieval/**/*.test.ts</files>
  <read_first>
    @packages/server/src/lib/retrieval/orchestrator.ts
    @packages/server/src/lib/retrieval/capsule-recall.ts
    @packages/server/src/lib/retrieval/filters.ts
    @packages/server/src/lib/retrieval/assembly.ts
    @packages/server/src/lib/retrieval/intent.ts
    @.planning/phases/29-rag-mode-routing/29-01-PLAN.md
  </read_first>
  <action>
    Rework server-side retrieval execution so the shared router selects among governed strategies before assembly, but governance checks still happen first for the relevant data family. Preserve current legacy entry-search channels and capsule ranking. If Phase 29 introduces a missing v2 parity channel such as profile/global or mix-mode composition, implement it only through governed, typed helpers and thread the chosen strategy plus fallback metadata through the execution result.
  </action>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval/orchestrator.ts` contains `selectedMode`
    - `packages/server/src/lib/retrieval/orchestrator.ts` contains `channelsUsed`
    - `packages/server/src/lib/retrieval/orchestrator.ts` still contains governance filtering before recall logic
    - retrieval tests include at least one assertion that forbidden content is absent for a routed non-default strategy
    - `pnpm test -- --run packages/server/src/lib/retrieval packages/server/src/routes/retrieval` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --run packages/server/src/lib/retrieval packages/server/src/routes/retrieval</automated>
  </verify>
  <done>
    Shared routing is active in governed execution paths, routed non-default strategies still honor authorization boundaries, and integration tests prove forbidden content stays absent.
  </done>
</task>

<task type="execute">
  <name>Task 29-02-02: Preserve route compatibility while surfacing trace-aware retrieval behavior</name>
  <files>packages/server/src/routes/retrieval.ts, packages/server/src/lib/retrieval/assembly.ts, packages/server/src/routes/retrieval.test.ts</files>
  <read_first>
    @packages/server/src/routes/retrieval.ts
    @packages/server/src/lib/retrieval/assembly.ts
    @packages/contracts/src/domain/retrieval.ts
    @packages/server/src/routes/retrieval.test.ts
  </read_first>
  <action>
    Update route handlers and any response assembly surfaces needed so Phase 29 trace metadata can be returned or logged without breaking existing request compatibility. Keep `/v1/retrieval/search` accepting `semantic`, `hybrid`, and `graph-assisted`. Keep `/v2/retrieval/search` functioning for current callers whether or not an optional mode field is added. Add route-level tests that prove both families still validate and that trace-aware behavior is observable in a stable, documented place.
  </action>
  <acceptance_criteria>
    - `packages/server/src/routes/retrieval.ts` still contains `/v1/retrieval/search`
    - `packages/server/src/routes/retrieval.ts` still contains `/v2/retrieval/search`
    - route tests assert current v1 mode inputs continue to pass validation
    - route tests assert trace-aware metadata or logging hooks are exercised for routed requests
    - `pnpm test -- --run packages/server/src/routes/retrieval` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --run packages/server/src/routes/retrieval</automated>
  </verify>
  <done>
    Route handlers preserve current v1 and v2 compatibility, and route-level tests confirm trace-aware execution is observable without breaking validation for existing callers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Governed dataset/artifact filtering -> routed recall | Routing must not bypass approval, team, or security constraints |
| Router decision -> route response | Trace metadata exposure must not leak forbidden content or implementation-only secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | E | routed recall | mitigate | Run governance filtering before any mode-specific recall or merge logic. |
| T-29-05 | I | trace exposure | mitigate | Limit trace output to mode IDs, reasons, and channel names rather than raw forbidden candidate details. |
| T-29-06 | T | route compatibility | mitigate | Keep explicit tests for existing v1 request modes and current v2 behavior during the refactor. |
</threat_model>

<verification>
Run retrieval library and route integration tests. Confirm routed strategies do not bypass governance and existing route contracts still work.
</verification>

<success_criteria>
Phase 29-02 makes the router effective in production server flows while preserving governance-first behavior and backward-compatible route usage.
</success_criteria>
