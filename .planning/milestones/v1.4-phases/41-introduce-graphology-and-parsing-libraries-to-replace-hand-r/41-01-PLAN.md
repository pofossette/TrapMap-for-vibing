---
phase: 41
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-introduce-graphology-and-parsing-libraries-to-replace-hand-r/41-RESEARCH.md
  - packages/server/package.json
  - packages/server/src/lib/ids.ts
  - packages/server/src/lib/ids.test.ts
  - packages/server/src/lib/rag-log.ts
  - packages/server/src/lib/rag-log.test.ts
  - packages/server/src/lib/candidates/detector.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [P41-01, P41-02, P41-03]
must_haves:
  truths:
    - "Graphology and parsing library adoption remains hidden behind local wrappers instead of leaking third-party details through the codebase"
    - "Server-generated IDs no longer use ad hoc Date.now/Math.random concatenation"
    - "Phase 41 completes the dependency-boundary groundwork without pulling Phase 42 graph-runtime migration into scope"
  artifacts:
    - path: "packages/server/src/lib/ids.ts"
      provides: "Shared nanoid-backed helper for prefixed server identifiers"
    - path: "packages/server/src/lib/rag-log.ts"
      provides: "Query IDs generated through the shared helper"
    - path: "packages/server/src/lib/candidates/detector.ts"
      provides: "Duplicate-case IDs generated through the shared helper"
---

<objective>
Finish the remaining Phase 41 wrapper work.

Purpose: keep the already-introduced graphology and parsing dependencies behind local boundaries, then replace the last ad hoc server ID generation paths with one explicit `nanoid`-backed helper.
Output: one shared server ID module, migrated callsites, and regression coverage proving the prefix-preserving ID format.
</objective>

<tasks>
  <task>
    <name>Task 1: Add the shared server ID boundary</name>
    <read_first>
      - packages/server/package.json
      - packages/server/src/lib/rag-log.ts
      - packages/server/src/lib/candidates/detector.ts
      - .planning/phases/41-introduce-graphology-and-parsing-libraries-to-replace-hand-r/41-CONTEXT.md
      - .planning/phases/41-introduce-graphology-and-parsing-libraries-to-replace-hand-r/41-RESEARCH.md
    </read_first>
    <files>packages/server/package.json, packages/server/src/lib/ids.ts, pnpm-lock.yaml</files>
    <action>
      Add `nanoid` as an explicit dependency of `@trapmap/server`, then create `packages/server/src/lib/ids.ts` with a helper that returns `${prefix}_${nanoid()}` identifiers for server-owned query and duplicate-case records. Keep the helper local to the server package so contracts stay library-agnostic.
    </action>
    <acceptance_criteria>
      - `packages/server/package.json` contains `nanoid`
      - `packages/server/src/lib/ids.ts` exports a prefixed ID helper
      - the helper does not expose `nanoid` types or implementation details outside the server package
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Replace ad hoc ID generation at live callsites</name>
    <read_first>
      - packages/server/src/lib/ids.ts
      - packages/server/src/lib/rag-log.ts
      - packages/server/src/lib/candidates/detector.ts
      - packages/server/src/lib/rag-log.test.ts
    </read_first>
    <files>packages/server/src/lib/rag-log.ts, packages/server/src/lib/rag-log.test.ts, packages/server/src/lib/candidates/detector.ts, packages/server/src/lib/ids.test.ts</files>
    <action>
      Route `generateQueryId()` and duplicate-case ID creation through the new shared helper. Update or add tests so query IDs still carry the `qry_` prefix, duplicate-case IDs carry the `dupcase_` prefix, and repeated generation remains unique.
    </action>
    <acceptance_criteria>
      - `packages/server/src/lib/rag-log.ts` no longer builds IDs with `Date.now()` or `Math.random()`
      - `packages/server/src/lib/candidates/detector.ts` no longer builds duplicate-case IDs with `Date.now()` or `Math.random()`
      - `packages/server/src/lib/ids.test.ts` verifies prefix and uniqueness behavior
      - `packages/server/src/lib/rag-log.test.ts` passes with the new query ID format
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/ids.test.ts src/lib/rag-log.test.ts`
  2. `pnpm --filter @trapmap/server typecheck`
</verification>
