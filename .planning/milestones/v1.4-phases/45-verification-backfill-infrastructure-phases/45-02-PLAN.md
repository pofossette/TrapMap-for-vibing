---
wave: 2
depends_on:
  - 45-01
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EOPS-01
  - EOPS-02
---

# Plan 45-02: Verify EOPS-01/EOPS-02 Infrastructure and Update REQUIREMENTS.md

## Context

EOPS-01 and EOPS-02 were implemented in Phase 31 (CI regression reporting). The Phase 31 VERIFICATION.md confirms these requirements are satisfied. This plan verifies the implementation against the requirements and updates REQUIREMENTS.md to mark them as complete.

**EOPS-01**: Evaluation outputs machine-readable and human-readable reports that compare results across endpoint and retrieval mode combinations

**EOPS-02**: Repo scripts support a fast smoke evaluation path for pull requests and a broader core evaluation path for regression tracking

## Tasks

### Task 1: Verify EOPS-01 Implementation

<read_first>
- .planning/phases/31-ci/VERIFICATION.md
- evals/scripts/eval-ci.ts
- packages/contracts/src/domain/evals/report.ts
</read_first>

<objective>
Verify that EOPS-01 is satisfied by examining the evaluation output formats.
</objective>

<action>
Verify the following capabilities exist:
1. Machine-readable JSON report output (eval-ci.ts writes to `reports/eval-report.json`)
2. Human-readable console output (formatCompactSummary, formatRegressionResult functions)
3. Mode comparisons in reports (ModeComparison schema, buildModeComparisons function)
4. Endpoint comparisons via slice aggregation by endpoint and mode
5. Cohort summaries for cross-dimensional comparison
</action>

<acceptance_criteria>
- Grep `writeFileSync.*eval-report.json` in eval-ci.ts returns a match
- Grep `formatCompactSummary\|formatRegressionResult` in eval-ci.ts returns matches
- Grep `ModeComparison\|modeComparisons` in report.ts returns matches
- Grep `buildModeComparisons\|buildCohortSummaries` in evals/retrieval/lib/report.ts returns matches
- Phase 31 VERIFICATION.md explicitly lists EOPS-01 as complete
</acceptance_criteria>

---

### Task 2: Verify EOPS-02 Implementation

<read_first>
- .planning/phases/31-ci/VERIFICATION.md
- .github/workflows/eval.yml
- package.json (for eval:ci and eval:ci:core scripts)
</read_first>

<objective>
Verify that EOPS-02 is satisfied by examining the CI configuration and npm scripts.
</objective>

<action>
Verify the following capabilities exist:
1. Fast smoke evaluation path (TIER=smoke, eval-smoke job in workflow)
2. Broader core evaluation path (TIER=core, eval-core-scheduled job in workflow)
3. Smoke runs on PRs (on: pull_request triggers eval-smoke)
4. Core runs on schedule (on: schedule triggers eval-core-scheduled)
5. Baseline comparison for regression detection
6. PR comment with regression summary
</action>

<acceptance_criteria>
- `.github/workflows/eval.yml` contains `eval-smoke` job
- `.github/workflows/eval.yml` contains `eval-core-scheduled` job
- `.github/workflows/eval.yml` has `on: pull_request` trigger
- `.github/workflows/eval.yml` has `on: schedule` trigger
- `eval-ci.ts` contains `setGitHubOutput('has_regressions'` for regression detection
- Phase 31 VERIFICATION.md explicitly lists EOPS-02 as complete
</acceptance_criteria>

---

### Task 3: Update REQUIREMENTS.md to Mark EOPS-01 and EOPS-02 Complete

<read_first>
- .planning/REQUIREMENTS.md
</read_first>

<objective>
Update REQUIREMENTS.md to mark EOPS-01 and EOPS-02 as complete.
</objective>

<action>
Update the REQUIREMENTS.md file:
1. Change `- [ ] **EOPS-01**:` to `- [x] **EOPS-01**:`
2. Change `- [ ] **EOPS-02**:` to `- [x] **EOPS-02**:`
3. Update the Traceability table:
   - Change `EOPS-01 | Phase 28, Phase 45, Phase 47 | Pending` to `EOPS-01 | Phase 31, Phase 44 | Complete`
   - Change `EOPS-02 | Phase 28, Phase 45, Phase 46, Phase 47 | Pending` to `EOPS-02 | Phase 31, Phase 44 | Complete`
4. Update the Last updated date
</action>

<acceptance_criteria>
- Line containing `EOPS-01` shows `- [x]` (checked)
- Line containing `EOPS-02` shows `- [x]` (checked)
- Traceability table shows EOPS-01 with `Complete` status
- Traceability table shows EOPS-02 with `Complete` status
</acceptance_criteria>

---

## Verification Criteria

- `grep "\- \[x\].*EOPS-01" .planning/REQUIREMENTS.md` returns exactly 1 match
- `grep "\- \[x\].*EOPS-02" .planning/REQUIREMENTS.md` returns exactly 1 match
- `grep "EOPS-01.*Complete" .planning/REQUIREMENTS.md` returns a match
- `grep "EOPS-02.*Complete" .planning/REQUIREMENTS.md` returns a match

## Must-Haves

- [ ] EOPS-01 verified as satisfied by codebase inspection
- [ ] EOPS-02 verified as satisfied by codebase inspection
- [ ] REQUIREMENTS.md updated with EOPS-01 and EOPS-02 marked complete
- [ ] Traceability table updated with correct phase references
