---
wave: 2
depends_on:
  - 45-01
files_modified: []
autonomous: true
requirements:
  - EOPS-01
  - EOPS-02
---

# Plan 45-03: Verify Existing VERIFICATION.md Files for Phases 31-35

## Context

VERIFICATION.md files already exist for phases 31-35. This plan verifies that each file is truthful and accurately reflects the implementation state. No modifications are needed unless discrepancies are found.

## Tasks

### Task 1: Spot-Check Phase 31 VERIFICATION.md Accuracy

<read_first>
- .planning/phases/31-ci/VERIFICATION.md
- evals/scripts/eval-ci.ts
- packages/contracts/src/domain/evals/report.ts
</read_first>

<objective>
Verify Phase 31 VERIFICATION.md claims are accurate by sampling key artifacts.
</objective>

<action>
Sample verification of key claims:
1. Verify QueryTypeCohort schema exists in report.ts
2. Verify ModeComparison schema exists in report.ts
3. Verify BaselineReport schema exists in report.ts
4. Verify compareWithBaseline function exists in eval-ci.ts
5. Verify TIER_THRESHOLDS exists in report.ts
6. Verify .github/workflows/eval.yml has baseline download/upload steps
</action>

<acceptance_criteria>
- `grep "QueryTypeCohort" packages/contracts/src/domain/evals/report.ts` returns matches
- `grep "ModeComparison" packages/contracts/src/domain/evals/report.ts` returns matches
- `grep "BaselineReport" packages/contracts/src/domain/evals/report.ts` returns matches
- `grep "compareWithBaseline" evals/scripts/eval-ci.ts` returns matches
- `grep "TIER_THRESHOLDS" packages/contracts/src/domain/evals/report.ts` returns matches
- `grep "baseline" .github/workflows/eval.yml` returns matches
</acceptance_criteria>

---

### Task 2: Spot-Check Phase 32 VERIFICATION.md Accuracy

<read_first>
- .planning/phases/32-skill-trap-cli/VERIFICATION.md
- packages/server/src/lib/governance/index.ts
- packages/server/src/routes/traps.ts
</read_first>

<objective>
Verify Phase 32 VERIFICATION.md claims are accurate by sampling key artifacts.
</objective>

<action>
Sample verification of key claims:
1. Verify governance module exists at packages/server/src/lib/governance/
2. Verify isGovernanceEligible function is exported
3. Verify trapRoutes is exported from routes/traps.ts
4. Verify /v1/traps routes are documented in app.ts documentedRoutes
</action>

<acceptance_criteria>
- File `packages/server/src/lib/governance/index.ts` exists
- `grep "export.*isGovernanceEligible" packages/server/src/lib/governance/index.ts` returns a match
- `grep "export.*trapRoutes" packages/server/src/routes/traps.ts` returns a match
- `grep "'POST /v1/traps'" packages/server/src/app.ts` returns a match
</acceptance_criteria>

---

### Task 3: Spot-Check Phase 33 VERIFICATION.md Accuracy

<read_first>
- .planning/phases/33-async-candidate-ingest-and-duplicate-decision-queue/VERIFICATION.md
- packages/server/src/lib/candidates/index.ts
- packages/server/src/routes/candidates.ts
</read_first>

<objective>
Verify Phase 33 VERIFICATION.md claims are accurate by sampling key artifacts.
</objective>

<action>
Sample verification of key claims:
1. Verify candidates module exists
2. Verify computeCandidateFingerprint function is exported
3. Verify detectDuplicates function is exported
4. Verify candidateRoutes is registered in app.ts
5. Verify startup recovery hook exists in app.ts
</action>

<acceptance_criteria>
- File `packages/server/src/lib/candidates/index.ts` exists
- `grep "computeCandidateFingerprint" packages/server/src/lib/candidates/index.ts` returns a match
- `grep "detectDuplicates\|findInterruptedCandidates" packages/server/src/lib/candidates/index.ts` returns a match
- `grep "candidateRoutes" packages/server/src/app.ts` returns a match
- `grep "findInterruptedCandidates" packages/server/src/app.ts` returns a match
</acceptance_criteria>

---

### Task 4: Spot-Check Phase 34 VERIFICATION.md Accuracy

<read_first>
- .planning/phases/34-builtin-duplicate-job-fetch-command-and-manual-result-intake/VERIFICATION.md
- packages/contracts/src/domain/candidates.ts
- packages/cli/src/commands/skill.ts
</read_first>

<objective>
Verify Phase 34 VERIFICATION.md claims are accurate by sampling key artifacts.
</objective>

<action>
Sample verification of key claims:
1. Verify ManualResultDecisionSchema exists in candidates.ts
2. Verify DuplicateJobBundleResponseSchema exists in candidates.ts
3. Verify duplicate-job fetch command exists in skill.ts
4. Verify duplicate-job resolve command exists in skill.ts
</action>

<acceptance_criteria>
- `grep "ManualResultDecisionSchema" packages/contracts/src/domain/candidates.ts` returns a match
- `grep "DuplicateJobBundleResponseSchema" packages/contracts/src/domain/candidates.ts` returns a match
- `grep "duplicate-job.*fetch" packages/cli/src/commands/skill.ts` returns a match
- `grep "duplicate-job.*resolve" packages/cli/src/commands/skill.ts` returns a match
</acceptance_criteria>

---

### Task 5: Spot-Check Phase 35 VERIFICATION.md Accuracy

<read_first>
- .planning/phases/35-manual-result-revalidation-and-publish-merge-reconciliation/VERIFICATION.md
- packages/server/src/lib/candidates/reconcile.ts
</read_first>

<objective>
Verify Phase 35 VERIFICATION.md claims are accurate by sampling key artifacts.
</objective>

<action>
Sample verification of key claims:
1. Verify revalidateManualResult function exists
2. Verify publishTrapCandidate function exists
3. Verify publishSkillCandidate function exists
4. Verify recordMergeLineage function exists
5. Verify apply-resolution endpoint exists in routes/candidates.ts
</action>

<acceptance_criteria>
- `grep "export function revalidateManualResult" packages/server/src/lib/candidates/reconcile.ts` returns a match
- `grep "export function publishTrapCandidate" packages/server/src/lib/candidates/reconcile.ts` returns a match
- `grep "export function publishSkillCandidate" packages/server/src/lib/candidates/reconcile.ts` returns a match
- `grep "export function recordMergeLineage" packages/server/src/lib/candidates/reconcile.ts` returns a match
- `grep "apply-resolution" packages/server/src/routes/candidates.ts` returns a match
</acceptance_criteria>

---

## Verification Criteria

- All 5 tasks pass their acceptance criteria
- No discrepancies found between VERIFICATION.md claims and actual code
- If any discrepancies are found, they must be documented and flagged for correction

## Must-Haves

- [ ] Phase 31 VERIFICATION.md claims verified as accurate
- [ ] Phase 32 VERIFICATION.md claims verified as accurate
- [ ] Phase 33 VERIFICATION.md claims verified as accurate
- [ ] Phase 34 VERIFICATION.md claims verified as accurate
- [ ] Phase 35 VERIFICATION.md claims verified as accurate
