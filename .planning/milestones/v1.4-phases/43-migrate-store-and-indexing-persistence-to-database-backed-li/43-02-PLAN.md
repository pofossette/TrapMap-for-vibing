---
phase: 43
plan: 02
type: implementation
wave: 2
depends_on: [43-01]
files_modified:
  - packages/server/src/lib/context.ts
  - packages/server/src/lib/audit.ts
  - packages/server/src/lib/session.ts
  - packages/server/src/lib/knowledge.ts
  - packages/server/src/lib/import-export.ts
  - packages/server/src/lib/artifacts/edit.ts
  - packages/server/src/lib/artifacts/model.ts
  - packages/server/src/lib/candidates/store.ts
  - packages/server/src/lib/candidates/processor.ts
  - packages/server/src/lib/candidates/reconcile.ts
  - packages/server/src/lib/indexing/events.ts
  - packages/server/src/lib/indexing/pipeline.ts
  - packages/server/src/lib/indexing/reconcile.ts
  - packages/server/src/lib/indexing/skill-events.ts
  - packages/server/src/lib/indexing/adapters/graph.ts
autonomous: true
requirements: [P43-02, P43-03]
must_haves:
  truths:
    - "Production services no longer require the concrete JsonStore type in order to compile or run"
    - "The highest-risk indexing and candidate workflows continue to operate against the shared store contract without behavior-specific rewrites"
    - "Regression coverage proves the new store contract preserves ID allocation and transactional aggregate mutation behavior"
  artifacts:
    - path: "packages/server/src/lib/context.ts"
      provides: "Server service surface widened to the shared store contract"
    - path: "packages/server/src/lib/indexing/reconcile.ts"
      provides: "Highest-risk aggregate transaction hotspot compiled against the shared store contract"
    - path: "packages/server/src/lib/store.test.ts"
      provides: "Regression coverage for database-backed store semantics"
---

<objective>
Propagate the shared store contract through production callers and verify the behavior.

Purpose: make the rest of the server consume the new database-capable store seam instead of the concrete file-backed class, then prove the new seam preserves current aggregate mutation semantics.
Output: widened production type signatures plus focused regression tests for the database-backed store and runtime bootstrap selection.
</objective>

<tasks>
  <task>
    <name>Task 1: Replace concrete JsonStore type dependencies in production modules</name>
    <read_first>
      - packages/server/src/lib/context.ts
      - packages/server/src/lib/session.ts
      - packages/server/src/lib/candidates/reconcile.ts
      - packages/server/src/lib/indexing/reconcile.ts
      - packages/server/src/lib/store.ts
    </read_first>
    <files>packages/server/src/lib/context.ts, packages/server/src/lib/audit.ts, packages/server/src/lib/session.ts, packages/server/src/lib/knowledge.ts, packages/server/src/lib/import-export.ts, packages/server/src/lib/artifacts/edit.ts, packages/server/src/lib/artifacts/model.ts, packages/server/src/lib/candidates/store.ts, packages/server/src/lib/candidates/processor.ts, packages/server/src/lib/candidates/reconcile.ts, packages/server/src/lib/indexing/events.ts, packages/server/src/lib/indexing/pipeline.ts, packages/server/src/lib/indexing/reconcile.ts, packages/server/src/lib/indexing/skill-events.ts, packages/server/src/lib/indexing/adapters/graph.ts</files>
    <action>
      Widen production service and helper signatures from the concrete `JsonStore` class to the shared store contract introduced in Plan 01. Keep the caller behavior unchanged; this is a type and wiring migration, not a repository rewrite.
    </action>
    <acceptance_criteria>
      - production modules compile against the shared store contract
      - no runtime code outside the persistence layer depends on `JsonStore`-specific internals
      - indexing and candidate workflow hotspots still use the same aggregate mutation behavior
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Add regression coverage for PostgreSQL-backed store behavior</name>
    <read_first>
      - packages/server/src/lib/persistence/postgres-store.ts
      - packages/server/src/app.ts
      - packages/server/src/lib/store.ts
    </read_first>
    <files>packages/server/src/lib/store.test.ts, packages/server/src/app.test.ts</files>
    <action>
      Add focused tests that exercise the new PostgreSQL-backed store semantics through the shared contract: empty-store initialization, snapshot round-tripping, transactional aggregate mutation, deterministic `nextId` allocation, and runtime store selection in bootstrap/config.
    </action>
    <acceptance_criteria>
      - tests cover the database-backed store through the shared contract instead of filesystem assumptions
      - tests prove `nextId` still advances inside a transaction
      - tests show the server selects the correct store implementation for configured database vs file-backed modes
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/server test -- src/lib/indexing/reconcile.test.ts src/lib/candidates/reconcile.test.ts`
</verification>
