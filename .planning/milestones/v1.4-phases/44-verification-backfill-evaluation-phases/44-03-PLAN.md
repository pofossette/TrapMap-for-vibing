---
phase: 44-verification-backfill-evaluation-phases
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - .planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md
  - .planning/phases/29-rag-mode-routing/VERIFICATION.md
  - .planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REVAL-01
  - REVAL-02
  - REVAL-03
  - REVAL-04
  - SEVAL-01
  - SEVAL-02
  - EOPS-03
must_haves:
  truths:
    - "Phase 28 receives its missing VERIFICATION.md without hiding live unified-runner or CI workflow defects."
    - "Phase 29 verification proves baseline/failure-policy capability separately from CI health."
    - "Phase 44 ends with an aggregate closure matrix for REVAL, SEVAL, and EOPS-03 that marks satisfied, caveated, or deferred outcomes truthfully."
  artifacts:
    - path: ".planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md"
      provides: "Truthful EOPS-01/EOPS-02 backfill evidence with deferred defect list"
    - path: ".planning/phases/29-rag-mode-routing/VERIFICATION.md"
      provides: "Truthful EOPS-03 baseline/failure-policy verification"
    - path: ".planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md"
      provides: "Phase-level closure matrix for evaluation verification backfill"
    - path: ".planning/ROADMAP.md"
      provides: "Verified Phase 44 roadmap entry, updated when the current goal or plan list overstates unconditional requirement satisfaction"
  key_links:
    - from: ".planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md"
      to: ".github/workflows/eval.yml"
      via: "Deferred workflow-defect evidence"
      pattern: "steps\\.eval\\.outputs|id: eval"
    - from: ".planning/phases/29-rag-mode-routing/VERIFICATION.md"
      to: "evals/scripts/eval-ci.ts"
      via: "Baseline comparison and failure-policy evidence"
      pattern: "baseline|regression"
    - from: ".planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md"
      to: ".planning/phases/27-summary-evaluation-and-judge-integration/VERIFICATION.md"
      via: "SEVAL-01 aggregate closure decision"
      pattern: "SEVAL-01|citation adherence"
---

<objective>
Backfill the missing Phase 28 verification artifact, refresh Phase 29 verification for EOPS-03, and produce the Phase 44 aggregate closure report with roadmap verification/update-if-needed.

Purpose: Finish the evaluation-phase verification reconciliation without turning Phase 44 into a hidden CI-fix or feature-expansion phase.
Output: New/updated `VERIFICATION.md` artifacts for phases 28, 29, and 44 plus a verified Phase 44 roadmap entry that changes only if stale.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-verification-backfill-evaluation-phases/44-RESEARCH.md
@.planning/phases/28-ci-integration-and-evaluation-reporting/28-01-SUMMARY.md
@.planning/phases/28-ci-integration-and-evaluation-reporting/28-02-SUMMARY.md
@.planning/phases/29-rag-mode-routing/29-01-SUMMARY.md
@.planning/phases/29-rag-mode-routing/29-02-SUMMARY.md
@.planning/phases/29-rag-mode-routing/29-03-SUMMARY.md
@.planning/phases/29-rag-mode-routing/VERIFICATION.md
@evals/scripts/eval-all.ts
@evals/scripts/eval-ci.ts
@.github/workflows/eval.yml
@package.json
@packages/contracts/src/domain/evals/report.ts

<interfaces>
From [packages/contracts/src/domain/evals/report.ts](/home/wunai/project/TrapMap-for-vibing/packages/contracts/src/domain/evals/report.ts:267):

```ts
export const baselineReportSchema = z.object({
  tier: z.enum(['smoke', 'core']),
  generatedAt: z.string().datetime(),
  slices: z.array(baselineSliceSchema),
  cohorts: z.array(baselineCohortSchema).default([]),
  governanceFailures: z.array(baselineGovernanceFailureSchema).default([]),
});
```

From [packages/contracts/src/domain/evals/report.ts](/home/wunai/project/TrapMap-for-vibing/packages/contracts/src/domain/evals/report.ts:302):

```ts
export const regressionThresholdsSchema = z.object({
  maxHitAt1Drop: z.number().min(0).max(1),
  maxMrrDrop: z.number().min(0).max(1),
  maxPassRateDrop: z.number().min(0).max(1),
  maxGovernanceFailureIncrease: z.number().int().min(0),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 28 verification with explicit deferred defects</name>
  <files>.planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md</files>
  <action>Create the missing Phase 28 verification artifact from scratch. Use the phase summaries, `evals/scripts/eval-all.ts`, `evals/scripts/eval-ci.ts`, `package.json`, and `.github/workflows/eval.yml` as canonical sources. Per constraints 2, 5, and 6, distinguish implemented operational surfaces from current defects: document the unified runner module-resolution failure, the missing `id: eval` workflow step for `steps.eval.outputs.*`, and any baseline upload mismatches as deferred issues for later EOPS phases instead of silently fixing them here.</action>
  <verify>
    <automated>rtk rg -n "eval-all|eval-ci|baseline-smoke|baseline-core" package.json evals/scripts/eval-all.ts evals/scripts/eval-ci.ts && rtk rg -n "steps\\.eval\\.outputs|id: eval" .github/workflows/eval.yml && rtk rg -n "eval-all.ts|eval-ci.ts|id: eval|steps\\.eval\\.outputs|deferred|Phase 46|Phase 47" .planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md</automated>
  </verify>
  <done>Phase 28 has a verification artifact that proves the intended EOPS surface exists while explicitly deferring live CI/unified-runner defects.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Phase 29 verification for EOPS-03 baseline policy evidence</name>
  <files>.planning/phases/29-rag-mode-routing/VERIFICATION.md</files>
  <action>Refresh Phase 29 verification so it emphasizes the baseline and failure-policy surface that exists now: baseline report schema, regression thresholds, retrieval report baseline fields, runner flags, and `eval-ci.ts` comparison behavior. Per constraints 2 and 3, separate this capability proof from whether CI wiring is currently healthy. Do not let Phase 28 workflow defects erase valid EOPS-03 evidence, but do mention them as deferred operational caveats.</action>
  <verify>
    <automated>rtk rg -n "baselineReportSchema|regressionThresholdsSchema" packages/contracts/src/domain/evals/report.ts && rtk rg -n "baseline|regression|threshold" evals/retrieval/run.ts evals/scripts/eval-ci.ts && rtk rg -n "EOPS-03|baselineReportSchema|regressionThresholdsSchema|eval-ci.ts|deferred operational caveat" .planning/phases/29-rag-mode-routing/VERIFICATION.md</automated>
  </verify>
  <done>Phase 29 verification clearly proves baseline/failure-policy capability and records CI-health caveats without conflating the two.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Phase 44 aggregate closure report and verify/update roadmap only if needed</name>
  <files>.planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md, .planning/ROADMAP.md</files>
  <action>Create a Phase 44 `VERIFICATION.md` that aggregates phases 25-29 into one requirement closure matrix for `REVAL-01/02/03/04`, `SEVAL-01/02`, and `EOPS-03`. The matrix must record `satisfied`, `satisfied with caveat`, or `deferred blocker` based on the refreshed per-phase artifacts. Per constraints 2 and 6, only mark a requirement satisfied when the evidence is explicit; if SEVAL-01 still lacks explicit citation-adherence evidence, keep that boundary visible. Verify the existing Phase 44 roadmap section against this revised plan set and update `.planning/ROADMAP.md` if the current goal overstates unconditional requirement satisfaction or if the plan count/objective lines are stale; the revised goal must explicitly allow truthful confirmation, caveats, and deferred blockers.</action>
  <verify>
    <automated>rtk rg -n "REVAL-01|REVAL-02|REVAL-03|REVAL-04|SEVAL-01|SEVAL-02|EOPS-03" .planning/phases/25-evaluation-contracts-and-golden-dataset-foundation/VERIFICATION.md .planning/phases/26-retrieval-metrics-runner-and-governance-checks/VERIFICATION.md .planning/phases/27-summary-evaluation-and-judge-integration/VERIFICATION.md .planning/phases/28-ci-integration-and-evaluation-reporting/VERIFICATION.md .planning/phases/29-rag-mode-routing/VERIFICATION.md && rtk rg -n "truthful confirmation|caveats|deferred blockers|44-01-PLAN\\.md|44-02-PLAN\\.md|44-03-PLAN\\.md" .planning/ROADMAP.md && rtk rg -n "satisfied with caveat|deferred blocker|SEVAL-01|EOPS-03" .planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md</automated>
  </verify>
  <done>Phase 44 ends with an aggregate closure artifact and a roadmap entry whose goal text matches the truthful closure boundary, with explicit satisfied/caveated/deferred status handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation summaries → verification signoff | Phase summaries can overstate reality if not checked against current code and workflows |
| aggregate closure report → future requirement closure | A misleading Phase 44 matrix could cause false closure in later EOPS verification phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | R | 28/29/44 VERIFICATION.md | mitigate | Require every aggregate status to cite a refreshed per-phase verification artifact or live workflow/report source |
| T-44-03-02 | T | Phase 28 defect handling | mitigate | Explicitly record CI/unified-runner defects as deferred items tied to later roadmap phases instead of “fixing by wording” |
</threat_model>

<verification>
Run the source-truth workflow/contract checks above, then confirm the Phase 44 roadmap section was only edited if its existing plan count or concise objectives were stale.
</verification>

<success_criteria>
Phase 28 is no longer missing verification, Phase 29 has current EOPS-03 evidence, and Phase 44 has an aggregate closure matrix plus a roadmap entry that avoids redundant churn.
</success_criteria>

<output>
After completion, create `.planning/phases/44-verification-backfill-evaluation-phases/44-03-SUMMARY.md`
</output>
