---
phase: 26-retrieval-metrics-runner-and-governance-checks
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - evals/retrieval/run.ts
  - evals/retrieval/README.md
  - evals/retrieval/lib/assertions.ts
  - evals/retrieval/lib/report.ts
  - evals/retrieval/lib/format.ts
  - evals/retrieval/lib/types.ts
  - packages/contracts/src/domain/evals/report.ts
  - packages/contracts/src/index.ts
  - packages/contracts/src/index.test.ts
  - evals/retrieval/**/*.test.ts
autonomous: true
requirements:
  - REVAL-01
  - REVAL-03
  - REVAL-04
must_haves:
  truths:
    - "Governance failures surface explicitly and independently from ranking metrics."
    - "Per-slice summaries show endpoint, tier, and mode breakdowns suitable for regression review."
    - "The retrieval evaluator emits both machine-readable and human-readable output from one canonical report structure."
    - "Compatibility warnings and fallback execution details remain visible in reports instead of being silently swallowed."
  artifacts:
    - path: "evals/retrieval/lib/assertions.ts"
      provides: "Governance and expectation evaluation"
      contains: "forbidden-hit, scope, and empty-result mismatch checks"
    - path: "evals/retrieval/lib/report.ts"
      provides: "Canonical retrieval evaluation report builder"
      contains: "case results, slice summaries, and failure breakdowns"
    - path: "evals/retrieval/lib/format.ts"
      provides: "Human-readable terminal formatting"
      contains: "terminal summary for slices, metrics, and governance failures"
    - path: "packages/contracts/src/domain/evals/report.ts"
      provides: "Machine-readable report schema"
      contains: "retrieval eval report contract"
    - path: "evals/retrieval/run.ts"
      provides: "CLI flags for JSON and human output"
      contains: "report emission and failure exit behavior"
  key_links:
    - from: "evals/retrieval/lib/types.ts"
      to: "evals/retrieval/lib/assertions.ts"
      via: "normalized case result evaluation"
      pattern: "governanceVerdict|expectedOutcome"
    - from: "evals/retrieval/lib/assertions.ts"
      to: "evals/retrieval/lib/report.ts"
      via: "case verdict aggregation"
      pattern: "failures|warnings|sliceKey"
    - from: "packages/contracts/src/domain/evals/report.ts"
      to: "evals/retrieval/run.ts"
      via: "JSON output validation"
      pattern: "retrievalEvalReportSchema"
---

<objective>
Add governance assertion evaluation, per-slice reporting, and regression-friendly machine/human output on top of the Phase 26 execution and metric substrate.

Purpose: Finish REVAL-04 and complete the maintainers’ reporting surface for REVAL-01 and REVAL-03.
Output: Canonical case verdicts, explicit governance failure reporting, slice summaries, and validated JSON plus terminal report output.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-RESEARCH.md
@.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-VALIDATION.md
@.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-01-PLAN.md
@packages/contracts/src/domain/evals/retrieval.ts
@packages/contracts/src/domain/retrieval.ts
@packages/contracts/src/index.ts
@packages/contracts/src/index.test.ts
@evals/retrieval/README.md
@evals/retrieval/datasets/core/v1-retrieval-core.ts
@evals/retrieval/datasets/core/v2-retrieval-core.ts
@packages/server/src/routes/retrieval.test.ts
@packages/server/src/lib/retrieval.test.ts

<interfaces>
Plan `26-01` delivers execution, normalization, and metric primitives. Consume those directly; do not rebuild a second runner model here.
Any machine-readable report added in this plan should validate through `packages/contracts` so future CLI or automation consumers can rely on one contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluate governance and expectation failures as first-class verdicts</name>
  <files>evals/retrieval/run.ts, evals/retrieval/lib/assertions.ts, evals/retrieval/lib/types.ts, evals/retrieval/**/*.test.ts</files>
  <read_first>
    @packages/contracts/src/domain/evals/retrieval.ts
    @packages/server/src/lib/retrieval.test.ts
    @packages/server/src/routes/retrieval.test.ts
    @evals/retrieval/datasets/core/v1-retrieval-core.ts
    @evals/retrieval/datasets/core/v2-retrieval-core.ts
    @.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: Forbidden-result leakage, unexpected emptiness, and scope/shape mismatches fail clearly at the case level.
    - Test 2: Governance verdicts remain separate from ranking metric summaries.
    - Test 3: Adapter warnings and fallback usage propagate into case results rather than disappearing from output.
  </behavior>
  <action>Implement a dedicated assertion layer that consumes normalized execution results and the authored expected contract. Evaluate forbidden IDs, expected empty/non-empty outcome, bucket/profile-hint shape checks, and any scope-distribution mismatches needed by the current datasets. Record governance verdicts separately from ranking metrics so the runner can fail a case with good ranking but bad policy behavior. Update the runner’s exit behavior so failing governance assertions produce a non-zero exit code while still emitting complete diagnostics.</action>
  <verify>
    <automated>pnpm exec vitest run evals/retrieval/**/*.test.ts packages/server/src/lib/retrieval.test.ts packages/server/src/lib/retrieval/assembly.test.ts</automated>
  </verify>
  <done>The evaluator produces explicit case-level governance verdicts and fails clearly for leakage, shape, or empty-result expectation mismatches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build canonical JSON and terminal reports with slice summaries</name>
  <files>evals/retrieval/README.md, evals/retrieval/lib/report.ts, evals/retrieval/lib/format.ts, evals/retrieval/lib/types.ts, packages/contracts/src/domain/evals/report.ts, packages/contracts/src/index.ts, packages/contracts/src/index.test.ts, evals/retrieval/**/*.test.ts</files>
  <read_first>
    @package.json
    @packages/contracts/src/index.ts
    @packages/contracts/src/index.test.ts
    @evals/retrieval/run.ts
    @.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-RESEARCH.md
    @.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-01-PLAN.md
  </read_first>
  <behavior>
    - Test 1: The evaluator can emit a validated JSON report suitable for regression tooling.
    - Test 2: The terminal formatter shows per-slice metrics, case failures, and adapter warnings in a predictable order.
    - Test 3: JSON and terminal output are generated from one canonical report structure rather than two divergent code paths.
  </behavior>
  <action>Add a canonical retrieval-eval report schema in `packages/contracts` and build report/format modules on top of the case results from Plan 26-01 plus Task 1. Aggregate metrics and verdicts by endpoint, tier, and mode, and include explicit failure records for governance mismatches, empty-result mismatches, and execution warnings. Extend the runner CLI to support machine-readable output (for example `--json`) alongside terminal-readable summaries, but ensure both views are derived from the same in-memory report object. Document the report contract and maintainer workflow in `evals/retrieval/README.md`.</action>
  <verify>
    <automated>pnpm exec vitest run packages/contracts/src/index.test.ts evals/retrieval/**/*.test.ts</automated>
  </verify>
  <done>The evaluator emits validated JSON and readable terminal reports with stable per-slice summaries and explicit governance failure diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalized execution results -> governance assertions | Policy failures must not be downgraded into metric noise |
| Canonical report object -> JSON / terminal output | Two presentation paths must not diverge and tell different stories |
| Compatibility warnings -> maintainer review | Fallbacks and adapter defects must stay visible so regressions are actionable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | I | governance assertion layer | mitigate | Evaluate forbidden IDs, expected emptiness, and endpoint-specific shapes in a dedicated pass that emits explicit failures. |
| T-26-06 | R | report generation | mitigate | Build JSON and terminal output from one canonical report object validated through shared contracts. |
| T-26-07 | S | regression output | mitigate | Sort slices, case IDs, and failure records deterministically so tooling and humans see stable comparisons. |
| T-26-08 | E | runner exit handling | mitigate | Exit non-zero when any governance or execution failure occurs, even if metric output still exists. |
</threat_model>

<verification>
Run the retrieval-eval tests plus contract tests. Confirm governance failures are explicit, JSON output validates through shared contracts, and the terminal summary surfaces slice metrics and warnings without hiding failures.
</verification>

<success_criteria>
Phase 26-02 completes the retrieval evaluator: governance failures are first-class verdicts, slice summaries are stable and actionable, and maintainers can consume either terminal or JSON output from one canonical report structure.
</success_criteria>

<output>
After completion, create `.planning/phases/26-retrieval-metrics-runner-and-governance-checks/26-02-SUMMARY.md`
</output>
