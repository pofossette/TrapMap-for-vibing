---
phase: 30-fixture-trace
plan: 02
type: execute
wave: 1
depends_on:
  - phase: 29
    status: complete
files_modified:
  - packages/contracts/src/domain/retrieval.ts
  - packages/server/src/lib/retrieval/orchestrator.ts
  - packages/server/src/lib/retrieval/summary.ts
autonomous: true
requirements:
  - EOPS-01
must_haves:
  truths:
    - "v2 summary must be wired through buildCapsuleSummary when includeSummary is true."
    - "Citations must be derived from already-governed CapsuleMatch records to preserve filtering guarantees."
    - "The includeSummary field on v2 query schema must be backward-compatible with default false."
  artifacts:
    - path: "packages/contracts/src/domain/retrieval.ts"
      provides: "v2 query schema with includeSummary"
      contains: "retrievalV2QuerySchema with includeSummary: z.boolean().default(false)"
    - path: "packages/server/src/lib/retrieval/summary.ts"
      provides: "capsule-to-citation conversion"
      contains: "buildCapsuleCitations function"
    - path: "packages/server/src/lib/retrieval/orchestrator.ts"
      provides: "v2 summary integration"
      contains: "buildCapsuleSummary call in searchKnowledgeV2"
  key_links:
    - from: "packages/server/src/lib/retrieval/summary.ts"
      to: "packages/server/src/lib/retrieval/orchestrator.ts"
      via: "buildCapsuleSummary, buildCapsuleCitations"
      pattern: "capsules, citations"
---

<objective>
Wire buildCapsuleSummary into the v2 retrieval pipeline so /v2/retrieval/search produces real summaries when includeSummary is true, completing the v2 path for summary evaluation.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-fixture-trace/30-CONTEXT.md
@.planning/phases/30-fixture-trace/30-PATTERNS.md
@packages/contracts/src/domain/retrieval.ts
@packages/server/src/lib/retrieval/orchestrator.ts
@packages/server/src/lib/retrieval/summary.ts
</context>

<tasks>

<task type="execute">
  <name>Task 30-02-01: Add includeSummary to v2 query schema</name>
  <files>packages/contracts/src/domain/retrieval.ts</files>
  <read_first>
    @packages/contracts/src/domain/retrieval.ts
    @.planning/phases/30-fixture-trace/30-PATTERNS.md
  </read_first>
  <action>
    Add `includeSummary` field to `retrievalV2QuerySchema` in `packages/contracts/src/domain/retrieval.ts`:

    The schema currently has: `seed`, `filters`, `maxResults`
    Add: `includeSummary: z.boolean().default(false)`

    This is additive and backward-compatible -- existing callers that don't send includeSummary get false by default. The v1 schema already has this field at line 59; v2 needs the same addition.
  </action>
  <acceptance_criteria>
    - `grep -c "includeSummary" packages/contracts/src/domain/retrieval.ts` returns at least 2 (v1 and v2 schemas)
    - `retrievalV2QuerySchema` includes `includeSummary` field
    - `pnpm -r test` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r test</automated>
  </verify>
  <done>
    v2 query schema accepts optional includeSummary field with default false, matching v1 contract.
  </done>
</task>

<task type="execute">
  <name>Task 30-02-02: Create capsule-to-citation helper and wire v2 summary in orchestrator</name>
  <files>packages/server/src/lib/retrieval/summary.ts, packages/server/src/lib/retrieval/orchestrator.ts</files>
  <read_first>
    @packages/server/src/lib/retrieval/orchestrator.ts
    @packages/server/src/lib/retrieval/summary.ts
    @packages/contracts/src/domain/retrieval.ts
    @.planning/phases/30-fixture-trace/30-PATTERNS.md
  </read_first>
  <action>
    **Part A: Create buildCapsuleCitations in summary.ts**

    Add a new exported function that converts CapsuleMatch[] to RetrievalCitation[]:
    - Import CapsuleMatch and RetrievalCitation types
    - Map each capsule to a citation with:
      - source.entryId: capsule.capsuleId
      - source.scope: capsule.scope
      - source.shortcut: capsule.situation (closest analog)
      - snippet: capsule.content
      - tags: capsule.labels
      - recallChannels: ['semantic'] (safe fallback since capsule channel may not be in enum)
      - scores: { semantic: null, keyword: null, graph: null, preRerank: capsule.score ?? 0, final: capsule.score ?? 0 }

    **Part B: Wire summary in orchestrator.ts**

    In searchKnowledgeV2 function (around line 751-882):
    1. Import buildCapsuleSummary and buildCapsuleCitations from './summary.js'
    2. After building capsules (around line 841), before buildV2RetrievalResponse (line 855):
       - Check if `parsed.includeSummary` is true
       - If true and capsules.length > 0:
         - Call `buildCapsuleCitations(capsules)` to get citations array
         - Call `buildCapsuleSummary({ query: parsed.seed, includeSummary: true, capsules, citations })`
         - Store in local variable `v2Summary`
       - Else: `v2Summary = null`
    3. Replace the hardcoded `null` in buildV2RetrievalResponse call with `v2Summary`
    4. Update RAG log metadata `includeSummary` field from hardcoded `false` to `parsed.includeSummary ?? false`
  </action>
  <acceptance_criteria>
    - `grep -n "buildCapsuleSummary" packages/server/src/lib/retrieval/orchestrator.ts` returns at least 1 match
    - `grep -n "buildCapsuleCitations" packages/server/src/lib/retrieval/summary.ts` returns at least 1 match
    - `grep -n "buildV2RetrievalResponse(capsules, profileHints, null" packages/server/src/lib/retrieval/orchestrator.ts` returns 0 matches (null replaced)
    - `pnpm -r test` exits with code 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r test</automated>
  </verify>
  <done>
    v2 retrieval path builds and returns summaries when includeSummary=true, using governed capsule data.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Governed capsules -> summary builder | buildCapsuleSummary must only receive already-filtered CapsuleMatch records |
| v2 query -> summary generation | includeSummary must default false to avoid surprising behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-01 | I | summary bypass | mitigate | buildCapsuleSummary receives only governed CapsuleMatch[] from pipeline |
| T-30-02-02 | D | citation schema | mitigate | Use safe recallChannels fallback that matches Zod enum |
| T-30-02-03 | T | default behavior | mitigate | includeSummary defaults false, preserving existing v2 behavior |
</threat_model>

<verification>
1. v2 query schema accepts includeSummary field
2. v2 response includes non-null summary when includeSummary=true and capsules exist
3. Existing v2 calls without includeSummary still return null summary (backward compatible)
4. All tests pass: `pnpm -r test`
</verification>

<success_criteria>
v2 retrieval endpoint produces real summaries when requested, enabling summary evaluation on the v2 path.
</success_criteria>
