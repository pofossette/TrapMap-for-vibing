---
phase: 46-verification-backfill-platform-phases-ci-fix
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/VERIFICATION.md
  - .github/workflows/eval.yml
autonomous: true
requirements: [EOPS-02]
must_haves:
  truths:
    - "Phase 43 completed the Drizzle/PostgreSQL migration with PostgresStore, SkillShareerStore interface, and createSkillShareerStore factory"
    - "The eval.yml PR comment step references output variables that require a step id to access"
    - "EOPS-02 requires CI integration for regression tracking, which depends on correct workflow output variable wiring"
  artifacts:
    - path: ".planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/VERIFICATION.md"
      provides: "Goal-achievement verification for Phase 43 database migration"
    - path: ".github/workflows/eval.yml"
      provides: "Fixed output variable integration for PR comments"
---

<objective>
Backfill VERIFICATION.md for Phase 43 (Drizzle/PostgreSQL migration) and fix the GitHub Actions eval.yml output variable integration gap where the PR comment step references `steps.eval.outputs.*` but the "Run smoke evaluation" step lacks the required `id: eval`.

Purpose: Complete the verification artifact trail for the platform database migration and restore CI PR comment functionality.
</objective>

<tasks>
  <task>
    <name>Task 1: Create VERIFICATION.md for Phase 43 database migration</name>
    <read_first>
      - .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/43-01-SUMMARY.md
      - .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/43-02-SUMMARY.md
      - .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/43-03-SUMMARY.md
      - .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/43-CONTEXT.md
      - packages/server/src/lib/persistence/schema.ts
      - packages/server/src/lib/persistence/postgres-store.ts
      - packages/server/src/lib/persistence/create-store.ts
      - packages/server/src/lib/store.test.ts
      - .planning/phases/44-verification-backfill-evaluation-phases/VERIFICATION.md
    </read_first>
    <files>.planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/VERIFICATION.md</files>
    <action>
      Create a VERIFICATION.md for Phase 43 following the established pattern from Phase 44/45 VERIFICATION.md files. Document:
      1. Executive summary confirming phase completion
      2. Must-have verification table for each of the 3 plans
      3. Codebase artifact verification (schema.ts, postgres-store.ts, create-store.ts, store.test.ts)
      4. Test coverage verification (18 store contract tests for both implementations)
      5. Key truths preserved (SkillShareerStore interface, runtime selection, JSONB snapshot pattern)
      Use concrete file paths and line references as evidence.
    </action>
    <acceptance_criteria>
      - VERIFICATION.md exists at `.planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/VERIFICATION.md`
      - Document includes frontmatter with `phase`, `verified`, `status`, `verifier` fields
      - All three plan summaries (43-01, 43-02, 43-03) have must-have verification entries
      - Evidence includes concrete file paths: `packages/server/src/lib/persistence/schema.ts`, `postgres-store.ts`, `create-store.ts`, `store.test.ts`
      - Document confirms `PostgresStore` implements `SkillShareerStore` interface with JSONB snapshot pattern
      - Document confirms `createSkillShareerStore` factory provides runtime selection based on `TRAPMAP_DATABASE_URL`
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Fix eval.yml output variable integration gap</name>
    <read_first>
      - .github/workflows/eval.yml
      - evals/scripts/eval-ci.ts
    </read_first>
    <files>.github/workflows/eval.yml</files>
    <action>
      Add `id: eval` to the "Run smoke evaluation" step (currently around line 49) so that the "Comment regression summary on PR" step can correctly reference `${{ steps.eval.outputs.has_regressions }}`, `${{ steps.eval.outputs.regressed_count }}`, and `${{ steps.eval.outputs.improved_count }}`. The eval-ci.ts script already sets these outputs via `setGitHubOutput()` but the workflow step lacks the id needed to reference them.
    </action>
    <acceptance_criteria>
      - The "Run smoke evaluation" step has `id: eval` added
      - The "Comment regression summary on PR" step correctly references `steps.eval.outputs.has_regressions`, `steps.eval.outputs.regressed_count`, `steps.eval.outputs.improved_count`
      - No other changes to the workflow structure
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. Verify VERIFICATION.md exists: `ls -la .planning/phases/43-migrate-store-and-indexing-persistence-to-database-backed-li/VERIFICATION.md`
  2. Verify eval.yml has step id: `grep -n "id: eval" .github/workflows/eval.yml`
  3. Verify output variable references match: `grep -n "steps.eval.outputs" .github/workflows/eval.yml`
</verification>
