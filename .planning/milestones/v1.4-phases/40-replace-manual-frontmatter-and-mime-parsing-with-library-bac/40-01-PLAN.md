---
phase: 40
plan: 01
type: implementation
wave: 1
depends_on: []
files_modified:
  - packages/contracts/package.json
  - packages/contracts/src/domain/parsing.ts
  - packages/contracts/src/domain/parsing.test.ts
  - packages/contracts/src/index.ts
  - pnpm-lock.yaml
autonomous: true
requirements: [P40-01, P40-02]
must_haves:
  truths:
    - "Frontmatter parsing and MIME detection move to one shared library-backed utility instead of separate hand-rolled implementations"
    - "The shared utility preserves stable text/code MIME overrides for repository artifacts while using maintained libraries underneath"
  artifacts:
    - path: "packages/contracts/src/domain/parsing.ts"
      provides: "Shared gray-matter and mime-types wrappers for SKILL.md metadata and media-type detection"
    - path: "packages/contracts/src/domain/parsing.test.ts"
      provides: "Regression coverage for quoted YAML, label lists, and MIME fallback behavior"
---

<objective>
Create the shared parsing surface for Phase 40.

Purpose: replace brittle ad hoc frontmatter and MIME helpers with thin, reusable wrappers that both the CLI and server can import without changing contract boundaries.
Output: dependency additions plus one shared parsing module with focused regression tests.
</objective>

<tasks>
  <task>
    <name>Task 1: Add shared library-backed parsing helpers</name>
    <files>packages/contracts/package.json, packages/contracts/src/domain/parsing.ts, packages/contracts/src/index.ts, pnpm-lock.yaml</files>
    <action>
      Add `gray-matter` and `mime-types` to `@trapmap/contracts`, expose a shared parsing module, and implement helpers for SKILL frontmatter normalization, MIME lookup, and text-like media detection.
    </action>
    <acceptance_criteria>
      - one shared helper parses SKILL metadata for both CLI and server use
      - MIME detection uses maintained library lookup with stable project-specific overrides
      - the new helper is exported through `@trapmap/contracts`
    </acceptance_criteria>
  </task>
  <task>
    <name>Task 2: Add parsing utility regression coverage</name>
    <files>packages/contracts/src/domain/parsing.test.ts</files>
    <action>
      Cover quoted YAML, list labels, title-only frontmatter, unknown extensions, and text-like MIME behavior so downstream packages can rely on the new shared helper.
    </action>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/contracts test -- src/domain/parsing.test.ts`
  2. `pnpm --filter @trapmap/contracts typecheck`
</verification>
