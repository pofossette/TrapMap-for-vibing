---
phase: 40
plan: 02
type: implementation
wave: 1
depends_on: [40-01]
files_modified:
  - packages/cli/src/commands/operations.ts
  - packages/cli/src/commands/operations.test.ts
  - packages/server/src/lib/import-export.ts
  - packages/server/src/lib/artifacts/derive.ts
  - packages/server/src/routes/operations.test.ts
autonomous: true
requirements: [P40-03, P40-04]
must_haves:
  truths:
    - "CLI import/export flows and server legacy import paths consume the shared parser instead of maintaining their own string-splitting logic"
    - "Derivation frontmatter reads stay behavior-compatible while gaining support for the shared parsed labels/title surface"
  artifacts:
    - path: "packages/cli/src/commands/operations.ts"
      provides: "CLI import/export integration with shared parsing and text-like MIME handling"
    - path: "packages/server/src/lib/import-export.ts"
      provides: "Server legacy SKILL import parsing routed through the shared helper"
    - path: "packages/server/src/lib/artifacts/derive.ts"
      provides: "Derivation frontmatter reads backed by the shared parser"
---

<objective>
Apply the new shared helpers across the live CLI and server callsites.

Purpose: remove duplicate parsing logic where TrapMap currently imports, derives, and classifies skill artifacts while preserving legacy behavior and adding regression coverage for the migrated paths.
Output: switched CLI/server callsites and green regression tests.
</objective>

<tasks>
  <task>
    <name>Task 1: Replace CLI parsing and MIME logic</name>
    <files>packages/cli/src/commands/operations.ts, packages/cli/src/commands/operations.test.ts</files>
    <action>
      Route single-file imports, directory bundle metadata extraction, and file text/binary decisions through the shared contracts helper. Extend CLI tests for YAML list labels in `SKILL.md`.
    </action>
  </task>
  <task>
    <name>Task 2: Replace server parsing callsites</name>
    <files>packages/server/src/lib/import-export.ts, packages/server/src/lib/artifacts/derive.ts, packages/server/src/routes/operations.test.ts</files>
    <action>
      Swap legacy server import parsing and derivation frontmatter reads to the shared helper, then cover quoted YAML values in the operations route regression tests.
    </action>
    <acceptance_criteria>
      - no duplicated frontmatter parsing remains in the targeted CLI/server callsites
      - legacy import behavior still requires SKILL frontmatter with `name`
      - route and command regression tests pass after the migration
    </acceptance_criteria>
  </task>
</tasks>

<verification>
  1. `pnpm --filter @trapmap/cli test -- src/commands/operations.test.ts`
  2. `pnpm --filter @trapmap/server test -- src/routes/operations.test.ts`
  3. `pnpm --filter @trapmap/cli typecheck`
  4. `pnpm --filter @trapmap/server typecheck`
</verification>
