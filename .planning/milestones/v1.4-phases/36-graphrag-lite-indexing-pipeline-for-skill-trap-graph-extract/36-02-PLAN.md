---
phase: 36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - packages/server/src/lib/retrieval/graph-extract.ts
  - packages/server/src/lib/indexing/adapters/graph.ts
  - packages/server/src/lib/indexing/adapters/index.ts
  - packages/server/src/lib/indexing/adapters/graph-builders.ts
  - packages/server/src/lib/retrieval/recall/graph-assisted.ts
  - packages/server/src/lib/indexing/adapters/graph.test.ts
  - packages/server/src/lib/indexing/pipeline.test.ts
  - packages/server/src/lib/retrieval/recall/graph-assisted.test.ts
autonomous: true
requirements:
  - P36-01
must_haves:
  truths:
    - "Approved trap revisions produce durable graph documents instead of process-local cache state."
    - "Trap graph extraction emits only the bounded TrapMap relation vocabulary needed for later compilation."
    - "Graph-assisted recall can read persisted graph documents after restart."
  artifacts:
    - path: packages/server/src/lib/retrieval/graph-extract.ts
      provides: "TrapMap-specific deterministic trap extraction."
    - path: packages/server/src/lib/indexing/adapters/graph-builders.ts
      provides: "Pure trap graph-document candidate builder for adapter sync and reconciliation."
    - path: packages/server/src/lib/indexing/adapters/graph.ts
      provides: "Store-backed graph adapter implementation."
    - path: packages/server/src/lib/indexing/adapters/index.ts
      provides: "Default adapter registration including graph."
  key_links:
    - from: packages/server/src/lib/indexing/adapters/graph.ts
      to: packages/server/src/lib/indexing/graph-lite/store.ts
      via: "graph document upsert/remove helpers"
      pattern: "upsertGraphIndexDocument"
    - from: packages/server/src/lib/indexing/reconcile.ts
      to: packages/server/src/lib/indexing/adapters/graph-builders.ts
      via: "pre-persist trap graph candidate assembly"
      pattern: "buildTrapGraphDocument"
    - from: packages/server/src/lib/indexing/adapters/graph.ts
      to: packages/server/src/lib/indexing/graph-lite/graphology.ts
      via: "pre-persist hard-edge cycle validation"
      pattern: "assertNoHardDependencyCycles"
    - from: packages/server/src/lib/retrieval/recall/graph-assisted.ts
      to: packages/server/src/lib/indexing/adapters/graph.ts
      via: "durable graph document access"
      pattern: "getGraphIndexDocuments"
---

<objective>
Replace the old in-memory graph adapter path for traps with durable GraphRAG-lite extraction and retrieval-safe persistence.

Purpose: satisfy the trap-side indexing half of Phase 36 and make graph state survive process restarts while staying governance-scoped.
Output: TrapMap-specific trap extraction, store-backed graph adapter, graph adapter registration, and persistence-aware recall tests.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md
@packages/server/src/lib/retrieval/graph-extract.ts
@packages/server/src/lib/indexing/adapters/graph.ts
@packages/server/src/lib/indexing/adapters/index.ts
@packages/server/src/lib/retrieval/recall/graph-assisted.ts
@packages/server/src/lib/indexing/graph-lite/documents.ts
@packages/server/src/lib/indexing/graph-lite/store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace generic trap extraction with TrapMap-specific graph semantics</name>
  <files>packages/server/src/lib/retrieval/graph-extract.ts, packages/server/src/lib/indexing/adapters/graph-builders.ts, packages/server/src/lib/indexing/adapters/graph.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md, packages/server/src/lib/retrieval/graph-extract.ts, packages/server/src/lib/indexing/graph-lite/documents.ts, packages/server/src/lib/indexing/adapters/graph.test.ts</read_first>
  <behavior>
    - Test 1: approved trap text with trigger/mitigation wording yields node kinds from the exact set `trap`, `cue`, `tool`, `environment`, `prerequisite`, `mitigation`.
    - Test 2: extracted edges use only `mitigates`, `requires`, `order`, `risk-blocks`, or `co-occurs-with`.
    - Test 3: extractor evidence distinguishes hard vs soft edges so later DAG projection can ignore `order` and soft co-occurrence links.
    - Test 4: a pure `buildTrapGraphDocument(...)` helper can assemble a candidate graph document for an approved trap revision without persisting it, so reconciliation can validate additions before any upsert.
  </behavior>
  <action>Rewrite `packages/server/src/lib/retrieval/graph-extract.ts` around TrapMap-specific extraction for traps per D-01 and D-04. Define node records around the trap itself plus deterministic cue/tool/environment/prerequisite/mitigation pivots from `shortcut`, `detail`, and `labels`. Remove the old generic entity categories `service`, `symptom`, `root-cause`, `fix`, and relation categories `mentions`, `causes`, `fixed-by`, `observed-in`, `uses-tool`, `runs-in`. Emit only the locked relation set with explicit `strength` metadata: use `requires` or `risk-blocks` as `hard` when the text includes mandatory blockers such as `must`, `requires`, `blocked`, `before`, `depends on`; emit `order` and `co-occurs-with` as `soft`; emit `mitigates` as `hard` only when the mitigation is phrased as required to clear the trap, otherwise `soft`. In `packages/server/src/lib/indexing/adapters/graph-builders.ts`, add an exported pure helper `buildTrapGraphDocument(normalizedDocument)` that converts trap extraction output into a `GraphIndexDocumentRecord` candidate without persisting it, so both the graph adapter and reconciliation can validate candidate additions before any write.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/adapters/graph.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/retrieval/graph-extract.ts` contains the exact strings `'mitigates'`, `'requires'`, `'order'`, `'risk-blocks'`, and `'co-occurs-with'`.
    - `packages/server/src/lib/indexing/adapters/graph-builders.ts` exports `buildTrapGraphDocument`.
    - `packages/server/src/lib/retrieval/graph-extract.ts` does not contain the exact strings `'mentions'`, `'causes'`, `'fixed-by'`, `'observed-in'`, `'uses-tool'`, or `'runs-in'`.
    - `packages/server/src/lib/indexing/adapters/graph.test.ts` asserts node or relation output against the new TrapMap-specific vocabulary.
    - The automated test command passes.
  </acceptance_criteria>
  <done>The extractor yields only the bounded Phase 36 trap semantics, `buildTrapGraphDocument(...)` provides a reusable pre-persist candidate builder, and tests prove the old generic relation vocabulary is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist trap graph state through the graph adapter and recall path</name>
  <files>packages/server/src/lib/indexing/adapters/graph.ts, packages/server/src/lib/indexing/adapters/index.ts, packages/server/src/lib/retrieval/recall/graph-assisted.ts, packages/server/src/lib/indexing/pipeline.test.ts, packages/server/src/lib/retrieval/recall/graph-assisted.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, packages/server/src/lib/indexing/adapters/graph.ts, packages/server/src/lib/indexing/adapters/graph-builders.ts, packages/server/src/lib/indexing/adapters/index.ts, packages/server/src/lib/retrieval/recall/graph-assisted.ts, packages/server/src/lib/indexing/graph-lite/store.ts, packages/server/src/lib/indexing/graph-lite/graphology.ts, packages/server/src/lib/indexing/pipeline.test.ts</read_first>
  <behavior>
    - Test 1: graph adapter sync upserts a durable graph document for an approved trap revision and remove deletes it for the same source.
    - Test 2: `buildDefaultIndexAdapters()` includes the graph adapter so trap lifecycle sync uses vector, keyword, and graph adapters together.
    - Test 3: graph-assisted recall reads durable graph documents rather than module-level in-memory maps.
    - Test 4: trap sync rejects a candidate document that would introduce a hard `requires`/`risk-blocks` cycle and leaves the previously persisted graph state unchanged.
  </behavior>
  <action>Refactor `packages/server/src/lib/indexing/adapters/graph.ts` to use `getGraphIndexDocuments`, `upsertGraphIndexDocument`, and `removeGraphIndexDocumentsForSource` instead of module-global `Map` caches. Make the adapter call `buildTrapGraphDocument(normalizedDocument)` to produce one trap graph document candidate per approved revision with `sourceType: 'trap'`, `sourceId: document.entryId`, `revision: document.revision`, and governance fields copied from the normalized document per D-03. Before any trap document is persisted, load the existing graph documents excluding the current trap source/revision, append the candidate trap document, and call `assertNoHardDependencyCycles` so cyclic hard dependencies are rejected before `upsertGraphIndexDocument` runs. Keep adapter idempotency by comparing `contentHash` and `revision`. Update `packages/server/src/lib/indexing/adapters/index.ts` so `buildDefaultIndexAdapters()` returns `[vectorIndexAdapter, keywordIndexAdapter, graphIndexAdapter]`. Update `packages/server/src/lib/retrieval/recall/graph-assisted.ts` to assemble graph state from persisted graph documents instead of `getGlobalGraphIndex()` from process-local cache, preserving the existing caller-side governance intersection.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/adapters/graph.test.ts src/lib/indexing/pipeline.test.ts src/lib/retrieval/recall/graph-assisted.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/indexing/adapters/index.ts` contains the exact string `return [vectorIndexAdapter, keywordIndexAdapter, graphIndexAdapter];`.
    - `packages/server/src/lib/indexing/adapters/graph.ts` contains the exact strings `buildTrapGraphDocument`, `sourceType: 'trap'`, `upsertGraphIndexDocument`, and `assertNoHardDependencyCycles`.
    - `packages/server/src/lib/retrieval/recall/graph-assisted.ts` imports from `graph-lite/store` or `graph-lite/graphology` instead of relying only on module-level cache access.
    - The automated test command passes.
  </acceptance_criteria>
  <done>Trap approvals and updates write durable graph documents through the registered graph adapter only after hard-edge cycle validation passes, and graph-assisted recall reads persisted graph state rather than volatile process memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved trap document -> graph extractor | Authored trap text becomes graph nodes and relations |
| persisted graph docs -> recall candidate generation | Durable graph state influences retrieval candidate expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | T | `graph-extract.ts` | mitigate | Emit only the locked relation vocabulary and deterministic evidence fields so later compiler logic does not act on fuzzy generic edges. |
| T-36-06 | I | `graph.ts` | mitigate | Copy `teamId`, `scope`, `requiredLevel`, and `revision` into persisted graph documents so query-time expansion can respect source governance. |
| T-36-07 | S | `graph-assisted.ts` | mitigate | Preserve the existing eligible-entry intersection before scoring graph-derived candidates so durable graph docs cannot bypass authorization. |
| T-36-08 | D | `adapters/index.ts` + `graph.ts` | mitigate | Keep adapter sync idempotent on `contentHash` and `revision` to avoid repeated expensive graph rebuilds on unchanged content. |
</threat_model>

<verification>
Run `pnpm --filter @trapmap/server test -- src/lib/indexing/adapters/graph.test.ts src/lib/indexing/pipeline.test.ts src/lib/retrieval/recall/graph-assisted.test.ts`.
</verification>

<success_criteria>
Approved trap revisions are indexed through the graph adapter into durable graph documents, the graph adapter is part of the default indexing pipeline, and graph-assisted recall still works after process restart because it reads persisted state.
</success_criteria>

<output>
After completion, create `.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-02-SUMMARY.md`
</output>
