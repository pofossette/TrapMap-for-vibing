---
phase: 36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/package.json
  - pnpm-lock.yaml
  - packages/server/src/lib/store.ts
  - packages/server/src/lib/indexing/graph-lite/documents.ts
  - packages/server/src/lib/indexing/graph-lite/store.ts
  - packages/server/src/lib/indexing/graph-lite/graphology.ts
  - packages/server/src/lib/indexing/graph-lite/documents.test.ts
  - packages/server/src/lib/indexing/graph-lite/graphology.test.ts
autonomous: true
requirements:
  - P36-04
must_haves:
  truths:
    - "The server can persist durable GraphRAG-lite source documents for approved graph content."
    - "Hard dependency edges can be projected and cycle-checked without rebuilding ad hoc graph structures."
    - "Later plans can assemble bounded graph views from stored documents without indexing raw activation-only files."
  artifacts:
    - path: packages/server/src/lib/indexing/graph-lite/documents.ts
      provides: "Typed graph document builders for trap and skill sources."
    - path: packages/server/src/lib/indexing/graph-lite/store.ts
      provides: "Store-backed load/save/remove helpers for graph documents."
    - path: packages/server/src/lib/indexing/graph-lite/graphology.ts
      provides: "Graphology assembly, hard-edge projection, cycle validation, and bounded expansion helpers."
    - path: packages/server/src/lib/store.ts
      provides: "Durable graph document collection in StoreData."
  key_links:
    - from: packages/server/src/lib/indexing/graph-lite/store.ts
      to: packages/server/src/lib/store.ts
      via: "StoreData.graphIndexDocuments"
      pattern: "graphIndexDocuments"
    - from: packages/server/src/lib/indexing/graph-lite/graphology.ts
      to: packages/server/src/lib/indexing/graph-lite/documents.ts
      via: "graph document node/edge records"
      pattern: "GraphIndexDocumentRecord"
---

<objective>
Create the durable GraphRAG-lite document and graph utility foundation that all later Phase 36 indexing work builds on.

Purpose: establish store-backed graph persistence and graphology-based hard-edge validation before trap and skill lifecycle hooks start writing graph state.
Output: store schema additions, graph-lite document/store modules, graphology helpers, and focused tests.
</objective>

<execution_context>
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/workflows/execute-plan.md
@/home/wunai/project/TrapMap-for-vibing/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md
@.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-VALIDATION.md
@packages/server/package.json
@packages/server/src/lib/store.ts
@packages/server/src/lib/indexing/types.ts

<interfaces>
From packages/server/src/lib/indexing/types.ts:
```typescript
export interface IndexAdapter {
  kind: 'vector' | 'keyword' | 'graph';
  sync(document: NormalizedIndexDocument): Promise<IndexSyncResult>;
  remove(ref: { entryId: string; revision: number }): Promise<void>;
}
```

From packages/server/src/lib/store.ts:
```typescript
export interface StoreData {
  knowledgeEntries: KnowledgeRecord[];
  skillArtifacts: SkillArtifactRecord[];
  artifactFilePayloads: ArtifactFilePayloadRecord[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable graph document contracts and store persistence</name>
  <files>packages/server/package.json, pnpm-lock.yaml, packages/server/src/lib/store.ts, packages/server/src/lib/indexing/graph-lite/documents.ts, packages/server/src/lib/indexing/graph-lite/store.ts, packages/server/src/lib/indexing/graph-lite/documents.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-VALIDATION.md, packages/server/package.json, packages/server/src/lib/store.ts</read_first>
  <behavior>
    - Test 1: building a trap graph document writes `sourceType: 'trap'`, retains `sourceId`, `revision`, `teamId`, `scope`, `requiredLevel`, and stores only typed nodes/edges.
    - Test 2: building a skill graph document writes `sourceType: 'skill'`, retains `artifactId` lineage, and rejects activation-only body input by requiring caller-supplied derived text only.
    - Test 3: store helpers upsert by `{sourceType, sourceId, revision}` and remove stale documents for the same source without deleting unrelated sources.
  </behavior>
  <action>Install `graphology`, `graphology-dag`, `graphology-operators`, and `graphology-shortest-path` in `packages/server/package.json` per D-graphology. Add `graphIndexDocuments: GraphIndexDocumentRecord[]` to `StoreData` and `EMPTY_STORE` in `packages/server/src/lib/store.ts`. In `packages/server/src/lib/indexing/graph-lite/documents.ts`, define exact record types `GraphNodeKind = 'trap' | 'skill' | 'cue' | 'tool' | 'environment' | 'prerequisite' | 'mitigation'`, `GraphRelationType = 'mitigates' | 'requires' | 'order' | 'risk-blocks' | 'co-occurs-with'`, `GraphRelationStrength = 'hard' | 'soft'`, plus `GraphIndexDocumentRecord` keyed by `{id, sourceType, sourceId, revision, contentHash}` and carrying `teamId`, `scope`, `requiredLevel`, `nodes`, `edges`, `evidence`, and timestamps. In `packages/server/src/lib/indexing/graph-lite/store.ts`, add concrete helpers `upsertGraphIndexDocument`, `removeGraphIndexDocumentsForSource`, `getGraphIndexDocuments`, and `getGraphIndexDocumentsForSource`. Ensure the builders accept only caller-provided derived text and metadata; do not read `assets/` or `scripts/` payload bodies here per D-01/D-02.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/graph-lite/documents.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/store.ts` contains the exact string `graphIndexDocuments: GraphIndexDocumentRecord[];`.
    - `packages/server/src/lib/indexing/graph-lite/documents.ts` contains the exact string `type GraphRelationType = 'mitigates' | 'requires' | 'order' | 'risk-blocks' | 'co-occurs-with';`.
    - `packages/server/src/lib/indexing/graph-lite/store.ts` exports `upsertGraphIndexDocument`, `removeGraphIndexDocumentsForSource`, `getGraphIndexDocuments`, and `getGraphIndexDocumentsForSource`.
    - `packages/server/package.json` lists `graphology`, `graphology-dag`, `graphology-operators`, and `graphology-shortest-path` in `dependencies`.
  </acceptance_criteria>
  <done>Durable graph document records exist in the JSON store, dependency installation is declared, and document persistence tests prove deterministic upsert/remove behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add graphology assembly and hard-edge validation helpers</name>
  <files>packages/server/src/lib/indexing/graph-lite/graphology.ts, packages/server/src/lib/indexing/graph-lite/graphology.test.ts</files>
  <read_first>.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-CONTEXT.md, .planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-RESEARCH.md, packages/server/src/lib/indexing/graph-lite/documents.ts, packages/server/src/lib/indexing/graph-lite/store.ts</read_first>
  <behavior>
    - Test 1: assembling multiple graph documents yields stable graphology node and edge keys scoped to document node ids and relation ids.
    - Test 2: projecting only hard `requires` and hard `risk-blocks` edges surfaces cycles through `graphology-dag`.
    - Test 3: bounded expansion returns a subgraph limited to requested seed nodes and shortest-path reachable neighbors.
  </behavior>
  <action>Create `packages/server/src/lib/indexing/graph-lite/graphology.ts` with concrete helpers `buildGraphFromDocuments(documents)`, `projectHardDependencyGraph(documents)`, `assertNoHardDependencyCycles(documents)`, and `buildLocalExpansionView({ documents, seedNodeIds, maxDepth })`. Use `Graph` from `graphology`, `hasCycle` and `topologicalGenerations` from `graphology-dag`, `subgraph` from `graphology-operators`, and `singleSourceLength` or equivalent helper from `graphology-shortest-path`. Hard-edge projection must include only edges whose `strength === 'hard'` and whose `relationType` is `requires` or `risk-blocks`; `order` and `co-occurs-with` must stay out of the DAG projection per D-05 and the GraSP hardness distinction. Surface deterministic error text `hard dependency cycle detected` when a cycle exists.</action>
  <verify>
    <automated>pnpm --filter @trapmap/server test -- src/lib/indexing/graph-lite/graphology.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/server/src/lib/indexing/graph-lite/graphology.ts` exports `buildGraphFromDocuments`, `projectHardDependencyGraph`, `assertNoHardDependencyCycles`, and `buildLocalExpansionView`.
    - `packages/server/src/lib/indexing/graph-lite/graphology.ts` contains the exact string `hard dependency cycle detected`.
    - `packages/server/src/lib/indexing/graph-lite/graphology.ts` imports from `graphology`, `graphology-dag`, `graphology-operators`, and `graphology-shortest-path`.
    - The automated test command passes.
  </acceptance_criteria>
  <done>Graph documents can be assembled into graphology graphs, hard edges are validated separately from soft precedence edges, and bounded local expansion is available for later query-time compilation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact/trap text -> graph document builder | Untrusted authored text becomes durable graph state |
| JSON store -> graphology runtime | Persisted records become executable graph structure for later retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | T | `graph-lite/documents.ts` | mitigate | Persist only typed nodes/edges built from approved caller-supplied text, keep `sourceType`, `sourceId`, `revision`, `scope`, `teamId`, and `requiredLevel` on every document for auditability. |
| T-36-02 | I | `graph-lite/store.ts` | mitigate | Do not persist activation-only script or asset bodies; store only derived text, evidence snippets, and lineage metadata per D-01/D-02. |
| T-36-03 | D | `graph-lite/graphology.ts` | mitigate | Reject hard dependency cycles with deterministic validation before later compiler phases consume the graph. |
| T-36-04 | E | `store.ts` graph document collection | mitigate | Keep governance inheritance on every document record so later query expansion cannot widen team or security scope beyond the source artifact/entry. |
</threat_model>

<verification>
Run `pnpm --filter @trapmap/server test -- src/lib/indexing/graph-lite/documents.test.ts src/lib/indexing/graph-lite/graphology.test.ts`.
</verification>

<success_criteria>
The repo has a durable graph document collection, graphology helpers are available for hard-edge validation and local expansion, and all foundation tests pass without indexing any raw activation-only content.
</success_criteria>

<output>
After completion, create `.planning/phases/36-graphrag-lite-indexing-pipeline-for-skill-trap-graph-extract/36-01-SUMMARY.md`
</output>
