---
wave: 3
depends_on:
  - 83-02-PLAN.md
files_modified:
  - packages/server/src/lib/users/repository.ts
  - packages/server/src/lib/teams/repository.ts
  - packages/server/src/lib/context.ts
  - packages/server/src/app.ts
requirements: []
autonomous: true
---

# Phase 83-03: UserRepository + TeamRepository + MembershipRepository

## Objective

Create UserRepository, TeamRepository, and MembershipRepository interfaces with InMemory implementations, following the established pattern.

## Context

From 83-RESEARCH.md:
- UserRecord defined in store.ts (lines 25-31)
- TeamRecord defined in store.ts (lines 33-40)
- MembershipRecord defined in store.ts (lines 42-52)
- routes/teams.ts and routes/members.ts use store.transact() for CRUD operations
- Pattern established in 83-01 for auth repositories

## Tasks

<task id="83-03-T01">
<objective>Create UserRepository interface and InMemoryUserRepository</objective>
<read_first>
- packages/server/src/lib/store.ts (lines 25-31 for UserRecord type)
- packages/server/src/lib/auth/repository.ts (reference pattern from Wave 1)
- packages/server/src/routes/members.ts (user creation pattern at lines 32-40)
</read_first>
<action>
1. Create packages/server/src/lib/users/ directory if not exists
2. Create packages/server/src/lib/users/repository.ts with:
   - UserRepository interface with methods:
     - nextId(): Promise<string>
     - insert(user: UserRecord): Promise<void>
     - getById(userId: string): Promise<UserRecord | null>
     - getByHandle(handle: string): Promise<UserRecord | null>
     - update(userId: string, updates: Partial<UserRecord>): Promise<void>
   - InMemoryUserRepository class implementing UserRepository using SkillShareerStore
   - createUserRepository factory function
3. Create packages/server/src/lib/users/index.ts exporting repository types
</action>
<acceptance_criteria>
- File packages/server/src/lib/users/repository.ts exists
- UserRepository interface with 5 methods: nextId, insert, getById, getByHandle, update
- InMemoryUserRepository class implements all methods
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="83-03-T02">
<objective>Create TeamRepository interface and InMemoryTeamRepository</objective>
<read_first>
- packages/server/src/lib/store.ts (lines 33-40 for TeamRecord type)
- packages/server/src/lib/auth/repository.ts (reference pattern)
- packages/server/src/routes/teams.ts (team creation/listing patterns)
</read_first>
<action>
1. Create packages/server/src/lib/teams/ directory if not exists
2. Create packages/server/src/lib/teams/repository.ts with:
   - TeamRepository interface with methods:
     - nextId(): Promise<string>
     - insert(team: TeamRecord): Promise<void>
     - getById(teamId: string): Promise<TeamRecord | null>
     - getBySlug(slug: string): Promise<TeamRecord | null>
     - listAll(): Promise<TeamRecord[]>
     - update(teamId: string, updates: Partial<TeamRecord>): Promise<void>
   - InMemoryTeamRepository class implementing TeamRepository
   - createTeamRepository factory function
3. Create packages/server/src/lib/teams/index.ts exporting repository types
</action>
<acceptance_criteria>
- File packages/server/src/lib/teams/repository.ts exists
- TeamRepository interface with 6 methods: nextId, insert, getById, getBySlug, listAll, update
- InMemoryTeamRepository class implements all methods
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="83-03-T03">
<objective>Create MembershipRepository interface and InMemoryMembershipRepository</objective>
<read_first>
- packages/server/src/lib/store.ts (lines 42-52 for MembershipRecord type)
- packages/server/src/lib/auth/repository.ts (reference pattern)
- packages/server/src/routes/members.ts (membership operations at lines 42-54, 77-105)
</read_first>
<action>
1. Add to packages/server/src/lib/teams/repository.ts:
   - MembershipRepository interface with methods:
     - nextId(): Promise<string>
     - insert(membership: MembershipRecord): Promise<void>
     - getById(membershipId: string): Promise<MembershipRecord | null>
     - findByUserAndTeam(userId: string, teamId: string): Promise<MembershipRecord | null>
     - listByUser(userId: string): Promise<MembershipRecord[]>
     - listByTeam(teamId: string): Promise<MembershipRecord[]>
     - update(membershipId: string, updates: Partial<MembershipRecord>): Promise<void>
   - InMemoryMembershipRepository class implementing MembershipRepository
   - createMembershipRepository factory function
2. Export MembershipRepository from packages/server/src/lib/teams/index.ts
</action>
<acceptance_criteria>
- MembershipRepository interface exists with 7 methods
- InMemoryMembershipRepository class implements all methods
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="83-03-T04">
<objective>Register new repositories in SkillShareerServices context</objective>
<read_first>
- packages/server/src/lib/context.ts (current SkillShareerServices with sessionRepo, accessKeyRepo)
- packages/server/src/app.ts (repository initialization)
</read_first>
<action>
1. Update packages/server/src/lib/context.ts:
   - Add imports for UserRepository, TeamRepository, MembershipRepository
   - Add to SkillShareerServices interface:
     - userRepo: UserRepository | undefined
     - teamRepo: TeamRepository | undefined
     - membershipRepo: MembershipRepository | undefined
2. Update packages/server/src/app.ts:
   - Add imports for createUserRepository, createTeamRepository, createMembershipRepository
   - Initialize in onReady hook:
     - app.skillShareer.userRepo = createUserRepository({ pool, store })
     - app.skillShareer.teamRepo = createTeamRepository({ pool, store })
     - app.skillShareer.membershipRepo = createMembershipRepository({ pool, store })
</action>
<acceptance_criteria>
- SkillShareerServices contains userRepo, teamRepo, membershipRepo fields
- app.ts initializes all new repositories
- All existing tests pass (pnpm test)
- TypeScript compiles without errors
</acceptance_criteria>
</task>

## Verification

```bash
# TypeScript compilation
pnpm tsc --noEmit

# Run all tests
pnpm test

# Verify interfaces exist
grep -n "export interface UserRepository" packages/server/src/lib/users/repository.ts
grep -n "export interface TeamRepository" packages/server/src/lib/teams/repository.ts
grep -n "export interface MembershipRepository" packages/server/src/lib/teams/repository.ts

# Verify context has all repos
grep -n "userRepo\|teamRepo\|membershipRepo" packages/server/src/lib/context.ts
```

## must_haves

- UserRepository interface with nextId, insert, getById, getByHandle, update methods
- TeamRepository interface with nextId, insert, getById, getBySlug, listAll, update methods
- MembershipRepository interface with 7 methods including findByUserAndTeam, listByUser, listByTeam
- All repositories registered in SkillShareerServices context
- All existing tests pass

## truths

- Repository pattern matches KnowledgeRepository and SessionRepository structure
- InMemory implementations use store.transact() for writes
- Factory pattern supports optional PostgreSQL pool
- Repositories are optional (undefined when not initialized)
