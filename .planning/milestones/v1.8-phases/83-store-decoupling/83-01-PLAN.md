---
wave: 1
depends_on: []
files_modified:
  - packages/server/src/lib/auth/repository.ts
  - packages/server/src/lib/auth/index.ts
  - packages/server/src/lib/context.ts
  - packages/server/src/app.ts
requirements: []
autonomous: true
---

# Phase 83-01: SessionRepository + AccessKeyRepository Interfaces

## Objective

Create SessionRepository and AccessKeyRepository interfaces with InMemory implementations, following the existing KnowledgeRepository pattern from Phase 62.

## Context

From 83-RESEARCH.md:
- Repository pattern already exists for Knowledge, Artifacts, Candidates (Phases 61-63)
- SessionRecord and AccessKeyRecord types defined in store.ts (lines 68-77, 54-66)
- Current auth.ts uses `store.transact()` for session/accessKey operations
- Pattern: Interface + InMemory implementation + optional Pg implementation + DualWrite wrapper

## Tasks

<task id="83-01-T01">
<objective>Create SessionRepository interface and InMemorySessionRepository</objective>
<read_first>
- packages/server/src/lib/store.ts (lines 68-77 for SessionRecord type)
- packages/server/src/lib/knowledge/repository.ts (reference pattern for interface + InMemory)
- packages/server/src/lib/session.ts (current session operations: createSession, deleteSession, resolveAuthContext)
</read_first>
<action>
1. Create packages/server/src/lib/auth/ directory if not exists
2. Create packages/server/src/lib/auth/repository.ts with:
   - SessionRepository interface with methods:
     - nextId(): Promise<string>
     - create(session: Omit<SessionRecord, 'id' | 'createdAt' | 'updatedAt'>): Promise<SessionRecord>
     - getByTokenHash(tokenHash: string): Promise<SessionRecord | null>
     - deleteByTokenHash(tokenHash: string): Promise<void>
     - updateActiveTeam(sessionId: string, teamId: string | null): Promise<SessionRecord>
   - InMemorySessionRepository class implementing SessionRepository using SkillShareerStore
   - createSessionRepository factory function (mirrors createKnowledgeRepository pattern)
3. Export SessionRepository interface and createSessionRepository from packages/server/src/lib/auth/index.ts
</action>
<acceptance_criteria>
- File packages/server/src/lib/auth/repository.ts exists
- SessionRepository interface exported with 5 methods: nextId, create, getByTokenHash, deleteByTokenHash, updateActiveTeam
- InMemorySessionRepository class implements all SessionRepository methods
- TypeScript compiles without errors (pnpm tsc --noEmit)
</acceptance_criteria>
</task>

<task id="83-01-T02">
<objective>Create AccessKeyRepository interface and InMemoryAccessKeyRepository</objective>
<read_first>
- packages/server/src/lib/store.ts (lines 54-66 for AccessKeyRecord type)
- packages/server/src/lib/knowledge/repository.ts (reference pattern)
- packages/server/src/lib/session.ts (findAccessKeyByToken function at line 314)
</read_first>
<action>
1. Add to packages/server/src/lib/auth/repository.ts:
   - AccessKeyRepository interface with methods:
     - insert(key: AccessKeyRecord): Promise<void>
     - getByTokenHash(tokenHash: string): Promise<AccessKeyRecord | null>
     - getById(keyId: string): Promise<AccessKeyRecord | null>
     - revoke(keyId: string): Promise<void>
     - listByMember(memberId: string): Promise<AccessKeyRecord[]>
   - InMemoryAccessKeyRepository class implementing AccessKeyRepository using SkillShareerStore
2. Export AccessKeyRepository from packages/server/src/lib/auth/index.ts
</action>
<acceptance_criteria>
- AccessKeyRepository interface exists in packages/server/src/lib/auth/repository.ts
- AccessKeyRepository has 5 methods: insert, getByTokenHash, getById, revoke, listByMember
- InMemoryAccessKeyRepository class implements all AccessKeyRepository methods
- TypeScript compiles without errors
</acceptance_criteria>
</task>

<task id="83-01-T03">
<objective>Register repositories in SkillShareerServices context</objective>
<read_first>
- packages/server/src/lib/context.ts (current SkillShareerServices interface)
- packages/server/src/app.ts (repository initialization pattern around line with knowledgeRepo)
</read_first>
<action>
1. Update packages/server/src/lib/context.ts:
   - Add import for SessionRepository and AccessKeyRepository from './auth/index.js'
   - Add to SkillShareerServices interface:
     - sessionRepo: SessionRepository | undefined
     - accessKeyRepo: AccessKeyRepository | undefined
2. Update packages/server/src/app.ts:
   - Add import for createSessionRepository, createAccessKeyRepository from './lib/auth/index.js'
   - In onReady hook, after knowledgeRepo/artifactRepo initialization, add:
     - app.skillShareer.sessionRepo = createSessionRepository({ pool, store })
     - app.skillShareer.accessKeyRepo = createAccessKeyRepository({ pool, store })
</action>
<acceptance_criteria>
- SkillShareerServices interface in context.ts contains sessionRepo and accessKeyRepo fields
- app.ts initializes sessionRepo and accessKeyRepo in onReady hook
- TypeScript compiles without errors
- All existing tests pass (pnpm test)
</acceptance_criteria>
</task>

## Verification

```bash
# TypeScript compilation
pnpm tsc --noEmit

# Run all tests
pnpm test

# Verify interface exists
grep -n "export interface SessionRepository" packages/server/src/lib/auth/repository.ts
grep -n "export interface AccessKeyRepository" packages/server/src/lib/auth/repository.ts
```

## must_haves

- SessionRepository interface defined in lib/auth/repository.ts with nextId, create, getByTokenHash, deleteByTokenHash, updateActiveTeam methods
- AccessKeyRepository interface defined in lib/auth/repository.ts with insert, getByTokenHash, getById, revoke, listByMember methods
- InMemorySessionRepository and InMemoryAccessKeyRepository classes exist
- sessionRepo and accessKeyRepo fields added to SkillShareerServices in context.ts
- All existing tests pass

## truths

- Repository pattern matches existing KnowledgeRepository structure (interface + InMemory + factory)
- InMemory implementations use store.transact() for writes and store.snapshot() for reads
- Factory pattern supports optional PostgreSQL pool for future Pg implementations
- Backward compatibility maintained (repositories are optional in context)
