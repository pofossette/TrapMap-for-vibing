---
context: default
status: in_progress
last_updated: 2026-04-29T07:26:57.005Z
---

# CI Evaluation Fix Session

## Current State

Working on fixing local CI evaluation flow (`pnpm eval:ci`). Multiple fixes applied but tests still failing.

**Test Results**: Retrieval 5/9 | Summary 1/3 | **Total 6/12 passed**

## Completed Work

1. ✅ Created `evals/tsconfig.json` - tsx can now resolve `@trapmap/contracts`
2. ✅ Fixed `runner-api.ts` - added `seedScenarioFixtures` import and call
3. ✅ Isolated ExecutionContext per test case to prevent data bleeding
4. ✅ Fixed capsule validation in `adapters.ts`:
   - `sourcePaths: ['mock-source.md']` (was empty array)
   - `errorText: ''` (was null)

## Remaining Work

1. **Fix test data isolation** - Empty tests get data from previous tests despite isolated ExecutionContext
2. **Fix v2-capsule-positive-smoke** - profileHints not returning expected artifact ID
3. **Fix summary evaluation** - groundedness/coverage scores below threshold

## Key Files Modified

- `evals/tsconfig.json` (new)
- `evals/retrieval/lib/runner-api.ts`
- `evals/retrieval/lib/adapters.ts`

## Blockers

- Test data accumulates between test cases - `createExecutionContext` may not be creating truly isolated data files

## Next Action

```bash
pnpm exec tsx --tsconfig tsconfig.base.json evals/scripts/eval-ci.ts
```

Then investigate `createExecutionContext` dataFile parameter to ensure true isolation.

## Context

The CI workflow (`.github/workflows/eval.yml`) runs `pnpm eval:ci` which executes smoke-tier evaluation tests. Tests cover:
- v1 retrieval (bucketed)
- v2 retrieval (capsule-first)
- v3 retrieval (graph-plan)
- Summary evaluation (groundedness/coverage)
