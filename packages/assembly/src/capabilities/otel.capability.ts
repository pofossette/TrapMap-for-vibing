// @ts-nocheck
import { defineNode } from '../define-node.js';
export const otelCapability = defineNode({
  id: 'otel',
  apply: (ctx) => {
    // otel capability placeholder for P2 assembly
  },
});
