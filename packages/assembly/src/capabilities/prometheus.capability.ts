// @ts-nocheck
import { defineNode } from '../define-node.js';
export const prometheusCapability = defineNode({
  id: 'prometheus',
  apply: (_ctx) => {
    // prometheus capability placeholder for P2 assembly
  },
});
