CREATE TABLE IF NOT EXISTS "task_queue" ("id" text PRIMARY KEY, "type" text NOT NULL, "payload" text NOT NULL, "status" text NOT NULL DEFAULT 'pending', "priority" integer NOT NULL DEFAULT 0, "attempts" integer NOT NULL DEFAULT 0, "max_attempts" integer NOT NULL DEFAULT 3, "last_error" text, "process_after" timestamptz NOT NULL DEFAULT now(), "worker_id" text, "started_at" timestamptz, "heartbeat_at" timestamptz, "lease_until" timestamptz, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), "completed_at" timestamptz);
--> statement-breakpoint
CREATE TABLE IF NOT EXISTS "workflow_runs" ("run_id" text PRIMARY KEY, "workflow_type" text NOT NULL, "subject_id" text NOT NULL, "status" text NOT NULL, "step_name" text, "attempt" integer NOT NULL DEFAULT 0, "started_at" timestamptz, "completed_at" timestamptz, "last_error" text, "stats" jsonb NOT NULL DEFAULT '{}'::jsonb, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now());
