# AI Providers

`@trapmap/ai-providers` owns shared AI provider contracts, environment configuration, and provider factories for chat and embeddings.

It does not own server prompt construction, prompt templates, caching, parsing, or dynamic prompt helpers. Those server-owned capabilities remain under `packages/server/src/lib/ai/`.
