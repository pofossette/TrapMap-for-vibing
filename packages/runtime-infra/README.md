# @trapmap/runtime-infra

Shared runtime infrastructure for TrapMap hosts and services.

This package provides the runtime seam for store, async transport, adapter registry, AI providers, graph query backend, and event bus assembly.
